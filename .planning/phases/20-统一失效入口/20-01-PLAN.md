---
phase: 20-统一失效入口
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
autonomous: true
requirements: [INVAL-01, INVAL-02]

must_haves:
  truths:
    - "Phase 20 在动实现前先有统一失效入口的验证契约，执行者不需要猜什么叫单入口、同帧合并和最小 dirty rect。"
    - "Phase 20 的 RED 基线先把 dirty rect 合并、full repaint 提升和 interactive priority 保持写成明确测试事实。"
    - "Phase 18/19 的 projected playhead 与主层场景守护继续作为 Phase 20 的回归底线，不允许结构收敛时偷改既有播放语义。"
  artifacts:
    - path: ".planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md"
      provides: "Phase 20 verification source of truth"
      contains: "Phase 20: Unified Invalidation Tests"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 20 unified invalidation RED regressions"
      contains: "runPhase20UnifiedInvalidationTests"
  key_links:
    - from: ".planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "验证文档先写死必须出现的 Phase 20 测试名与 gate 命令"
      pattern: "runPhase20UnifiedInvalidationTests"
    - from: "Tests/TestMain.cpp"
      to: "Source/Standalone/UI/PianoRoll/PianoRollVisualInvalidation.h"
      via: "RED 测试先锁定后续 helper contract 的精确类型名与行为"
      pattern: "PianoRollVisualInvalidationState"
---

<objective>
先把 Phase 20 的 test-verification 契约和 unified invalidation RED baseline 固定下来，再进入实现。Phase 20 继续遵守 test-driven-spec：先定义验证，再允许改 `PianoRollComponent` 的结构。

Purpose: 如果不先把 `INVAL-01` / `INVAL-02` 对应的测试事实写死，执行阶段很容易把“删掉几个 repaint 调用”误当成结构收敛完成，或者在 Phase 20 越权偷做 Phase 21 的时钟收敛。

Output: 更新后的 `20-TEST-VERIFICATION.md` 与 `Tests/TestMain.cpp` 中的 Phase 20 unified invalidation RED regressions。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/CONVENTIONS.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@.planning/phases/19-主层场景归一/19-VERIFICATION.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/FrameScheduler.h

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
void onHeartbeatTick();
void onScrollVBlankCallback(double timestampSec);
void setScrollOffset(int offset);
void setZoomLevel(double zoom);
juce::Rectangle<int> getPlayheadDirtyBounds(double playheadTimeSeconds) const;
void updateMainLayerPlayhead(double playheadTimeSeconds);
void requestInteractiveRepaint();
void requestInteractiveRepaint(const juce::Rectangle<int>& dirtyArea);
```

From `Source/Standalone/UI/FrameScheduler.h`:
```cpp
void requestInvalidate(juce::Component& component,
                       const juce::Rectangle<int>& dirtyArea,
                       Priority priority = Priority::Normal);
void requestInvalidate(juce::Component& component,
                       Priority priority = Priority::Normal);
```

Phase 18/19 guards that must stay green:
```cpp
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
LAYER_01_PianoRollHasNoPlayheadOverlayChild
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 固化 Phase 20 test-verification 契约</name>
  <files>.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - .planning/phases/19-主层场景归一/19-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/FrameScheduler.h
  </read_first>
  <action>
    创建 `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md`，把 Phase 20 的验证源固定成明确可执行的四级 gate，要求逐条写死：
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase20-docs -Target OpenTuneTests`
    - L2 命令固定为 `& ".\build-phase20-docs\OpenTuneTests.exe"`
    - L4 静态审计必须点名 `invalidateVisual(`、`flushPendingVisualInvalidation(`、`PianoRollVisualInvalidationState`、`PianoRollVisualInvalidationRequest`、`requestInteractiveRepaint`、`requestRepaint`、`FrameScheduler::instance().requestInvalidate`、`repaint(`，并明确要求 `requestInteractiveRepaint` / `requestRepaint` 归零、`FrameScheduler::instance().requestInvalidate` 在 `PianoRollComponent.cpp` 里只剩一个 flush 出口
    - L6 命令继续使用 `& ".\build-phase20-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：`INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`、`INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons`、`INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush`，并继续列出 `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`、`TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead`、`LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `Evidence To Capture` 必须精确写出：`PianoRollComponent` 暴露统一 `invalidateVisual(...)` 入口、`PianoRollToolHandler` 不再保留 `requestRepaint` hook、旧 `requestInteractiveRepaint()` API 已删除、helper reducer 能证明 dirty union/full repaint/priority merge 三条事实
  </action>
  <acceptance_criteria>
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `Phase 20: Unified Invalidation Tests`
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons`
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush`
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `requestInteractiveRepaint`
    - `.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md` contains `FrameScheduler::instance().requestInvalidate`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 20: Unified Invalidation Tests|INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame|INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons|INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush|requestInteractiveRepaint|FrameScheduler::instance\(\)\.requestInvalidate" ".planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 20 已有先于实现存在的验证契约，执行者不需要再猜应该证明什么。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写 Phase 20 unified invalidation RED regressions</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame` 证明同一帧的两个 dirty rect 会先 union，再决定 flush 区域。
    - Test 2: `INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons` 证明 full repaint 原因会覆盖单个 dirty rect 请求，而不是保留并行双路径。
    - Test 3: `INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush` 证明 interactive invalidation 不会在 merge 过程中被 normal/background 优先级稀释。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md
    - .planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md
    - .planning/phases/19-主层场景归一/19-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/FrameScheduler.h
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase20UnifiedInvalidationTests()` 并接入主测试流程。测试内容必须精确使用下面这组未来 contract 名称，不允许临时发明别名：
    - `#include "Standalone/UI/PianoRoll/PianoRollVisualInvalidation.h"`
    - Test 1 `INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`
      - 创建 `PianoRollVisualInvalidationState state;`
      - 依次 `merge()` 两个 `PianoRollVisualInvalidationRequest`
      - 第一个 request：`reasonsMask = static_cast<uint32_t>(PianoRollVisualInvalidationReason::Playhead)`、`fullRepaint = false`、`hasDirtyArea = true`、`dirtyArea = {10, 20, 30, 40}`、`priority = PianoRollVisualInvalidationPriority::Interactive`
      - 第二个 request：`reasonsMask = static_cast<uint32_t>(PianoRollVisualInvalidationReason::Interaction)`、`fullRepaint = false`、`hasDirtyArea = true`、`dirtyArea = {40, 20, 30, 40}`、`priority = PianoRollVisualInvalidationPriority::Interactive`
      - 调 `makeVisualFlushDecision(state, {0, 0, 200, 120})` 后断言 `fullRepaint == false`、`hasDirtyArea == true`、`dirtyArea == juce::Rectangle<int>(10, 20, 60, 40)`
    - Test 2 `INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons`
      - 先 merge 一个 playhead dirty request，再 merge 一个 `reasonsMask = static_cast<uint32_t>(PianoRollVisualInvalidationReason::Content)`、`fullRepaint = true`、`hasDirtyArea = false`、`priority = PianoRollVisualInvalidationPriority::Normal` 的 request
      - `makeVisualFlushDecision(...)` 后断言 `fullRepaint == true`、`hasDirtyArea == false`
    - Test 3 `INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush`
      - 先 merge 一个 `priority = PianoRollVisualInvalidationPriority::Normal` 的 content request，再 merge 一个 `priority = PianoRollVisualInvalidationPriority::Interactive` 的 interaction dirty request
      - `makeVisualFlushDecision(...)` 后断言 `priority == PianoRollVisualInvalidationPriority::Interactive`
    - 主测试入口必须显式调用 `runPhase20UnifiedInvalidationTests();`
    - 现有 `TIME_01_*` 与 `LAYER_01_*` 守护保持原名、原断言，不允许放宽
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase20UnifiedInvalidationTests`
    - `Tests/TestMain.cpp` contains `INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`
    - `Tests/TestMain.cpp` contains `INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons`
    - `Tests/TestMain.cpp` contains `INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush`
    - `Tests/TestMain.cpp` contains `PianoRollVisualInvalidationState`
    - `Tests/TestMain.cpp` contains `makeVisualFlushDecision`
    - `Tests/TestMain.cpp` contains `runPhase20UnifiedInvalidationTests();`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "runPhase20UnifiedInvalidationTests|INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame|INVAL_02_PianoRollVisualInvalidationPromotesFullRepaintAcrossReasons|INVAL_02_PianoRollVisualInvalidationPreservesInteractivePriorityUntilFlush|PianoRollVisualInvalidationState|makeVisualFlushDecision" "Tests/TestMain.cpp"</automated>
  </verify>
  <done>
    Phase 20 的 RED 阶段已完成：同帧合并、full repaint 提升和 priority 保持三条事实先于实现被写死。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification contract -> implementation | 如果 Phase 20 没有先写死验证契约，执行者会把“删几个 repaint 调用”误判成结构收敛完成 |
| regression suite -> invalidation reducer contract | 如果 RED 基线不先锁定 helper 类型名和行为，后续 merge 逻辑会继续散落在组件里，无法形成唯一解释 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-01 | R | Phase 20 verification contract | mitigate | 在 `20-TEST-VERIFICATION.md` 中写死 L1/L2/L4/L6 命令、测试名和静态审计点，防止执行阶段靠口头解释补验证。 |
| T-20-02 | T | unified invalidation regression coverage | mitigate | 在 `Tests/TestMain.cpp` 先加入 `runPhase20UnifiedInvalidationTests()`，直接约束 dirty union / full repaint / priority merge 三条事实。 |
| T-20-03 | D | phase boundary drift | accept | Phase 20 接受 VBlank / heartbeat / timer 结构仍存在，因为单时钟收敛被 roadmap 明确分配到 Phase 21。 |
</threat_model>

<verification>
- `20-TEST-VERIFICATION.md` 已成为 Phase 20 的验证源文件。
- `Tests/TestMain.cpp` 已定义 Phase 20 unified invalidation RED 回归入口。
- Phase 18/19 的 projected playhead 与 single-host 守护继续被列为 Phase 20 的底线回归。
</verification>

<success_criteria>
- [ ] Phase 20 已有先行 test-verification 契约。
- [ ] Phase 20 已有覆盖 dirty union / full repaint / priority merge 的 RED baseline。
- [ ] 执行者可以在不猜测需求的前提下进入 invalidation reducer 与 component wiring 实现。
</success_criteria>

<output>
After completion, create `.planning/phases/20-统一失效入口/20-01-SUMMARY.md`
</output>
