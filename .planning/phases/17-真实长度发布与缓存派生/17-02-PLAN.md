---
phase: 17-真实长度发布与缓存派生
plan: 02
type: execute
wave: 2
depends_on: [17-01]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md
  - .planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md
autonomous: true
requirements: [TASK-05, SAMP-03]

must_haves:
  truths:
    - "Phase 17 必须像 Phase 15/16 一样留下真实自动化证据，不能只凭代码阅读判断 cache 发布语义正确。"
    - "test-verification 文档必须先存在，再补 Phase 17 测试与结果，遵守 test-driven-spec。"
    - "verification 文档必须把 TASK-05 与 SAMP-03 逐项绑定到真实命令或测试名。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 17 regression coverage"
      contains: "runPhase17RealLengthPublishTests"
    - path: ".planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md"
      provides: "L1/L2/L4/L6 verification commands and real outcomes"
      contains: "OpenTuneTests"
    - path: ".planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md"
      provides: "requirement traceability for TASK-05 and SAMP-03"
      contains: "TASK-05"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/Inference/RenderCache.cpp"
      via: "Phase 17 regression tests enforce sample-authoritative publish semantics"
      pattern: "runPhase17RealLengthPublishTests"
---

<objective>
为 Phase 17 补齐 test-driven-spec 要求的自动化回归与 traceability：证明 base/resampled cache 确实只发布真实长度，且显示时间确实是 sample truth 的投影结果。

Purpose: 如果这里没有自动化守护，后续 Phase 18 的 UI 时间轴改造很容易再次把 seconds-based publish 或 padded tail 洩漏带回 shared core。

Output: Phase 17 regression tests、`17-TEST-VERIFICATION.md`、`17-VERIFICATION.md`。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md
@.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md
@.planning/phases/17-真实长度发布与缓存派生/17-01-PLAN.md
@Source/Inference/RenderCache.h
@Source/Inference/RenderCache.cpp
@Source/PluginProcessor.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 先创建 Phase 17 的 test-verification 骨架</name>
  <files>.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    在实现测试前先创建 `17-TEST-VERIFICATION.md`，至少写明：
    - L1 build 命令
    - L2 `OpenTuneTests` 命令
    - L4 static contract audit 命令
    - L6 regression suite 命令（若与 L2 共用同一命令，要明确写出来）
    - 每一级的 pass criteria

    约束：
    - 不允许把任何测试默认标成 skipped。
    - 若某个命令后来因为环境问题无法执行，必须停下说明，而不是文档里提前回避。
  </action>
  <acceptance_criteria>
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` exists
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` contains `L1`
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` contains `L2`
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` contains `L4`
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` contains `L6`
  </acceptance_criteria>
  <verify>
    <automated>rg "L1|L2|L4|L6|OpenTuneTests" .planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md</automated>
  </verify>
  <done>
    Phase 17 的测试计划文档已经先于测试实现存在，满足 test-driven-spec 的硬约束。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 为真实长度发布与 sample->time 投影添加回归测试</name>
  <files>Tests/TestMain.cpp</files>
  <read_first>
    - Tests/TestMain.cpp
    - Source/Inference/RenderCache.h
    - Source/Inference/RenderCache.cpp
    - Source/PluginProcessor.cpp
    - .planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 新增 `runPhase17RealLengthPublishTests()`，至少覆盖：

    - `TASK_05_BaseChunkPublishesTrimmedTrueSampleSpan`
      - 构造一个 `requestRenderPending()` 时 seconds 与 sample span 故意不一致的 chunk。
      - 发布 base chunk 后，验证 `getPublishedChunks()` 暴露的 sample span 和 seconds 都来自 sample truth，而不是沿用错误 seconds。
    - `TASK_05_ResampledCacheUsesBaseSampleProjection`
      - 在同样的场景下写入 resampled chunk。
      - 验证 `readAtTimeForRate()` 在真实 sample span 之外不会继续命中 resampled cache。
    - `SAMP_03_PublishedChunkDisplayTimeIsProjectedFromSamples`
      - 验证对外暴露的 `startSeconds/endSeconds` 是 `startSample/endSampleExclusive` 的投影结果。

    同时把新测试接入 `main()`。
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase17RealLengthPublishTests`
    - `Tests/TestMain.cpp` contains `TASK_05_BaseChunkPublishesTrimmedTrueSampleSpan`
    - `Tests/TestMain.cpp` contains `TASK_05_ResampledCacheUsesBaseSampleProjection`
    - `Tests/TestMain.cpp` contains `SAMP_03_PublishedChunkDisplayTimeIsProjectedFromSamples`
  </acceptance_criteria>
  <verify>
    <automated>rg "runPhase17RealLengthPublishTests|TASK_05_BaseChunkPublishesTrimmedTrueSampleSpan|TASK_05_ResampledCacheUsesBaseSampleProjection|SAMP_03_PublishedChunkDisplayTimeIsProjectedFromSamples" Tests/TestMain.cpp</automated>
  </verify>
  <done>
    Phase 17 的核心语义已经被 `OpenTuneTests` 直接守住，而不是只靠人工阅读代码。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 执行验证并写出 traceability 文档</name>
  <files>.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md, .planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md
    - .planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    运行真实验证命令并回填文档：

    - 执行 L1/L2/L4/L6 命令，把真实命令、结果与环境路径写回 `17-TEST-VERIFICATION.md`。
    - 新建 `17-VERIFICATION.md`，把 `TASK-05` 与 `SAMP-03` 分别映射到具体测试名或 `rg` 证据。
    - 明确记录 gate evaluation 与最终结论。

    约束：
    - 不能写“应该通过”。
    - 不能省略失败重试或环境修正细节。
  </action>
  <acceptance_criteria>
    - `.planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md` contains `PASS`
    - `.planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md` contains `TASK-05`
    - `.planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md` contains `SAMP-03`
    - `.planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md` contains `Gate status`
  </acceptance_criteria>
  <verify>
    <automated>rg "TASK-05|SAMP-03|Gate status|PASS" .planning/phases/17-真实长度发布与缓存派生/17-TEST-VERIFICATION.md .planning/phases/17-真实长度发布与缓存派生/17-VERIFICATION.md</automated>
  </verify>
  <done>
    Phase 17 的 requirement traceability 和测试证据已经补齐，可以进入 phase-level summary / state update。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RenderCache publish contract -> regression tests | 自动化必须直接覆盖真实长度发布与 sample projection 语义 |
| requirements -> verification docs | 每个 requirement 都必须绑定到真实命令或测试名 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | T | regression coverage | mitigate | 用故意错配的 seconds/sample 输入证明 cache 只认 sample truth。 |
| T-17-05 | R | verification docs | mitigate | 记录真实命令与结果，阻止“未验证先宣告完成”。 |
</threat_model>

<verification>
- `OpenTuneTests` 必须包含 Phase 17 新增测试。
- `17-TEST-VERIFICATION.md` 必须记录真实执行命令与结果。
- `17-VERIFICATION.md` 必须把 `TASK-05` / `SAMP-03` 对应到具体证据。
</verification>

<success_criteria>
- [ ] Phase 17 regression tests 已加入并执行。
- [ ] `17-TEST-VERIFICATION.md` 记录了真实 L1/L2/L4/L6 结果。
- [ ] `17-VERIFICATION.md` 完成 TASK-05 与 SAMP-03 的 traceability。
</success_criteria>

<output>
After completion, create `.planning/phases/17-真实长度发布与缓存派生/17-02-SUMMARY.md`
</output>
