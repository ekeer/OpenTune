---
phase: 06-编译验证
plan: 02
type: execute
wave: 2
depends_on: [06-01]
files_modified: []
autonomous: true
requirements: [TEST-01]

must_haves:
  truths:
    - "CMake configure 成功生成 Standalone 目标"
    - "Standalone Debug 编译成功"
    - "Standalone Release 编译成功"
    - "生成的可执行文件可以启动（无链接错误）"
  artifacts:
    - path: "build-standalone/Debug/OpenTune_Standalone.exe"
      provides: "Standalone Debug 可执行文件"
      validation: "文件存在且可执行"
    - path: "build-standalone/Release/OpenTune_Standalone.exe"
      provides: "Standalone Release 可执行文件"
      validation: "文件存在且可执行"
  key_links:
    - from: "CMakeLists.txt"
      to: "build-standalone/"
      via: "cmake -DFORMATS=Standalone"
      pattern: "cmake.*-B.*build-standalone"
---

<objective>
验证 Standalone 编译目标成功构建，生成可执行文件

Purpose: 确认 Standalone 格式独立编译通过，作为双格式构建验证的第一步
Output: Debug 和 Release 配置的 OpenTune_Standalone.exe
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-编译验证/06-01-SUMMARY.md
</context>

<interfaces>
<!-- Build commands -->

Standalone Debug build:
```bash
cmake -DFORMATS=Standalone -DCMAKE_BUILD_TYPE=Debug -B build-standalone
cmake --build build-standalone --config Debug
```

Standalone Release build:
```bash
cmake --build build-standalone --config Release
```

Expected output files:
- build-standalone/Debug/OpenTune_Standalone.exe
- build-standalone/Release/OpenTune_Standalone.exe
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: CMake Configure for Standalone</name>
  <files>CMakeLists.txt</files>
  <action>
    配置 CMake 生成 Standalone 构建目标：

    ```bash
    cmake -DFORMATS=Standalone -DCMAKE_BUILD_TYPE=Debug -B build-standalone
    ```

    **验证点**：
    1. CMake 成功完成配置（退出码 0）
    2. ARA SDK 找到（ARA SDK found at: ...）
    3. JUCE 配置正确
    4. ONNX Runtime 配置正确
    5. 生成的构建系统包含 OpenTune_Standalone 目标

    如果配置失败，检查：
    - ARA_SDK-releases-2.2.0 目录存在
    - JUCE-master 目录存在
    - onnxruntime-dml-1.24.4 目录存在
    - Visual Studio 构建工具已安装
  </action>
  <acceptance_criteria>
    - cmake 命令返回码为 0
    - build-standalone/ 目录已创建
    - CMakeCache.txt 文件存在
  </acceptance_criteria>
  <verify>
    <automated>if (Test-Path "build-standalone/CMakeCache.txt") { "PASS" } else { "FAIL" }</automated>
    Expected: PASS
  </verify>
  <done>CMake 配置成功，Standalone 目标已生成</done>
</task>

<task type="auto">
  <name>Task 2: Build Standalone Debug</name>
  <files>build-standalone/</files>
  <action>
    编译 Standalone Debug 配置：

    ```bash
    cmake --build build-standalone --config Debug --parallel
    ```

    **编译时间估计**：首次编译约 5-10 分钟（取决于机器性能）

    **验证点**：
    1. 所有源文件编译成功（无编译错误）
    2. 链接成功（无未解析符号）
    3. OpenTune_Standalone.exe 生成
    4. POST_BUILD 脚本正确复制 DLL 和模型文件

    **常见问题**：
    - 编译错误：检查缺少的 include 或语法错误
    - 链接错误：检查库路径配置
    - POST_BUILD 错误：检查 DLL 和模型文件路径
  </action>
  <acceptance_criteria>
    - 编译命令返回码为 0
    - OpenTune_Standalone.exe 存在于 build-standalone/Debug/
    - 文件大小 > 1MB（确保不是空文件）
  </acceptance_criteria>
  <verify>
    <automated>if ((Test-Path "build-standalone/Debug/OpenTune_Standalone.exe") -and ((Get-Item "build-standalone/Debug/OpenTune_Standalone.exe").Length -gt 1MB)) { "PASS" } else { "FAIL" }</automated>
    Expected: PASS
  </verify>
  <done>Standalone Debug 编译成功</done>
</task>

<task type="auto">
  <name>Task 3: Build Standalone Release</name>
  <files>build-standalone/</files>
  <action>
    编译 Standalone Release 配置：

    ```bash
    cmake --build build-standalone --config Release --parallel
    ```

    **验证点**：
    1. Release 优化编译成功
    2. OpenTune_Standalone.exe 生成
    3. Release 版本比 Debug 版本小（优化效果）
  </action>
  <acceptance_criteria>
    - 编译命令返回码为 0
    - OpenTune_Standalone.exe 存在于 build-standalone/Release/
    - 文件大小 > 500KB
  </acceptance_criteria>
  <verify>
    <automated>if ((Test-Path "build-standalone/Release/OpenTune_Standalone.exe") -and ((Get-Item "build-standalone/Release/OpenTune_Standalone.exe").Length -gt 500KB)) { "PASS" } else { "FAIL" }</automated>
    Expected: PASS
  </verify>
  <done>Standalone Release 编译成功</done>
</task>

<task type="auto">
  <name>Task 4: Verify Runtime Dependencies</name>
  <files>build-standalone/Debug/, build-standalone/Release/</files>
  <action>
    验证运行时依赖文件已复制：

    1. **检查 DLL 文件**：
       - onnxruntime.dll
       - DirectML.dll（如果在 Windows DML 模式）
       - D3D12/D3D12Core.dll
       - D3D12/D3D12SDKLayers.dll

    2. **检查模型文件**：
       - models/rmvpe.onnx
       - models/hifigan.onnx（或类似名称）

    3. **验证可执行**：
       - 尝试启动 OpenTune_Standalone.exe
       - 确认无缺少 DLL 的启动错误

    ```bash
    # 列出 Debug 目录所有文件
    Get-ChildItem build-standalone/Debug -Recurse -File
    ```
  </action>
  <acceptance_criteria>
    - onnxruntime.dll 存在于 Debug/Release 目录
    - models/ 目录存在且包含模型文件
    - 可执行文件能启动（即使立即退出）
  </acceptance_criteria>
  <verify>
    <automated>if (Test-Path "build-standalone/Debug/onnxruntime.dll") { "PASS" } else { "FAIL" }</automated>
    Expected: PASS
  </verify>
  <done>运行时依赖文件验证通过</done>
</task>

</tasks>

<verification>
## 编译验证清单

1. **CMake Configure**：
   ```bash
   cmake -DFORMATS=Standalone -DCMAKE_BUILD_TYPE=Debug -B build-standalone
   ```
   预期：成功生成构建文件

2. **Debug Build**：
   ```bash
   cmake --build build-standalone --config Debug
   ```
   预期：编译和链接成功

3. **Release Build**：
   ```bash
   cmake --build build-standalone --config Release
   ```
   预期：编译和链接成功

4. **输出文件存在**：
   - build-standalone/Debug/OpenTune_Standalone.exe
   - build-standalone/Release/OpenTune_Standalone.exe

5. **依赖文件验证**：
   - onnxruntime.dll
   - models/*.onnx
</verification>

<success_criteria>
- [ ] CMake 配置成功
- [ ] Standalone Debug 编译成功
- [ ] Standalone Release 编译成功
- [ ] 可执行文件生成
- [ ] 运行时依赖文件复制正确
</success_criteria>

<output>
After completion, create `.planning/phases/06-编译验证/06-02-SUMMARY.md`
</output>
