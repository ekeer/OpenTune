---
phase: 06-编译验证
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: [TEST-04]

must_haves:
  truths:
    - "所有 #include 引用的文件都存在"
    - "没有条件编译导致的符号引用断裂"
    - "双格式源文件路径正确配置"
    - "ARA 条件编译守卫正确放置"
  artifacts:
    - path: "Source/"
      provides: "完整源文件树"
      validation: "无孤儿 include"
  key_links:
    - from: "Source/Plugin/PluginEditor.cpp"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "#include"
      pattern: "#include.*OpenTuneDocumentController"
    - from: "Source/PluginProcessor.cpp"
      to: "Source/ARA/"
      via: "#if JucePlugin_Enable_ARA"
      pattern: "JucePlugin_Enable_ARA"
---

<objective>
编译前静态验证：确保代码无孤儿 include、无符号冲突、条件编译正确

Purpose: 在实际编译前发现潜在的编译错误，减少试错成本
Output: 静态验证报告，确认代码结构完整性
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/VST3Merge.md
</context>

<interfaces>
<!-- Key patterns to verify -->

From CMakeLists.txt:
```cmake
FORMATS Standalone VST3
IS_ARA_EFFECT TRUE
```

Expected conditional compilation guards:
- `#if JucePlugin_Enable_ARA` in ARA source files
- `#if JucePlugin_Build_Standalone` in Standalone-specific files
- `#if !JucePlugin_Build_Standalone` in VST3-specific files
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: 验证 ARA 条件编译守卫</name>
  <files>Source/ARA/*.cpp</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/ARA/PlayheadSync.cpp
    - Source/ARA/TimelineSync.cpp
  </read_first>
  <action>
    验证所有 ARA 源文件正确使用条件编译守卫：

    1. 检查每个 `.cpp` 文件第 1 行是否为 `#if JucePlugin_Enable_ARA`
    2. 检查每个 `.cpp` 文件最后一行是否为 `#endif // JucePlugin_Enable_ARA`
    3. 确认 4 个文件：OpenTuneDocumentController.cpp, OpenTunePlaybackRenderer.cpp, PlayheadSync.cpp, TimelineSync.cpp

    **期望模式**：
    ```cpp
    #if JucePlugin_Enable_ARA

    // ... implementation code ...

    #endif // JucePlugin_Enable_ARA
    ```

    如果发现缺失守卫的文件，记录文件名和行号。
  </action>
  <acceptance_criteria>
    - 4 个 ARA cpp 文件都有 `#if JucePlugin_Enable_ARA` 开头
    - 4 个 ARA cpp 文件都有 `#endif` 结尾
    - 守卫位置正确（文件首尾）
  </acceptance_criteria>
  <verify>
    <automated>rg -l "#if JucePlugin_Enable_ARA" Source/ARA/*.cpp | wc -l</automated>
    Expected: 4
  </verify>
  <done>ARA 条件编译守卫验证通过</done>
</task>

<task type="auto">
  <name>Task 2: 验证编辑器工厂条件编译</name>
  <files>Source/Standalone/EditorFactoryStandalone.cpp, Source/Editor/EditorFactoryPlugin.cpp</files>
  <read_first>
    - Source/Standalone/EditorFactoryStandalone.cpp
    - Source/Editor/EditorFactoryPlugin.cpp
  </read_first>
  <action>
    验证两个编辑器工厂文件使用正确的条件编译：

    1. **EditorFactoryStandalone.cpp**：
       - 应包含 `#if JucePlugin_Build_Standalone` 守卫
       - 或者在 CMakeLists.txt 中通过条件编译排除

    2. **EditorFactoryPlugin.cpp**：
       - 应包含 `#if !JucePlugin_Build_Standalone` 守卫
       - 或者在 CMakeLists.txt 中通过条件编译排除

    确认两个工厂实现互不冲突，不会同时编译。
  </action>
  <acceptance_criteria>
    - EditorFactoryStandalone.cpp 有 Standalone 守卫
    - EditorFactoryPlugin.cpp 有 Plugin 守卫
    - 两个守卫条件互斥
  </acceptance_criteria>
  <verify>
    <automated>rg "#if.*JucePlugin_Build_Standalone" Source/Standalone/EditorFactoryStandalone.cpp Source/Editor/EditorFactoryPlugin.cpp | wc -l</automated>
    Expected: 2
  </verify>
  <done>编辑器工厂条件编译验证通过</done>
</task>

<task type="auto">
  <name>Task 3: 验证 HostIntegration 条件编译</name>
  <files>Source/Host/HostIntegrationStandalone.cpp, Source/Host/HostIntegrationPlugin.cpp</files>
  <read_first>
    - Source/Host/HostIntegrationStandalone.cpp
    - Source/Host/HostIntegrationPlugin.cpp
  </read_first>
  <action>
    验证两个 HostIntegration 实现使用正确的条件编译：

    1. **HostIntegrationStandalone.cpp**：
       - 应包含 `#if JucePlugin_Build_Standalone` 守卫

    2. **HostIntegrationPlugin.cpp**：
       - 应包含 `#if !JucePlugin_Build_Standalone` 守卫

    确认两个实现互不冲突。
  </action>
  <acceptance_criteria>
    - HostIntegrationStandalone.cpp 有 Standalone 守卫
    - HostIntegrationPlugin.cpp 有 Plugin 守卫
    - 两个守卫条件互斥
  </acceptance_criteria>
  <verify>
    <automated>rg "#if.*JucePlugin_Build_Standalone" Source/Host/HostIntegrationStandalone.cpp Source/Host/HostIntegrationPlugin.cpp | wc -l</automated>
    Expected: 2
  </verify>
  <done>HostIntegration 条件编译验证通过</done>
</task>

<task type="auto">
  <name>Task 4: 检查孤儿 include</name>
  <files>Source/**/*</files>
  <action>
    扫描所有源文件，检查 `#include` 引用的文件是否存在：

    1. 收集所有 `#include "..."` 形式的引用
    2. 验证被引用的文件在 Source/ 目录下存在
    3. 特别关注跨条件编译边界的 include（例如 VST3 文件 include Standalone 文件）

    **检查命令**：
    ```bash
    # 查找所有 include 语句
    rg '#include "(.*)"' Source/ --replace '$1' -o | sort | uniq
    ```

    对于每个 include，验证文件存在性。
  </action>
  <acceptance_criteria>
    - 所有 `#include "..."` 引用的文件都存在
    - 没有跨条件编译边界的非法 include
  </acceptance_criteria>
  <verify>
    <automated>rg '#include "(.*)"' Source/ -o | wc -l</automated>
    Note: This counts total includes; manual verification needed for existence
  </verify>
  <done>孤儿 include 检查完成，无断裂引用</done>
</task>

<task type="auto">
  <name>Task 5: 检查符号冲突风险</name>
  <files>Source/PluginProcessor.h, Source/ARA/*.h</files>
  <read_first>
    - Source/PluginProcessor.h (多轨道 vs 单 CLIP 声明)
    - Source/ARA/OpenTuneDocumentController.h
  </read_first>
  <action>
    检查可能导致符号冲突的声明：

    1. 验证 `PluginProcessor.h` 中多轨道 API 已标记 `[[deprecated]]`
    2. 验证 VST3 路径不会调用多轨道 API（检查 `Source/Plugin/PluginEditor.cpp`）
    3. 确认 ARA 相关声明在 `#if JucePlugin_Enable_ARA` 内

    **关键检查点**：
    - `MAX_TRACKS` 常量是否存在
    - `tracks_` 数组是否存在
    - 多轨道方法（`setActiveTrack`, `getClipGainById` 等）是否有 deprecated 标记
  </action>
  <acceptance_criteria>
    - 多轨道 API 有 `[[deprecated]]` 标记
    - VST3 编辑器不调用多轨道 API
    - ARA 声明在条件编译内
  </acceptance_criteria>
  <verify>
    <automated>rg "\[\[deprecated\]\]" Source/PluginProcessor.h | wc -l</automated>
    Expected: At least 1 (multiple track APIs should be deprecated)
  </verify>
  <done>符号冲突风险检查完成</done>
</task>

</tasks>

<verification>
## 静态验证清单

1. **ARA 条件编译**：
   ```bash
   rg "#if JucePlugin_Enable_ARA" Source/ARA/*.cpp
   ```
   预期：4 个文件

2. **编辑器工厂守卫**：
   ```bash
   rg "#if.*JucePlugin_Build_Standalone" Source/Standalone/EditorFactoryStandalone.cpp
   rg "#if.*!JucePlugin_Build_Standalone" Source/Editor/EditorFactoryPlugin.cpp
   ```
   预期：各 1 处

3. **HostIntegration 守卫**：
   ```bash
   rg "#if.*JucePlugin_Build_Standalone" Source/Host/HostIntegrationStandalone.cpp
   rg "#if.*!JucePlugin_Build_Standalone" Source/Host/HostIntegrationPlugin.cpp
   ```
   预期：各 1 处

4. **孤儿 include 检查**：
   手动验证关键 include 路径存在

5. **符号冲突检查**：
   ```bash
   rg "\[\[deprecated\]\]" Source/PluginProcessor.h
   ```
   预期：多处（多轨道 API）
</verification>

<success_criteria>
- [ ] 4 个 ARA cpp 文件都有条件编译守卫
- [ ] 编辑器工厂条件编译正确
- [ ] HostIntegration 条件编译正确
- [ ] 无孤儿 include
- [ ] 多轨道 API 已标记 deprecated
- [ ] 无符号冲突风险
</success_criteria>

<output>
After completion, create `.planning/phases/06-编译验证/06-01-SUMMARY.md`
</output>
