---
phase: 23-snapshot-region
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
autonomous: true
requirements: [SNAP-01, SNAP-02, SNAP-03]

must_haves:
  truths:
    - "完成后，controller 对外只剩 immutable snapshot 读取面；`AudioSourceState*`、source-level range getter 与 `currentPlaybackAudioSource_` 不再构成读侧 contract。"
    - "完成后，published snapshot 的真相单位已经是 playback region，`preferredRegion_` 表达明确的 region identity，而不是最后一个 source。"
    - "完成后，shared processor 边界保持不变；Phase 23 没有通过扩 `Source/PluginProcessor.*` 回避 adapter 结构问题。"
  artifacts:
    - path: ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"
      provides: "Phase 23 verification source of truth created before implementation"
      contains: "Phase 23: Snapshot Model And Region Truth Guards"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "Phase 23 snapshot contract, source/region slot types, published snapshot read API"
      contains: "loadSnapshot"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "region-keyed truth identity instead of source-level single-slot mapping"
      contains: "RegionSlot"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "snapshot helpers and contract-preserving implementation scaffolding"
      contains: "buildPublishedSnapshot"
  key_links:
    - from: "Source/ARA/OpenTuneDocumentController.h"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "header 先冻结 snapshot contract，cpp 再承接 builder / load API 的实现骨架"
      pattern: "loadSnapshot"
    - from: ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "Wave 1 先固定 verification gate，再在 controller 内建立 snapshot contract 与 build scaffolding，保证 tree 继续可编译"
      pattern: "build-phase23-docs"
---

<objective>
先把 `OpenTuneDocumentController` 的状态与读侧 contract 冻结到 Phase 23 目标形态：`sources_ + regions_ + preferredRegion_ + publishedSnapshot_`，并让对外读取面只剩 immutable snapshot 入口。

Purpose: 当前 live tree 的根问题不是实现细节，而是 header/cpp 暴露了 `AudioSourceState*`、source-level range getter、`currentPlaybackAudioSource_` 这类错误 truth carrier。若不先冻结 contract，后续 reducer、publish gate 和测试都会继续围绕旧结构打补丁。

Output: `OpenTuneDocumentController` 的 snapshot contract、slot 类型、读侧入口，以及与之配套的 cpp builder / load scaffolding。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-snapshot-region/RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Live-tree contracts that Phase 23 must retire from the public read-side:
```cpp
AudioSourceState* getAudioSourceState (ARAAudioSource* audioSource) const;
std::optional<juce::Range<double>> getPlaybackRangeForAudioSource (ARAAudioSource* audioSource) const;
std::optional<juce::Range<double>> getSourceRangeForAudioSource (ARAAudioSource* audioSource) const;
ARAAudioSource* getCurrentPlaybackAudioSource() const;
```

Phase-23 target direction from roadmap/project:
```text
sources_ + regions_ + preferredRegion_ + publishedSnapshot_
renderer/editor read side -> loadSnapshot()
playback truth unit -> PlaybackRegion
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 先创建 Phase 23 test-verification source of truth</name>
  <files>.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/codebase/TESTING.md
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    创建 `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md`，把 Phase 23 的验证契约前移到 Wave 1。要求精确如下：
    - 标题固定为 `Phase 23: Snapshot Model And Region Truth Guards`
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests`
    - L2 / L6 命令固定为 `& ".\build-phase23-docs\OpenTuneTests.exe"`
    - `Required Journeys` 至少写明：`SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd`、`SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource`、`SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`、`SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite`
    - 明确记录：Wave 1 即使只冻结 controller contract，也必须通过 snapshot-backed 过渡外皮与 L1 build gate 保持 tree 可编译，不能允许 tree 断裂后再滚到后续 plan
  </action>
  <acceptance_criteria>
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `Phase 23: Snapshot Model And Region Truth Guards`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `build-phase23-docs`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 23: Snapshot Model And Region Truth Guards" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "build-phase23-docs" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 23 的 verification source of truth 已先于实现存在，后续 contract/reducer 改造必须显式引用这份 gate。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 header 冻结 source/region slot 与 immutable snapshot contract</name>
  <files>Source/ARA/OpenTuneDocumentController.h</files>
  <read_first>
    - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    在 `Source/ARA/OpenTuneDocumentController.h` 把 Phase 23 的结构 contract 一次定型，要求精确如下：
    - 定义 source/region/published-view/snapshot 相关类型，名字可以调整，但语义必须稳定映射到：
      - `SourceSlot`：只承载 source metadata、copied audio、content revision、binding truth
      - `RegionSlot`：只承载 region identity、source identity、playback/source mapping、mapping revision
      - `PublishedRegionView`：对读侧发布的 region-level truth view
      - `PublishedSnapshot`：不可变读模型，至少要带 `epoch`、preferred region identity、published region views
    - controller 私有状态必须显式收敛到等价于 `sources_`、`regions_`、`preferredRegion_`、`publishedSnapshot_` 的结构；旧 `audioSourceStates_`、`audioSourceSyncStates_`、`audioSourceClipBindings_`、`currentPlaybackAudioSource_` 不能再作为长期 truth carrier 留在 header 中
    - 对外读 API 必须新增/改成 `loadSnapshot()` 或等价 immutable snapshot 入口；如果为了保持 Wave 1 可编译而暂时保留旧 getter 名称，这些符号也只能是 snapshot projection 的过渡外皮，不能再暴露 mutable carrier、source-level truth 或独立 state 表
    - snapshot / preferred region 必须带明确 region identity，不能退化成“最后一个 source”或“最后一次 mapping”
    - 不得在 header 引入 sample access lifecycle callback、retry、cleanup phase 的新兜底接口；这些属于后续 phase
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.h` contains `loadSnapshot`
    - `Source/ARA/OpenTuneDocumentController.h` contains `SourceSlot`
    - `Source/ARA/OpenTuneDocumentController.h` contains `RegionSlot`
    - `Source/ARA/OpenTuneDocumentController.h` contains `PublishedSnapshot`
    - `Source/ARA/OpenTuneDocumentController.h` contains `PublishedRegionView`
    - `Source/ARA/OpenTuneDocumentController.h` contains `preferredRegion`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `audioSourceStates_`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `audioSourceSyncStates_`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `audioSourceClipBindings_`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `currentPlaybackAudioSource_`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "loadSnapshot" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SourceSlot" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "RegionSlot" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "PublishedSnapshot" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "PublishedRegionView" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "preferredRegion" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "audioSourceStates_|audioSourceSyncStates_|audioSourceClipBindings_|currentPlaybackAudioSource_" Source/ARA/OpenTuneDocumentController.h) { exit 1 }</automated>
  </verify>
  <done>
    Header 已把 Phase 23 的正确结构写成唯一 contract，执行者不需要再从 research 里反推类型边界。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在 cpp 建立 snapshot builder / load API 的实现骨架，并通过 L1 build gate 守住可编译状态</name>
  <files>Source/ARA/OpenTuneDocumentController.cpp</files>
  <read_first>
    - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    在 `Source/ARA/OpenTuneDocumentController.cpp` 为 Task 1 的 contract 补足实现骨架，要求精确如下：
    - 实现 `loadSnapshot()`，返回 immutable snapshot 的稳定读取面；如果内部采用 `std::shared_ptr<const PublishedSnapshot>` 或等价形式，必须保证读侧拿到的是不可变发布物
    - 新增 `buildPublishedSnapshot()` 或等价 helper，把 mutable `sources_/regions_/preferredRegion_` 投影成 region-level published views
    - 把旧 source-level truth 相关 helper 从“对外读契约”降级为内部迁移辅助；如果 Wave 1 为了保持 tree 可编译而暂时保留旧 getter 名称，它们也只能投影 immutable snapshot，不能再直接返回 mutable carrier 或 source-level truth
    - 这一步只建立 builder / load / contract scaffolding，不提前把 `didEndEditing()` 完整 publish gate、sample access lifecycle、removal cleanup 全部做完；这些属于 23-02 或后续 phase
    - Task 结束时必须通过 `23-TEST-VERIFICATION.md` 的 L1 build gate，保证 tree 仍可编译、可集成，再进入 23-02
    - 不修改 `Source/PluginProcessor.*`，也不向 processor 新增 ARA 专属读取 API
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `loadSnapshot`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `buildPublishedSnapshot`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `PublishedRegionView`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `currentPlaybackAudioSource_`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `audioSourceStates_`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `audioSourceSyncStates_`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `audioSourceClipBindings_`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "loadSnapshot" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "buildPublishedSnapshot" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "PublishedRegionView" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "currentPlaybackAudioSource_|audioSourceStates_|audioSourceSyncStates_|audioSourceClipBindings_" Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    cpp 骨架已经落地，并通过 L1 build gate 证明 controller contract 收敛没有把 tree 改断；renderer/editor 的 snapshot-only 主迁移继续留给 23-03。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| mutable controller model -> published snapshot | 如果 snapshot contract 不先冻结，后续 reducer/publish 仍会围绕旧 source-level getter 打补丁 |
| adapter layer -> shared processor core | 如果执行者把 ARA state 问题误转成 processor 扩口，Phase 23 会越过 shared core 边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | T | controller public contract | mitigate | 先在 header/cpp 一次定型 `loadSnapshot()` 与 slot/snapshot 类型，阻断旧 getter 继续成为主读取面。 |
| T-23-02 | D | region truth identity | mitigate | `preferredRegion` 和 `PublishedRegionView` 必须明确携带 region identity，禁止退回 source-level 单槽映射。 |
| T-23-03 | S | phase scope drift | accept | 本计划接受 renderer/editor 的最终 UX 逻辑仍留在 Phase 25，但不接受它们继续读 mutable model；Phase 23 只先冻结 controller contract。 |
</threat_model>

<verification>
- `rg` 确认 `OpenTuneDocumentController.h/.cpp` 中存在 `loadSnapshot`、slot 类型、snapshot 类型和 builder helper。
- `rg` 确认 header/cpp 不再把 `getCurrentPlaybackAudioSource`、`getAudioSourceState`、source-level range getter 作为主 contract 保留。
- 执行 23-01 后再进入 23-02，且 23-01 结束时必须已经通过 L1 build gate，避免 reducer 与 publish gate 建立在不可编译的中间态上。
</verification>

<success_criteria>
- [ ] `OpenTuneDocumentController` 对外读契约已冻结为 immutable snapshot 入口。
- [ ] region truth 的身份单位在 header/cpp 中已经明确是 playback region，而不是 audio source。
- [ ] shared processor 边界保持不变，Phase 23 没有通过扩 core 回避 adapter 结构问题。
</success_criteria>

<output>
After completion, create `.planning/phases/23-snapshot-region/23-01-SUMMARY.md`
</output>
