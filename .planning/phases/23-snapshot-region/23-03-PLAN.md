---
phase: 23-snapshot-region
plan: 03
type: execute
wave: 3
depends_on: [23-02]
files_modified:
  - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
  - Source/ARA/OpenTunePlaybackRenderer.cpp
  - Source/Plugin/PluginEditor.cpp
autonomous: true
requirements: [SNAP-01, SNAP-02, SNAP-03]

must_haves:
  truths:
    - "完成后，Phase 23 已有先行 test-verification 契约与可执行 build/test gate，不能只靠 grep 测试名宣布 snapshot 收敛完成。"
    - "完成后，`OpenTunePlaybackRenderer.cpp` 与 `PluginEditor.cpp` 的读路径已经改成 snapshot-only 或 snapshot 薄投影，不再直接读取 controller mutable model。"
    - "完成后，renderer/editor 仍未抢跑 `appliedRegion` / epoch consumer / cleanup lifecycle 等 Phase 25/24/26 逻辑。"
  artifacts:
    - path: ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"
      provides: "Phase 23 test-verification source of truth"
      contains: "Phase 23: Snapshot Model And Region Truth Guards"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 23 reducer/snapshot RED and GREEN guards"
      contains: "runPhase23SnapshotRegionTests"
    - path: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      provides: "snapshot-only read-side adaptation to new controller API without crossing into Phase 25 final UX logic"
      contains: "loadSnapshot"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "snapshot-only read-side adaptation to new controller API without crossing into Phase 25 final UX logic"
      contains: "loadSnapshot"
  key_links:
    - from: ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "验证文档先写死 Phase 23 journeys、L1/L2/L4/L6 和 snapshot-only 读侧边界"
      pattern: "runPhase23SnapshotRegionTests"
    - from: "Tests/TestMain.cpp"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "测试直接锁住 reducer/snapshot 事实，防止执行者靠口头说明宣布 Phase 23 完成"
      pattern: "PublishedSnapshot"
---

<objective>
在 Wave 1 已创建的 test-verification source of truth 之上，按 Test-Driven Spec 补完 Phase 23 的 snapshot/reducer 守护，并完成 renderer/editor 的 snapshot-only 读侧收敛。

Purpose: Wave 1 已经把 verification gate 固化下来，但当前测试仍几乎不覆盖 controller snapshot 结构。若不把 RED/GREEN 守护补齐并绑定到读侧迁移，执行阶段仍可能用“编过了”替代 `SNAP-01` / `SNAP-02` / `SNAP-03` 的真实证据。

Output: `23-TEST-VERIFICATION.md`、`Tests/TestMain.cpp` 中的 Phase 23 守护，以及 renderer/editor 的 snapshot-only 读侧收敛。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/23-snapshot-region/RESEARCH.md
@.planning/phases/23-snapshot-region/23-01-PLAN.md
@.planning/phases/23-snapshot-region/23-02-PLAN.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Phase-23 journeys that tests must lock by exact name:
```text
SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd
SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource
SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier
SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite
```

Phase-23 read-side scope for renderer/editor:
```text
OpenTunePlaybackRenderer.cpp -> switch block read path to snapshot-only or snapshot thin projection
PluginEditor.cpp -> switch timer/import read path to snapshot-only or snapshot thin projection
Not allowed: appliedRegion logic, epoch consumer UX, sample-access lifecycle, retry cleanup
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在既有 verification source 上补完 Phase 23 RED/GREEN gate 细节</name>
  <files>.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/phases/23-snapshot-region/23-01-PLAN.md
    - .planning/phases/23-snapshot-region/23-02-PLAN.md
    - .planning/codebase/TESTING.md
    - Tests/TestMain.cpp
  </read_first>
  <action>
    更新 `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md`，把 Wave 1 已创建的验证源补全为真正支撑 RED/GREEN 执行的四级 gate，要求逐条写死：
    - 标题固定为 `Phase 23: Snapshot Model And Region Truth Guards`
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests`
    - L2 / L6 命令固定为 `& ".\build-phase23-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：
      - `SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd`
      - `SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource`
      - `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`
      - `SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite`
    - L4 静态审计必须点名 `loadSnapshot`、`PublishedSnapshot`、`PublishedRegionView`、`preferredRegion`、`didEndEditing`、`getAudioSourceState`、`getPlaybackRangeForAudioSource`、`getSourceRangeForAudioSource`、`getCurrentPlaybackAudioSource`
    - `Evidence To Capture` 必须写清：
      - 同一 source 的两个 region 同时存在时 snapshot 中仍保留两个 region truth
      - preferred region 切换即使 content 不变，也会改变 snapshot 的 region identity / selection truth
      - controller 对外不再暴露 `AudioSourceState*` 这类 mutable carrier
      - `OpenTunePlaybackRenderer.cpp` / `PluginEditor.cpp` 的本 phase 验收是 snapshot-only 读取，不是“仅仅编过了”
      - `OpenTunePlaybackRenderer.cpp` / `PluginEditor.cpp` 不得出现 `appliedRegion` / epoch consumer / retry cleanup 等越界逻辑
  </action>
  <acceptance_criteria>
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `Phase 23: Snapshot Model And Region Truth Guards`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`
    - `.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md` contains `build-phase23-docs`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 23: Snapshot Model And Region Truth Guards" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "build-phase23-docs" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 23 已有先于实现存在的验证契约，执行者不需要再猜 snapshot/reducer 必须证明什么。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 按 Test-Driven Spec 先写 Phase 23 reducer/snapshot guards</name>
  <files>Tests/TestMain.cpp, .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md</files>
  <behavior>
    - Test 1: `SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd` 证明 publish 只在 edit-batch end 后对外可见。
    - Test 2: `SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource` 证明同一 source 下多个 region 同时存在时不会被 source-level mapping 压扁。
    - Test 3: `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier` 证明读侧 contract 不再把 `AudioSourceState*` 作为公开读取面。
    - Test 4: `SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite` 证明 preferred region 切换可独立改变 snapshot truth，而不依赖 content rewrite。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/23-snapshot-region/RESEARCH.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase23SnapshotRegionTests()` 并接入主测试流程。测试必须围绕 reducer/snapshot helper 或 controller contract 本身构造，不允许退化成纯 grep 伪测试。要求精确如下：
    - `SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd`
      - 构造 controller 的最小 mutable model 或对应 helper，先执行 region/source 更新但不结束 edit batch
      - 断言 `loadSnapshot()` 仍返回上一版 published snapshot
      - 再触发 `didEndEditing()` 或等价 publish gate，断言 snapshot epoch / contents 发生一次性更新
    - `SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource`
      - 构造同一 source 下两个 region 的 reducer 输入
      - 断言 published snapshot 中存在两个独立 region truth，而不是单一 source range
    - `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`
      - 直接锁定 controller public API：测试或编译期断言不再依赖 `getAudioSourceState()` / `AudioSourceState*` 作为读侧入口
      - 同时增加对 renderer/editor 的静态或编译期守护，证明它们不再调用 `getAudioSourceState()` / `getPlaybackRangeForAudioSource()` / `getSourceRangeForAudioSource()` / `getCurrentPlaybackAudioSource()`
    - `SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite`
      - 构造 content revision 不变、preferred region 从 A 切到 B 的场景
      - 断言 snapshot 中的 preferred region identity 已变化
    - 写完测试后必须先构建 `build-phase23-docs` 并运行测试；如果当前 live tree 仍为 RED，必须把失败输出原样写回 `23-TEST-VERIFICATION.md`
    - 当实现进入 GREEN 后，必须再次运行相同 L1/L2/L6 命令，并把 PASS 输出与 journey 结果回写到 `23-TEST-VERIFICATION.md`
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase23SnapshotRegionTests`
    - `Tests/TestMain.cpp` contains `SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd`
    - `Tests/TestMain.cpp` contains `SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource`
    - `Tests/TestMain.cpp` contains `SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier`
    - `Tests/TestMain.cpp` contains `SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite`
    - `Tests/TestMain.cpp` contains `runPhase23SnapshotRegionTests();`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase23-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase23-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase23SnapshotRegionTests" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase23SnapshotRegionTests\(\);" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite" "Tests/TestMain.cpp"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_01_ControllerPublishesSingleSnapshotAtEditBatchEnd" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_02_ControllerPreservesMultipleRegionsPerAudioSource" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_ControllerReadSideNoLongerExposesMutableAudioSourceCarrier" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "SNAP_03_PreferredRegionIdentityChangesWithoutContentRewrite" ".planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 23 的结构守护已经按 Test-Driven Spec 写成可执行测试，后续执行者不能再拿口头说明替代 snapshot 证据。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 对 renderer/editor 做 snapshot-only 读侧迁移，并严格限制在 Phase 23 边界内</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/Plugin/PluginEditor.cpp</files>
  <read_first>
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/23-snapshot-region/RESEARCH.md
  </read_first>
  <action>
    把 `OpenTunePlaybackRenderer.cpp` 与 `PluginEditor.cpp` 的读路径改成 snapshot-only 或 snapshot 薄投影，要求精确如下：
    - `OpenTunePlaybackRenderer.cpp` 必须围绕 `loadSnapshot()` 建立 block read path，不再直接调用 `getAudioSourceState()`、`getPlaybackRangeForAudioSource()`、`getSourceRangeForAudioSource()`、`getCurrentPlaybackAudioSource()`
    - `PluginEditor.cpp` 的 timer/import 读路径必须围绕 `loadSnapshot()` 建立 region truth 读取面，不再直接调用上述旧 getter
    - 如需临时桥接 helper，它只能是对 immutable snapshot 的薄投影，不得重新引入 source-level 真相表或 mutable carrier
    - 不允许在这里新增 `appliedRegion`、epoch consumer UX、retry、sample access lifecycle、removal cleanup 或 editor heartbeat 语义
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` contains `loadSnapshot`
    - `Source/Plugin/PluginEditor.cpp` contains `loadSnapshot`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getAudioSourceState`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getPlaybackRangeForAudioSource`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getSourceRangeForAudioSource`
    - `Source/Plugin/PluginEditor.cpp` does not contain `getAudioSourceState`
    - `Source/Plugin/PluginEditor.cpp` does not contain `getPlaybackRangeForAudioSource`
    - `Source/Plugin/PluginEditor.cpp` does not contain `getSourceRangeForAudioSource`
    - `Source/Plugin/PluginEditor.cpp` does not contain `getCurrentPlaybackAudioSource`
    - `Source/Plugin/PluginEditor.cpp` does not contain `appliedRegion`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase23-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase23-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "loadSnapshot" Source/ARA/OpenTunePlaybackRenderer.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "loadSnapshot" Source/Plugin/PluginEditor.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getAudioSourceState|getPlaybackRangeForAudioSource|getSourceRangeForAudioSource|getCurrentPlaybackAudioSource" Source/ARA/OpenTunePlaybackRenderer.cpp) { exit 1 }; if (rg -n "getAudioSourceState|getPlaybackRangeForAudioSource|getSourceRangeForAudioSource|getCurrentPlaybackAudioSource|appliedRegion" Source/Plugin/PluginEditor.cpp) { exit 1 }</automated>
  </verify>
  <done>
    renderer/editor 已完成 snapshot-only 读侧迁移，并且通过 build/test 与 grep-zero 证明没有越过 Phase 23 的边界。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test contract -> implementation | 如果不先写 reducer/snapshot guards，执行者会把“编译通过”误判成 snapshot contract 完成 |
| controller API migration -> read-side files | 如果 read-side 只做“编过了”的 compile-follow，`SNAP-03` 会继续落空 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-21 | R | Phase 23 verification contract | mitigate | 在 `23-TEST-VERIFICATION.md` 先写死 L1/L2/L4/L6、journey 名称和 snapshot-only 读侧边界。 |
| T-23-22 | T | reducer/snapshot regression coverage | mitigate | 在 `Tests/TestMain.cpp` 先加入 `runPhase23SnapshotRegionTests()`，直接约束 single snapshot publish、多 region truth 和 preferred region identity。 |
| T-23-23 | S | phase-25 logic leakage | accept | 本计划接受 renderer/editor 的最终 UX 仍未完成，但不接受它们继续读 mutable model；那会直接违背 `SNAP-03`。 |
</threat_model>

<verification>
- `23-TEST-VERIFICATION.md` 已成为 Phase 23 的验证源文件，并记录 L1/L2/L6 的 RED/GREEN 证据。
- `Tests/TestMain.cpp` 已定义 `runPhase23SnapshotRegionTests()` 和四条 Phase 23 journeys。
- renderer/editor 已通过 build/test + grep-zero 证明读路径改为 snapshot-only，同时未引入 `appliedRegion` / epoch consumer / retry cleanup 等越界逻辑。
</verification>

<success_criteria>
- [ ] Phase 23 已有围绕 snapshot/reducer 的 Test-Driven Spec 守护。
- [ ] 同一 source 多 region、single published snapshot、preferred region identity 都有直接测试证据。
- [ ] audio thread 与 editor 读路径都已改为 snapshot-only，且没有越过 Phase 23 边界。
</success_criteria>

<output>
After completion, create `.planning/phases/23-snapshot-region/23-03-SUMMARY.md`
</output>
