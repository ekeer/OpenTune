---
phase: 23-snapshot-region
plan: 02
type: execute
wave: 2
depends_on: [23-01]
files_modified:
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
autonomous: true
requirements: [SNAP-01, SNAP-02, SNAP-03]

must_haves:
  truths:
    - "完成后，同一 `ARAAudioSource` 下多个 `ARAPlaybackRegion` 会同时保留在 mutable model 与 published snapshot 中，不再被 source-level 单槽覆盖。"
    - "完成后，外部读侧只能在 edit-batch 结束后看到一次性替换的 published snapshot，而不是半更新 mixed state。"
    - "完成后，Phase 24/25/26 的生命周期、最终 consumer、cleanup 工作仍保持独立边界，没有被本 plan 提前混入。"
  artifacts:
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "region-keyed reducer and batch-end snapshot publication"
      contains: "didEndEditing"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "preferred region selection and publish trigger"
      contains: "preferredRegion"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "mutable model fields aligned with reducer implementation"
      contains: "publishedSnapshot_"
  key_links:
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "cpp reducer 只能写 23-01 已冻结的 source/region/preferred/snapshot 结构"
      pattern: "publishedSnapshot_"
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: ".planning/phases/23-snapshot-region/RESEARCH.md"
      via: "publish gate 和 phase 边界必须与 research 中的 live-tree 结论一致"
      pattern: "Phase 24"
---

<objective>
把 `OpenTuneDocumentController` 的 live ARA 回调收敛到新的 mutable model reducer，并在 edit-batch 结束时一次性生成/替换 immutable published snapshot。

Purpose: 23-01 只冻结了 contract；真正决定 `SNAP-01` / `SNAP-02` / `SNAP-03` 是否成立的是 reducer 与 publish gate。如果仍沿用 source-level 覆写或在半更新状态下直接对外可见，Phase 23 只会变成名字上的快照化。

Output: region-keyed reducer、preferred region 维护、edit-batch end snapshot publish 路径。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-snapshot-region/RESEARCH.md
@.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
@.planning/phases/23-snapshot-region/23-01-PLAN.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp

<interfaces>
Phase-23 publish target:
```text
mutable model: sources_ + regions_ + preferredRegion_
publish gate: didEndEditing()
read side: loadSnapshot()
```

Explicitly deferred to later phases:
```text
doUpdateAudioSourceContent()
willEnableAudioSourceSamplesAccess()
didEnableAudioSourceSamplesAccess()
willRemovePlaybackRegionFromAudioModification()
renderer/editor final snapshot consumption
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 把 ARA source/region 变更收敛到 region-keyed mutable model reducer</name>
  <files>Source/ARA/OpenTuneDocumentController.cpp, Source/ARA/OpenTuneDocumentController.h</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/phases/23-snapshot-region/23-01-PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    重写 `OpenTuneDocumentController.cpp` 中与 Phase 23 直接相关的 live callbacks，使其只写入新的 mutable model，要求精确如下：
    - `didUpdatePlaybackRegionProperties()`、`didAddPlaybackRegionToAudioModification()`、以及任何当前负责刷新 playback/source mapping 的路径，必须改成按 playback region identity 更新 `regions_`
    - 同一 source 下两个 region 同时存在时，mutable model 中必须保留两个 region slot，不能继续通过 source key 覆盖成单一 range
    - `preferredRegion_` 必须在 reducer 里被显式维护；它表达的是“当前 editor/import 要关注的 region”，不是“最后一个 source”
    - 旧 source-level sync/binding map 如果为了迁移期编译还保留符号，必须降级成 snapshot projection 或局部桥接，不能继续拥有独立真相
    - 不接 sample access enable/disable、content dirty lifecycle 和 removal lifecycle；这些 callback 相关工作留给 Phase 24
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `didUpdatePlaybackRegionProperties`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `didAddPlaybackRegionToAudioModification`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `preferredRegion_`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `regions_`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `updatePlaybackRangeFromRegion(audioSource`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "didUpdatePlaybackRegionProperties" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "didAddPlaybackRegionToAudioModification" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "preferredRegion_" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "regions_" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "updatePlaybackRangeFromRegion\(audioSource" Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    旧 source-level 覆写逻辑已被替换成 region-keyed reducer，Phase 23 的真相单位终于与 requirement 对齐。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 edit-batch 结束点统一发布 immutable snapshot</name>
  <files>Source/ARA/OpenTuneDocumentController.cpp, Source/ARA/OpenTuneDocumentController.h</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/23-snapshot-region/RESEARCH.md
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    以 `didEndEditing()` 或等价 ARA edit-batch end 钩子作为发布门，统一把 mutable model 投影成 immutable snapshot，要求精确如下：
    - 新 snapshot 只在 edit-batch 结束后替换 `publishedSnapshot_`；不要在 getter、timer 或每次 region callback 里即时对外发布半状态
    - 发布时必须把 source copied audio + binding truth + region mapping 投影成 `PublishedRegionView` 列表，保证读侧未来可以一次读取完整 region truth
    - snapshot 发布后不得再原地修改其内容；任何后续变化都必须重新 build 并整体替换
    - 若当前 live tree 尚未实现 `didEndEditing()`，则在本 plan 中补上最小正确实现，用它来统一触发 publish gate
    - 不在本 plan 中重写 `OpenTunePlaybackRenderer.cpp` / `PluginEditor.cpp` 为最终 snapshot consumer；它们最多只做编译跟随所需的最小签名适配
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `didEndEditing`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `publishedSnapshot_`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `buildPublishedSnapshot`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `PublishedSnapshot`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "didEndEditing" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "publishedSnapshot_" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "buildPublishedSnapshot" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "PublishedSnapshot" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase23-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    publish gate 已从散落式拼装收敛到 batch-end 一次发布，并为 23-03 的 snapshot-only 读侧迁移与 Phase 25 的最终 epoch/preferred-region UX 收敛提供唯一发布基础。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARA callback stream -> mutable model | 如果 reducer 继续按 source 覆写 range，同 source 多 region 会被结构性抹平 |
| mutable model -> published snapshot | 如果 publish 不以 edit-batch end 为门，读侧会看到半更新 mixed state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-11 | T | region reducer | mitigate | 所有 playback/source mapping 都改为 region-keyed slot 更新，禁止继续通过 source key 覆盖。 |
| T-23-12 | I | snapshot publish gate | mitigate | 统一在 `didEndEditing()` 结束点整体替换 `publishedSnapshot_`，阻断半更新状态暴露。 |
| T-23-13 | S | phase boundary drift | accept | sample access lifecycle / removal cleanup 明确接受延后到 Phase 24，不在本 plan 中抢跑。 |
</threat_model>

<verification>
- `rg` 确认 `didUpdatePlaybackRegionProperties` / `didAddPlaybackRegionToAudioModification` / `preferredRegion_` / `regions_` 已围绕新模型实现。
- `rg` 确认 `didEndEditing` 与 `publishedSnapshot_` / `buildPublishedSnapshot` 已建立统一 publish gate。
- 通过 `23-TEST-VERIFICATION.md` 的 L1 build gate，证明 23-02 结束时 tree 仍保持可编译、可集成。
- 若执行器需要碰 `OpenTunePlaybackRenderer.cpp` / `PluginEditor.cpp`，只能记录为 compile-follow，不得把最终读侧收敛提前到本 plan。
</verification>

<success_criteria>
- [ ] 同一 source 下多个 region 可以同时存在于 mutable model 与 published snapshot 中。
- [ ] `publishedSnapshot_` 只在 edit-batch 结束点统一替换，读侧不再依赖散落 getter 看到半状态。
- [ ] Phase 24/25/26 的工作边界保持清晰，没有被本 plan 提前混入。
</success_criteria>

<output>
After completion, create `.planning/phases/23-snapshot-region/23-02-SUMMARY.md`
</output>
