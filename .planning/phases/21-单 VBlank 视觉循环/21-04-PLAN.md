---
phase: 21-单 VBlank 视觉循环
plan: 04
type: execute
wave: 4
depends_on: [21-03]
files_modified:
  - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
  - .planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [CLOCK-01, CLOCK-02, FLOW-02]

must_haves:
  truths:
    - "Phase 21 的 closure 以 fresh build + fresh regression 为准，不允许拿 Phase 20 binary 或口头解释代替。"
    - "`CLOCK-01` / `CLOCK-02` / `FLOW-02` 必须在 verification 文档里直接绑定到 single-VBlank cadence 与 editor-shell 边界证据。"
    - "只有 gate 全过，roadmap/state 才能把 Phase 21 写成完成；如果 gate 失败，文档必须忠实记录失败。"
  artifacts:
    - path: ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"
      provides: "fresh L1/L2/L4/L6 evidence for Phase 21"
      contains: "Phase 21: Single VBlank Visual Loop Tests"
    - path: ".planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md"
      provides: "requirement-level traceability for CLOCK-01, CLOCK-02, FLOW-02"
      contains: "Gate status:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 21 status aligned to real gate result"
      contains: "21-04-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "next-step state aligned to real gate result"
      contains: "Phase 21"
  key_links:
    - from: ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"
      to: ".planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md"
      via: "verification 报告直接消费 fresh gate 结果，不再靠主观总结"
      pattern: "Phase 21: Single VBlank Visual Loop Tests"
    - from: ".planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md"
      to: ".planning/ROADMAP.md"
      via: "只有 gate 结果真实写成 PASS 才允许 roadmap/state 标成完成"
      pattern: "Gate status: PASS"
---

<objective>
在 Phase 21 代码 closure 之后，用 fresh build dir 重建 `OpenTuneTests`、执行 L1/L2/L4/L6，并把 verification / roadmap / state 全部对齐到真实 gate 结果。

Purpose: 当前最大的流程风险不是代码没改，而是 stale binary、旧 build 目录和过时状态文件一起制造假阳性。Phase 21 只有在 fresh binary 真正执行了 `Phase 21: Single VBlank Visual Loop Tests` 之后才算可关闭。

Output: fresh Phase 21 gate 证据闭环，以及与证据一致的 verification / roadmap / state 文档。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/PluginEditor.h
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 执行 fresh L1/L2/L4/L6 gate 并回写 21-TEST-VERIFICATION</name>
  <files>.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/PluginEditor.cpp
    - Source/Plugin/PluginEditor.cpp
    - Tests/TestMain.cpp
  </read_first>
  <action>
    用 fresh build dir 运行并记录四级 gate，精确如下：
    - L1：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase21-docs -Target OpenTuneTests`
    - L2：`& ".\build-phase21-docs\OpenTuneTests.exe"`，必须记录 `=== Phase 21: Single VBlank Visual Loop Tests ===`、三个 `CLOCK_*` journeys，以及 retained `TIME_01_*` / `LAYER_01_*` / `INVAL_*` guards
    - L4：执行静态审计，要求结果必须直接写回文档：
      - `Source/Standalone/UI/PianoRollComponent.cpp` 中 `onVisualVBlankCallback(` 恰好 1 处
      - `Source/Standalone/UI/PianoRollComponent.cpp` 中 `juce::VBlankAttachment(` 恰好 1 处
      - `Source/Standalone/PluginEditor.cpp` 与 `Source/Plugin/PluginEditor.cpp` 中 `pianoRoll_.onVisualVBlankCallback` / `pianoRoll_.flushPendingVisualInvalidation` / `pianoRoll_.invalidateVisual` 为 0 命中
      - `Source/Standalone/PluginEditor.cpp` 中 `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_` 为 0 命中
    - L6：再次运行 `& ".\build-phase21-docs\OpenTuneTests.exe"`，记录完整回归是否继续通过
    - `21-TEST-VERIFICATION.md` 必须把每一级的命令、结果、关键输出、失败时的具体 blocker 都写明，禁止只写“Passed/Failed”而不附命令和直接证据
  </action>
  <acceptance_criteria>
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `build-phase21-docs`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `Phase 21: Single VBlank Visual Loop Tests`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "build-phase21-docs|Phase 21: Single VBlank Visual Loop Tests|CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll|FrameScheduler::instance\(\)\.requestInvalidate\(safeThis->pianoRoll_" ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 21 的四级自动化 gate 已有 fresh build 证据，不再依赖旧 `build/` 产物或口头解释。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 写 verification traceability 并把 roadmap/state 对齐到真实 gate 结果</name>
  <files>.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
    - .planning/phases/20-统一失效入口/20-VERIFICATION.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/PluginEditor.cpp
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    基于 fresh gate 结果生成 `.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md`，并按 gate 结果更新 `.planning/ROADMAP.md` 与 `.planning/STATE.md`。要求精确如下：
    - `21-VERIFICATION.md` 必须包含 `Observable Truths`、`Requirements Coverage`、`Artifact Verification`、`Key Link Verification`、`Behavioral Spot-Checks`、`Remaining Risks`
    - `Observable Truths` 至少写这三条并绑定代码/命令证据：
      1. `PianoRoll` 的播放头、自动滚动、spinner 与波形增量都在同一个 `onVisualVBlankCallback(...)` tick 中推进并 flush
      2. `PianoRoll` 的 visual loop 不再依赖 editor heartbeat / timer
      3. Standalone 与 VST3 editor 继续只承担 transport/state 与业务壳层职责，不再 direct invalidate PianoRoll
    - `Requirements Coverage` 必须直接映射 `CLOCK-01`、`CLOCK-02`、`FLOW-02`
    - 如果 L1/L2/L4/L6 全部通过，则：
      - `21-VERIFICATION.md` 写 `Verdict: passed` 与 `Gate status: PASS`
      - `.planning/ROADMAP.md` 把 Phase 21 的 plan list 勾满、progress 行改成 `4/4 | Complete | {实际日期}`
      - `.planning/STATE.md` 更新为“Phase 21 complete; next step plan/execute Phase 22”
    - 如果任一级失败，则：
      - `21-VERIFICATION.md` 写真实 blocker，禁止假阳性标成通过
      - `.planning/ROADMAP.md` / `.planning/STATE.md` 只能反映“Phase 21 verification blocked / gaps found”，不能写 completed
  </action>
  <acceptance_criteria>
    - `.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md` contains `Gate status:`
    - `.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md` contains `CLOCK-01`
    - `.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md` contains `CLOCK-02`
    - `.planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md` contains `FLOW-02`
    - `.planning/ROADMAP.md` contains `21-04-PLAN.md`
    - `.planning/STATE.md` contains `Phase 21`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Gate status:|CLOCK-01|CLOCK-02|FLOW-02|21-04-PLAN.md|Phase 21" ".planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md" ".planning/ROADMAP.md" ".planning/STATE.md"</automated>
  </verify>
  <done>
    Phase 21 的 requirements、roadmap 和 state 已重新回到同一份真实 gate 证据上，不再出现文档说完成、fresh binary 却没执行的分裂状态。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh build/test evidence -> verification docs | 如果 verification 继续引用 stale binary，Phase 21 会出现和 Phase 20 早期类似的假阳性收口风险 |
| verification verdict -> roadmap/state | 只有真实 gate 结果才能更新项目状态，否则 Phase 22 会建立在错误 baseline 上 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-30 | R | Phase 21 evidence trail | mitigate | 用 `build-phase21-docs` fresh binary 重跑 L1/L2/L6，并把命令与输出原样写回 `21-TEST-VERIFICATION.md`。 |
| T-21-31 | T | verification / roadmap / state consistency | mitigate | 让 `21-VERIFICATION.md` 直接消费 fresh gate 结果；只有 gate 真正 PASS 才允许 roadmap/state 标 completed。 |
| T-21-32 | D | stale verification reuse | mitigate | Phase 21 明确禁止复用旧 `build/` 或旧日志；所有 evidence 都必须带 `build-phase21-docs` 前缀。 |
</threat_model>

<verification>
- `21-TEST-VERIFICATION.md` 已记录 fresh L1/L2/L4/L6 结果。
- `21-VERIFICATION.md` 已完成 requirement 级 traceability。
- roadmap/state 已对齐真实 gate 状态，而不是沿用口头结论。
</verification>

<success_criteria>
- [ ] Phase 21 的 closure 只依赖 fresh build + fresh regression 证据。
- [ ] `CLOCK-01` / `CLOCK-02` / `FLOW-02` 都在 verification 文档里有直接证据。
- [ ] roadmap/state 与真实 gate 结果完全一致。
</success_criteria>

<output>
After completion, create `.planning/phases/21-单 VBlank 视觉循环/21-04-SUMMARY.md`
</output>
