---
phase: 21-单 VBlank 视觉循环
plan: 03
type: execute
wave: 3
depends_on: [21-02]
files_modified:
  - Source/Standalone/PluginEditor.h
  - Source/Standalone/PluginEditor.cpp
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
  - Tests/TestMain.cpp
autonomous: true
requirements: [CLOCK-02, FLOW-02]

must_haves:
  truths:
    - "Standalone 与 VST3 editor 仍然可以轮询 transport/state，但不能再承担 PianoRoll 的视觉时钟职责。"
    - "editor 里任何 direct invalidate / flush / VBlank 代打都必须清零，尤其是 Standalone import 完成后的 `FrameScheduler::requestInvalidate(pianoRoll_)` 残留。"
    - "双格式 UI 隔离继续成立：Standalone 和 VST3 各自改自己的 shell，不把一侧的 cadence 语义泄漏到另一侧。"
  artifacts:
    - path: "Source/Standalone/PluginEditor.cpp"
      provides: "standalone shell without piano-roll visual clock"
      contains: "timerCallback"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "vst3 shell without piano-roll visual clock"
      contains: "timerCallback"
  key_links:
    - from: "Source/Standalone/PluginEditor.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.cpp"
      via: "Standalone editor no longer schedules PianoRoll invalidation directly"
      pattern: "FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_"
    - from: "Source/Plugin/PluginEditor.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.cpp"
      via: "VST3 editor only pushes state, never drives PianoRoll flush cadence"
      pattern: "pianoRoll_.onVisualVBlankCallback"
---

<objective>
切断 Standalone/VST3 editor 中仍残留的 PianoRoll 视觉时钟和 direct invalidate 路径，把两套 editor 收敛成只提供 transport/state 与业务壳层的职责边界。

Purpose: Phase 21 如果只改 `PianoRollComponent` 不清理 editor 壳层，`timerCallback()` 和导入后 direct invalidate 仍然会形成第二个可写视觉入口，导致 `FLOW-02` 继续不成立。

Output: 清理后的 Standalone/VST3 editor shell，以及与之匹配的静态/运行时守护。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
@Source/Standalone/PluginEditor.h
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Tests/TestMain.cpp

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 清零 Standalone editor 的 PianoRoll visual-clock 残留</name>
  <files>Source/Standalone/PluginEditor.h, Source/Standalone/PluginEditor.cpp</files>
  <read_first>
    - Source/Standalone/PluginEditor.h
    - Source/Standalone/PluginEditor.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
  </read_first>
  <action>
    以 Standalone shell 为准完成下列收敛：
    - 删除 `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_, ...)` 这一 editor->PianoRoll direct invalidate 残留
    - 保持 `timerCallback()` 只负责 transport/state、parameter panel、arrangement heartbeat 与异步业务队列；它不能直接代替 PianoRoll 的 visual cadence
    - 如有必要，重命名仅表达壳层职责的 heartbeat 常量或私有 helper，但禁止引入第二视觉时钟、兼容分支或并行结构
  </action>
  <acceptance_criteria>
    - `Source/Standalone/PluginEditor.cpp` does not contain `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_`
    - `Source/Standalone/PluginEditor.cpp` does not contain `pianoRoll_.onVisualVBlankCallback`
    - `Source/Standalone/PluginEditor.cpp` does not contain `pianoRoll_.flushPendingVisualInvalidation`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "FrameScheduler::instance\(\)\.requestInvalidate\(safeThis->pianoRoll_|pianoRoll_\.onVisualVBlankCallback|pianoRoll_\.flushPendingVisualInvalidation" "Source/Standalone/PluginEditor.cpp"</automated>
  </verify>
  <done>
    Standalone editor 不再承担 PianoRoll 视觉刷新职责。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 清零 VST3 editor 的 PianoRoll visual-clock 残留并保留 host/state 壳层职责</name>
  <files>Source/Plugin/PluginEditor.h, Source/Plugin/PluginEditor.cpp</files>
  <read_first>
    - Source/Plugin/PluginEditor.h
    - Source/Plugin/PluginEditor.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
  </read_first>
  <action>
    以 VST3 shell 为准完成下列收敛：
    - 保留 host polling 与 clip/state 同步所需的 `timerCallback()`，但它只能同步 transport/state/overlay/ARA 业务，不得直接驱动 PianoRoll flush cadence
    - 明确 VST3 editor 不调用任何 PianoRoll visual-clock API（`onVisualVBlankCallback`、`flushPendingVisualInvalidation`、`invalidateVisual`）
    - 保持 `syncClipToPianoRoll()`、transport snapshot 与 ARA host 同步边界不变，不引入 Standalone 语义泄漏
  </action>
  <acceptance_criteria>
    - `Source/Plugin/PluginEditor.cpp` does not contain `pianoRoll_.onVisualVBlankCallback`
    - `Source/Plugin/PluginEditor.cpp` does not contain `pianoRoll_.flushPendingVisualInvalidation`
    - `Source/Plugin/PluginEditor.cpp` does not contain `pianoRoll_.invalidateVisual`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "pianoRoll_\.onVisualVBlankCallback|pianoRoll_\.flushPendingVisualInvalidation|pianoRoll_\.invalidateVisual" "Source/Plugin/PluginEditor.cpp"</automated>
  </verify>
  <done>
    VST3 editor 已回到 host/state 壳层职责，不再构成 PianoRoll 第二视觉时钟。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| editor shell -> piano-roll scene | 如果 editor 仍能 direct invalidate 或代打 VBlank，Phase 21 的“唯一视觉时钟”就是假命题 |
| standalone shell -> vst3 shell | 如果一侧的修复方法泄漏到另一侧，会破坏双格式隔离边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-20 | T | Standalone editor direct invalidate residue | mitigate | 删除 `FrameScheduler::requestInvalidate(pianoRoll_)` 残留，禁止 editor 直接调度 PianoRoll repaint。 |
| T-21-21 | T | Plugin editor visual-clock drift | mitigate | 以静态审计确保 VST3 timer 不调用任何 PianoRoll visual API。 |
| T-21-22 | E | UI responsibility leak across formats | mitigate | 只在各自 editor shell 内改职责边界，不改 shared processor 与对侧 editor 语义。 |
</threat_model>

<verification>
- Standalone editor 已移除 direct invalidate 残留。
- VST3 editor 已被证明不调用 PianoRoll visual-clock API。
- 两套 editor 都继续保留各自的 host/state 壳层职责。
</verification>

<success_criteria>
- [ ] Standalone editor 不再 direct invalidate PianoRoll。
- [ ] VST3 editor 不再调用任何 PianoRoll visual-clock API。
- [ ] 双格式 UI 隔离与 host/state shell 边界继续成立。
</success_criteria>

<output>
After completion, create `.planning/phases/21-单 VBlank 视觉循环/21-03-SUMMARY.md`
</output>
