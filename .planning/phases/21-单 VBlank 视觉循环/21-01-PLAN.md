---
phase: 21-单 VBlank 视觉循环
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
autonomous: true
requirements: [CLOCK-01, CLOCK-02, FLOW-02]

must_haves:
  truths:
    - "Phase 21 在动实现前先有单 VBlank 视觉循环的验证契约，执行者不需要猜什么叫唯一视觉时钟、editor 只提供状态。"
    - "RED regressions 必须先把播放头/滚动 flush、render decoration、波形增量与 editor 边界残留写成明确测试事实。"
    - "Phase 18/19/20 的 projected playhead、single-host scene 与 unified invalidation 继续作为 Phase 21 的回归底线。"
  artifacts:
    - path: ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"
      provides: "Phase 21 verification source of truth"
      contains: "Phase 21: Single VBlank Visual Loop Tests"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 21 single-VBlank RED regressions"
      contains: "runPhase21SingleVBlankVisualLoopTests"
  key_links:
    - from: ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "验证文档先写死必须出现的 Phase 21 测试名与 gate 命令"
      pattern: "runPhase21SingleVBlankVisualLoopTests"
    - from: "Tests/TestMain.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.cpp"
      via: "RED 测试先锁定唯一视觉时钟仍然只能走 onVisualVBlankCallback(...)"
      pattern: "onVisualVBlankCallback"
---

<objective>
先把 Phase 21 的 test-verification 契约和 failing regressions 固定下来，再进入单 VBlank 视觉循环实现。Phase 21 继续遵守 test-driven-spec：先定义验证，再允许修改 PianoRoll cadence 与 editor 边界。

Purpose: 如果不先把 `CLOCK-01` / `CLOCK-02` / `FLOW-02` 的测试事实写死，执行阶段很容易把“PianoRoll 已经有 VBlankAttachment”误当成收敛完成，或者继续保留 editor timer / direct invalidate 残留而不自知。

Output: 更新后的 `21-TEST-VERIFICATION.md` 与 `Tests/TestMain.cpp` 中的 Phase 21 single-VBlank RED regressions。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@.planning/phases/19-主层场景归一/19-VERIFICATION.md
@.planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md
@.planning/phases/20-统一失效入口/20-VERIFICATION.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/PluginEditor.h
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
void onVisualVBlankCallback(double timestampSec);
void setIsPlaying(bool playing);
void setBpm(double bpm);
void setTimeSignature(int numerator, int denominator);
void setAudioBuffer(std::shared_ptr<const juce::AudioBuffer<float>> buffer, int sampleRate);
void invalidateVisual(const PianoRollVisualInvalidationRequest& request);
void flushPendingVisualInvalidation();
```

From `Source/Standalone/PluginEditor.h` and `Source/Plugin/PluginEditor.h`:
```cpp
void timerCallback() override;
```

Phase 18/19/20 guards that must stay green:
```cpp
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
LAYER_01_PianoRollHasNoPlayheadOverlayChild
INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame
INVAL_01_PianoRollVisualVBlankFlushesPendingInteractiveInvalidation
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 固化 Phase 21 test-verification 契约</name>
  <files>.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/20-统一失效入口/20-TEST-VERIFICATION.md
    - .planning/phases/20-统一失效入口/20-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/PluginEditor.cpp
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    创建 `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md`，把 Phase 21 的验证源固定成明确可执行的四级 gate，要求逐条写死：
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase21-docs -Target OpenTuneTests`
    - L2 命令固定为 `& ".\build-phase21-docs\OpenTuneTests.exe"`
    - L4 静态审计必须点名 `onVisualVBlankCallback(`、`visualVBlankAttachment_`、`FrameScheduler::instance().requestInvalidate`、`timerCallback(`、`pianoRoll_.onVisualVBlankCallback`、`pianoRoll_.flushPendingVisualInvalidation`、`pianoRoll_.invalidateVisual`、`FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_`，并明确要求：
      - `Source/Standalone/UI/PianoRollComponent.cpp` 中 `onVisualVBlankCallback(` 只保留一个实现入口
      - `Source/Standalone/UI/PianoRollComponent.cpp` 中 `juce::VBlankAttachment` 恰好 1 处
      - `Source/Standalone/PluginEditor.cpp` 与 `Source/Plugin/PluginEditor.cpp` 中 `pianoRoll_.onVisualVBlankCallback` / `pianoRoll_.flushPendingVisualInvalidation` / `pianoRoll_.invalidateVisual` 为 0 命中
      - `Source/Standalone/PluginEditor.cpp` 中 `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_` 为 0 命中
    - L6 命令继续使用 `& ".\build-phase21-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：`CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll`、`CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork`、`CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush`，并继续列出 `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`、`TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead`、`LAYER_01_PianoRollHasNoPlayheadOverlayChild`、`INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`、`INVAL_01_PianoRollVisualVBlankFlushesPendingInteractiveInvalidation`
    - `Evidence To Capture` 必须精确写出：`PianoRoll` 的播放头/scroll/spinner/波形增量都从 `onVisualVBlankCallback(...)` 推进；editor timer 不直接驱动 PianoRoll flush；Standalone/VST3 editor 只剩状态同步与异步业务壳层职责
  </action>
  <acceptance_criteria>
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `Phase 21: Single VBlank Visual Loop Tests`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush`
    - `.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md` contains `FrameScheduler::instance().requestInvalidate(safeThis->pianoRoll_`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 21: Single VBlank Visual Loop Tests|CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll|CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork|CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush|FrameScheduler::instance\(\)\.requestInvalidate\(safeThis->pianoRoll_" ".planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 21 已有先于实现存在的验证契约，执行者不需要再猜应该证明什么。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写 Phase 21 single-VBlank RED regressions</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll` 证明播放头推进、自动滚动与 pending invalidation drain 在同一个 `onVisualVBlankCallback(...)` tick 内完成。
    - Test 2: `CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork` 证明 render decoration 与 waveform incremental work 同样由同一个 VBlank tick flush，而不是额外 timer。
    - Test 3: `CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush` 证明只要组件可见、pending invalidation 已存在，即使不触发 editor timer，也能通过 `onVisualVBlankCallback(...)` 完成 flush。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
    - .planning/phases/20-统一失效入口/20-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase21SingleVBlankVisualLoopTests()` 并接入主测试流程。测试内容必须精确使用下面这组未来 contract 名称，不允许临时发明别名：
    - Test 1 `CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll`
      - 创建 `juce::Component host` + `PianoRollComponent pianoRoll` 桌面宿主
      - 给 `pianoRoll` 绑定 `std::shared_ptr<std::atomic<double>>` position source、`setScrollMode(PianoRollComponent::ScrollMode::Continuous)`、`setIsPlaying(true)`
      - 先通过 `invalidateVisual(...)` 放入一条 interactive dirty request，再把 playhead 原子值推进到一个需要滚动的位置
      - 调用一次 `pianoRoll.onVisualVBlankCallback(0.0)` 后断言：pending invalidation 被 drain、`lastVisualFlushMs_` 前进、`getScrollOffset()` 不再停留在初值
    - Test 2 `CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork`
      - 使用非空 audio buffer 调 `setAudioBuffer(...)` 让 waveform mipmap 进入 incremental build 路径，再用 `setRenderingProgress(0.5f, 1)` 置入 decoration work
      - 连续调用两次 `onVisualVBlankCallback(...)`，断言第二次 `lastVisualFlushMs_` 继续前进且 pending invalidation 仍被 drain
    - Test 3 `CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush`
      - 明确不实例化 Standalone/VST3 editor，只在桌面 host 中构造 `PianoRollComponent`
      - 放入 pending interaction invalidation 后直接调用 `onVisualVBlankCallback(...)`
      - 断言 flush 成功，借此锁定“visual loop 不依赖 editor timer”的最小事实
    - 主测试入口必须显式调用 `runPhase21SingleVBlankVisualLoopTests();`
    - 现有 `TIME_01_*`、`LAYER_01_*`、`INVAL_02_*` 与 `INVAL_01_*` 守护保持原名、原断言，不允许放宽
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase21SingleVBlankVisualLoopTests`
    - `Tests/TestMain.cpp` contains `CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll`
    - `Tests/TestMain.cpp` contains `CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork`
    - `Tests/TestMain.cpp` contains `CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush`
    - `Tests/TestMain.cpp` contains `runPhase21SingleVBlankVisualLoopTests();`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "runPhase21SingleVBlankVisualLoopTests|CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll|CLOCK_01_PianoRollSingleVisualTickDrainsDecorationAndWaveformWork|CLOCK_02_PianoRollVisualLoopDoesNotNeedEditorTimerToFlush" "Tests/TestMain.cpp"</automated>
  </verify>
  <done>
    Phase 21 的 RED 阶段已完成：单 tick 汇总推进与 editor timer 非依赖事实先于实现被写死。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification contract -> implementation | 如果 Phase 21 没有先写死验证契约，执行者会把“已经有 VBlankAttachment”误判成单时钟收敛完成 |
| regression suite -> cadence architecture | 如果 RED baseline 不先锁定 single-tick 行为，后续很容易继续允许 editor timer / direct invalidate 作为旁路 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | R | Phase 21 verification contract | mitigate | 在 `21-TEST-VERIFICATION.md` 中写死 L1/L2/L4/L6 命令、测试名和静态审计点，防止执行阶段靠口头解释补验证。 |
| T-21-02 | T | single-VBlank regression coverage | mitigate | 在 `Tests/TestMain.cpp` 先加入 `runPhase21SingleVBlankVisualLoopTests()`，直接约束 playhead/scroll/deco/waveform 与 editor-timer 非依赖三条事实。 |
| T-21-03 | D | phase boundary drift | accept | clip-aware strip repaint 与 full integration 守护明确属于 Phase 22，本计划只锁定 Phase 21 的单时钟与 editor 边界。 |
</threat_model>

<verification>
- `21-TEST-VERIFICATION.md` 已成为 Phase 21 的验证源文件。
- `Tests/TestMain.cpp` 已定义 Phase 21 single-VBlank RED 回归入口。
- Phase 18/19/20 的 projected playhead、single-host scene 和 unified invalidation 守护继续被列为 Phase 21 的底线回归。
</verification>

<success_criteria>
- [ ] Phase 21 已有先行 test-verification 契约。
- [ ] Phase 21 已有覆盖 single-tick cadence 与 editor-timer 非依赖的 RED baseline。
- [ ] 执行者可以在不猜测需求的前提下进入 VBlank visual loop 实现。
</success_criteria>

<output>
After completion, create `.planning/phases/21-单 VBlank 视觉循环/21-01-SUMMARY.md`
</output>
