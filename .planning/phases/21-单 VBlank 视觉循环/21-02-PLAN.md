---
phase: 21-单 VBlank 视觉循环
plan: 02
type: execute
wave: 2
depends_on: [21-01]
files_modified:
  - Source/Standalone/UI/PianoRollComponent.h
  - Source/Standalone/UI/PianoRollComponent.cpp
  - Tests/TestMain.cpp
autonomous: true
requirements: [CLOCK-01, CLOCK-02]

must_haves:
  truths:
    - "PianoRoll 的播放头、自动滚动、spinner 与波形增量必须在同一个 VBlank tick 内推进，然后最多 flush 一次。"
    - "Phase 21 不能通过恢复 heartbeat、timer 或第二个 cadence helper 来止血；重复的 page/continuous scroll 语义要回到单一 VBlank 路径。"
    - "sample-authoritative playhead 投影、Phase 20 unified invalidation reducer 和单层主场景约束都必须继续成立。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "single visual-loop implementation"
      contains: "onVisualVBlankCallback"
    - path: "Tests/TestMain.cpp"
      provides: "single-VBlank cadence regressions"
      contains: "CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll"
  key_links:
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Tests/TestMain.cpp"
      via: "component-level cadence must satisfy Phase 21 RED regressions"
      pattern: "CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll"
---

<objective>
把 PianoRoll 自身的视觉推进收敛成单一 VBlank visual tick：播放头、自动滚动、render decoration、波形增量与 pending invalidation flush 全部在同一个 cadence 内完成。

Purpose: 当前 live tree 虽然已经有 `visualVBlankAttachment_`，但 `onVisualVBlankCallback(...)` 仍然混杂重复滚动语义，且 Phase 21 还没有把“唯一时钟”写成可证明的结构事实。这个计划负责把组件内部的 visual tick 明确成唯一解释。

Output: 更新后的 `PianoRollComponent` 单 VBlank visual loop 实现，以及转绿的 Phase 21 component-level regressions。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PianoRoll/PianoRollVisualInvalidation.h
@Source/Standalone/UI/PianoRoll/PianoRollVisualInvalidation.cpp

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 把组件内部 cadence 收敛成单一 VBlank visual tick</name>
  <files>Source/Standalone/UI/PianoRollComponent.h, Source/Standalone/UI/PianoRollComponent.cpp</files>
  <read_first>
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PianoRoll/PianoRollVisualInvalidation.h
    - Source/Standalone/UI/PianoRoll/PianoRollVisualInvalidation.cpp
    - Tests/TestMain.cpp
  </read_first>
  <action>
    在 `PianoRollComponent` 内部完成下列结构收敛，禁止恢复 heartbeat / timer / overlay：
    - 明确 `onVisualVBlankCallback(...)` 是唯一 visual cadence 入口，所有 per-frame 工作都从这里推进
    - 把播放头推进、continuous/page scroll、render decoration、waveform incremental build、correction result consumption 与最终 `flushPendingVisualInvalidation()` 收敛到同一个 tick 顺序中
    - 清理 `updateAutoScroll()` 与 `onVisualVBlankCallback(...)` 中重复的滚动语义，避免 page scroll 逻辑双写
    - 保证每个 VBlank tick 最多只调用一次 `flushPendingVisualInvalidation()`，且该 flush 继续只桥接到 Phase 20 的 reducer + `FrameScheduler`
    - 保持 `readProjectedPlayheadTime()` 的 sample-authoritative 投影语义，不允许重新引入第二套播放头事实源
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `onVisualVBlankCallback`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `flushPendingVisualInvalidation();`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain a second visual cadence entry besides `onVisualVBlankCallback(...)`
    - `Tests/TestMain.cpp` Phase 21 journeys pass on the modified component
  </acceptance_criteria>
  <verify>
    <automated>rg -n "onVisualVBlankCallback|flushPendingVisualInvalidation\(|updateAutoScroll\(" "Source/Standalone/UI/PianoRollComponent.cpp"</automated>
  </verify>
  <done>
    PianoRoll 自身已经形成唯一 visual tick，播放头/scroll/spinner/波形增量不再分裂到多条 cadence 路径。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 跑 Phase 21 component-level regressions 并转绿</name>
  <files>Tests/TestMain.cpp</files>
  <read_first>
    - Tests/TestMain.cpp
    - Source/Standalone/UI/PianoRollComponent.cpp
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
  </read_first>
  <action>
    让 `runPhase21SingleVBlankVisualLoopTests()` 在 live tree 上转绿，同时保持以下既有 guards 不回归：
    - `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
    - `TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead`
    - `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `INVAL_02_PianoRollVisualInvalidationMergesDirtyRectsInSameFrame`
    - `INVAL_01_PianoRollVisualVBlankFlushesPendingInteractiveInvalidation`
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` Phase 21 journeys are green
    - Phase 18/19/20 retained guards remain green
  </acceptance_criteria>
  <verify>
    <automated>& ".\build-phase21-docs\OpenTuneTests.exe"</automated>
  </verify>
  <done>
    单 VBlank cadence 的 component-level 行为已经被自动化事实覆盖。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component cadence -> visual correctness | 如果 scroll/playhead/deco/waveform 仍分裂到多条路径，Phase 21 就会继续保留第二时钟幻觉 |
| phase 20 reducer -> phase 21 loop | 如果 Phase 21 绕开 unified invalidation reducer，单时钟会退化成新的并行 flush 结构 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-10 | T | PianoRoll visual cadence | mitigate | 只允许 `onVisualVBlankCallback(...)` 推进 per-frame visual work，并在同 tick 内完成 single flush。 |
| T-21-11 | D | duplicate auto-scroll semantics | mitigate | 清理重复 page/continuous scroll 逻辑，避免 `updateAutoScroll()` 和 visual tick 双写。 |
| T-21-12 | R | projected playhead regression | mitigate | 保留 Phase 18 的 projected playhead tests 作为 blocking guard。 |
</threat_model>

<verification>
- PianoRoll 内部的 visual cadence 已收敛为单 tick。
- Phase 21 component-level regressions 已转绿。
- Phase 18/19/20 guards 继续通过。
</verification>

<success_criteria>
- [ ] `onVisualVBlankCallback(...)` 成为组件内部唯一 visual cadence。
- [ ] 同一 VBlank tick 内推进播放头/滚动/装饰/波形并只 flush 一次。
- [ ] sample-authoritative playhead 与 Phase 20 unified invalidation contract 均未回归。
</success_criteria>

<output>
After completion, create `.planning/phases/21-单 VBlank 视觉循环/21-02-SUMMARY.md`
</output>
