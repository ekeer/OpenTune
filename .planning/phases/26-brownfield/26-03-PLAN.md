---
phase: 26-brownfield
plan: 03
type: execute
wave: 3
depends_on: [26-02]
files_modified:
  - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
autonomous: true
requirements: [CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Phase 26 不能只做到代码已删；必须把 fresh build/test/grep-zero 证据写回 verification source，作为 milestone v1.3.2 的 closure evidence。"
    - "Phase 26 的 GREEN evidence 必须同时证明三件事：legacy 残留已消失、consumer 没回退、processor/Standalone 边界仍成立。"
    - "verification source 必须成为 execute-phase 与后续 roadmap/state 更新的唯一证据入口。"
  artifacts:
    - path: ".planning/phases/26-brownfield/26-TEST-VERIFICATION.md"
      provides: "Phase 26 GREEN verification evidence"
      contains: "GREEN Verification"
  key_links:
    - from: ".planning/phases/26-brownfield/26-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "L1/L2/L6 的 fresh evidence 与四条 CLEAN journeys 必须一一对应"
      pattern: "CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup"
---

<objective>
用 fresh `build-phase26-docs` build/test/grep-zero evidence 关闭 Phase 26 verification source，让 milestone v1.3.2 的最后一段 brownfield cleanup 拥有可追溯、可复核、可交接的 closure artifact。

Purpose: Phase 26 的风险不在“删文件”本身，而在于删完以后没有统一 closure evidence。只有把 GREEN verification 与 L4 static audit 写回 `26-TEST-VERIFICATION.md`，后续 execute-phase、roadmap/state 收尾、甚至 milestone 复盘才有同一份事实源。

Output: 回写完 GREEN evidence 的 `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md`。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-brownfield/26-RESEARCH.md
@.planning/phases/26-brownfield/26-TEST-VERIFICATION.md
@.planning/phases/26-brownfield/26-02-PLAN.md
@CMakeLists.txt
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Source/PluginProcessor.h
@Source/Standalone/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Phase-26 journeys that the closure evidence must reference by exact name:
```text
CLEAN_01_RemovesLegacyAudioSourceStateArtifact
CLEAN_01_RemovesLegacySourceLevelControllerHelpers
CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup
CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 运行 fresh L1/L2/L6 与 Phase 26 static audit</name>
  <files>.planning/phases/26-brownfield/26-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - CMakeLists.txt
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/PluginProcessor.h
    - Source/Standalone/PluginEditor.cpp
  </read_first>
  <action>
    精确运行并记录以下 fresh evidence：
    - L1：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase26-docs -Target OpenTuneTests`
    - L2：`& ".\build-phase26-docs\OpenTuneTests.exe"`
    - L6：`& ".\build-phase26-docs\OpenTuneTests.exe"`
    - L4 static audit 必须同时记录：
      - `Source/ARA/AudioSourceState.h` 已不存在
      - `Source/ARA/OpenTuneDocumentController.h/.cpp` grep-zero：`getAudioSourceContentRevision`、`getAudioSourceMappingRevision`、`getAudioSourceClipBinding`、`getPlaybackRegionClipBinding`、`findAudioSourceByClipId`、`findProjectedRegionForAudioSource`、`findFirstRegionForAudioSource`
      - `OpenTunePlaybackRenderer.cpp` 继续只有一次 `loadSnapshot()`，并 grep-zero：`getAudioSourceState`、`getPlaybackRangeForAudioSource`、`getSourceRangeForAudioSource`、`getCurrentPlaybackAudioSource`
      - `PluginEditor.cpp` 继续包含 `resolvePreferredAraRegionView(*snapshot)`、`binding.appliedRegion`、`registerPlaybackRegionClipBinding`，并 grep-zero：`getAudioSourceContentRevision`、`getAudioSourceMappingRevision`、`getAudioSourceClipBinding`、`getPlaybackRegionClipBinding`、`findAudioSourceByClipId`、`getCurrentPlaybackAudioSource`
      - `PluginProcessor.h` 继续包含既有 import/replace/playback APIs，并 grep-zero：`PublishedSnapshot`、`AudioSourceClipBinding`、`RegionSlot`、`SourceSlot`、`ARAPlaybackRegion`
      - `Source/Standalone/PluginEditor.cpp` grep-zero：`loadSnapshot`、`registerPlaybackRegionClipBinding`、`updatePlaybackRegionBindingRevisions`、`clearPlaybackRegionClipBinding`、`RegionIdentity`、`PublishedSnapshot`
    - 若任一 L1/L2/L4/L6 失败，不允许跳过或只写“已知问题”；必须先修复后再记录 GREEN evidence
  </action>
  <acceptance_criteria>
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `GREEN Verification`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_01_RemovesLegacyAudioSourceStateArtifact`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase26-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase26-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase26-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 26 的 fresh build/test/static-audit 证据已经齐备，closure 不再依赖旧 binary 或口头摘要。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 把 GREEN evidence 与 exit condition 写回 verification source</name>
  <files>.planning/phases/26-brownfield/26-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - CMakeLists.txt
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/PluginProcessor.h
    - Source/Standalone/PluginEditor.cpp
  </read_first>
  <action>
    用 Task 1 的 fresh 结果回写 `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md`。要求精确如下：
    - 写入 `GREEN Verification` 段，逐条记录四个 `CLEAN_*` journey 的 PASS 行
    - 记录 L1、L2、L6 的命令与关键结果
    - 记录 L4 audit 的 grep-zero 结果与 boundary 结论，必须点名：orphan file 删除、controller helper 删除、renderer/editor 仍是 snapshot-only / preferred-region-only、`PluginProcessor.h` 边界未扩张、Standalone shell 未吸入 cleanup 符号
    - `Exit Condition For Phase 26` 必须明确写出：
      - orphan ARA source artifact 已删除
      - source-level helper/projection helper 已删除
      - renderer/editor consumer 未回退
      - shared processor API 与 Standalone UI 边界保持不变
  </action>
  <acceptance_criteria>
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `GREEN Verification`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_01_RemovesLegacySourceLevelControllerHelpers`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `Exit Condition For Phase 26`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "GREEN Verification|CLEAN_01_RemovesLegacySourceLevelControllerHelpers|CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup|Exit Condition For Phase 26" .planning/phases/26-brownfield/26-TEST-VERIFICATION.md; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    `26-TEST-VERIFICATION.md` 已成为 Phase 26 closure 的唯一 evidence source，可直接支撑 execute-phase、roadmap/state 更新与 milestone 收尾。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| fresh evidence -> phase closure | 如果 Phase 26 不把 build/test/grep-zero 结果写回 verification source，cleanup 仍然不可追溯 |
| closure evidence -> milestone boundary claims | 如果 Phase 26 只记“旧结构已删”而不记 processor/Standalone boundary，v1.3.2 的 brownfield 守护就没有完成证据 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-21 | R | closure evidence | mitigate | 用 `build-phase26-docs` 的 L1/L2/L6 fresh outputs 回写 `26-TEST-VERIFICATION.md`。 |
| T-26-22 | T | static audit | mitigate | L4 必须同时覆盖 orphan file、legacy helper、consumer path、processor boundary、Standalone isolation。 |
| T-26-23 | I | milestone claim | mitigate | `Exit Condition For Phase 26` 明确写出删除结果与 brownfield guard 双重完成条件。 |
</threat_model>

<verification>
- `build-phase26-docs` 的 L1/L2/L6 fresh evidence 必须存在并写回 verification source。
- `26-TEST-VERIFICATION.md` 必须包含 GREEN verification 与明确 exit conditions。
</verification>

<success_criteria>
- [ ] Phase 26 已有 fresh build/test/grep-zero evidence。
- [ ] verification source 已记录 orphan deletion、helper removal、consumer clean path、processor/Standalone boundary 结论。
- [ ] Phase 26 closure evidence 足以直接支撑 v1.3.2 brownfield 收尾。
</success_criteria>

<output>
After completion, create `.planning/phases/26-brownfield/26-03-SUMMARY.md`
</output>
