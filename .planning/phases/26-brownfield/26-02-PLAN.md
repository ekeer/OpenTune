---
phase: 26-brownfield
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - CMakeLists.txt
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - Source/ARA/AudioSourceState.h
  - Tests/TestMain.cpp
autonomous: true
requirements: [CLEAN-01, CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Phase 26 必须把 orphan file 与 source-level helper 从生产代码里真正删掉，而不是只在 tests 里绕过。"
    - "删除 legacy helper 后，renderer/editor consumer 仍然只读 snapshot / preferred-region truth，不允许回补新的 source-level fallback。"
    - "shared processor 核心与 Standalone shell 只做守护，不接受 cleanup 借机扩 core 或泄漏 VST3/ARA 语义。"
  artifacts:
    - path: "CMakeLists.txt"
      provides: "ARA target source list without orphan AudioSourceState header"
      contains: "Source/ARA/OpenTuneDocumentController.cpp"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "controller contract without legacy source-level helper declarations"
      contains: "registerPlaybackRegionClipBinding"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "controller implementation without legacy source-level projection helper definitions"
      contains: "registerPlaybackRegionClipBinding"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 26 guards updated to the post-cleanup section boundaries"
      contains: "runPhase26CleanupBrownfieldTests"
  key_links:
    - from: "CMakeLists.txt"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "删除 orphan header 后，ARA target 只剩当前 live implementation 所需文件"
      pattern: "Source/ARA/OpenTuneDocumentController.cpp"
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: "Tests/TestMain.cpp"
      via: "删掉 legacy helper 后，Phase 26 tests 必须用新的稳定 marker 和 grep-zero 断言继续守住 cleanup 结果"
      pattern: "CLEAN_01_RemovesLegacySourceLevelControllerHelpers"
---

<objective>
真正执行 Phase 26 的生产 cleanup：删除 orphan file 与 source-level helper 残留，并把测试的 section marker / static audit 同步迁移到新的 post-cleanup 结构上，让 Phase 26 guards 从 RED 变成 GREEN。

Purpose: 前一 plan 已经把验收口径固定，当前 plan 的职责就是把“还在 live tree 里但已经无 consumer 的旧结构”彻底删掉，同时避免因为删 helper 导致 tests 继续依赖旧 marker 或迫使 consumer 回补 fallback。

Output: 无 orphan `AudioSourceState.h` 的 ARA source list、无 source-level helper surface 的 controller、以及能在 `build-phase26-docs` 上转绿的 Phase 26 guards。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-brownfield/26-RESEARCH.md
@.planning/phases/26-brownfield/26-01-PLAN.md
@.planning/phases/26-brownfield/26-TEST-VERIFICATION.md
@.planning/phases/25-snapshot-editor/25-04-SUMMARY.md
@CMakeLists.txt
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Source/PluginProcessor.h
@Tests/TestMain.cpp

<interfaces>
Legacy helper surface that must be deleted fully from production code:
```cpp
uint64_t getAudioSourceContentRevision(juce::ARAAudioSource* source) const;
uint64_t getAudioSourceMappingRevision(juce::ARAAudioSource* source) const;
AudioSourceClipBinding getAudioSourceClipBinding(juce::ARAAudioSource* source) const;
AudioSourceClipBinding getPlaybackRegionClipBinding(juce::ARAPlaybackRegion* playbackRegion) const;
juce::ARAAudioSource* findAudioSourceByClipId(uint64_t clipId) const;
```

Correct binding mutation contract that must remain after cleanup:
```cpp
void registerPlaybackRegionClipBinding(juce::ARAPlaybackRegion* playbackRegion, ...);
void updatePlaybackRegionBindingRevisions(juce::ARAPlaybackRegion* playbackRegion, ...);
void clearPlaybackRegionClipBinding(juce::ARAPlaybackRegion* playbackRegion);
SnapshotHandle loadSnapshot() const;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 删除 orphan `AudioSourceState.h` 与它在构建图中的登记</name>
  <files>CMakeLists.txt, Source/ARA/AudioSourceState.h</files>
  <read_first>
    - CMakeLists.txt
    - Source/ARA/AudioSourceState.h
    - .planning/phases/26-brownfield/26-RESEARCH.md
    - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
  </read_first>
  <action>
    精确执行以下 cleanup：
    - 直接删除 `Source/ARA/AudioSourceState.h`
    - 从 `CMakeLists.txt` 的 `target_sources(OpenTune PRIVATE ...)` ARA 文件列表中删除唯一一条 `Source/ARA/AudioSourceState.h`
    - 不创建替代 stub、compat include、空 header、或任何“先删文件但保留路径”的兼容层
    - 除删除 orphan entry 外，不调整同一 ARA source list 里的其他文件顺序与含义
  </action>
  <acceptance_criteria>
    - `Source/ARA/AudioSourceState.h` does not exist
    - `CMakeLists.txt` does not contain `Source/ARA/AudioSourceState.h`
    - `CMakeLists.txt` contains `Source/ARA/OpenTuneDocumentController.cpp`
    - `CMakeLists.txt` contains `Source/ARA/OpenTunePlaybackRenderer.cpp`
  </acceptance_criteria>
  <verify>
    <automated>if (Test-Path "Source/ARA/AudioSourceState.h") { exit 1 }; if (rg -n "Source/ARA/AudioSourceState\.h" CMakeLists.txt) { exit 1 }; rg -n "Source/ARA/OpenTuneDocumentController\.cpp|Source/ARA/OpenTunePlaybackRenderer\.cpp" CMakeLists.txt; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    orphan header 已从仓库与构建图同时消失，Phase 26 的 cleanup 不再留下假删除残影。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 删除 controller 的 source-level helper surface，并让 Phase 26 guards 转绿</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp, Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: Phase 26 guards 证明 controller header/cpp 不再声明或实现 source-level helper 与 projection helper。
    - Test 2: Phase 26 guards 证明 renderer/editor consumer 继续 snapshot-only / preferred-region-only，没有因为 helper 删除回退。
    - Test 3: Phase 26 guards 证明 shared processor 边界与 Standalone isolation 继续成立。
  </behavior>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/PluginProcessor.h
    - Tests/TestMain.cpp
    - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
  </read_first>
  <action>
    精确执行以下 cleanup，并保证 `build-phase26-docs` 上的 Phase 26 guards 转绿：
    - 从 `Source/ARA/OpenTuneDocumentController.h` 删除以下 declaration：
      - `getAudioSourceContentRevision(...)`
      - `getAudioSourceMappingRevision(...)`
      - `getAudioSourceClipBinding(...)`
      - `getPlaybackRegionClipBinding(...)`
      - `findAudioSourceByClipId(...)`
    - 从 `Source/ARA/OpenTuneDocumentController.h` 的 `PublishedSnapshot` 中删除 `findFirstRegionForAudioSource(...)`
    - 从 `Source/ARA/OpenTuneDocumentController.cpp` 删除以下 definition / helper：
      - `findProjectedRegionForAudioSource(...)`
      - `getAudioSourceContentRevision(...)`
      - `getAudioSourceMappingRevision(...)`
      - `getAudioSourceClipBinding(...)`
      - `getPlaybackRegionClipBinding(...)`
      - `findAudioSourceByClipId(...)`
    - **不得** 删除或重命名当前正确结构：`loadSnapshot()`、`PublishedSnapshot`、`registerPlaybackRegionClipBinding(...)`、`updatePlaybackRegionBindingRevisions(...)`、`clearPlaybackRegionClipBinding(...)`、`binding.appliedRegion`
    - 更新 `Tests/TestMain.cpp` 中依赖旧 helper 名作为 `extractSection(...)` end-marker 的位置，改成 cleanup 后仍稳定存在的 marker；不得为了让测试通过而放宽 grep-zero 列表
    - 不修改 `Source/PluginProcessor.h/.cpp`、`Source/Standalone/PluginEditor.cpp`、`Source/ARA/OpenTunePlaybackRenderer.cpp`、`Source/Plugin/PluginEditor.cpp` 的业务逻辑；Phase 26 对它们的要求是守护和编译通过，不是重做 consumer
    - 完成后运行：
      - `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase26-docs -Target OpenTuneTests`
      - `& ".\build-phase26-docs\OpenTuneTests.exe"`
      直到 `runPhase26CleanupBrownfieldTests()` 与前序 Phase 24/25 guards 一起通过
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getAudioSourceContentRevision`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getAudioSourceMappingRevision`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getAudioSourceClipBinding`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getPlaybackRegionClipBinding`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `findAudioSourceByClipId`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `findProjectedRegionForAudioSource`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `registerPlaybackRegionClipBinding`
    - `Tests/TestMain.cpp` contains `runPhase26CleanupBrownfieldTests`
  </acceptance_criteria>
  <verify>
    <automated>if (rg -n "getAudioSourceContentRevision|getAudioSourceMappingRevision|getAudioSourceClipBinding|getPlaybackRegionClipBinding|findAudioSourceByClipId" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; if (rg -n "findProjectedRegionForAudioSource|findFirstRegionForAudioSource" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase26-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase26-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    legacy source-level helper surface 已从生产代码移除，Phase 26 cleanup/brownfield guards 在 fresh build 上全部转绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller cleanup -> renderer/editor consumer | 删除 source-level helper 时如果误删正确 binding mutation 或 snapshot contract，consumer 会被迫回退到旧模型 |
| ARA adapter cleanup -> processor / Standalone | 如果 cleanup 越界到 shared core 或 Standalone shell，会破坏 milestone 明确禁止触碰的边界 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-11 | T | controller cleanup | mitigate | 只删 source-level helper / projection helper，显式保留 `loadSnapshot()` 与 binding mutation entry points。 |
| T-26-12 | D | build/test stability | mitigate | cleanup 后立即在 `build-phase26-docs` 重跑 `OpenTuneTests`，以 fresh binary 验证 guards。 |
| T-26-13 | E | shared core / Standalone boundary | mitigate | Task action 明确禁止修改 `Source/PluginProcessor.*` 与 `Source/Standalone/PluginEditor.cpp` 业务逻辑，tests 继续做 boundary audit。 |
</threat_model>

<verification>
- orphan file 与 controller legacy helper 必须 grep-zero。
- `build-phase26-docs` 上的 `OpenTuneTests.exe` 必须 fresh 通过，证明 cleanup 没把前序 guard 打回去。
</verification>

<success_criteria>
- [ ] `AudioSourceState.h` 已从仓库与构建图删除。
- [ ] controller 中的 source-level helper/projection helper 已全部删除。
- [ ] Phase 26 guards 在 fresh build 上通过，且 shared processor / Standalone 边界未被污染。
</success_criteria>

<output>
After completion, create `.planning/phases/26-brownfield/26-02-SUMMARY.md`
</output>
