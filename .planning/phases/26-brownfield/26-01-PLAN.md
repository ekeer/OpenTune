---
phase: 26-brownfield
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
autonomous: true
requirements: [CLEAN-02, CLEAN-03]

must_haves:
  truths:
    - "Phase 26 必须先有 verification source of truth 和 executable/static guards，再允许删除生产残留。"
    - "Phase 26 的 cleanup 不能只证明旧结构消失，还必须证明 renderer/editor consumer 没回退、shared processor 边界没扩张、Standalone 没被污染。"
    - "Phase 26 的通过条件必须绑定 `build-phase26-docs` fresh evidence，而不是沿用 Phase 25 叙述性结论。"
  artifacts:
    - path: ".planning/phases/26-brownfield/26-TEST-VERIFICATION.md"
      provides: "Phase 26 verification source of truth"
      contains: "Phase 26: Legacy Cleanup And Brownfield Guard"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 26 executable cleanup and boundary guards"
      contains: "runPhase26CleanupBrownfieldTests"
  key_links:
    - from: ".planning/phases/26-brownfield/26-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "verification source 先写死 journey 名、build dir、grep-zero 列表，再由 test harness 承接同名 guards"
      pattern: "CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold"
    - from: "Tests/TestMain.cpp"
      to: "Source/PluginProcessor.h"
      via: "Phase 26 tests 必须直接审计 shared processor 边界，防止 cleanup 借机把 adapter 语义重新塞进 core"
      pattern: "readPlaybackAudio"
---

<objective>
先冻结 Phase 26 的 verification source 与 cleanup/brownfield guards：把“删什么、如何证明删干净、如何证明没破坏 shared processor 与 Standalone”一次写死，后续生产 cleanup 只能沿这套口径执行。

Purpose: Phase 25 已经把 renderer/editor consumer 收敛到 snapshot / preferred-region truth，Phase 26 的风险不再是架构选型，而是 brownfield cleanup 时把旧残影删不干净，或者为了删残影误伤 shared processor / Standalone 边界。必须先把验收口径固定下来，避免后续执行退化成“grep 看着差不多”。

Output: `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md`，以及 `Tests/TestMain.cpp` 中的 `runPhase26CleanupBrownfieldTests()` 与主测试流程接线。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/26-brownfield/26-RESEARCH.md
@.planning/phases/24-callback-sample-access/24-03-SUMMARY.md
@.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
@.planning/phases/25-snapshot-editor/25-03-SUMMARY.md
@.planning/phases/25-snapshot-editor/25-04-SUMMARY.md
@.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
@CMakeLists.txt
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Source/PluginProcessor.h
@Source/Standalone/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Current removable legacy helper surface in `OpenTuneDocumentController`:
```cpp
uint64_t getAudioSourceContentRevision(juce::ARAAudioSource* source) const;
uint64_t getAudioSourceMappingRevision(juce::ARAAudioSource* source) const;
AudioSourceClipBinding getAudioSourceClipBinding(juce::ARAAudioSource* source) const;
AudioSourceClipBinding getPlaybackRegionClipBinding(juce::ARAPlaybackRegion* playbackRegion) const;
juce::ARAAudioSource* findAudioSourceByClipId(uint64_t clipId) const;
```

Shared processor boundary that Phase 26 must preserve exactly as the live-tree core contract:
```cpp
bool prepareImportClip(int trackId, ...);
bool commitPreparedImportClip(PreparedImportClip&& prepared);
PlaybackReadResult readPlaybackAudio(const PlaybackReadRequest& request) const;
bool getPluginPlaybackReadSource(uint64_t clipId, PlaybackReadSource& out) const;
bool setClipStartSecondsById(int trackId, uint64_t clipId, double startSeconds);
bool setPluginClipStartSeconds(uint64_t clipId, double startSeconds);
bool replaceClipAudioById(int trackId, uint64_t clipId, ...);
bool enqueuePartialRenderById(int trackId, uint64_t clipId, double relStartSeconds, double relEndSeconds);
bool enqueuePluginClipPartialRender(uint64_t clipId, double relStartSeconds, double relEndSeconds);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 Phase 26 verification source of truth</name>
  <files>.planning/phases/26-brownfield/26-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/26-brownfield/26-RESEARCH.md
    - .planning/codebase/TESTING.md
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
  </read_first>
  <action>
    创建 `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md`，把 Phase 26 的唯一 verification source 固定下来。要求精确如下：
    - 标题固定为 `Phase 26: Legacy Cleanup And Brownfield Guard`
    - `Purpose` 必须写明：Phase 26 目标是删除旧 ARA source-level helper / orphan artifact，并用 brownfield guards 证明 shared processor API 不扩 core、Standalone 不回归
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase26-docs -Target OpenTuneTests`
    - L2 / L6 命令固定为 `& ".\build-phase26-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：
      - `CLEAN_01_RemovesLegacyAudioSourceStateArtifact`
      - `CLEAN_01_RemovesLegacySourceLevelControllerHelpers`
      - `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup`
      - `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold`
    - `L4 Static Audit Checklist` 必须点名：`AudioSourceState.h`、`getAudioSourceContentRevision`、`getAudioSourceMappingRevision`、`getAudioSourceClipBinding`、`getPlaybackRegionClipBinding`、`findAudioSourceByClipId`、`findProjectedRegionForAudioSource`、`findFirstRegionForAudioSource`、`registerPlaybackRegionClipBinding`、`snapshot->epoch`、`readPlaybackAudio`、`getPluginPlaybackReadSource`、`Source/Standalone/PluginEditor.cpp`
    - `Evidence To Capture` 必须写明：orphan file 删除、controller helper grep-zero、renderer/editor 继续 snapshot-only / preferred-region-only、`PluginProcessor.h` 继续保留当前 import/replace/playback APIs、Standalone shell 不出现 snapshot cleanup 符号
  </action>
  <acceptance_criteria>
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `Phase 26: Legacy Cleanup And Brownfield Guard`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `build-phase26-docs`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_01_RemovesLegacyAudioSourceStateArtifact`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_01_RemovesLegacySourceLevelControllerHelpers`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup`
    - `.planning/phases/26-brownfield/26-TEST-VERIFICATION.md` contains `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 26: Legacy Cleanup And Brownfield Guard|build-phase26-docs|CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold" ".planning/phases/26-brownfield/26-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 26 的验收口径已经在实现前固定，后续 cleanup 不能再自创 grep 关键词或 build 目录。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在 `Tests/TestMain.cpp` 写 Phase 26 cleanup 与 brownfield guards</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `CLEAN_01_RemovesLegacyAudioSourceStateArtifact` 证明 `Source/ARA/AudioSourceState.h` 文件与 `CMakeLists.txt` 中的 target_sources 登记都已消失。
    - Test 2: `CLEAN_01_RemovesLegacySourceLevelControllerHelpers` 证明 controller header/cpp 不再声明或实现 source-level helper 与 projection helper。
    - Test 3: `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup` 证明 renderer/editor consumer 仍只走 snapshot / preferred-region truth，cleanup 没把旧 getter 或 fallback 带回来。
    - Test 4: `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold` 证明 `PluginProcessor.h` 仍保留当前 import/replace/playback API，且 `Source/Standalone/PluginEditor.cpp` 不出现 ARA cleanup 泄漏符号。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/26-brownfield/26-TEST-VERIFICATION.md
    - .planning/phases/26-brownfield/26-RESEARCH.md
    - CMakeLists.txt
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/PluginProcessor.h
    - Source/Standalone/PluginEditor.cpp
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase26CleanupBrownfieldTests()` 并接入 `main()`。测试要求精确如下：
    - 使用现有 `loadRepositoryTextFile(...)`、`extractSection(...)`、`countOccurrences(...)` helpers，不新增第二套测试基础设施
    - `CLEAN_01_RemovesLegacyAudioSourceStateArtifact`
      - 读取 `CMakeLists.txt`
      - 断言 `Source/ARA/AudioSourceState.h` 文件路径不存在于仓库
      - 断言 `CMakeLists.txt` 不包含 `Source/ARA/AudioSourceState.h`
    - `CLEAN_01_RemovesLegacySourceLevelControllerHelpers`
      - 读取 `Source/ARA/OpenTuneDocumentController.h` 与 `.cpp`
      - 断言两者都不包含：`getAudioSourceContentRevision`、`getAudioSourceMappingRevision`、`getAudioSourceClipBinding`、`getPlaybackRegionClipBinding`、`findAudioSourceByClipId`、`findProjectedRegionForAudioSource`、`findFirstRegionForAudioSource`
      - 对仍需保留的正确结构，显式断言 header/cpp 继续包含：`loadSnapshot()`、`registerPlaybackRegionClipBinding`、`updatePlaybackRegionBindingRevisions`、`clearPlaybackRegionClipBinding`
    - `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup`
      - 读取 `Source/ARA/OpenTunePlaybackRenderer.cpp` 的 `processBlock()` 函数体，断言 `loadSnapshot()` 只出现一次，且 grep-zero：`getAudioSourceState`、`getPlaybackRangeForAudioSource`、`getSourceRangeForAudioSource`、`getCurrentPlaybackAudioSource`
      - 读取 `Source/Plugin/PluginEditor.cpp` 的 `recordRequested()` 与 `syncImportedAraClipIfNeeded()`，断言保留 `resolvePreferredAraRegionView(*snapshot)`、`binding.appliedRegion`、`registerPlaybackRegionClipBinding`，且 grep-zero：`getAudioSourceContentRevision`、`getAudioSourceMappingRevision`、`getAudioSourceClipBinding`、`getPlaybackRegionClipBinding`、`findAudioSourceByClipId`、`getCurrentPlaybackAudioSource`
    - `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold`
      - 读取 `Source/PluginProcessor.h`，断言保留：`prepareImportClip`、`commitPreparedImportClip`、`readPlaybackAudio`、`getPluginPlaybackReadSource`、`setClipStartSecondsById`、`setPluginClipStartSeconds`、`replaceClipAudioById`、`enqueuePartialRenderById`、`enqueuePluginClipPartialRender`
      - 同时断言 `Source/PluginProcessor.h` grep-zero：`PublishedSnapshot`、`AudioSourceClipBinding`、`RegionSlot`、`SourceSlot`、`ARAPlaybackRegion`
      - 读取 `Source/Standalone/PluginEditor.cpp`，断言 grep-zero：`loadSnapshot`、`registerPlaybackRegionClipBinding`、`updatePlaybackRegionBindingRevisions`、`clearPlaybackRegionClipBinding`、`RegionIdentity`、`PublishedSnapshot`
    - `main()` 中必须新增 `runPhase26CleanupBrownfieldTests();`，且位置放在 `runPhase25SnapshotConsumerTests();` 之后、`runRenderStatusSnapshotTests();` 之前
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase26CleanupBrownfieldTests`
    - `Tests/TestMain.cpp` contains `CLEAN_01_RemovesLegacyAudioSourceStateArtifact`
    - `Tests/TestMain.cpp` contains `CLEAN_01_RemovesLegacySourceLevelControllerHelpers`
    - `Tests/TestMain.cpp` contains `CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup`
    - `Tests/TestMain.cpp` contains `CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold`
    - `Tests/TestMain.cpp` contains `runPhase26CleanupBrownfieldTests();`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "runPhase26CleanupBrownfieldTests|CLEAN_01_RemovesLegacyAudioSourceStateArtifact|CLEAN_01_RemovesLegacySourceLevelControllerHelpers|CLEAN_02_ConsumerAndGrepZeroGuardsStayCleanAfterCleanup|CLEAN_03_SharedProcessorBoundaryAndStandaloneIsolationHold" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase26CleanupBrownfieldTests\(\);" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 26 的 cleanup / brownfield guards 已经进入 `OpenTuneTests` 主流程，后续生产 cleanup 必须以这些 guards 转绿为完成标准。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification source -> cleanup implementation | 如果先删代码再补 guards，cleanup 很容易删不干净或误删 shared boundary，最后只能靠口头说明 |
| ARA adapter cleanup -> shared processor / Standalone shell | 如果 Phase 26 没把 core 与 Standalone 的不变量写成检查项，执行时很容易把 adapter 语义继续扩散进别的层 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | T | verification contract | mitigate | 先创建 `26-TEST-VERIFICATION.md`，固定 `build-phase26-docs`、journey 名和 grep-zero 范围。 |
| T-26-02 | R | cleanup completeness | mitigate | 在 `Tests/TestMain.cpp` 为 orphan file、legacy helper、consumer grep-zero、processor boundary、Standalone isolation 分别建立显式 guards。 |
| T-26-03 | E | processor boundary | mitigate | tests 直接审计 `Source/PluginProcessor.h` 现有 API 和 adapter-type grep-zero，防止 cleanup 借机扩 core。 |
</threat_model>

<verification>
- `rg` 确认 `26-TEST-VERIFICATION.md` 已固定 Phase 26 标题、build dir、journey 名。
- `rg` 确认 `Tests/TestMain.cpp` 已新增 `runPhase26CleanupBrownfieldTests()` 与四条 `CLEAN_*` guards。
</verification>

<success_criteria>
- [ ] Phase 26 verification source of truth 已创建。
- [ ] Phase 26 executable/static guards 已接到 `OpenTuneTests` 主流程。
- [ ] 后续 cleanup 已有明确的 fresh evidence 入口，不再靠 summary 叙述闭环。
</success_criteria>

<output>
After completion, create `.planning/phases/26-brownfield/26-01-SUMMARY.md`
</output>
