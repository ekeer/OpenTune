---
phase: 08-playback-read
plan: 02
type: execute
wave: 1
depends_on: [08-01]
files_modified:
  - Source/ARA/OpenTunePlaybackRenderer.h
  - Source/ARA/OpenTunePlaybackRenderer.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [READ-01, READ-02]

must_haves:
  truths:
    - "ARA PlaybackRenderer 只负责区域遍历与时间映射，不再内嵌独立读取策略"
    - "ARA 与常规播放在同一 clip/time 下调用同一 Processor read API"
    - "ARA 路径不新增并行旧链路或兼容层"
  artifacts:
    - path: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      provides: "ARA 到统一读核的单点接线"
    - path: "Source/PluginProcessor.cpp"
      provides: "供 ARA 与常规共用的读取实现"
  key_links:
    - from: "OpenTunePlaybackRenderer::processBlock"
      to: "OpenTuneAudioProcessor::readClipAudioForPlayback"
      via: "mapped playback time + clip binding"
      pattern: "ARA maps time, Processor reads audio"
---

<objective>
将 ARA 回放路径接入 Processor 统一读核，实现 ARA/常规播放同语义、同策略、同实现。

Purpose: 满足 READ-01，彻底消除 ARA 与常规播放双实现分叉。
Output: ARA read path 仅保留映射层，读取策略完全由共享核控制。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-playback-read/08-CONTEXT.md
@.planning/phases/08-playback-read/08-01-PLAN.md
@Source/ARA/OpenTunePlaybackRenderer.h
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
</context>

<interfaces>
From Source/ARA/OpenTunePlaybackRenderer.cpp:
```cpp
bool processBlock(juce::AudioBuffer<float>& buffer,
                  juce::AudioProcessor::Realtime realtime,
                  const juce::AudioPlayHead::PositionInfo& positionInfo) noexcept override;
```

Mapping baseline from Phase 7:
```cpp
int64_t mapPlaybackTimeToSourceSample(...);
```

Unified read call target:
```cpp
PlaybackReadResult readClipAudioForPlayback(const PlaybackReadRequest& request) const;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 将 ARA 读取职责收敛为“映射 + 调用统一读核”</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/ARA/OpenTunePlaybackRenderer.h</files>
  <behavior>
    - Test 1: PlaybackRenderer 仍基于 playback/source range 完成时间映射。
    - Test 2: 实际音频读取通过 Processor 统一 read API 执行，不再直接读 RenderCache。
  </behavior>
  <action>
    重构 PlaybackRenderer：
    - 保留并复用 Phase 7 的时间映射函数；
    - 删除/替换 ARA 路径内独立读取策略分支；
    - 构造统一 read request 并调用 Processor API。

    明确边界：Renderer 不决定回退优先级，只消费统一读核结果。
  </action>
  <verify>
    <automated>rg -n "readClipAudioForPlayback|RenderCache::readAtTimeForRate|getPlaybackRangeForAudioSource|getSourceRangeForAudioSource" Source/ARA/OpenTunePlaybackRenderer.cpp</automated>
  </verify>
  <done>ARA 读取层职责纯化，统一策略边界稳定。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 对齐 ARA/常规混音语义与失败语义</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: 同一 clip/time 下，ARA 与常规路径命中层级一致。
    - Test 2: 渲染不可用时两路径都按 dry 优先，不出现单侧 blank。
    - Test 3: 源无效时两路径都进入 blank/silence，且不会产生随机串音。
  </behavior>
  <action>
    统一两条播放路径的混音写入语义和失败语义：
    - 同步命中层级定义和空数据处理策略；
    - 去除 ARA 侧特例逻辑，避免出现第二语义。

    不新增末端处理（限幅/压缩/并行补偿）。
  </action>
  <verify>
    <automated>rg -n "blank|silence|dry|addFrom|copyFrom|fallback" Source/ARA/OpenTunePlaybackRenderer.cpp Source/PluginProcessor.cpp</automated>
  </verify>
  <done>ARA/常规播放语义完全对齐，READ-01 主体闭环。</done>
</task>

<task type="auto">
  <name>Task 3: 清理遗留分叉并补齐最小诊断日志</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/PluginProcessor.cpp</files>
  <action>
    清理 ARA 旧读取分叉残留（函数、条件、日志口径），保证读取策略仅在 Processor 共享层维护。

    增加最小诊断日志：clipId、time、targetRate、命中层级、readSamples，便于 Phase 8 验收追踪。
  </action>
  <verify>
    <automated>rg -n "read-layer|clipId|targetRate|readSamples|legacy|deprecated" Source/ARA/OpenTunePlaybackRenderer.cpp Source/PluginProcessor.cpp</automated>
  </verify>
  <done>双路径分叉清理完成，统一读核可观测。</done>
</task>

</tasks>

<verification>
- 结构验证：PlaybackRenderer 不再直接实现四级回退。
- 行为验证：同场景下 ARA 与常规命中层级一致。
- 守护验证：Standalone 行为与交互语义保持不变。
</verification>

<success_criteria>
- [ ] ARA/常规两路径共用同一 read API。
- [ ] ARA 路径不再持有独立读取策略实现。
- [ ] READ-01 在代码结构与运行行为层面均成立。
</success_criteria>

<output>
After completion, create `.planning/phases/08-playback-read/08-02-SUMMARY.md`
</output>
