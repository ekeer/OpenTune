---
phase: 08-playback-read
plan: 04
type: execute
wave: 1
depends_on: [08-01, 08-02, 08-03]
files_modified: [Source/PluginProcessor.h, Source/PluginProcessor.cpp, Source/ARA/AudioSourceState.h]
autonomous: true
requirements: [READ-01, READ-02]
gap_closure: true
must_haves:
  truths:
    - "ARA 与常规播放调用同一读 API（参数适配层不算双路径）"
    - "ARA 路径支持完整的四级回退：Resampled → Rendered → Dry → Blank"
    - "AudioSourceState 具备 renderCache 能力"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "统一 read API 与四级回退声明"
      exports: ["PlaybackReadResult", "PlaybackReadLayer"]
    - path: "Source/PluginProcessor.cpp"
      provides: "四级回退实现与 ARA 路径接入"
      contains: "readClipAudioForPlayback"
    - path: "Source/ARA/AudioSourceState.h"
      provides: "AudioSourceState 的 renderCache 支持"
      contains: "renderCache"
  key_links:
    - from: "OpenTunePlaybackRenderer::processBlock"
      to: "Processor::readClipAudioForPlayback"
      via: "统一 API 调用"
      pattern: "readClipAudioForPlayback"
---

<objective>
收敛 ARA 与常规播放到同一读 API，并为 ARA 路径启用完整的四级回退策略。

Purpose: 实现 READ-01（同一读路径）和 READ-02（完整四级策略）的结构目标。
Output: 统一的 readClipAudioForPlayback API，AudioSourceState 具备 renderCache 能力。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-playback-read/08-CONTEXT.md
@.planning/phases/08-playback-read/08-VERIFICATION.md

<interfaces>
<!-- Key types and contracts from codebase -->

From Source/PluginProcessor.h:
```cpp
enum class PlaybackReadLayer : uint8_t {
    None = 0, Resampled, Rendered, Dry, Blank
};

struct PlaybackReadRequest {
    const TrackState::AudioClip* clip{nullptr};
    double readStartSeconds{0.0};
    double targetSampleRate{44100.0};
    int numSamples{0};
};

struct ARAPlaybackReadRequest {
    struct AudioSourceState* audioSourceState{nullptr};
    double readStartSeconds{0.0};
    double targetSampleRate{44100.0};
    int numSamples{0};
};

struct PlaybackReadResult {
    int samplesRead{0};
    PlaybackReadLayer hitLayer{PlaybackReadLayer::None};
    bool silenceFallback{false};
    std::vector<float> audio;
};
```

From Source/ARA/AudioSourceState.h:
```cpp
struct AudioSourceState {
    juce::String name;
    std::shared_ptr<juce::AudioBuffer<float>> audioBuffer;
    double sampleRate = 44100.0;
    int numChannels = 2;
    int64_t numSamples = 0;
    // MISSING: renderCache for Level 1/2 support
};
```

From Source/Inference/RenderCache.h:
```cpp
class RenderCache {
    static constexpr double kSampleRate = 44100.0;
    static constexpr size_t kDefaultGlobalCacheLimitBytes = 1536 * 1024 * 1024;
    int readAtTimeForRate(float* outBuffer, int numSamples, double timeSeconds, int sampleRate, bool exactMatch);
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract unified four-level read core</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <action>
从 `readClipAudioForPlayback` 中提取四级回退核心逻辑为独立函数 `executeFourLevelFallback`。

参数设计：
```cpp
struct FourLevelReadContext {
    RenderCache* renderCache{nullptr};        // Level 1/2：可选
    const AudioBuffer<float>* dryBuffer{nullptr}; // Level 3
    double sourceSampleRate{44100.0};
    double readStartSeconds{0.0};
    double targetSampleRate{44100.0};
    int numSamples{0};
    bool useDrySignalFallback{false};
};
PlaybackReadResult executeFourLevelFallback(const FourLevelReadContext& ctx);
```

重构要点：
1. 将 `readClipAudioForPlayback` 改为构造 `FourLevelReadContext` 并调用 `executeFourLevelFallback`
2. 保持现有行为不变，只是提取公共逻辑
3. 添加单元测试验证提取后行为一致

约束：
- 不改变 `readClipAudioForPlayback` 的公共 API
- 保持 Standalone 行为不变（SAFE-01）
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests && .\build\Debug\OpenTuneTests.exe</automated>
  </verify>
  <done>四级回退逻辑提取为独立函数，所有测试通过，常规播放路径行为不变</done>
</task>

<task type="auto">
  <name>Task 2: Add renderCache to AudioSourceState</name>
  <files>Source/ARA/AudioSourceState.h, Source/ARA/OpenTuneDocumentController.cpp</files>
  <action>
为 `AudioSourceState` 添加 `renderCache` 字段以支持四级回退的 Level 1/2。

变更：
1. 在 `AudioSourceState.h` 添加：
   ```cpp
   #include "Inference/RenderCache.h"
   // ...
   struct AudioSourceState {
       // ... existing fields ...
       std::shared_ptr<RenderCache> renderCache;  // For Level 1/2 read support
   };
   ```

2. 在 `OpenTuneDocumentController.cpp` 的 `tryReadAudioData` 中初始化 renderCache：
   ```cpp
   state->renderCache = std::make_shared<RenderCache>();
   ```

3. 确保 renderCache 在音频源销毁时正确清理（已有 clear() 方法）

注意事项：
- RenderCache 是线程安全的，可在音频线程安全读取
- 初始化时机与 audioBuffer 相同（音频数据加载完成后）
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_VST3</automated>
  </verify>
  <done>AudioSourceState 具备 renderCache 字段，VST3 构建成功</done>
</task>

<task type="auto">
  <name>Task 3: Refactor readAudioSourceForARA to use unified core</name>
  <files>Source/PluginProcessor.cpp, Source/ARA/OpenTunePlaybackRenderer.cpp</files>
  <action>
重构 `readAudioSourceForARA` 使用统一的四级回退核心。

实现步骤：
1. 在 `readAudioSourceForARA` 中构造 `FourLevelReadContext`：
   ```cpp
   FourLevelReadContext ctx;
   ctx.renderCache = state->renderCache.get();  // Now available after Task 2
   ctx.dryBuffer = state->audioBuffer.get();
   ctx.sourceSampleRate = state->sampleRate;
   ctx.readStartSeconds = request.readStartSeconds;
   ctx.targetSampleRate = request.targetSampleRate;
   ctx.numSamples = request.numSamples;
   ctx.useDrySignalFallback = useDrySignalFallback_.load();
   
   return executeFourLevelFallback(ctx);
   ```

2. 移除 `readAudioSourceForARA` 中的重复四级回退实现代码

3. 更新注释说明 ARA 路径现在使用统一核心

关键变更：
- ARA 路径现在支持完整的四级回退（Resampled → Rendered → Dry → Blank）
- 两个 API 共享相同的回退逻辑实现
- 保持 `readAudioSourceForARA` 的公共 API 不变（向后兼容）
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests && .\build\Debug\OpenTuneTests.exe</automated>
  </verify>
  <done>ARA 路径使用统一的四级回退核心，支持完整 Level 1-4</done>
</task>

<task type="auto">
  <name>Task 4: Add test accessor for clip pointer construction</name>
  <files>Source/PluginProcessor.h, Tests/TestMain.cpp</files>
  <action>
添加测试访问器，使测试能够构造有效的 `PlaybackReadRequest`。

实现方案：
1. 在 `PluginProcessor.h` 添加 friend class 声明：
   ```cpp
   #ifdef RUN_TESTS
   friend class OpenTuneAudioProcessorTestAccessor;
   #endif
   ```

2. 在 `TestMain.cpp` 添加测试访问器类：
   ```cpp
   class OpenTuneAudioProcessorTestAccessor {
   public:
       static TrackState::AudioClip* createTestClip(OpenTuneAudioProcessor& proc, 
                                                      int trackId,
                                                      const juce::AudioBuffer<float>& buffer) {
           // Use existing import API to create clip
           // Return pointer to created clip for test use
       }
       
       static const TrackState::AudioClip* getClipPointer(const OpenTuneAudioProcessor& proc,
                                                           int trackId, int clipIndex) {
           // Return const pointer for read tests
       }
   };
   ```

3. 添加测试验证四级回退优先级：
   ```cpp
   TEST(PlaybackReadFourLevel, Level1_Resampled_HasHighestPriority) {
       // Setup: create clip with renderCache populated
       // Expect: readClipAudioForPlayback returns Resampled layer
   }
   
   TEST(PlaybackReadFourLevel, Level3_Dry_FallsBackWhenCacheMiss) {
       // Setup: clip with audio but empty cache
       // Expect: readClipAudioForPlayback returns Dry layer
   }
   ```

约束：
- 测试访问器只在测试构建中可用
- 不改变公共 API
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests && .\build\Debug\OpenTuneTests.exe</automated>
  </verify>
  <done>测试可构造有效 clip 指针，四级回退优先级有自动化验证</done>
</task>

</tasks>

<verification>
## Phase 8 Gap Closure 验证

### READ-01 验证（同一读路径）
- [ ] `readClipAudioForPlayback` 和 `readAudioSourceForARA` 共享相同的四级回退实现
- [ ] 两个 API 的差异仅在于参数适配层，核心逻辑完全相同
- [ ] 代码审查确认无独立的回退实现分支

### READ-02 验证（完整四级策略）
- [ ] ARA 路径支持 Level 1 (Resampled)
- [ ] ARA 路径支持 Level 2 (Rendered)
- [ ] ARA 路径支持 Level 3 (Dry)
- [ ] ARA 路径支持 Level 4 (Blank)
- [ ] 四级优先级顺序正确

### SAFE-01 验证（Standalone 守护）
- [ ] Standalone 构建通过
- [ ] Standalone 测试通过
- [ ] 无 Standalone 分支行为变更

### 自动化验证
```bash
cmake --build build --config Debug --target OpenTuneTests
.\build\Debug\OpenTuneTests.exe
cmake --build build --config Debug --target OpenTune_VST3
cmake --build build --config Debug --target OpenTune_Standalone
```
</verification>

<success_criteria>
1. ARA 与常规播放共享同一四级回退核心实现
2. `AudioSourceState` 具备 `renderCache` 字段
3. ARA 路径支持完整的四级回退策略
4. 所有自动化测试通过
5. Standalone 行为不变（SAFE-01）
</success_criteria>

<output>
After completion, create `.planning/phases/08-playback-read/08-04-SUMMARY.md`
</output>
