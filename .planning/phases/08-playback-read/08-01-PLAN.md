---
phase: 08-playback-read
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/Inference/RenderCache.h
  - Source/Inference/RenderCache.cpp
autonomous: true
requirements: [READ-01, READ-02]

must_haves:
  truths:
    - "常规播放改为调用 Processor 共享层统一 read API，不再内联第二套读取逻辑"
    - "统一读策略固定为 resampled -> rendered -> dry -> blank/silence"
    - "回退语义以 dry 优先，只有源无效/越界/不可读才进入 blank/silence"
  artifacts:
    - path: "Source/PluginProcessor.cpp"
      provides: "统一 Playback Read 核心实现与常规播放接线"
    - path: "Source/PluginProcessor.h"
      provides: "统一 read API 契约与调用边界"
  key_links:
    - from: "OpenTuneAudioProcessor::processBlock"
      to: "OpenTuneAudioProcessor::readClipAudioForPlayback (new unified API)"
      via: "single read-path call"
      pattern: "resampled -> rendered -> dry -> blank/silence"
---

<objective>
在 Processor 共享层建立统一 Playback Read 核心，并先将常规播放路径完整切换到该核心。

Purpose: 先收敛常规路径，消除 processBlock 内联分叉读取逻辑，为 ARA 接线提供唯一真相源。
Output: 统一 read API + 固定四级回退策略 + 常规播放单路径接线。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-playback-read/08-CONTEXT.md
@.planning/phases/07-控制与时间基线收敛/07-01-SUMMARY.md
@.planning/phases/07-控制与时间基线收敛/07-02-SUMMARY.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Inference/RenderCache.h
@Source/Inference/RenderCache.cpp
</context>

<interfaces>
From Source/PluginProcessor.cpp (target call site):
```cpp
void processBlock(juce::AudioBuffer<float>& buffer, juce::MidiBuffer& midiMessages) override;
```

Planned unified API (Processor shared layer):
```cpp
PlaybackReadResult readClipAudioForPlayback(const PlaybackReadRequest& request) const;
```

Required strategy order:
```txt
resampled cache -> rendered -> dry -> blank/silence
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义统一 read API 契约与结果语义</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: 给定同一 clip/time/rate，请求结构可以完整表达读取上下文。
    - Test 2: 返回结构可区分命中层级（resampled/rendered/dry/blank）与读取样本数。
  </behavior>
  <action>
    在 Processor 共享层引入统一 read 请求/结果结构与单入口函数，明确：
    - 输入至少包含 clip 句柄、读取起点时间、目标采样率、目标样本数；
    - 输出至少包含读取样本数、命中层级、是否 silence fallback；
    - API 不暴露 ARA 专有类型，保持 Standalone/VST3 共享可用。

    该任务只建立契约和最小骨架，不做路径切换。
  </action>
  <verify>
    <automated>rg -n "PlaybackRead(Request|Result)|readClipAudioForPlayback|enum class.*Read.*Layer" Source/PluginProcessor.h Source/PluginProcessor.cpp</automated>
  </verify>
  <done>统一 read API 契约落地，后续调用方可无分叉接入。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 在统一 API 内实现固定四级回退策略</name>
  <files>Source/PluginProcessor.cpp, Source/Inference/RenderCache.cpp</files>
  <behavior>
    - Test 1: resampled 命中时不再继续尝试 rendered/dry。
    - Test 2: rendered 命中失败且 dry 可读时走 dry，不进入 blank。
    - Test 3: 源无效/越界/不可读时输出 blank/silence，且无未定义噪声。
  </behavior>
  <action>
    在统一 API 中按固定顺序实现读取：
    1) 先读 resampled cache；
    2) 未命中则读 rendered cache；
    3) 仍未命中且源有效时读 dry；
    4) 最后才 blank/silence。

    统一每级失败判据与日志字段，禁止动态优先级、禁止旁路分叉。
  </action>
  <verify>
    <automated>rg -n "resampled|rendered|dry|blank|silence" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>四级回退策略在单函数内闭环，行为可审计。</done>
</task>

<task type="auto">
  <name>Task 3: 常规播放路径切换到统一 read API</name>
  <files>Source/PluginProcessor.cpp</files>
  <action>
    将 processBlock 中 clip 读取与混音逻辑改为调用统一 read API，移除旧的内联读取分支。

    保持混音语义与现有输出一致（不新增限幅/压缩），确保 Standalone 分支行为不变。
  </action>
  <verify>
    <automated>rg -n "readClipAudioForPlayback|processBlock\(" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>常规播放完成单路径接线，Processor 成为唯一读核提供方。</done>
</task>

</tasks>

<verification>
- 静态验证：常规播放代码路径只剩统一 read API 调用入口。
- 行为验证：当 rendered 缺失但 dry 存在时输出可持续，且不进入 blank。
- 守护验证：`JucePlugin_Build_Standalone` 分支无行为语义漂移。
</verification>

<success_criteria>
- [ ] 常规播放对 clip 读取只有一个入口（统一 read API）。
- [ ] 固定四级优先级可由日志/代码直接验证。
- [ ] READ-02 在常规播放侧成立（dry 优先于 blank）。
</success_criteria>

<output>
After completion, create `.planning/phases/08-playback-read/08-01-SUMMARY.md`
</output>
