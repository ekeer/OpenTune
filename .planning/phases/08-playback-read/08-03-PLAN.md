---
phase: 08-playback-read
plan: 03
type: verify
wave: 2
depends_on: [08-01, 08-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/08-playback-read/08-VERIFICATION.md
autonomous: true
requirements: [READ-01, READ-02]

must_haves:
  truths:
    - "READ-01/READ-02 必须有可执行自动化验证与可复现实机验证步骤"
    - "验证证据可追溯到统一读核命中层级日志"
    - "未获人工证据时不得误判 Phase 8 完成"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "统一读策略优先级与语义一致性测试"
    - path: ".planning/phases/08-playback-read/08-VERIFICATION.md"
      provides: "Phase 8 验证清单、结果与 gate 结论"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/08-playback-read/08-VERIFICATION.md"
      via: "Requirement Traceability"
      pattern: "READ-01|READ-02"
---

<objective>
建立并执行 Phase 8 的验证协议，确认统一读核在自动化与 DAW 场景下都满足 READ-01/READ-02。

Purpose: 在进入 Phase 9 前锁定播放读路径一致性，避免把双路径漂移风险带入生命周期绑定改造。
Output: 可审计的验证文档与测试证据。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/08-playback-read/08-CONTEXT.md
@Source/PluginProcessor.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为统一读策略优先级补充自动化测试</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: resampled 与 rendered 同时可用时，始终命中 resampled。
    - Test 2: rendered 可用但 resampled 不可用时，命中 rendered。
    - Test 3: rendered 不可用但 dry 可读时，命中 dry。
    - Test 4: 源无效/越界/不可读时，命中 blank/silence。
  </behavior>
  <action>
    在 TestMain 增加专门 section，构造可控输入场景验证四级回退顺序与命中层级。

    测试必须直接绑定统一 read API 返回语义，避免仅用日志字符串判定。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests; if ($?) { .\build\Debug\OpenTuneTests.exe }</automated>
  </verify>
  <done>READ-02 的固定优先级具备自动化回归保护。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 ARA/常规同语义一致性测试与验证脚本</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: 同一 clip/time/rate 输入下，ARA 与常规读取命中层级一致。
    - Test 2: 两路径读取样本数与空白回退语义一致。
  </behavior>
  <action>
    基于统一 read API 设计一致性测试夹具：
    - 以同一数据源驱动 ARA 映射后请求和常规请求；
    - 对比命中层级、readSamples、silence 标记。

    若当前测试框架无法直接模拟 ARA 宿主对象，需提供等价映射输入的测试替身并在文档注明边界。
  </action>
  <verify>
    <automated>rg -n "READ-01|playback read|read layer|ARA" Tests/TestMain.cpp</automated>
  </verify>
  <done>READ-01 的代码级一致性获得自动化证据。</done>
</task>

<task type="auto">
  <name>Task 3: 编写并执行 Phase 8 验证文档与 Gate 判定</name>
  <files>.planning/phases/08-playback-read/08-VERIFICATION.md</files>
  <action>
    编写验证文档并执行记录，至少包含：
    1) READ-01/READ-02 Traceability；
    2) 自动化命令、期望输出、失败判据；
    3) REAPER 手动流程（同 clip 同时刻对比 ARA/常规听感与语义）；
    4) 统一日志字段采集（clipId/time/readLayer/readSamples）；
    5) Gate 结论（PASS/human_needed/gaps_found）。

    若人工证据缺失，状态必须标记 `human_needed`，不得直接宣告完成。
  </action>
  <verify>
    <automated>rg -n "READ-01|READ-02|Traceability|REAPER|Gate|human_needed" .planning/phases/08-playback-read/08-VERIFICATION.md</automated>
  </verify>
  <done>Phase 8 验证协议和结论可复验、可审计。</done>
</task>

</tasks>

<verification>
- 自动化：OpenTuneTests 覆盖统一策略优先级与同语义一致性。
- 手动：REAPER 中完成 ARA/常规同场景对照验证。
- Gate：READ-01/READ-02 均有证据，且无 Standalone 回归。
</verification>

<success_criteria>
- [ ] READ-01 自动化一致性测试通过并留档。
- [ ] READ-02 固定四级优先级测试通过并留档。
- [ ] Phase 8 Verification 文档给出明确 Gate 结论。
</success_criteria>

<output>
After completion, create `.planning/phases/08-playback-read/08-03-SUMMARY.md`
</output>
