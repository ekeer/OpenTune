---
phase: 18-时间轴投影与 Standalone 守护
plan: 03
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - Source/Standalone/UI/ArrangementViewComponent.h
  - Source/Standalone/UI/ArrangementViewComponent.cpp
autonomous: true
requirements: [TIME-02, TIME-03]

must_haves:
  truths:
    - "ArrangementView 的 clip 范围必须来自 shared clip projection，而不是自己临时把 startSeconds + numSamples/44100 拼成另一套边界。"
    - "ArrangementView 的时间轴滚动/播放头像素换算必须和绘制路径使用同一套 converter 语义，不能一边画图走 TimeConverter，一边 auto-scroll 走手写 100*zoom 算法。"
  artifacts:
    - path: "Source/Standalone/UI/ArrangementViewComponent.cpp"
      provides: "sample-authoritative arrangement clip bounds and unified time-axis math"
      contains: "buildProjectedClipBounds"
---

<objective>
把 ArrangementView 的 clip range 和时间轴滚动换算统一到 shared clip projection + `TimeConverter`，消除当前绘制、hit test、auto-scroll 三处各自算时间的漂移风险。

Purpose: TIME-02/TIME-03 的关键是让 ArrangementView 真正只消费 sample 投影，而不是“部分路径已 sample 化，剩余路径继续沿用手写 seconds 像素公式”。

Output: `ArrangementViewComponent` 改为通过 shared projection 计算 clip bounds，并复用统一的时间换算路径。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-01-PLAN.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Standalone/UI/ArrangementViewComponent.h
@Source/Standalone/UI/ArrangementViewComponent.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 让 Arrangement clip bounds 改用 shared clip projection</name>
  <files>Source/Standalone/UI/ArrangementViewComponent.h, Source/Standalone/UI/ArrangementViewComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/ArrangementViewComponent.h
    - Source/Standalone/UI/ArrangementViewComponent.cpp
    - Source/PluginProcessor.h
  </read_first>
  <action>
    在 `ArrangementViewComponent` 中新增内部 helper（例如 `buildProjectedClipBounds(...)`），要求：
    - clip bounds 读取 `getClipTimelineProjectionById(...)`
    - clip width 只能来自 projection 的 `startSeconds/endSeconds`
    - `paint()`、`hitTestClip()`、其他依赖 clip bounds 的路径统一走这一 helper
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/ArrangementViewComponent.cpp` contains `buildProjectedClipBounds`
    - `Source/Standalone/UI/ArrangementViewComponent.cpp` contains `getClipTimelineProjectionById`
  </acceptance_criteria>
  <verify>
    <automated>rg "buildProjectedClipBounds|getClipTimelineProjectionById" Source/Standalone/UI/ArrangementViewComponent.cpp</automated>
  </verify>
  <done>
    Arrangement 的 clip 范围已经统一来自 shared clip projection。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 统一 ArrangementView 的时间轴换算与自动滚动路径</name>
  <files>Source/Standalone/UI/ArrangementViewComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/ArrangementViewComponent.cpp
    - Source/Standalone/UI/TimeConverter.h
  </read_first>
  <action>
    把 `updateAutoScroll()` 与 `onScrollVBlankCallback()` 里的手写 `100.0 * zoomLevel_` / `playheadTime * pixelsPerSecond` 路径收敛到与绘制相同的换算 helper（`timeToX()` / `xToTime()` / `TimeConverter`），避免时间轴显示与滚动使用不同像素基线。
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/ArrangementViewComponent.cpp` no longer needs raw `100.0 * zoomLevel_` inside `updateAutoScroll`
    - `Source/Standalone/UI/ArrangementViewComponent.cpp` no longer needs raw `100.0 * zoomLevel_` inside `onScrollVBlankCallback`
  </acceptance_criteria>
  <verify>
    <automated>rg "100\.0 \* zoomLevel_" Source/Standalone/UI/ArrangementViewComponent.cpp</automated>
  </verify>
  <done>
    Arrangement 的绘制、滚动和播放头像素换算已经收敛到同一条时间轴数学路径。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processor projection -> ArrangementView | ArrangementView 不能继续保留独立 clip 边界推导公式 |
| TimeConverter -> auto-scroll | 滚动与绘制必须共享同一条时间轴换算 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-05 | T | Arrangement clip bounds | mitigate | hit test / paint / bounds 统一读 shared projection helper。 |
| T-18-06 | T | Arrangement auto-scroll math | mitigate | 用 `timeToX()` / `TimeConverter` 统一滚动与绘制像素换算。 |
</threat_model>

<verification>
- Arrangement clip bounds 已经来自 shared projection。
- Arrangement auto-scroll 与绘制使用同一套时间轴换算。
</verification>

<success_criteria>
- [ ] TIME-02 的 Arrangement 路径完成收敛。
- [ ] TIME-03 在 Standalone arrangement timeline 路径上不再存在第二套基线。
</success_criteria>

<output>
After completion, create `.planning/phases/18-时间轴投影与 Standalone 守护/18-03-SUMMARY.md`
</output>
