---
phase: 18-时间轴投影与 Standalone 守护
plan: 02
type: execute
wave: 2
depends_on: [18-01]
files_modified:
  - Source/Standalone/UI/PianoRollComponent.h
  - Source/Standalone/UI/PianoRollComponent.cpp
autonomous: true
requirements: [TIME-01, TIME-03]

must_haves:
  truths:
    - "PianoRoll 的播放头、自动滚动和聚焦基线必须消费 shared-core 的 clip playhead projection，而不是直接拿 raw seconds 原子继续当事实源。"
    - "PianoRoll 的可见 duration/focus baseline 也必须来自 clip projection，不能再自己单独拼 buffer length。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "sample-authoritative piano roll playhead projection consumption"
      contains: "readProjectedPlayheadTime"
  key_links:
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Source/PluginProcessor.cpp"
      via: "clip playhead projection"
      pattern: "projectClipPlayheadById"
---

<objective>
把 PianoRoll 的播放头、自动滚动和可见时间基线收敛到 Phase 18 的 shared projection contract，让钢琴卷帘不再直接消费 raw absolute seconds。

Purpose: TIME-01 的核心不是把秒值换个名字，而是让 clip-local focus baseline 真正绑定到 sample truth，并在 clip 边界处停止继续漂移。

Output: `PianoRollComponent` 改为消费 `projectClipPlayheadById(...)` / `getClipTimelineProjectionById(...)`。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-01-PLAN.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 让 PianoRoll 读取 sample-authoritative playhead projection</name>
  <files>Source/Standalone/UI/PianoRollComponent.h, Source/Standalone/UI/PianoRollComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/PluginProcessor.h
  </read_first>
  <action>
    在 `PianoRollComponent` 中新增内部 helper（例如 `readProjectedPlayheadTime()`），要求：
    - 当存在 `processor_ + currentTrackId_ + currentClipId_` 时，优先调用 `projectClipPlayheadById(...)`
    - 返回值必须是投影后的 absolute seconds，而不是原始原子值
    - `updateAutoScroll()`、`onScrollVBlankCallback()`、播放头 overlay 更新都统一使用这个投影后的时间
    - clip 边界之外的 playhead 必须被 clamp 到 clip sample span 对应的时间
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `readProjectedPlayheadTime`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `projectClipPlayheadById`
  </acceptance_criteria>
  <verify>
    <automated>rg "readProjectedPlayheadTime|projectClipPlayheadById" Source/Standalone/UI/PianoRollComponent.cpp</automated>
  </verify>
  <done>
    钢琴卷帘的播放头与自动滚动不再直接消费 raw playhead seconds。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 让 PianoRoll 的可见 duration/focus baseline 复用 clip projection</name>
  <files>Source/Standalone/UI/PianoRollComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/PluginProcessor.h
  </read_first>
  <action>
    把 `fitToScreen()` 等基于 clip duration 的逻辑改成优先读取 shared `ClipTimelineProjection`：
    - clip duration 来自 projection 的 `durationSeconds`
    - 可见 baseline 与 `trackOffsetSeconds_` / clip projection 保持一致
    - 不能再在这些路径里单独用 `audioBuffer_->getNumSamples()/44100` 重新定义另一套 UI duration truth
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `getClipTimelineProjectionById`
  </acceptance_criteria>
  <verify>
    <automated>rg "getClipTimelineProjectionById" Source/Standalone/UI/PianoRollComponent.cpp</automated>
  </verify>
  <done>
    PianoRoll 的焦点时长基线已经和 shared core 统一到同一份 clip projection。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processor projection -> PianoRoll | 钢琴卷帘不能保留 raw seconds 与 sample projection 双轨基线 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-03 | T | PianoRoll auto-scroll | mitigate | auto-scroll / overlay 都统一读 projected playhead。 |
| T-18-04 | I | PianoRoll duration baseline | mitigate | fit/focus 时长统一走 ClipTimelineProjection。 |
</threat_model>

<verification>
- PianoRoll 已改用 shared playhead projection。
- PianoRoll duration/focus baseline 已改用 shared clip projection。
</verification>

<success_criteria>
- [ ] TIME-01 的 PianoRoll 消费路径完成收敛。
- [ ] TIME-03 在 PianoRoll 路径上不再存在第二套边界基线。
</success_criteria>

<output>
After completion, create `.planning/phases/18-时间轴投影与 Standalone 守护/18-02-SUMMARY.md`
</output>
