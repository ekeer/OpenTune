---
phase: 18-时间轴投影与 Standalone 守护
plan: 04
type: execute
wave: 3
depends_on: [18-02, 18-03]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
  - .planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03]

must_haves:
  truths:
    - "Phase 18 必须留下像 Phase 15/16/17 一样的真实自动化证据，不能只靠代码阅读判断 UI 时间轴已经收敛。"
    - "verification 文档必须把 TIME-01/02/03 逐条绑定到测试名或静态审计证据。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 18 regression coverage"
      contains: "runPhase18TimelineProjectionTests"
    - path: ".planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md"
      provides: "requirement traceability for TIME-01, TIME-02, TIME-03"
      contains: "TIME-01"
---

<objective>
为 Phase 18 补齐自动化回归与 verification traceability，证明 PianoRoll 与 ArrangementView 已经统一消费 sample->time projection，且 shared core / UI 不再使用不同边界基线。

Purpose: 没有自动化守护的话，后续 UI 维护极容易把 raw seconds 或手写像素换算重新带回时间轴路径。

Output: Phase 18 regression tests、真实执行结果、`18-VERIFICATION.md`。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/ArrangementViewComponent.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 为 Phase 18 时间轴投影补回归测试</name>
  <files>Tests/TestMain.cpp</files>
  <read_first>
    - Tests/TestMain.cpp
    - Source/PluginProcessor.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/ArrangementViewComponent.cpp
    - .planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 新增 `runPhase18TimelineProjectionTests()`，至少覆盖：
    - `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
    - `TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead`
    - `TIME_02_ClipTimelineProjectionUsesStoredSampleSpan`
    - `TIME_03_ClipPlayheadProjectionClampsToStoredSampleSpan`

    同时把新测试接入 `main()`。
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase18TimelineProjectionTests`
    - `Tests/TestMain.cpp` contains `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
    - `Tests/TestMain.cpp` contains `TIME_02_ClipTimelineProjectionUsesStoredSampleSpan`
    - `Tests/TestMain.cpp` contains `TIME_03_ClipPlayheadProjectionClampsToStoredSampleSpan`
  </acceptance_criteria>
  <verify>
    <automated>rg "runPhase18TimelineProjectionTests|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead|TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead|TIME_02_ClipTimelineProjectionUsesStoredSampleSpan|TIME_03_ClipPlayheadProjectionClampsToStoredSampleSpan" Tests/TestMain.cpp</automated>
  </verify>
  <done>
    Phase 18 的时间轴收敛语义已经有直接自动化覆盖。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 执行 Phase 18 验证并完成 traceability 文档</name>
  <files>.planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md, .planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    运行真实验证命令并回填文档：
    - 执行 L1/L2/L4/L6 命令，并把真实命令与结果写回 `18-TEST-VERIFICATION.md`
    - 新建 `18-VERIFICATION.md`，逐条映射 `TIME-01`、`TIME-02`、`TIME-03` 到具体测试名或静态审计证据
    - 明确记录 gate status 与最终结论
  </action>
  <acceptance_criteria>
    - `.planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md` contains `Passed`
    - `.planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md` contains `TIME-01`
    - `.planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md` contains `TIME-02`
    - `.planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md` contains `TIME-03`
    - `.planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md` contains `Gate status`
  </acceptance_criteria>
  <verify>
    <automated>rg "TIME-01|TIME-02|TIME-03|Gate status|Passed" .planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md .planning/phases/18-时间轴投影与 Standalone 守护/18-VERIFICATION.md</automated>
  </verify>
  <done>
    Phase 18 的测试证据和 requirement traceability 已经补齐，可以进入 phase summary / state update。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| requirements -> verification docs | TIME requirements 必须绑定到真实命令和测试名 |
| projection helpers -> regression tests | Phase 18 自动化必须直接覆盖 shared projection 语义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-07 | T | regression coverage | mitigate | 用 PianoRoll scroll + Processor projection helper 回归覆盖 TIME-01/02/03。 |
| T-18-08 | R | verification docs | mitigate | 记录真实命令、结果和 Gate status，阻止未验证先宣告完成。 |
</threat_model>

<verification>
- `OpenTuneTests` 必须包含 Phase 18 新增测试。
- `18-TEST-VERIFICATION.md` 必须记录真实执行命令与结果。
- `18-VERIFICATION.md` 必须把 TIME-01/02/03 对应到具体证据。
</verification>

<success_criteria>
- [ ] Phase 18 regression tests 已加入并执行。
- [ ] `18-TEST-VERIFICATION.md` 记录了真实 L1/L2/L4/L6 结果。
- [ ] `18-VERIFICATION.md` 完成 TIME-01、TIME-02、TIME-03 的 traceability。
</success_criteria>

<output>
After completion, create `.planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md`
</output>
