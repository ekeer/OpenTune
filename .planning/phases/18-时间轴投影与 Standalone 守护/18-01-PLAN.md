---
phase: 18-时间轴投影与 Standalone 守护
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [TIME-01, TIME-02, TIME-03]

must_haves:
  truths:
    - "Phase 18 必须先在 shared core 建立唯一的 UI 时间投影契约，不能让各个 UI 组件继续各自拿 startSeconds / numSamples / playhead seconds 即席拼边界。"
    - "clip sample span 的真实长度必须继续来自 44.1kHz stored audio sample count，并且所有 UI 时间值都只能是这份 sample truth 的投影。"
    - "Standalone 工作流保持不变；这里只允许收敛投影语义，不允许引入第二套时间模型或兼容层。"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "Phase 18 shared UI timeline projection contract"
      contains: "ClipTimelineProjection"
    - path: "Source/PluginProcessor.cpp"
      provides: "sample-authoritative clip/playhead projection helpers"
      contains: "projectClipPlayheadById"
  key_links:
    - from: "Source/PluginProcessor.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.cpp"
      via: "UI reads projected clip playhead instead of raw seconds truth"
      pattern: "projectClipPlayheadById"
    - from: "Source/PluginProcessor.cpp"
      to: "Source/Standalone/UI/ArrangementViewComponent.cpp"
      via: "Arrangement clip bounds read shared clip projection"
      pattern: "getClipTimelineProjectionById"
---

<objective>
先在 shared core 定义 Phase 18 的唯一时间投影契约：把 clip sample span、clip duration、clip timeline end、以及 clip-local playhead 投影都收敛到 Processor 的共享 helper，后续 UI 只能消费这些投影结果。

Purpose: 如果这里不先建立统一 contract，PianoRoll 与 Arrangement 仍会继续各自用 seconds/raw buffer length 算边界，TIME-01/02/03 只会变成表面修补。

Output: `ClipTimelineProjection` / `ClipPlayheadProjection` 结构，以及对应的 shared-core getter/helper。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-真实长度发布与缓存派生/17-02-SUMMARY.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 Processor 中定义 clip timeline projection 结构</name>
  <files>Source/PluginProcessor.h</files>
  <read_first>
    - Source/PluginProcessor.h
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    在 `Source/PluginProcessor.h` 中新增 Phase 18 的共享只读结构，至少包括：
    - `ClipTimelineProjection`
      - `clipId`
      - `startSeconds`
      - `endSeconds`
      - `durationSeconds`
      - `startSample`
      - `endSampleExclusive`
    - `ClipPlayheadProjection`
      - `requestedAbsoluteSeconds`
      - `projectedAbsoluteSeconds`
      - `clipRelativeSeconds`
      - `clipRelativeSample`
      - `clipEndSeconds`

    同时声明 shared-core API：
    - `getClipTimelineProjectionById(...)`
    - `getPluginClipTimelineProjection(...)`
    - `projectClipPlayheadById(...)`
    - `projectPluginClipPlayhead(...)`
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.h` contains `ClipTimelineProjection`
    - `Source/PluginProcessor.h` contains `ClipPlayheadProjection`
    - `Source/PluginProcessor.h` contains `getClipTimelineProjectionById`
    - `Source/PluginProcessor.h` contains `projectClipPlayheadById`
  </acceptance_criteria>
  <verify>
    <automated>rg "ClipTimelineProjection|ClipPlayheadProjection|getClipTimelineProjectionById|projectClipPlayheadById" Source/PluginProcessor.h</automated>
  </verify>
  <done>
    UI 时间投影 contract 已经从 shared core 暴露出来，不再是 UI 私有约定。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 实现 sample-authoritative clip/playhead projection helper</name>
  <files>Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.cpp
    - Source/PluginProcessor.h
  </read_first>
  <action>
    在 `Source/PluginProcessor.cpp` 中实现这些 helper，要求：
    - clip duration / endSeconds 必须从 stored-audio sample count 投影得到
    - clip-local playhead 必须先把 `absoluteSeconds - clip.startSeconds` 投影为 stored sample，再 clamp 到 `[0, clip sample count]`
    - `projectedAbsoluteSeconds` 必须来自 `clip.startSeconds + samplesToSeconds(clampedSample)`
    - plugin helper 只能复用 shared helper，不能复制一套逻辑
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.cpp` contains `getClipTimelineProjectionById`
    - `Source/PluginProcessor.cpp` contains `projectClipPlayheadById`
    - `Source/PluginProcessor.cpp` contains `projectPluginClipPlayhead`
  </acceptance_criteria>
  <verify>
    <automated>rg "getClipTimelineProjectionById|projectClipPlayheadById|projectPluginClipPlayhead" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>
    shared core 已能给 UI 提供唯一正确的 clip 边界投影与 playhead 投影。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| shared core -> UI timeline consumers | UI 不能各自重建 clip duration / clip-local playhead truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | T | UI timeline projection | mitigate | 统一从 Processor 暴露 clip timeline/playhead projection contract。 |
| T-18-02 | I | plugin helper path | mitigate | plugin path 只能复用 shared helper，不允许复制投影算法。 |
</threat_model>

<verification>
- shared-core projection structs/helpers 已存在。
- clip duration / clip-local playhead 都通过 sample->time 投影得到。
</verification>

<success_criteria>
- [ ] Processor 暴露 Phase 18 统一投影 contract。
- [ ] plugin / standalone 都能复用同一套 helper。
</success_criteria>

<output>
After completion, create `.planning/phases/18-时间轴投影与 Standalone 守护/18-01-SUMMARY.md`
</output>
