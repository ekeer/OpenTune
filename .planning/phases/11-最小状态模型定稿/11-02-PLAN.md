---
phase: 11-最小状态模型定稿
plan: 02
type: execute
wave: 1
depends_on: [11-01]
files_modified:
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
  - Source/ARA/OpenTunePlaybackRenderer.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [TRUTH-01, TRUTH-03, SAFE-03]

must_haves:
  truths:
    - "VST3/ARA 路径先解析 `clipId`，再读取 clip semantic state 与 playback source，不再把 `track 0` 当作业务主语。"
    - "mapping-only 更新保持同一 `clipId`，只更新映射/状态字段，不通过替换 clip 身份来逃避语义。"
    - "`audioSource` 与 `playbackRegion` 只能作为 ARA 输入来源，不能直接成为渲染与播放读取的真相源。"
  artifacts:
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "clip-centric VST3 同步与 mapping-only 处理路径"
      contains: "mappingOnly=true"
    - path: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      provides: "按 clip 语义读取 playback source 的 ARA renderer"
      contains: "binding.clipId"
    - path: "Source/PluginProcessor.h"
      provides: "对 VST3/ARA 暴露的 clip-centric 查询/同步 API"
      contains: "clipId"
  key_links:
    - from: "Source/Plugin/PluginEditor.cpp"
      to: "Source/PluginProcessor.h"
      via: "clip-centric semantic state queries"
      pattern: "ClipSemanticState"
    - from: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      to: "Source/PluginProcessor.cpp"
      via: "binding.clipId -> readPlaybackAudio"
      pattern: "binding.clipId"
---

<objective>
把 VST3/ARA 入口真正收敛为 clip 语义消费方，让 `track` / `audioSource` / `playbackRegion` 退回容器和输入来源，不再主导播放与同步语义。

Purpose: 满足 TRUTH-01 / TRUTH-03 / SAFE-03，把现有 `clipId` 基础从“多数地方使用”提升为“入口契约强制使用”。

Output: clip-centric VST3/ARA 消费路径、mapping-only 同步路径和 processor 对外接口收敛。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-最小状态模型定稿/11-RESEARCH.md
@.planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 让 VST3 editor 按 clip semantic state 驱动同步</name>
  <files>Source/Plugin/PluginEditor.h, Source/Plugin/PluginEditor.cpp, Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/Plugin/PluginEditor.h
    - Source/Plugin/PluginEditor.cpp
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - .planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
  </read_first>
  <action>
    重构 VST3 editor 的状态读取与同步辅助函数，使其先获得 `clipId` 和对应 `ClipSemanticState`，再决定是否做 mapping-only 更新、内容替换或诊断显示。

    具体要求：
    - 保留 `clipId` 作为 VST3 editor 中唯一活跃 clip 身份键
    - 新增并使用以下 processor clip-centric API：
      - `bool getPluginClipSemanticState(uint64_t clipId, ClipSemanticState& out) const;`
      - `bool getPluginPlaybackReadSource(uint64_t clipId, PlaybackReadSource& out) const;`
      - `bool setPluginClipStartSeconds(uint64_t clipId, double startSeconds);`
      - `bool replacePluginClipAudio(uint64_t clipId, std::shared_ptr<const juce::AudioBuffer<float>> audioBuffer, double startSeconds, std::vector<SilentGap> silentGaps);`
      - `bool enqueuePluginClipPartialRender(uint64_t clipId, double relStartSeconds, double relEndSeconds);`
    - 将诊断/heartbeat 输出改为明确打印 `clipId / renderRevision / mappingRevision / layoutEpoch / publishedRenderRevision`
    - `syncImportedAraClipIfNeeded()` 的 mapping-only 分支只调用 `setPluginClipStartSeconds(...)` 与 binding 更新，不得调用 `replacePluginClipAudio(...)`
    - `recordRequested()` / `syncClipToPianoRoll()` / `resolveCurrentClipId()` 所依赖的 processor 接口优先使用上述 clip-centric 查询
  </action>
  <acceptance_criteria>
    - `Source/Plugin/PluginEditor.cpp` contains `getPluginClipSemanticState(`
    - `Source/Plugin/PluginEditor.cpp` contains `setPluginClipStartSeconds(`
    - `Source/Plugin/PluginEditor.cpp` contains `mappingOnly=true`
    - `Source/Plugin/PluginEditor.cpp` contains `replacePluginClipAudio(`
    - `Source/Plugin/PluginEditor.cpp` contains `layoutEpoch`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 让 ARA playback renderer 只把 source/region 当作映射输入</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
  </read_first>
  <action>
    调整 ARA playback renderer 的读取路径，让 `audioSource` / `playbackRegion` 只负责给出映射输入，真正的播放读取始终经由 `binding.clipId` 和 clip-centric processor API 完成。

    具体要求：
    - `binding.clipId` 是 renderer 进入 playback read 的唯一身份键
    - `mappedSourceTime` 只作为定位 clip 内读取起点的输入值
    - renderer 通过 `getPluginPlaybackReadSource(binding.clipId, source)` 获得读取源，不再传入固定 track 常量
    - renderer 不直接从 `AudioSourceState::audioBuffer` 决定业务语义，只允许它作为 ARA 输入来源与 fallback dry 所需底层数据来源
    - 删除 `constexpr int kAraPluginTrackId = 0;`
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` contains `binding.clipId`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` contains `getPluginPlaybackReadSource(`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `constexpr int kAraPluginTrackId`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 缩小 processor 对外暴露的 track-centric 语义面</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-最小状态模型定稿/11-RESEARCH.md
  </read_first>
  <action>
    在 processor 对 VST3/ARA 路径暴露的接口层中，把 clip-centric API 提升为默认语义入口，并把 track-centric API 明确限制为容器实现细节或 Standalone 多轨调用面。

    具体要求：
    - 为 VST3/ARA 路径新增并实现以下入口：
      - `bool getPluginClipSemanticState(uint64_t clipId, ClipSemanticState& out) const;`
      - `bool getPluginPlaybackReadSource(uint64_t clipId, PlaybackReadSource& out) const;`
      - `bool setPluginClipStartSeconds(uint64_t clipId, double startSeconds);`
      - `bool replacePluginClipAudio(uint64_t clipId, std::shared_ptr<const juce::AudioBuffer<float>> audioBuffer, double startSeconds, std::vector<SilentGap> silentGaps);`
      - `bool enqueuePluginClipPartialRender(uint64_t clipId, double relStartSeconds, double relEndSeconds);`
    - 保留现有多轨 API 供 Standalone 使用，但在注释与命名上明确其容器性质
    - 不新增第二套并行状态模型；所有新接口都必须回到同一 `tracks_` / `TrackState::AudioClip` 数据源
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.h` contains `bool getPluginClipSemanticState(`
    - `Source/PluginProcessor.h` contains `bool getPluginPlaybackReadSource(`
    - `Source/PluginProcessor.h` contains `bool setPluginClipStartSeconds(`
    - `Source/PluginProcessor.h` contains `bool replacePluginClipAudio(`
    - `Source/PluginProcessor.h` contains `bool enqueuePluginClipPartialRender(`
    - `Source/PluginProcessor.h` still keeps multi-track APIs marked as deprecated or container-oriented where appropriate
    - `Source/PluginProcessor.cpp` resolves clip semantic state from the existing `tracks_` storage
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Run L2-L4 checks from `11-TEST-VERIFICATION.md` after the clip-centric API and renderer/editor changes land.
- Confirm mapping-only operations do not call `replaceClipAudioById` on the path that only changes mapping/start offset.
- Confirm ARA playback still routes through `binding.clipId -> processor -> readPlaybackAudio()`.
</verification>

<success_criteria>
- [ ] VST3 editor and ARA renderer consume clip semantic state via `clipId`.
- [ ] Mapping-only updates keep the same `clipId` and avoid clip replacement.
- [ ] `track` remains an internal processor container concern rather than a VST3/ARA semantic actor.
</success_criteria>

<output>
After completion, create `.planning/phases/11-最小状态模型定稿/11-02-SUMMARY.md`
</output>
