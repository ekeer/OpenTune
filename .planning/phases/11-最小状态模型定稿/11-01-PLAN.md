---
phase: 11-最小状态模型定稿
plan: 01
type: execute
wave: 1
depends_on: [10-03]
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - .planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
autonomous: true
requirements: [TRUTH-01, TRUTH-03, SAFE-03]

must_haves:
  truths:
    - "共享 clip 语义层显式定义 `clipId / renderRevision / mappingRevision / layoutEpoch`，而不是继续只靠 ARA/source-side revision 和 RenderCache chunk 状态隐式表达。"
    - "Phase 11 只冻结状态模型与读取边界，不重写旧 `RenderCache / pending / revision` 队列本质。"
    - "VST3/ARA 下游可按 `clipId` 读取最小状态模型；`track` 仍可作为 processor 内部容器，但不再是对外语义主角。"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "clip-centric 最小状态模型定义与查询接口"
      contains: "struct ClipSemanticState"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "ARA binding 与 clip semantic state 的接缝定义"
      contains: "layoutEpoch"
    - path: ".planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md"
      provides: "Phase 11 冻结后的六抽象职责文档"
      contains: "RenderTask"
  key_links:
    - from: "Source/PluginProcessor.h"
      to: "Source/Inference/RenderCache.h"
      via: "ClipSemanticState 与 RenderCache 职责边界"
      pattern: "renderRevision"
    - from: "Source/ARA/OpenTuneDocumentController.h"
      to: "Source/PluginProcessor.h"
      via: "ARA source-side revisions -> clip semantic state bridge"
      pattern: "mappingRevision"
---

<objective>
冻结共享 clip 最小状态模型，让 `clipId / renderRevision / mappingRevision / layoutEpoch / RenderCache / RenderTask` 在代码与文档中都有明确归属。

Purpose: 在不偷跑旧队列重构的前提下，先把 clip 语义层定死，避免后续 Phase 12 / 13 继续混用 source-side revision、track 0 约定和 RenderCache chunk revision。

Output: 共享状态模型定义、processor 查询接口、ARA 接缝字段、状态模型文档。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-最小状态模型定稿/11-RESEARCH.md
@.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/Inference/RenderCache.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在共享 core 定义 clip 最小状态模型</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - Source/Inference/RenderCache.h
    - .planning/PROJECT.md
    - .planning/phases/11-最小状态模型定稿/11-RESEARCH.md
  </read_first>
  <action>
    在 `Source/PluginProcessor.h` 增加显式 clip 语义结构 `ClipSemanticState`，字段至少包含：
    - `uint64_t clipId`
    - `uint64_t renderRevision`
    - `uint64_t mappingRevision`
    - `uint64_t layoutEpoch`
    - `uint64_t publishedRenderRevision`
    - `bool hasRenderCache`

    同时在 `TrackState::AudioClip` 中加入最小状态模型所需的 clip-side 字段：
    - `mappingRevision`
    - `renderRevision`
    - `layoutEpoch`

    初始化要求：
    - 新插入 clip 时这三个字段从 `0` 起始
    - 现有 `RenderCache` chunk `desiredRevision / publishedRevision` 暂不重写；Phase 11 仅把它们通过 `ClipSemanticState` 显式映射出来

    在 `Source/PluginProcessor.h/.cpp` 增加以下只读查询接口与实现：
    - `bool getClipSemanticStateById(int trackId, uint64_t clipId, ClipSemanticState& out) const;`
    - `bool getPluginClipSemanticState(uint64_t clipId, ClipSemanticState& out) const;`

    映射要求：
    - `out.renderRevision = clip.renderRevision`
    - `out.mappingRevision = clip.mappingRevision`
    - `out.layoutEpoch = clip.layoutEpoch`
    - `out.publishedRenderRevision = renderState.maxPublishedRevision`
    - `out.hasRenderCache = (clip.renderCache != nullptr)`
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.h` contains `struct ClipSemanticState`
    - `Source/PluginProcessor.h` contains `uint64_t renderRevision` within clip semantic state or clip storage
    - `Source/PluginProcessor.h` contains `uint64_t mappingRevision` within clip semantic state or clip storage
    - `Source/PluginProcessor.h` contains `uint64_t layoutEpoch` within clip semantic state or clip storage
    - `Source/PluginProcessor.h` contains `bool getClipSemanticStateById`
    - `Source/PluginProcessor.h` contains `bool getPluginClipSemanticState`
    - `Source/PluginProcessor.cpp` contains `out.publishedRenderRevision = renderState.maxPublishedRevision`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 收敛 ARA 侧 binding 与 clip semantic state 的接缝</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp, Source/PluginProcessor.h</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/PluginProcessor.h
    - .planning/phases/09-lifecycle-binding/09-01-SUMMARY.md
    - .planning/phases/11-最小状态模型定稿/11-RESEARCH.md
  </read_first>
  <action>
    调整 `AudioSourceClipBinding` 与 ARA source sync 元数据，使其只承担“ARA 输入如何定位到 clip semantic state”的桥接职责，而不是继续暗含第二套状态模型。

    具体要求：
    - 在 `AudioSourceClipBinding` 中补入 `layoutEpoch` 快照字段
    - 在头文件注释中明确 `contentRevision / mappingRevision` 仍属于 ARA/source 输入层；`renderRevision / layoutEpoch` 属于 clip 语义层
    - 在 `OpenTuneDocumentController.cpp` 的注册/更新路径中同步该字段，但不要在本 phase 改写旧 render queue 调度
    - 保持 `findAudioSourceByClipId()` 和 binding registry 仍以 `clipId` 为唯一连接键
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.h` contains `layoutEpoch`
    - `Source/ARA/OpenTuneDocumentController.h` contains `contentRevision / mappingRevision` and `renderRevision / layoutEpoch` ownership note
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `binding.layoutEpoch =`
    - `Source/ARA/OpenTuneDocumentController.cpp` still contains `findAudioSourceByClipId`
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 冻结六个最小抽象的职责文档</name>
  <files>.planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md</files>
  <read_first>
    - .planning/PROJECT.md
    - .planning/ROADMAP.md
    - .planning/REQUIREMENTS.md
    - .planning/phases/11-最小状态模型定稿/11-RESEARCH.md
    - Source/PluginProcessor.h
    - Source/Inference/RenderCache.h
  </read_first>
  <action>
    创建 `11-STATE-MODEL.md`，明确写出六个抽象及其代码归属与禁止事项：
    - `clipId`：唯一身份键；ARA 播放、同步、渲染都必须先解析到它
    - `renderRevision`：clip-side 渲染语义版本；Phase 11 只定义字段与读取边界，不直接重写旧队列
    - `mappingRevision`：只表达映射变化；不进入旧 revision queue
    - `layoutEpoch`：表达 ARA 输入/布局换代；Phase 11 先冻结字段与职责
    - `RenderCache`：继续负责旧 chunk 队列与缓存，不承担 mapping 真相
    - `RenderTask`：定义 future contract 至少携带 `clipId + renderRevision + layoutEpoch + range`

    文档必须同时写明以下禁止事项：
    - 不允许 `track 0` 作为 VST3 clip 语义主角
    - 不允许把 `audioSourceId` 或 `playbackRegionId` 提升为渲染链路真相源
    - 不允许在 Phase 11 引入第二套队列、兼容层或并行状态模型
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md` exists
    - `11-STATE-MODEL.md` contains `clipId`
    - `11-STATE-MODEL.md` contains `renderRevision`
    - `11-STATE-MODEL.md` contains `mappingRevision`
    - `11-STATE-MODEL.md` contains `layoutEpoch`
    - `11-STATE-MODEL.md` contains `RenderCache`
    - `11-STATE-MODEL.md` contains `RenderTask`
    - `11-STATE-MODEL.md` contains `track 0`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Execute L0 and L4 checks from `11-TEST-VERIFICATION.md` to confirm the state model and contract doc exist with the required symbols.
- Build `OpenTuneTests`, `OpenTune_Standalone`, and `OpenTune_VST3` once state-model fields and APIs are added.
- Confirm no new queue implementation or second scheduler was introduced while adding the semantic state model.
</verification>

<success_criteria>
- [ ] `ClipSemanticState` exists and is queryable by `clipId`.
- [ ] `TrackState::AudioClip` explicitly stores clip-side `mappingRevision`, `renderRevision`, and `layoutEpoch`.
- [ ] `AudioSourceClipBinding` bridges into the clip semantic state without becoming a second state model.
- [ ] `11-STATE-MODEL.md` freezes the six abstractions and prohibited semantics.
</success_criteria>

<output>
After completion, create `.planning/phases/11-最小状态模型定稿/11-01-SUMMARY.md`
</output>
