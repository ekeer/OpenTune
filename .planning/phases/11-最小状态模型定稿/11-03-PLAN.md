---
phase: 11-最小状态模型定稿
plan: 03
type: execute
wave: 2
depends_on: [11-01, 11-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
  - .planning/phases/11-最小状态模型定稿/11-VERIFICATION.md
autonomous: true
requirements: [TRUTH-01, TRUTH-03, SAFE-03]

must_haves:
  truths:
    - "TRUTH-01 / TRUTH-03 / SAFE-03 必须有直接自动化证据，不只靠代码阅读。"
    - "Phase 11 的验证以 clip identity、mapping-only 语义和 track 语义退位为核心，不把 Phase 13 的 queue 行为提前当作本 phase 验收条件。"
    - "L5 仍需用户确认 REAPER/Standalone 手动验证，不能自动标 PASS。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 11 invariant tests"
      contains: "TRUTH_01"
    - path: ".planning/phases/11-最小状态模型定稿/11-VERIFICATION.md"
      provides: "Phase 11 traceability and gate decision"
      contains: "TRUTH-01"
    - path: ".planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md"
      provides: "Phase 11 executable verification source of truth"
      contains: "L5 Manual / Human Verification"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/PluginProcessor.h"
      via: "ClipSemanticState invariants"
      pattern: "ClipSemanticState"
    - from: ".planning/phases/11-最小状态模型定稿/11-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "traceability matrix"
      pattern: "SAFE-03"
---

<objective>
建立 Phase 11 的自动化验证与 phase 证据闭环，证明 clip-centric 最小状态模型已经成立，并把手动验证 gate 显式保留给用户确认。

Purpose: 避免 Phase 11 只停留在“接口存在”的层面，确保 TRUTH-01 / TRUTH-03 / SAFE-03 都有直接证据。

Output: Phase 11 invariant tests、验证报告、traceability matrix。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
@.planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
@Tests/TestMain.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Plugin/PluginEditor.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 Phase 11 增加最小状态模型不变量测试</name>
  <files>Tests/TestMain.cpp</files>
  <read_first>
    - Tests/TestMain.cpp
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - Source/Plugin/PluginEditor.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - .planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 增加 Phase 11 测试段，至少覆盖以下四类不变量：
    - `TRUTH_01_ClipIdentityIsSingleTruthAcrossAraBindingAndPlaybackRead`
    - `TRUTH_03_MappingOnlyChangePreservesClipId`
    - `SAFE_03_Vst3SemanticStateDoesNotRequireTrackZero`
    - `PHASE11_MinimalStateModelFieldsAreReadableByClipId`

    每个测试都必须直接断言以下可观测条件之一：
    - 同一 ARA 绑定与 playback read source 指向同一 `clipId`
    - mapping-only 变化前后 `clipId` 不变，pending/idle chunk 统计不因纯映射变化而改变
    - VST3/ARA 路径可以通过 clip-centric API 完成状态查询，而不是必须先依赖 `trackId == 0`
    - `ClipSemanticState` 中的 `renderRevision / mappingRevision / layoutEpoch` 可按 `clipId` 读取
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `TRUTH_01_ClipIdentityIsSingleTruthAcrossAraBindingAndPlaybackRead`
    - `Tests/TestMain.cpp` contains `TRUTH_03_MappingOnlyChangePreservesClipId`
    - `Tests/TestMain.cpp` contains `SAFE_03_Vst3SemanticStateDoesNotRequireTrackZero`
    - `Tests/TestMain.cpp` contains `PHASE11_MinimalStateModelFieldsAreReadableByClipId`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: 执行并记录 Phase 11 自动化验证</name>
  <files>.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
  </read_first>
  <action>
    按 `11-TEST-VERIFICATION.md` 依次执行 L1-L4 与 L6：
    - `cmake --build build --target OpenTuneTests --config Release`
    - `cmake --build build --target OpenTune_Standalone --config Release`
    - `cmake --build build --target OpenTune_VST3 --config Release`
    - `.\build\Release\OpenTuneTests.exe`

    然后把每一级结果写回 `11-TEST-VERIFICATION.md`，用 `Passed / Failed / Pending user confirmation` 明确记录状态与证据。
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md` contains `Status: Passed` or `Status: Pending user/manual execution`
    - `.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md` records build commands for `OpenTuneTests`, `OpenTune_Standalone`, and `OpenTune_VST3`
    - `.planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md` records `OpenTuneTests.exe`
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: 生成 Phase 11 verification report 与 traceability</name>
  <files>.planning/phases/11-最小状态模型定稿/11-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/11-最小状态模型定稿/11-TEST-VERIFICATION.md
    - .planning/phases/11-最小状态模型定稿/11-STATE-MODEL.md
    - .planning/phases/10-state-visualization-consistency-guard/10-VERIFICATION.md
  </read_first>
  <action>
    创建 `11-VERIFICATION.md`，至少包含：
    - Executive Summary
    - Requirement Traceability 表，逐条覆盖 `TRUTH-01 / TRUTH-03 / SAFE-03`
    - 对 `clipId` 单一真相源、mapping-only 语义、track 语义退位的证据列表
    - 自动化结果与 L5 手动验证 checkpoint
    - 最终 gate decision：`PASS / MANUAL VERIFICATION PENDING / FAIL`

    其中 traceability 表必须明确引用测试名、源码路径或执行命令，而不是写抽象描述。
  </action>
  <acceptance_criteria>
    - `.planning/phases/11-最小状态模型定稿/11-VERIFICATION.md` exists
    - `11-VERIFICATION.md` contains `TRUTH-01`
    - `11-VERIFICATION.md` contains `TRUTH-03`
    - `11-VERIFICATION.md` contains `SAFE-03`
    - `11-VERIFICATION.md` contains `Gate Decision:`
    - `11-VERIFICATION.md` contains `MANUAL VERIFICATION PENDING` or `PASS` or `FAIL`
  </acceptance_criteria>
</task>

</tasks>

<verification>
- Use `11-TEST-VERIFICATION.md` as the execution source of truth.
- All blocking automated levels must pass before claiming Phase 11 automation complete.
- L5 must remain pending until the user confirms the REAPER / Standalone manual check result.
</verification>

<success_criteria>
- [ ] Phase 11 invariant tests exist and run in `OpenTuneTests.exe`.
- [ ] `11-TEST-VERIFICATION.md` records actual execution results for L1-L4 and L6.
- [ ] `11-VERIFICATION.md` provides traceability for TRUTH-01, TRUTH-03, SAFE-03.
- [ ] Manual checkpoint is explicit and not auto-closed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-最小状态模型定稿/11-03-SUMMARY.md`
</output>
