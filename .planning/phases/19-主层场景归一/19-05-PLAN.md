---
phase: 19-主层场景归一
plan: 05
type: execute
wave: 5
depends_on: [19-04]
files_modified:
  - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  - .planning/phases/19-主层场景归一/19-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: true
requirements: [LAYER-01, LAYER-02]

must_haves:
  truths:
    - "Phase 19 gap closure 不能只把源码改对，还必须留下 fresh build + fresh regression 的直接证据。"
    - "验证必须以新配置目录为准，避免继续复用 stale `build/` 产物把旧结论误当成新证据。"
    - "`ROADMAP` 与 `STATE` 的 phase 状态必须回写真实 gate 结果，不能再保留与 live tree 冲突的完成表述。"
  artifacts:
    - path: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      provides: "fresh L1/L2/L4/L6 evidence"
      contains: "Phase 19: Main Layer Scene Tests"
    - path: ".planning/phases/19-主层场景归一/19-VERIFICATION.md"
      provides: "requirement traceability for LAYER-01 and LAYER-02"
      contains: "Gate status: PASS"
    - path: ".planning/ROADMAP.md"
      provides: "phase 19 status aligned with actual gate state"
      contains: "Phase 19"
  key_links:
    - from: ".planning/phases/19-主层场景归一/19-VERIFICATION.md"
      to: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      via: "verification 文档回填真实命令与 gate 结论"
      pattern: "Gate status"
    - from: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "fresh binary 必须实际执行 `runPhase19MainLayerSceneTests()`"
      pattern: "Phase 19: Main Layer Scene Tests"
---

<objective>
在 Phase 19 代码 closure 之后，用 fresh build dir 重建 `OpenTuneTests`、执行 L1/L2/L4/L6，并把 verification / roadmap / state 全部对齐到真实 gate 结果。

Purpose: 当前最大的流程风险不是只有代码没改完，而是 stale 二进制、旧构建缓存和过时文档一起制造了错误结论。gap closure 只有在 fresh binary 真正执行了 `Phase 19: Main Layer Scene Tests` 之后才算可关闭。

Output: fresh Phase 19 证据闭环，以及与证据一致的 planning 状态文件。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@.planning/phases/19-主层场景归一/19-VERIFICATION.md
@.planning/phases/19-主层场景归一/19-04-PLAN.md
@Tests/TestMain.cpp
@CMakeLists.txt

<interfaces>
From `19-TEST-VERIFICATION.md`:
```text
L1 -> build OpenTuneTests
L2 -> run OpenTuneTests.exe and inspect Phase 19 regressions
L4 -> static audit for overlay removal and drawPlayhead wiring
L6 -> rerun full suite
```

From `Tests/TestMain.cpp`:
```cpp
runPhase19MainLayerSceneTests()
LAYER_01_PianoRollHasNoPlayheadOverlayChild
LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval
LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 用 fresh build dir 执行 L1/L2/L4/L6 并回填验证证据</name>
  <files>.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - CMakeLists.txt
    - Tests/TestMain.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
  </read_first>
  <action>
    严格按照 `19-TEST-VERIFICATION.md` 执行并回填真实结果，但必须先切换到 fresh 配置目录，避免继续复用 stale `build/`：
    - 使用新的构建目录，例如 `build-phase19-gap`，运行 L1：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase19-gap -Target OpenTuneTests`
    - 运行 L2：`& ".\build-phase19-gap\OpenTuneTests.exe"`，记录 `Phase 19: Main Layer Scene Tests`、三个 `LAYER_*` 用例、以及两个 `TIME_01_*` 用例的结果
    - 运行 L4：静态审计 `PianoRollComponent.h/cpp` 不再包含 `PlayheadOverlayComponent` / `playheadOverlay_`，并确认 `drawPlayhead` 存在于 renderer 与 component wiring
    - 运行 L6：再次执行 `& ".\build-phase19-gap\OpenTuneTests.exe"`
    - 把四个 level 的 `Status` 更新为真实 `Passed` / `Failed`，禁止把环境失败写成跳过
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `Passed` or `Failed`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `build-phase19-gap`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `Phase 19: Main Layer Scene Tests`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "Passed|Failed|build-phase19-gap|Phase 19: Main Layer Scene Tests|LAYER_01_PianoRollHasNoPlayheadOverlayChild|LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval|LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead|TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead" ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 19 的四级自动化 gate 已有 fresh build 证据，不再依赖旧 `build/OpenTuneTests.exe` 的历史状态。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 回写 requirement traceability 与 phase 状态文件</name>
  <files>.planning/phases/19-主层场景归一/19-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - .planning/phases/19-主层场景归一/19-VERIFICATION.md
  </read_first>
  <action>
    把 verification 与状态文档全部改成与 fresh gate 一致：
    - `19-VERIFICATION.md` 逐条绑定 `LAYER-01` / `LAYER-02` 到 fresh L2/L4/L6 证据；若全部通过则写 `Gate status: PASS`，否则写真实失败原因
    - `ROADMAP.md` 把 Phase 19 的 `Plans` 列表、progress 表和 phase 状态更新到真实执行状态；如果 gap closure 全过，则 Phase 19 可回到 completed，否则保持 gap closure in progress
    - `STATE.md` 更新 `status`、`last_activity`、`stopped_at` 与 `next step`，不再停留在 "next step is /gsd-plan-phase 19 --gaps"
    - 如果 L1 仍被环境阻断，必须明确记录阻断点是 fresh configure 还是 fresh build，而不是继续沿用旧 onnx 路径报错结论
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `Gate status:`
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `.planning/ROADMAP.md` contains `19-04-PLAN.md`
    - `.planning/ROADMAP.md` contains `19-05-PLAN.md`
    - `.planning/STATE.md` does not contain `/gsd-plan-phase 19 --gaps`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "Gate status:|LAYER_01_PianoRollHasNoPlayheadOverlayChild|19-04-PLAN.md|19-05-PLAN.md|build-phase19-gap" ".planning/phases/19-主层场景归一/19-VERIFICATION.md" ".planning/ROADMAP.md" ".planning/STATE.md"</automated>
  </verify>
  <done>
    Phase 19 的 requirements、roadmap 和 state 已重新回到同一份真实证据上，不再出现 summary 说完成、live tree 却没完成的分裂状态。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stale build dir -> gate decision | 复用旧二进制会把历史测试结果误当成当前代码证据 |
| verification docs -> roadmap/state | 如果状态文件不回写 fresh gate 结果，Phase 20 之前仍会建立在错误事实之上 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-12 | R | build evidence | mitigate | 使用 fresh `build-phase19-gap` 执行 L1/L2/L6，不再复用旧 `build/` 二进制。 |
| T-19-13 | R | requirement traceability | mitigate | `19-VERIFICATION.md` 逐条绑定 `LAYER-01` / `LAYER-02` 到 fresh 证据，避免后续 phase 靠口头解释。 |
| T-19-14 | R | roadmap/state truth | mitigate | `ROADMAP.md` 与 `STATE.md` 必须同步更新到真实 gate 状态，清除过时的 gap-planning 下一步描述。 |
</threat_model>

<verification>
- `19-TEST-VERIFICATION.md` 已记录真实 fresh L1 / L2 / L4 / L6 结果。
- `19-VERIFICATION.md` 已把 `LAYER-01` / `LAYER-02` 映射到 fresh 证据。
- `ROADMAP.md` 与 `STATE.md` 已不再停留在 pre-gap-plan 状态。
</verification>

<success_criteria>
- [ ] Phase 19 regressions 已在 fresh binary 中实际执行。
- [ ] `LAYER-01` / `LAYER-02` 已有 fresh requirement-level traceability。
- [ ] `ROADMAP` / `STATE` 与真实 gate 状态一致。
</success_criteria>

<output>
After completion, create `.planning/phases/19-主层场景归一/19-05-SUMMARY.md`
</output>
