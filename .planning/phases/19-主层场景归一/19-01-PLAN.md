---
phase: 19-主层场景归一
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
autonomous: true
requirements: [LAYER-01, LAYER-02]

must_haves:
  truths:
    - "实现前先有自动化回归契约，明确 PianoRoll 不允许再依赖播放头 overlay 子层。"
    - "Phase 19 的回归测试先定义 no-overlay child、continuous scroll、page scroll 三条行为事实。"
    - "Phase 18 的 projected playhead scroll 回归必须继续作为 Phase 19 的既有守护。"
  artifacts:
    - path: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      provides: "Phase 19 verification source of truth"
      contains: "LAYER_01_PianoRollHasNoPlayheadOverlayChild"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 19 main-layer scene regressions"
      contains: "runPhase19MainLayerSceneTests"
  key_links:
    - from: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "verification 文档列出必须先写出的 Phase 19 测试名与命令"
      pattern: "runPhase19MainLayerSceneTests"
    - from: "Tests/TestMain.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.h"
      via: "测试直接实例化现有 PianoRoll 接口并验证 single-host 目标"
      pattern: "onScrollVBlankCallback"
---

<objective>
先把 Phase 19 的测试验证契约和 failing regressions 固定下来，再进入主层场景归一实现。Phase 19 的执行顺序必须遵守 test-driven-spec：先定义验证，再改实现。

Purpose: 如果 Phase 19 不先把 `LAYER-01` / `LAYER-02` 的回归测试写出来，后续执行很容易在删 overlay 的同时误伤 projected playhead scroll 行为，或者把旧双层假设偷偷带回去。

Output: 更新后的 `19-TEST-VERIFICATION.md` 与 `Tests/TestMain.cpp` Phase 19 主层场景回归入口。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/VST3Merge.md
@docs/plans/2026-04-14-pianoroll-refresh-implementation-plan.md
@docs/plans/2026-04-14-pianoroll-refresh-test-verification.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-02-SUMMARY.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PlayheadOverlayComponent.h

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
void setProcessor(OpenTuneAudioProcessor* processor);
void setCurrentClipContext(int trackId, uint64_t clipId);
void setTrackTimeOffset(double offsetSeconds);
void setPlayheadPositionSource(std::weak_ptr<std::atomic<double>> source);
void setScrollMode(ScrollMode mode);
void setZoomLevel(double zoom);
void setIsPlaying(bool playing);
int getScrollOffset() const;
void onScrollVBlankCallback(double timestampSec);
```

From `Tests/TestMain.cpp` (Phase 18 guards that must stay green):
```cpp
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 固化 Phase 19 test-verification 契约</name>
  <files>.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - docs/plans/2026-04-14-pianoroll-refresh-test-verification.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/18-时间轴投影与 Standalone 守护/18-TEST-VERIFICATION.md
  </read_first>
  <action>
    更新 `19-TEST-VERIFICATION.md`，把 Phase 19 的验证源固定成明确可执行的四级 gate：
    - L1 命令保持为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build -Target OpenTuneTests`
    - L2 命令保持为 `& ".\build\OpenTuneTests.exe"`
    - L4 静态审计必须点名 `playheadOverlay_`、`PlayheadOverlayComponent`、`drawPlayhead`、`runPhase19MainLayerSceneTests`
    - L6 命令保持为 `& ".\build\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：`LAYER_01_PianoRollHasNoPlayheadOverlayChild`、`LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval`、`LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval`，并继续列出 `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead` 与 `TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead`
    - `Evidence To Capture` 必须明确：`PianoRollComponent` 不再包含 `PlayheadOverlayComponent`；`PianoRollRenderer` 暴露 `drawPlayhead(...)`；`paint()` 在内容区 clip 内画播放头；continuous/page scroll 保持 projected playhead 语义
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `drawPlayhead`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "LAYER_01_PianoRollHasNoPlayheadOverlayChild|LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval|LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead|TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead|drawPlayhead" ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 19 已有先于实现存在的验证契约，执行者不需要再猜应该证明什么。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写 Phase 19 主层场景 failing regressions</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `LAYER_01_PianoRollHasNoPlayheadOverlayChild` 证明当前 PianoRoll 子组件树里不允许再出现 `PlayheadOverlayComponent`
    - Test 2: `LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval` 证明删掉 overlay 后 continuous scroll 仍跟随 projected clip playhead
    - Test 3: `LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval` 证明 viewport/page scroll 不需要第二层 bounds 同步也继续正确
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PlayheadOverlayComponent.h
    - .planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase19MainLayerSceneTests()` 并接入现有测试主流程。测试内容必须具体如下：
    - `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
      - `#include "Standalone/UI/PlayheadOverlayComponent.h"`
      - 用 desktop-attached `juce::Component host` 创建 `PianoRollComponent`
      - 遍历 `pianoRoll.getChildren()`；如果任何 child `dynamic_cast<PlayheadOverlayComponent*>` 非空则失败
    - `LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
      - 复用 Phase 18 continuous scroll 场景：`trackId = 0`、`clipId = 190101`、`startSeconds = 10.0`、`audioBuffer = 1 x 44100` samples、`processor.setPosition(12.0)`
      - 设置 `setProcessor(&processor)`、`setCurrentClipContext(0, clipId)`、`setTrackTimeOffset(10.0)`、`setPlayheadPositionSource(processor.getPositionAtomic())`、`setScrollMode(PianoRollComponent::ScrollMode::Continuous)`、`setZoomLevel(1.0)`、`setIsPlaying(true)`
      - 调 `onScrollVBlankCallback(0.0)` 后断言 `getScrollOffset() == 30`
    - `LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
      - 复用 Phase 18 page scroll 场景：`clipId = 190102`、其余 setup 相同，但 `setScrollMode(PianoRollComponent::ScrollMode::Page)`、`setScrollOffset(150)`
      - 调 `onScrollVBlankCallback(0.0)` 后断言 `getScrollOffset() == 0`
    - 现有 `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead` 与 `TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead` 保持原名、原断言，不允许放宽
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase19MainLayerSceneTests`
    - `Tests/TestMain.cpp` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `Tests/TestMain.cpp` contains `LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
    - `Tests/TestMain.cpp` contains `LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
    - `Tests/TestMain.cpp` contains `runPhase19MainLayerSceneTests();`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "runPhase19MainLayerSceneTests|LAYER_01_PianoRollHasNoPlayheadOverlayChild|LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval|LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval" "Tests/TestMain.cpp"</automated>
  </verify>
  <done>
    Phase 19 的 RED 阶段已完成：single-host 目标的三条测试事实先于实现被定义，并能在后续实现中直接转绿。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test-verification -> implementation | 如果验证契约先天含糊，执行者会用主观理解代替 requirement 事实 |
| regression suite -> PianoRoll layering | 没有回归测试时，overlay 子层或双层 scroll 假设会在后续 phase 回流 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-01 | R | Phase 19 verification contract | mitigate | 在 `19-TEST-VERIFICATION.md` 写死 L1/L2/L4/L6 命令与必需测试名，防止执行阶段靠口头解释补验证。 |
| T-19-02 | T | PianoRoll regression coverage | mitigate | 在 `Tests/TestMain.cpp` 先加入 `LAYER_01_*` / `LAYER_02_*` regressions，直接约束 no-overlay child 与 projected scroll 行为。 |
</threat_model>

<verification>
- `19-TEST-VERIFICATION.md` 已成为 Phase 19 的验证源文件。
- `Tests/TestMain.cpp` 已定义 Phase 19 三个主层场景回归用例。
- 既有 Phase 18 projected playhead 回归继续被列为 Phase 19 守护。
</verification>

<success_criteria>
- [ ] Phase 19 已有先行 test-verification 契约。
- [ ] Phase 19 已有 three-test RED baseline，覆盖 no-overlay child 与 projected scroll。
- [ ] 执行者可以在不猜测需求的前提下进入主层场景实现。
</success_criteria>

<output>
After completion, create `.planning/phases/19-主层场景归一/19-01-SUMMARY.md`
</output>
