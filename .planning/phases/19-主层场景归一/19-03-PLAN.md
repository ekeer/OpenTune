---
phase: 19-主层场景归一
plan: 03
type: execute
wave: 3
depends_on: [19-02]
files_modified:
  - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  - .planning/phases/19-主层场景归一/19-VERIFICATION.md
autonomous: true
requirements: [LAYER-01, LAYER-02]

must_haves:
  truths:
    - "Phase 19 不能只靠代码阅读判断做完，必须留下自动化证据证明 PianoRoll 已无 overlay 子层且 projected scroll 没有回归。"
    - "`19-VERIFICATION.md` 必须把 `LAYER-01` / `LAYER-02` 逐条绑定到具体测试或静态审计证据。"
    - "Phase 18 的 projected playhead regression 需要继续作为 Phase 19 的回归守护结果写回证据。"
  artifacts:
    - path: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      provides: "executed L1/L2/L4/L6 evidence"
      contains: "Passed"
    - path: ".planning/phases/19-主层场景归一/19-VERIFICATION.md"
      provides: "requirement traceability for LAYER-01 and LAYER-02"
      contains: "Gate status"
  key_links:
    - from: ".planning/phases/19-主层场景归一/19-VERIFICATION.md"
      to: ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"
      via: "verification 文档回填真实命令与 gate 结论"
      pattern: "Gate status"
    - from: ".planning/phases/19-主层场景归一/19-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "requirements 直接绑定 Phase 19 与 Phase 18 的测试名"
      pattern: "LAYER_01_PianoRollHasNoPlayheadOverlayChild"
---

<objective>
执行 Phase 19 的自动化验证并完成 requirement traceability，证明主层场景归一已经真实落地，而不是只把代码改成看起来像单层。

Purpose: Phase 19 是 repaint 架构收敛的第一步；如果不把 `LAYER-01` / `LAYER-02` 映射到真实证据，Phase 20/21 很容易在后续结构调整中重新引入 overlay 或双层 viewport 假设。

Output: 回填真实执行结果的 `19-TEST-VERIFICATION.md` 与 requirement 级 `19-VERIFICATION.md`。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-04-SUMMARY.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp

<interfaces>
From `19-TEST-VERIFICATION.md`:
```text
L1 -> build OpenTuneTests
L2 -> run OpenTuneTests.exe and inspect Phase 19 regressions
L4 -> static audit for overlay removal and drawPlayhead wiring
L6 -> rerun full suite
```

From `Tests/TestMain.cpp`:
```cpp
runPhase19MainLayerSceneTests()
LAYER_01_PianoRollHasNoPlayheadOverlayChild
LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval
LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 执行 L1/L2/L4/L6 并回填 19-TEST-VERIFICATION</name>
  <files>.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
  </read_first>
  <action>
    严格按照 `19-TEST-VERIFICATION.md` 执行并回填真实结果：
    - 运行 L1：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build -Target OpenTuneTests`
    - 运行 L2：`& ".\build\OpenTuneTests.exe"`，记录 `Phase 19: Main Layer Scene Tests`、三个 `LAYER_*` 用例、以及两个 `TIME_01_*` 用例的结果
    - 运行 L4：静态审计 `PianoRollComponent.h/cpp` 不再包含 `PlayheadOverlayComponent` / `playheadOverlay_`，并确认 `drawPlayhead` 存在于 renderer 和 component wiring
    - 运行 L6：再次执行 `& ".\build\OpenTuneTests.exe"`
    - 把四个 level 的 `Status` 从 `Pending` 更新为 `Passed` 或 `Failed`，并写明失败命令、错误摘要或通过证据
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `Passed` or `Failed`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `Phase 19: Main Layer Scene Tests`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
    - `.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md` contains `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "Passed|Failed|Phase 19: Main Layer Scene Tests|LAYER_01_PianoRollHasNoPlayheadOverlayChild|LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval|LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead|TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead" ".planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 19 的四级自动化 gate 已有真实执行证据，而不是停留在 Pending 模板状态。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 写 requirement 级 19-VERIFICATION traceability</name>
  <files>.planning/phases/19-主层场景归一/19-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
  </read_first>
  <action>
    新建 `.planning/phases/19-主层场景归一/19-VERIFICATION.md`，至少包含以下固定结构：
    - `## LAYER-01`
      - 绑定 `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
      - 绑定对 `Source/Standalone/UI/PianoRollComponent.h` / `Source/Standalone/UI/PianoRollComponent.cpp` 中 `PlayheadOverlayComponent` 与 `playheadOverlay_` 不存在的静态审计
      - 绑定 `PianoRollRenderer::drawPlayhead(...)` 与 `paint()` 主层调用证据
    - `## LAYER-02`
      - 绑定 `LAYER_01_PianoRollContinuousScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
      - 绑定 `LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval`
      - 绑定 `TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead` 与 `TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead` 仍然通过的证据
      - 绑定对 `PianoRollComponent.cpp` 不再同步 overlay bounds/scroll/zoom 的静态审计
    - `Gate status: PASS` 或 `Gate status: FAIL`
    - `Remaining risks:` 一节，明确指出 Phase 19 仍保留 `requestInteractiveRepaint()` / `FrameScheduler` / heartbeat / timer，是 roadmap 计划内的后续 phase 范围，不是遗忘项
  </action>
  <acceptance_criteria>
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `## LAYER-01`
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `## LAYER-02`
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `Gate status:`
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `Remaining risks:`
    - `.planning/phases/19-主层场景归一/19-VERIFICATION.md` contains `LAYER_01_PianoRollHasNoPlayheadOverlayChild`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "## LAYER-01|## LAYER-02|Gate status:|Remaining risks:|LAYER_01_PianoRollHasNoPlayheadOverlayChild|LAYER_02_PianoRollPageScrollStillUsesProjectedPlayheadAfterOverlayRemoval|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead" ".planning/phases/19-主层场景归一/19-VERIFICATION.md"</automated>
  </verify>
  <done>
    `LAYER-01` / `LAYER-02` 已有 requirement 级 traceability，Phase 19 可以被后续 workflow 直接验证而不是靠人工记忆解释。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| automated evidence -> gate decision | 如果 gate 结果不写回文档，后续 phase 会失去可追溯的结构证据 |
| requirements -> verification docs | `LAYER-01` / `LAYER-02` 必须绑定到真实证据，而不是只写结论 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-06 | R | verification evidence | mitigate | `19-TEST-VERIFICATION.md` 写回真实 L1/L2/L4/L6 结果与命令输出摘要，不允许继续保持 Pending。 |
| T-19-07 | R | requirement traceability | mitigate | `19-VERIFICATION.md` 逐条绑定 `LAYER-01` / `LAYER-02` 到测试名和静态审计，避免后续 phase 只能靠口头回忆解释。 |
</threat_model>

<verification>
- `19-TEST-VERIFICATION.md` 已记录真实 L1 / L2 / L4 / L6 结果。
- `19-VERIFICATION.md` 已把 `LAYER-01` / `LAYER-02` 映射到具体证据。
- Phase 18 的 projected playhead regression 结果已写回 Phase 19 证据。
</verification>

<success_criteria>
- [ ] Phase 19 regressions 已执行并留下结果。
- [ ] `19-TEST-VERIFICATION.md` 不再停留在 Pending 模板状态。
- [ ] `19-VERIFICATION.md` 已完成 requirement 级 traceability。
</success_criteria>

<output>
After completion, create `.planning/phases/19-主层场景归一/19-03-SUMMARY.md`
</output>
