---
phase: 19-主层场景归一
plan: 02
type: execute
wave: 2
depends_on: [19-01]
files_modified:
  - Source/Standalone/UI/PianoRollComponent.h
  - Source/Standalone/UI/PianoRollComponent.cpp
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
autonomous: true
requirements: [LAYER-01, LAYER-02]

must_haves:
  truths:
    - "PianoRollComponent 成为唯一场景宿主，播放头与音符、波形、F0 共享同一主层绘制路径。"
    - "滚动、缩放、track/alignment offset 与 seek 更新不再同步第二个 overlay 组件的坐标系。"
    - "Phase 19 只做单层场景归一，保留 `requestInteractiveRepaint()`、`FrameScheduler`、`onHeartbeatTick()`、`timerCallback()` 给 Phase 20/21 继续收敛。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRollComponent.h"
      provides: "single-host PianoRoll state without overlay member"
      contains: "mainLayerPlayheadSeconds_"
    - path: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.h"
      provides: "main-layer playhead draw contract"
      contains: "drawPlayhead"
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "main-layer playhead wiring inside paint/buildRenderContext"
      contains: "renderer_->drawPlayhead"
  key_links:
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"
      via: "paint() 在内容区 clip 内直接绘制播放头"
      pattern: "renderer_->drawPlayhead"
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Source/PluginProcessor.cpp"
      via: "继续用 `readProjectedPlayheadTime()` 保持 sample-authoritative 播放头时间源"
      pattern: "readProjectedPlayheadTime"
---

<objective>
把 PianoRoll 的播放头彻底并回主层：删除 `PlayheadOverlayComponent` 依赖，让 `PianoRollComponent` 自己持有播放头显示状态，并在内容主层里直接绘制它。

Purpose: 只要 PianoRoll 还依赖 `playheadOverlay_`，viewport / scroll / zoom / offset 仍然是双层同步问题，`LAYER-01` / `LAYER-02` 就不可能真的成立。

Output: 主层播放头渲染 contract、去掉 overlay 依赖后的 `PianoRollComponent` / `PianoRollRenderer` 实现。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/VST3Merge.md
@docs/plans/2026-04-14-pianoroll-refresh-implementation-plan.md
@docs/plans/2026-04-14-pianoroll-refresh-test-verification.md
@.planning/phases/18-时间轴投影与 Standalone 守护/18-02-SUMMARY.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
@Source/Standalone/UI/PlayheadOverlayComponent.h
@Source/Standalone/UI/PlayheadOverlayComponent.cpp

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
double readProjectedPlayheadTime() const;
double getPlayheadAbsolutePixelX(double playheadTimeSeconds) const;
int getPlayheadVisualX(double playheadTimeSeconds) const;
PianoRollRenderer::RenderContext buildRenderContext() const;
```

From `Source/Standalone/UI/PlayheadOverlayComponent.cpp` (要保留的视觉语义，不再保留组件本身):
```cpp
g.drawLine(playheadXf, 0.0f, playheadXf, static_cast<float>(getHeight()), 2.0f);
head.addTriangle(playheadXf - 6.0f, 0.0f,
                 playheadXf + 6.0f, 0.0f,
                 playheadXf, 6.0f);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 建立主层播放头渲染 contract</name>
  <files>Source/Standalone/UI/PianoRollComponent.h, Source/Standalone/UI/PianoRoll/PianoRollRenderer.h, Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Standalone/UI/PlayheadOverlayComponent.h
    - Source/Standalone/UI/PlayheadOverlayComponent.cpp
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  </read_first>
  <action>
    在 `Source/Standalone/UI/PianoRollComponent.h` / `Source/Standalone/UI/PianoRoll/PianoRollRenderer.*` 中建立主层播放头契约，要求精确如下：
    - 删除 `#include "PlayheadOverlayComponent.h"` 与 `PlayheadOverlayComponent playheadOverlay_`
    - 在 `PianoRollComponent` 中新增 `double mainLayerPlayheadSeconds_ = 0.0;` 与 `juce::Colour playheadColour_{0xFFE74C3C};`
    - 扩展 `PianoRollRenderer::RenderContext`，新增 `bool showPlayhead = true;`、`double playheadSeconds = 0.0;`、`juce::Colour playheadColour{0xFFE74C3C};`
    - 在 `PianoRollRenderer` 新增 `drawPlayhead(juce::Graphics& g, const RenderContext& ctx)`
    - `drawPlayhead(...)` 必须复用 overlay 当前视觉语义：2px 竖线 + 6px 顶部三角；x 坐标按 `ctx.coordinates.absoluteTimeToX(ctx.playheadSeconds)` 计算；只在 `x >= ctx.pianoKeyWidth && x < ctx.width` 时绘制
    - 绝对不要新增任何新的 `juce::Component` 子层来承载播放头
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.h` does not contain `PlayheadOverlayComponent`
    - `Source/Standalone/UI/PianoRollComponent.h` contains `mainLayerPlayheadSeconds_`
    - `Source/Standalone/UI/PianoRollComponent.h` contains `playheadColour_`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h` contains `drawPlayhead`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h` contains `playheadSeconds`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp` contains `absoluteTimeToX(ctx.playheadSeconds)`
  </acceptance_criteria>
  <verify>
    <automated>$header = Get-Content "Source/Standalone/UI/PianoRollComponent.h" -Raw; if ($header -match "PlayheadOverlayComponent|playheadOverlay_") { throw "overlay member still present in PianoRollComponent.h" }; rtk grep -n "mainLayerPlayheadSeconds_|playheadColour_|drawPlayhead|playheadSeconds|absoluteTimeToX\(ctx\.playheadSeconds\)" "Source/Standalone/UI/PianoRollComponent.h" "Source/Standalone/UI/PianoRoll/PianoRollRenderer.h" "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"</automated>
  </verify>
  <done>
    PianoRoll 已拥有主层播放头渲染 contract，后续实现不再需要 overlay 组件才能画播放头。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 把 PianoRoll 接线改成单层场景宿主</name>
  <files>Source/Standalone/UI/PianoRollComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Standalone/UI/PlayheadOverlayComponent.cpp
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  </read_first>
  <action>
    在 `Source/Standalone/UI/PianoRollComponent.cpp` 中把现有 overlay 接线改成主层模型，要求精确如下：
    - `initializeUIComponents()` 删除 `addAndMakeVisible(playheadOverlay_)` 与 `playheadOverlay_.setPianoKeyWidth(pianoKeyWidth_)`
    - `resized()` 删除 `playheadOverlay_.setBounds(getLocalBounds())`
    - `setIsPlaying()`、`setTrackTimeOffset()`、`setAlignmentOffset()`、`setPlayheadColour()`、`setAudioBuffer()`、`setScrollOffset()`、`setZoomLevel()`、`buildToolHandlerContext()->notifyPlayheadChange()`、`onScrollVBlankCallback()` 都不能再调用任何 `playheadOverlay_` setter 或 `playheadOverlay_.repaint()`
    - `notifyPlayheadChange()` 在非播放态下改为更新 `mainLayerPlayheadSeconds_ = time`，然后走组件自己的重绘路径，而不是第二层 overlay repaint
    - `onScrollVBlankCallback()` 改为更新 `mainLayerPlayheadSeconds_ = readProjectedPlayheadTime()`，然后继续维持当前 continuous/page scroll 逻辑
    - `buildRenderContext()` 必须设置 `ctx.showPlayhead = true`、`ctx.playheadSeconds = mainLayerPlayheadSeconds_`、`ctx.playheadColour = playheadColour_`
    - `paint()` 必须在内容区 clip 内直接调用 `renderer_->drawPlayhead(g, ctx)`，位置放在 notes / waveform / F0 之后、离开内容区 clip 之前，让播放头与内容真正共用同一坐标与裁剪语义
    - 保持 `requestInteractiveRepaint()`、`FrameScheduler`、`scrollVBlankAttachment_`、`onHeartbeatTick()`、`timerCallback()` 原样存在；Phase 19 不得提前删除单入口 / 单时钟结构
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `renderer_->drawPlayhead(g, ctx)`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `ctx.playheadSeconds = mainLayerPlayheadSeconds_`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `mainLayerPlayheadSeconds_ = readProjectedPlayheadTime()`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `addAndMakeVisible(playheadOverlay_)`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setBounds`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setPlayheadSeconds`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setScrollOffset`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setZoomLevel`
  </acceptance_criteria>
  <verify>
    <automated>$cpp = Get-Content "Source/Standalone/UI/PianoRollComponent.cpp" -Raw; if ($cpp -match "playheadOverlay_") { throw "overlay references still present in PianoRollComponent.cpp" }; rtk grep -n "renderer_->drawPlayhead\(g, ctx\)|ctx\.playheadSeconds = mainLayerPlayheadSeconds_|mainLayerPlayheadSeconds_ = readProjectedPlayheadTime\(\)|requestInteractiveRepaint\(|onHeartbeatTick\(|timerCallback\(" "Source/Standalone/UI/PianoRollComponent.cpp"</automated>
  </verify>
  <done>
    `PianoRollComponent` 已成为唯一场景宿主；viewport / seek / scroll / zoom 更新都落在同一个组件的主层绘制语义里。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Processor projected playhead -> PianoRoll paint | Phase 18 的 sample-authoritative 播放头时间源不能因为主层化而丢失 |
| PianoRoll scene state -> rendered scene | 如果仍保留 overlay 同步，Phase 19 的单层场景目标会失效 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-03 | T | PianoRoll scene layering | mitigate | 删除 `PlayheadOverlayComponent` 依赖，把播放头并回 `PianoRollComponent` 主层绘制，不再保留第二层子组件。 |
| T-19-04 | I | playhead time source | mitigate | `buildRenderContext()` 统一消费 `mainLayerPlayheadSeconds_`，其值继续来自 `readProjectedPlayheadTime()` 或明确的 seek 输入，不回退到 overlay 私有秒值换算。 |
| T-19-05 | D | PianoRoll repaint path | accept | Phase 19 接受现有 `FrameScheduler` / heartbeat / timer 仍存在，因为单入口与单时钟被 roadmap 明确分配到 Phase 20/21，不在本 phase 内冒然删除。 |
</threat_model>

<verification>
- `PianoRollComponent` 不再持有 `PlayheadOverlayComponent`。
- `PianoRollRenderer` 具备主层播放头绘制入口。
- `paint()` 已在内容区主层直接绘制播放头。
- Phase 19 没有越权删除 `requestInteractiveRepaint()` / `FrameScheduler` / heartbeat / timer 路径。
</verification>

<success_criteria>
- [ ] `LAYER-01` 的主层播放头绘制路径已经落地。
- [ ] `LAYER-02` 的第二层 overlay 坐标同步依赖已经从 PianoRoll 移除。
- [ ] Phase 19 仍然严格停留在“主层场景归一”，没有偷做 Phase 20/21。
</success_criteria>

<output>
After completion, create `.planning/phases/19-主层场景归一/19-02-SUMMARY.md`
</output>
