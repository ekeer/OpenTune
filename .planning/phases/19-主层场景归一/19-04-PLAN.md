---
phase: 19-主层场景归一
plan: 04
type: execute
wave: 4
depends_on: [19-03]
files_modified:
  - Source/Standalone/UI/PianoRollComponent.h
  - Source/Standalone/UI/PianoRollComponent.cpp
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
autonomous: true
requirements: [LAYER-01, LAYER-02]

must_haves:
  truths:
    - "gap closure 必须以 live tree 为准，不能继续采信 `19-02-SUMMARY.md` 里与当前源码冲突的完成表述。"
    - "`PianoRollComponent` 必须成为唯一场景宿主；播放头与音符、波形、F0 共享同一主层绘制与裁剪语义。"
    - "scroll/zoom/viewport/playhead 更新不能再同步第二视觉层；Phase 19 仍不得提前偷做 Phase 20/21 的单入口/单时钟收敛。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRollComponent.h"
      provides: "single-host PianoRoll state without overlay member"
      contains: "mainLayerPlayheadSeconds_"
    - path: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.h"
      provides: "main-layer playhead renderer contract"
      contains: "drawPlayhead"
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "main-layer playhead wiring and no overlay synchronization"
      contains: "renderer_->drawPlayhead"
  key_links:
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"
      via: "paint() 在内容区 clip 内直接绘制播放头"
      pattern: "renderer_->drawPlayhead"
    - from: "Source/Standalone/UI/PianoRollComponent.cpp"
      to: "Source/PluginProcessor.cpp"
      via: "继续用 `readProjectedPlayheadTime()` 保持 sample-authoritative 播放头时间源"
      pattern: "readProjectedPlayheadTime"
---

<objective>
按当前 live tree 重新落地 Phase 19 的主层场景 contract：先把缺失的 `PianoRollRenderer` 模块与当前 `PianoRollComponent` 的真实依赖关系校正到同一个文件布局，再把播放头彻底并回主层绘制，删除 overlay 子层依赖与所有第二坐标系同步路径。

Purpose: 现在的阻塞不是单一 bug，而是“计划假设的文件布局 + summary 叙述 + live tree”三者已经脱节。只有先把主层 renderer contract 和组件 wiring 在当前代码树里统一成一个真实结构，`LAYER-01` / `LAYER-02` 才有机会成立。

Output: 基于真实文件布局落地的单层 PianoRoll 主层绘制实现，以及不再依赖 overlay 的 `PianoRollComponent`。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/STRUCTURE.md
@.planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
@.planning/phases/19-主层场景归一/19-VERIFICATION.md
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PlayheadOverlayComponent.h
@Source/Standalone/UI/PlayheadOverlayComponent.cpp

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
double readProjectedPlayheadTime() const;
double getPlayheadAbsolutePixelX(double playheadTimeSeconds) const;
int getPlayheadVisualX(double playheadTimeSeconds) const;
PianoRollRenderer::RenderContext buildRenderContext() const;
```

From `Source/Standalone/UI/PlayheadOverlayComponent.cpp` (要保留的视觉语义，不再保留组件本身):
```cpp
g.drawLine(playheadXf, 0.0f, playheadXf, static_cast<float>(getHeight()), 2.0f);
head.addTriangle(playheadXf - 6.0f, 0.0f,
                 playheadXf + 6.0f, 0.0f,
                 playheadXf, 6.0f);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 把主层 renderer contract 校正到当前代码树</name>
  <files>Source/Standalone/UI/PianoRollComponent.h, Source/Standalone/UI/PianoRoll/PianoRollRenderer.h, Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
    - .planning/phases/19-主层场景归一/19-VERIFICATION.md
    - .planning/phases/19-主层场景归一/19-TEST-VERIFICATION.md
  </read_first>
  <action>
    在当前 live tree 中建立唯一真实的主层播放头 renderer contract，要求精确如下：
    - 如果 `Source/Standalone/UI/PianoRoll/` 目录确实缺失，则在该目录下补齐 `PianoRollRenderer.h/.cpp`，不允许继续让 `PianoRollComponent.h` 引用不存在的头文件
    - 删除 `#include "PlayheadOverlayComponent.h"` 与 `PlayheadOverlayComponent playheadOverlay_`
    - 在 `PianoRollComponent` 中新增 `double mainLayerPlayheadSeconds_ = 0.0;` 与 `juce::Colour playheadColour_{0xFFE74C3C};`
    - 扩展 `PianoRollRenderer::RenderContext`，新增 `bool showPlayhead = true;`、`double playheadSeconds = 0.0;`、`juce::Colour playheadColour{0xFFE74C3C};`
    - 在 `PianoRollRenderer` 新增 `drawPlayhead(juce::Graphics& g, const RenderContext& ctx)`
    - `drawPlayhead(...)` 必须复用 overlay 当前视觉语义：2px 竖线 + 6px 顶部三角；x 坐标按 `ctx.timeToX(ctx.playheadSeconds)` 或等价的主层时间投影计算；只在内容区范围内绘制
    - 绝对不要新增任何新的 `juce::Component` 子层来承载播放头
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.h` does not contain `PlayheadOverlayComponent`
    - `Source/Standalone/UI/PianoRollComponent.h` contains `mainLayerPlayheadSeconds_`
    - `Source/Standalone/UI/PianoRollComponent.h` contains `playheadColour_`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h` contains `drawPlayhead`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h` contains `playheadSeconds`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp` contains `drawPlayhead`
  </acceptance_criteria>
  <verify>
    <automated>rtk grep -n "mainLayerPlayheadSeconds_|playheadColour_|drawPlayhead|playheadSeconds" "Source/Standalone/UI/PianoRollComponent.h" "Source/Standalone/UI/PianoRoll/PianoRollRenderer.h" "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"</automated>
  </verify>
  <done>
    当前工作区终于存在一个可被静态审计的主层播放头 contract，而不是继续让计划引用不存在的 renderer 文件。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 把 PianoRoll 接线收敛成单层场景宿主</name>
  <files>Source/Standalone/UI/PianoRollComponent.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - .planning/phases/19-主层场景归一/19-VERIFICATION.md
  </read_first>
  <action>
    在 `Source/Standalone/UI/PianoRollComponent.cpp` 中把 overlay 接线彻底并回主层，要求精确如下：
    - `initializeUIComponents()` 删除 `addAndMakeVisible(playheadOverlay_)` 与全部 overlay 初始化
    - `resized()` 删除 `playheadOverlay_.setBounds(getLocalBounds())`
    - `setIsPlaying()`、`setTrackTimeOffset()`、`setAlignmentOffset()`、`setPlayheadColour()`、`setAudioBuffer()`、`setScrollOffset()`、`setZoomLevel()`、`buildToolHandlerContext()->notifyPlayheadChange()`、`onScrollVBlankCallback()` 都不能再调用任何 `playheadOverlay_` setter 或 `playheadOverlay_.repaint()`
    - `notifyPlayheadChange()` 在非播放态下改为更新 `mainLayerPlayheadSeconds_ = time`，然后走组件自己的重绘路径
    - `onScrollVBlankCallback()` 改为更新 `mainLayerPlayheadSeconds_ = readProjectedPlayheadTime()`，并继续维持当前 continuous/page scroll 逻辑
    - `buildRenderContext()` 必须设置 `ctx.showPlayhead = true`、`ctx.playheadSeconds = mainLayerPlayheadSeconds_`、`ctx.playheadColour = playheadColour_`
    - `paint()` 必须在内容区 clip 内直接调用 `renderer_->drawPlayhead(g, ctx)`，位置放在 notes / waveform / F0 之后、离开内容区 clip 之前
    - 保持 `requestInteractiveRepaint()`、`FrameScheduler`、`scrollVBlankAttachment_`、`onHeartbeatTick()`、`timerCallback()` 原样存在；Phase 19 不得越权删除单入口/单时钟结构
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `renderer_->drawPlayhead`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `ctx.playheadSeconds = mainLayerPlayheadSeconds_`
    - `Source/Standalone/UI/PianoRollComponent.cpp` contains `mainLayerPlayheadSeconds_ = readProjectedPlayheadTime()`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `addAndMakeVisible(playheadOverlay_)`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setBounds`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setPlayheadSeconds`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setScrollOffset`
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not contain `playheadOverlay_.setZoomLevel`
  </acceptance_criteria>
  <verify>
    <automated>$cpp = Get-Content "Source/Standalone/UI/PianoRollComponent.cpp" -Raw; if ($cpp -match "playheadOverlay_") { throw "overlay references still present in PianoRollComponent.cpp" }; rtk grep -n "renderer_->drawPlayhead|ctx\.playheadSeconds = mainLayerPlayheadSeconds_|mainLayerPlayheadSeconds_ = readProjectedPlayheadTime\(|requestInteractiveRepaint\(|onHeartbeatTick\(|timerCallback\(" "Source/Standalone/UI/PianoRollComponent.cpp"</automated>
  </verify>
  <done>
    `PianoRollComponent` 已回到唯一场景宿主位置；viewport / scroll / playhead 不再靠第二层组件同步。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live tree -> plan contract | 如果继续基于失真的 summary 做实现，gap closure 会再次落到错误文件或错误结构上 |
| projected playhead source -> main-layer paint | Phase 18 的 sample-authoritative 播放头时间源不能因为主层化而丢失 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-19-08 | T | plan/live-tree consistency | mitigate | 以当前文件系统为准恢复 `PianoRollRenderer` contract，不再引用不存在的文件路径。 |
| T-19-09 | T | PianoRoll scene layering | mitigate | 删除 `PlayheadOverlayComponent` 依赖，把播放头并回 `PianoRollComponent` 主层绘制，不再保留第二层子组件。 |
| T-19-10 | I | playhead time source | mitigate | `buildRenderContext()` 统一消费 `mainLayerPlayheadSeconds_`，其值继续来自 `readProjectedPlayheadTime()` 或明确的 seek 输入。 |
| T-19-11 | D | phase boundary drift | accept | Phase 19 接受 `FrameScheduler` / heartbeat / timer 仍存在，因为单入口与单时钟被 roadmap 明确分配到 Phase 20/21。 |
</threat_model>

<verification>
- `PianoRollComponent` 不再持有 `PlayheadOverlayComponent`。
- `PianoRollRenderer` 具备主层播放头绘制入口且在 live tree 中真实存在。
- `paint()` 已在内容区主层直接绘制播放头。
- Phase 19 没有越权删除 `requestInteractiveRepaint()` / `FrameScheduler` / heartbeat / timer 路径。
</verification>

<success_criteria>
- [ ] `LAYER-01` 的主层播放头绘制路径已经在当前代码树中真实落地。
- [ ] `LAYER-02` 的第二层 overlay 坐标同步依赖已经从 PianoRoll 移除。
- [ ] Phase 19 仍然严格停留在“主层场景归一”，没有偷做 Phase 20/21。
</success_criteria>

<output>
After completion, create `.planning/phases/19-主层场景归一/19-04-SUMMARY.md`
</output>
