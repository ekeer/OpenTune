---
phase: 25-snapshot-editor
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - Source/ARA/OpenTuneDocumentController.cpp
  - Source/ARA/OpenTunePlaybackRenderer.cpp
autonomous: true
requirements: [CONS-01, CONS-04]

must_haves:
  truths:
    - "同一 audio source 下的 region binding 必须能共享当前 clip application truth，同时显式暴露 `appliedRegion`。"
    - "renderer 继续每个 block 只读取一次 snapshot，并且只通过 playback region -> published view 解析播放真相。"
    - "Phase 25 不允许回流 source-level getter、currentPlaybackAudioSource、retry 或第二套 snapshot state。"
  artifacts:
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "same-source binding propagation with appliedRegion truth"
      contains: "binding.appliedRegion"
    - path: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      provides: "explicit single-snapshot playback-region consumer"
      contains: "loadSnapshot"
  key_links:
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: "Source/Plugin/PluginEditor.cpp"
      via: "controller 先把 same-source binding/application truth 投影到 preferred region view，editor 才能只从 preferred region view 决定是否同步"
      pattern: "appliedRegion"
---

<objective>
实现 Phase 25 的 controller-side binding truth，并把 renderer 的 single-snapshot / playback-region consumer contract 固化到 live tree：preferred region view 必须自己携带足够的 clip application truth，不能再依赖 source-level fallback 或 revision coincidence。

Purpose: 目前 `PluginEditor` 的 Phase 25 阻塞点不只是缺少 local epoch state，还包括 preferred region view 不一定携带当前 clip application truth。只有先让 controller 在 snapshot 中发布 `appliedRegion` 驱动的 same-source binding truth，editor 才能真正只读 preferred region view。

Output: `OpenTuneDocumentController.cpp` 中的 same-source binding propagation / clear / revision update 逻辑，以及明确保持 single-snapshot block read contract 的 `OpenTunePlaybackRenderer.cpp`。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-snapshot-editor/25-01-PLAN.md
@.planning/phases/25-snapshot-editor/25-02-PLAN.md
@.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Binding write target state for same-source region propagation:
```cpp
binding.clipId = clipId;
binding.appliedContentRevision = contentRevision;
binding.appliedMappingRevision = mappingRevision;
binding.sourceStartSeconds = sourceStartSeconds;
binding.sourceEndSeconds = sourceEndSeconds;
binding.playbackStartSeconds = playbackStartSeconds;
binding.appliedRegion = identity;
```

Renderer contract that must stay true:
```cpp
const auto snapshot = docController->loadSnapshot();
for (auto* region : getPlaybackRegions()) {
    const auto* publishedView = findRenderableRegionView(*snapshot, region);
    // no legacy getter fallback
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 让 same-source region views 都携带当前 clip application truth</name>
  <files>Source/ARA/OpenTuneDocumentController.cpp</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/Plugin/PluginEditor.cpp
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
  </read_first>
  <action>
    更新 `Source/ARA/OpenTuneDocumentController.cpp`，让 same-source region views 都能通过 preferred region view 直接看到当前 clip application truth。要求精确如下：
    - `registerPlaybackRegionClipBinding(...)` 在解析出 `identity.audioSource` 后，不能只更新当前 `playbackRegion`；它必须遍历 `regions_` 中所有 `identity.audioSource == targetAudioSource` 的 `RegionSlot`
    - 对所有同 source `RegionSlot`，统一写入：
      - `binding.clipId = clipId`
      - `binding.appliedContentRevision = contentRevision`
      - `binding.appliedMappingRevision = mappingRevision`
      - `binding.sourceStartSeconds = sourceStartSeconds`
      - `binding.sourceEndSeconds = sourceEndSeconds`
      - `binding.playbackStartSeconds = playbackStartSeconds`
      - `binding.appliedRegion = identity`
    - `updatePlaybackRegionBindingRevisions(...)` 必须同样按 source sibling 范围更新 `appliedContentRevision`、`appliedMappingRevision`、`appliedRegion`
    - `clearPlaybackRegionClipBinding(...)` 不能只清当前 region；它必须按当前 `playbackRegion` 的 `identity.audioSource` 清除所有 sibling region bindings
    - 不允许新增 source-level mutable getter、currentPlaybackAudioSource、second snapshot state 或 processor-side workaround
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `binding.appliedRegion = identity`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `slot.identity.audioSource == identity.audioSource`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `clear();`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `getCurrentPlaybackAudioSource`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "binding\.appliedRegion = identity" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "identity\.audioSource" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getCurrentPlaybackAudioSource" Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    preferred region view 现在自己就携带当前 clip application truth，editor 不需要再回退到 source-level fallback。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 把 renderer 的 single-snapshot / playback-region consumer contract 固化到 live tree</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp</files>
  <read_first>
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
  </read_first>
  <action>
    更新 `Source/ARA/OpenTunePlaybackRenderer.cpp`，把 Phase 25 的 renderer consumer contract 固化成代码结构。要求精确如下：
    - `processBlock()` 顶部只保留一次 `const auto snapshot = docController->loadSnapshot();`
    - region loop 中的 published view 解析固定通过 `findRenderableRegionView(*snapshot, region)` 完成
    - 不允许引入任何新的 docController getter、second snapshot load、currentPlaybackAudioSource、retry 或 source-level range fallback
    - 如果需要调整函数布局，只允许把 single snapshot load 与 region lookup 结构写得更显式；不得引入无关行为变化
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` contains `const auto snapshot = docController->loadSnapshot();`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` contains `findRenderableRegionView(*snapshot, region)`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getAudioSourceState`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getPlaybackRangeForAudioSource`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getSourceRangeForAudioSource`
    - `Source/ARA/OpenTunePlaybackRenderer.cpp` does not contain `getCurrentPlaybackAudioSource`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "const auto snapshot = docController->loadSnapshot\(\);" Source/ARA/OpenTunePlaybackRenderer.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "findRenderableRegionView\(\*snapshot, region\)" Source/ARA/OpenTunePlaybackRenderer.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getAudioSourceState|getPlaybackRangeForAudioSource|getSourceRangeForAudioSource|getCurrentPlaybackAudioSource" Source/ARA/OpenTunePlaybackRenderer.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    renderer 的 Phase 25 consumer contract 已经是显式的 one-snapshot / direct-region-read 结构，而不是默认碰巧成立。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| controller binding state -> preferred region view | 如果 same-source siblings 看不到当前 clip application truth，editor 仍会被迫绕回 source-level fallback |
| snapshot consumer -> audio thread | 如果 renderer 引入第二次 snapshot load 或 legacy getter，audio-thread read contract 会再次漂移 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-21 | I | same-source binding truth | mitigate | 把 binding write/clear/revision update 全部收敛到 source sibling propagation，并显式写入 `appliedRegion`。 |
| T-25-22 | R | renderer read contract | mitigate | 强制 `processBlock()` 只保留一次 `loadSnapshot()`，并维持 direct playback-region lookup。 |
| T-25-23 | T | fallback creep | mitigate | 明确禁止 currentPlaybackAudioSource、source-level getter、retry 和 processor-side workaround 回流。 |
</threat_model>

<verification>
- `OpenTuneDocumentController.cpp` 的 binding write/clear/revision update 都按 same-source sibling 范围收敛。
- `OpenTunePlaybackRenderer.cpp` 保持 one-snapshot-per-block 与 direct-region lookup contract。
- `build-phase25-docs` 的 L1 build gate 继续通过。
</verification>

<success_criteria>
- [ ] preferred region view 已能直接携带当前 clip application truth。
- [ ] renderer 仍只读 immutable snapshot，并继续以 playback region 解析 published view。
- [ ] legacy getter / fallback 没有回流到 controller 或 renderer。
</success_criteria>

<output>
After completion, create `.planning/phases/25-snapshot-editor/25-03-SUMMARY.md`
</output>
