---
phase: 25-snapshot-editor
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
  - Source/ARA/OpenTuneDocumentController.h
  - Source/Plugin/PluginEditor.h
autonomous: true
requirements: [CONS-01, CONS-02, CONS-03, CONS-04]

must_haves:
  truths:
    - "Phase 25 必须先有 test-verification source of truth，再允许 consumer 侧实现落地。"
    - "`AudioSourceClipBinding` 必须显式携带 `appliedRegion`，preferred-region 切换不能再只靠 revision 数字猜测。"
    - "VST3 editor 的 timer 同步状态必须显式保存 snapshot epoch 与 preferred region identity，而不是隐含在多 getter 或旧 source-level 状态里。"
  artifacts:
    - path: ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"
      provides: "Phase 25 verification source of truth"
      contains: "Phase 25: Snapshot Consumers And Preferred-Region Sync"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "Phase 25 binding contract with appliedRegion truth"
      contains: "appliedRegion"
    - path: "Source/Plugin/PluginEditor.h"
      provides: "Phase 25 editor sync-state contract"
      contains: "lastConsumedAraSnapshotEpoch_"
  key_links:
    - from: ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "verification source 先写死 appliedRegion 与 snapshot-epoch 验收语义，再冻结 binding contract"
      pattern: "CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump"
    - from: "Source/ARA/OpenTuneDocumentController.h"
      to: "Source/Plugin/PluginEditor.h"
      via: "binding contract 与 editor local sync-state 一起固定，避免后续 wave 各自发明 preferred-region truth"
      pattern: "lastConsumedPreferredAraRegion_"
---

<objective>
先冻结 Phase 25 的 verification source 与 header-level consumer contract：把 snapshot consumer、preferred-region truth、`appliedRegion`、以及 editor 的 local epoch state 一次写清，后续 TDD 和 consumer implementation 都只能沿着同一套契约执行。

Purpose: 当前 live tree 已完成 Phase 23/24 的 snapshot 与 lifecycle 收敛，但 Phase 25 仍缺少两个关键 contract：一是 preferred-region 切换的 applied truth；二是 editor timer 的 snapshot epoch consumer state。若不先把这两层写成唯一契约，后续测试和实现都会退回到“看起来能跑”的拼装式逻辑。

Output: `25-TEST-VERIFICATION.md`，`OpenTuneDocumentController.h` 中的 appliedRegion binding contract，以及 `PluginEditor.h` 中的 snapshot-epoch / preferred-region local state。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/23-snapshot-region/23-03-SUMMARY.md
@.planning/phases/24-callback-sample-access/24-03-SUMMARY.md
@.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
@Source/ARA/OpenTuneDocumentController.h
@Source/Plugin/PluginEditor.h

<interfaces>
Phase-25 journeys that the verification source must lock by exact name:
```text
CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock
CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth
CONS_03_RecordRequestedImportsPreferredRegionViewOnly
CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump
```

Header-level contract that must be frozen before implementation:
```cpp
struct AudioSourceClipBinding
{
    uint64_t clipId{0};
    uint64_t appliedContentRevision{0};
    uint64_t appliedMappingRevision{0};
    double sourceStartSeconds{0.0};
    double sourceEndSeconds{0.0};
    double playbackStartSeconds{0.0};
    RegionIdentity appliedRegion;
};
```

Editor local state that must exist before consumer implementation:
```cpp
uint64_t lastConsumedAraSnapshotEpoch_{0};
OpenTuneDocumentController::RegionIdentity lastConsumedPreferredAraRegion_{};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 Phase 25 test-verification source of truth</name>
  <files>.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/codebase/TESTING.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
  </read_first>
  <action>
    创建 `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md`，把 Phase 25 的 verification contract 固定成唯一 source of truth。要求精确如下：
    - 标题固定为 `Phase 25: Snapshot Consumers And Preferred-Region Sync`
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests`
    - L2 / L6 命令固定为 `& ".\build-phase25-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：
      - `CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock`
      - `CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth`
      - `CONS_03_RecordRequestedImportsPreferredRegionViewOnly`
      - `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump`
    - `L4 Static Audit Checklist` 必须点名：`processBlock`、`loadSnapshot`、`findRenderableRegionView`、`timerCallback`、`syncImportedAraClipIfNeeded`、`recordRequested`、`registerPlaybackRegionClipBinding`、`updatePlaybackRegionBindingRevisions`、`clearPlaybackRegionClipBinding`、`appliedRegion`、`snapshot->epoch`、`getCurrentPlaybackAudioSource`
    - `Evidence To Capture` 必须写明：renderer 每个 block 只读一次 snapshot；editor timer 只按 snapshot epoch 与 preferred region truth 决定 sync；手动导入只从 preferred region view 读 copied audio 与 mapping；同 source 下 preferred region 切换时，即使 `mappingRevision` 不变，也会通过 `appliedRegion` 暴露待同步真相
  </action>
  <acceptance_criteria>
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `Phase 25: Snapshot Consumers And Preferred-Region Sync`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `build-phase25-docs`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 25: Snapshot Consumers And Preferred-Region Sync" ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "build-phase25-docs" ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump" ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 25 的 test-verification source 已在实现前存在，后续 wave 不允许自创 consumer 验收口径。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 controller header 冻结 appliedRegion binding contract</name>
  <files>Source/ARA/OpenTuneDocumentController.h</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - .planning/phases/23-snapshot-region/23-03-SUMMARY.md
    - .planning/phases/24-callback-sample-access/24-03-SUMMARY.md
  </read_first>
  <action>
    更新 `Source/ARA/OpenTuneDocumentController.h`，把 Phase 25 的 binding contract 一次定型，要求精确如下：
    - 在 `AudioSourceClipBinding` 中新增 `RegionIdentity appliedRegion;`
    - `AudioSourceClipBinding` 继续保留 `clipId`、`appliedContentRevision`、`appliedMappingRevision`、`sourceStartSeconds`、`sourceEndSeconds`、`playbackStartSeconds`
    - 不允许恢复 `getCurrentPlaybackAudioSource()`、`getAudioSourceState()` 或任何 source-level mutable getter
    - `PublishedSnapshot` 继续只以 `epoch + preferredRegion + publishedRegions` 暴露读侧 contract，不新增第二套 snapshot state
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.h` contains `RegionIdentity appliedRegion;`
    - `Source/ARA/OpenTuneDocumentController.h` contains `uint64_t appliedMappingRevision{0};`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getCurrentPlaybackAudioSource`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `getAudioSourceState`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "RegionIdentity appliedRegion;" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "uint64_t appliedMappingRevision\{0\};" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getCurrentPlaybackAudioSource|getAudioSourceState" Source/ARA/OpenTuneDocumentController.h) { exit 1 }</automated>
  </verify>
  <done>
    controller header 已固定 appliedRegion contract，后续 consumer 逻辑不需要再猜 preferred-region switch 如何表达。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在 editor header 冻结 snapshot-epoch consumer state</name>
  <files>Source/Plugin/PluginEditor.h</files>
  <read_first>
    - Source/Plugin/PluginEditor.h
    - Source/ARA/OpenTuneDocumentController.h
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
  </read_first>
  <action>
    更新 `Source/Plugin/PluginEditor.h`，把 Phase 25 需要的 editor local sync-state 一次固定下来。要求精确如下：
    - 引入 `OpenTuneDocumentController::RegionIdentity` 可见声明；如果 `PluginEditor.h` 现有 include 不足，就显式补齐 `OpenTuneDocumentController.h` 所需 include
    - 在 private state 中新增：
      - `uint64_t lastConsumedAraSnapshotEpoch_{0};`
      - `OpenTuneDocumentController::RegionIdentity lastConsumedPreferredAraRegion_{};`
    - `timerCallback()`、`syncImportedAraClipIfNeeded()`、`recordRequested()` 的声明保持原有 public/private 边界，不新增第二套 timer 或 host-polling entry point
  </action>
  <acceptance_criteria>
    - `Source/Plugin/PluginEditor.h` contains `lastConsumedAraSnapshotEpoch_`
    - `Source/Plugin/PluginEditor.h` contains `lastConsumedPreferredAraRegion_`
    - `Source/Plugin/PluginEditor.h` contains `void timerCallback() override;`
    - `Source/Plugin/PluginEditor.h` contains `void syncImportedAraClipIfNeeded();`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "lastConsumedAraSnapshotEpoch_|lastConsumedPreferredAraRegion_" Source/Plugin/PluginEditor.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "void timerCallback\(\) override;|void syncImportedAraClipIfNeeded\(\);" Source/Plugin/PluginEditor.h; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    editor header 已具备 snapshot epoch 与 preferred-region local state，后续 cpp consumer 只需落地同一契约。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification source -> consumer implementation | 如果 Phase 25 先写实现再回填 verification，epoch/appliedRegion 会退回成事后解释 |
| controller binding contract -> editor timer state | 如果 appliedRegion 与 local snapshot epoch state 不同时冻结，preferred-region 切换会再次依赖隐式逻辑 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | T | verification contract | mitigate | 先创建 `25-TEST-VERIFICATION.md`，固定 L1/L2/L4/L6 与四条 consumer journeys。 |
| T-25-02 | I | binding truth | mitigate | 在 `AudioSourceClipBinding` 增加 `appliedRegion`，preferred-region 切换不再隐含在 revision 数字里。 |
| T-25-03 | R | editor timer state | mitigate | 在 `PluginEditor.h` 显式声明 `lastConsumedAraSnapshotEpoch_` 与 `lastConsumedPreferredAraRegion_`。 |
</threat_model>

<verification>
- `rg` 确认 `25-TEST-VERIFICATION.md`、`AudioSourceClipBinding.appliedRegion`、`lastConsumedAraSnapshotEpoch_`、`lastConsumedPreferredAraRegion_` 已落地。
- `rg` 确认 `OpenTuneDocumentController.h` 没有回流 legacy mutable getter。
</verification>

<success_criteria>
- [ ] Phase 25 verification source of truth 已创建。
- [ ] controller binding contract 已显式携带 `appliedRegion`。
- [ ] editor header 已具备 snapshot-epoch / preferred-region local state。
</success_criteria>

<output>
After completion, create `.planning/phases/25-snapshot-editor/25-01-SUMMARY.md`
</output>
