---
phase: 25-snapshot-editor
plan: 04
type: execute
wave: 4
depends_on: [25-03]
files_modified:
  - Source/Plugin/PluginEditor.cpp
  - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
autonomous: true
requirements: [CONS-02, CONS-03, CONS-04]

must_haves:
  truths:
    - "editor timer 只按 snapshot epoch 与 preferred-region truth 驱动 sync，不再每 tick 盲猜 revision。"
    - "手动导入与定时同步都只从 preferred region view 读取 copied audio 与 mapping，不再依赖 `currentPlaybackAudioSource` 或 retry。"
    - "same-source preferred-region switch 即使 revision 不变，也必须通过 `appliedRegion` 触发正确 mapping sync。"
  artifacts:
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "epoch + preferred-region consumer implementation"
      contains: "binding.appliedRegion"
    - path: ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"
      provides: "Phase 25 GREEN verification evidence"
      contains: "GREEN Verification"
  key_links:
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: "Source/Plugin/PluginEditor.cpp"
      via: "controller 先把 appliedRegion truth 发布到 preferred region view，editor 再只用 preferred region view 做 sync 决策"
      pattern: "binding.appliedRegion"
---

<objective>
完成 Phase 25 的 editor consumer 收敛，并用 fresh build/test/grep-zero 证据关闭本 phase：`timerCallback()` 只按 snapshot epoch 与 preferred region truth 驱动 sync，manual import 与定时同步都只消费 preferred region view，same-source preferred-region switch 则通过 `appliedRegion` 暴露的真相触发 mapping sync。

Purpose: controller 侧如果已经把 appliedRegion truth 发布进 snapshot，editor 的职责就不再是“拼装当前 ARA 真相”，而是消费 immutable snapshot 并据此决定是否同步 clip。Phase 25 的最后一步就是把这个 consumer 结构变成唯一实现，并写回 fresh GREEN evidence。

Output: `PluginEditor.cpp` 中的 Phase 25 consumer implementation，以及 `25-TEST-VERIFICATION.md` 中与 build/test/grep-zero 对应的 closure evidence。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
@.planning/phases/25-snapshot-editor/25-02-PLAN.md
@.planning/phases/25-snapshot-editor/25-03-PLAN.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Editor sync decision target state:
```cpp
const bool snapshotAdvanced = snapshot->epoch != lastConsumedAraSnapshotEpoch_;
const bool preferredRegionChanged = preferredRegionView->regionIdentity != lastConsumedPreferredAraRegion_;
const bool appliedRegionChanged = binding.appliedRegion != preferredRegionView->regionIdentity;
```

Region-only sync case that must be handled explicitly:
```cpp
if (!contentChanged && !mappingChanged && appliedRegionChanged) {
    documentController->registerPlaybackRegionClipBinding(... current preferred region ...);
    // update local epoch / preferred-region state
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 让 editor 只按 snapshot epoch 与 preferred-region truth 做 sync 决策</name>
  <files>Source/Plugin/PluginEditor.cpp</files>
  <read_first>
    - Source/Plugin/PluginEditor.h
    - Source/Plugin/PluginEditor.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
  </read_first>
  <action>
    更新 `Source/Plugin/PluginEditor.cpp`，把 Phase 25 的 consumer 逻辑收敛成唯一实现。要求精确如下：
    - `timerCallback()` 继续保留 transport / overlay 逻辑，但 ARA clip sync 仍只能通过 `syncImportedAraClipIfNeeded()` 进入；不得新增第二套 timer path
    - `syncImportedAraClipIfNeeded()` 顶部必须只读取一次 snapshot：`const auto snapshot = documentController->loadSnapshot();`
    - `syncImportedAraClipIfNeeded()` 顶部必须只解析一次 preferred region：`const auto* preferredRegionView = resolvePreferredAraRegionView(*snapshot);`
    - 函数中显式计算：
      - `snapshotAdvanced = snapshot->epoch != lastConsumedAraSnapshotEpoch_`
      - `preferredRegionChanged = preferredRegionView->regionIdentity != lastConsumedPreferredAraRegion_`
      - `appliedRegionChanged = binding.appliedRegion != preferredRegionView->regionIdentity`
    - 只有在 `snapshotAdvanced`、`preferredRegionChanged`、`contentChanged`、`mappingChanged`、`appliedRegionChanged` 全部为 false 时才允许 early return
    - 当 `!contentChanged && !mappingChanged && appliedRegionChanged` 时，必须显式执行 region-only sync：调用 `registerPlaybackRegionClipBinding(...)` 把 current preferred region 重新注册成 applied truth，并更新 `lastConsumedAraSnapshotEpoch_` / `lastConsumedPreferredAraRegion_`
    - 当 `recordRequested()` 成功导入当前 preferred region 时，导入路径继续只使用 `preferredRegionView->copiedAudio`、`preferredRegionView->sampleRate`、`preferredRegionView->sourceStartSeconds`、`preferredRegionView->sourceEndSeconds`、`preferredRegionView->playbackStartSeconds`
    - `PluginEditor.cpp` 不允许出现 `getCurrentPlaybackAudioSource`、retry、source-level getter、second snapshot load
  </action>
  <acceptance_criteria>
    - `Source/Plugin/PluginEditor.cpp` contains `snapshot->epoch`
    - `Source/Plugin/PluginEditor.cpp` contains `binding.appliedRegion`
    - `Source/Plugin/PluginEditor.cpp` contains `lastConsumedAraSnapshotEpoch_`
    - `Source/Plugin/PluginEditor.cpp` contains `lastConsumedPreferredAraRegion_`
    - `Source/Plugin/PluginEditor.cpp` contains `registerPlaybackRegionClipBinding`
    - `Source/Plugin/PluginEditor.cpp` does not contain `getCurrentPlaybackAudioSource`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "snapshot->epoch|binding\.appliedRegion|lastConsumedAraSnapshotEpoch_|lastConsumedPreferredAraRegion_|registerPlaybackRegionClipBinding" Source/Plugin/PluginEditor.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getCurrentPlaybackAudioSource" Source/Plugin/PluginEditor.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase25-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    editor 已改成 snapshot epoch / preferred-region truth consumer，same-source preferred switch 不再依赖 revision coincidence。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 用 fresh evidence 关闭 Phase 25 verification source</name>
  <files>.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    用 fresh build/test/grep-zero 结果回写 `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md`。要求精确如下：
    - 运行 L1：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests`
    - 运行 L2：`& ".\build-phase25-docs\OpenTuneTests.exe"`
    - 运行 L6：`& ".\build-phase25-docs\OpenTuneTests.exe"`
    - L4 必须同时记录：
      - `OpenTunePlaybackRenderer.cpp` 的 `processBlock()` 中 `loadSnapshot()` 只出现一次
      - `PluginEditor.cpp` 中存在 `snapshot->epoch`、`binding.appliedRegion`、`lastConsumedAraSnapshotEpoch_`、`lastConsumedPreferredAraRegion_`
      - `OpenTunePlaybackRenderer.cpp` 与 `PluginEditor.cpp` grep-zero：`getAudioSourceState`、`getPlaybackRangeForAudioSource`、`getSourceRangeForAudioSource`、`getCurrentPlaybackAudioSource`
    - `GREEN Verification` 必须逐条记录四个 `CONS_*` tests 的 PASS 行
    - 如果任一 blocking test 失败，不允许自动跳过；必须先修复再回写 GREEN evidence
  </action>
  <acceptance_criteria>
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `GREEN Verification`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `CONS_03_RecordRequestedImportsPreferredRegionViewOnly`
    - `.planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md` contains `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase25-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase25-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "GREEN Verification|CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock|CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth|CONS_03_RecordRequestedImportsPreferredRegionViewOnly|CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump" .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "getAudioSourceState|getPlaybackRangeForAudioSource|getSourceRangeForAudioSource|getCurrentPlaybackAudioSource" Source/ARA/OpenTunePlaybackRenderer.cpp Source/Plugin/PluginEditor.cpp) { exit 1 }</automated>
  </verify>
  <done>
    Phase 25 的 verification source 已经绑定 fresh build/test/grep-zero evidence，可以直接作为 execute-phase 的唯一验收口径。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snapshot truth -> editor sync | 如果 editor 不以 epoch + preferred-region truth 为唯一入口，same-source switch 仍会被遗漏 |
| verification source -> phase closure | 如果不把 fresh evidence 写回 `25-TEST-VERIFICATION.md`，Phase 25 closure 会再次沦为口头说明 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-31 | I | editor timer sync | mitigate | 用 `snapshot->epoch`、`binding.appliedRegion`、local epoch state 组成唯一 sync-decision truth。 |
| T-25-32 | R | manual import path | mitigate | `recordRequested()` 继续只从 preferred region view 读取 copied audio 与 mapping。 |
| T-25-33 | R | verification closure | mitigate | L1/L2/L4/L6 全部以 `build-phase25-docs` fresh evidence 回写到 verification source。 |
</threat_model>

<verification>
- `PluginEditor.cpp` 只按 snapshot epoch 与 preferred-region truth 驱动 sync。
- `recordRequested()` 与 `syncImportedAraClipIfNeeded()` 都继续只消费 preferred region view。
- `25-TEST-VERIFICATION.md` 已记录 fresh build/test/grep-zero 的 GREEN evidence。
</verification>

<success_criteria>
- [ ] editor timer 只按 snapshot epoch 与 preferred-region truth 决定 sync。
- [ ] manual import 与定时同步都继续只从 preferred region view 读取音频与 mapping。
- [ ] Phase 25 verification source 已用 fresh evidence 关闭。
</success_criteria>

<output>
After completion, create `.planning/phases/25-snapshot-editor/25-04-SUMMARY.md`
</output>
