---
phase: 25-snapshot-editor
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
autonomous: true
requirements: [CONS-01, CONS-02, CONS-03, CONS-04]

must_haves:
  truths:
    - "Phase 25 的 consumer claims 必须先由可执行测试和源码审计锁住，不能只靠人工阅读宣布 renderer/editor 已收敛。"
    - "`CONS-04` 的关键不是 mappingRevision 递增，而是 preferred region 切换时 snapshot 暴露出 `binding.appliedRegion != regionIdentity` 的待同步真相。"
    - "renderer/editor 的 legacy getter、`currentPlaybackAudioSource`、retry 思路必须继续 grep-zero。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 25 executable snapshot-consumer guards"
      contains: "runPhase25SnapshotConsumerTests"
    - path: ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"
      provides: "Phase 25 RED/GREEN evidence source"
      contains: "CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock"
  key_links:
    - from: ".planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "verification source 先写死四条 consumer journeys，再让 tests 承接 exact names 与 build/test commands"
      pattern: "runPhase25SnapshotConsumerTests"
---

<objective>
按 Test-Driven Spec 先补 Phase 25 的 executable guards：把 renderer single-snapshot read、editor epoch consumer、preferred-region-only import、以及 appliedRegion region-switch truth 全部写成可执行测试或静态审计，再允许 consumer implementation 宣称完成。

Purpose: 当前 live tree 已有 Phase 23/24 的 snapshot 与 lifecycle 基线，但 Phase 25 最容易滑回“看起来逻辑对、其实没有 guard”的 consumer 层。只有先补齐 tests 和 RED/GREEN evidence，后续 implementation 才不会再次变成一次性修补。

Output: `Tests/TestMain.cpp` 中的 `runPhase25SnapshotConsumerTests()`，以及 `25-TEST-VERIFICATION.md` 中与 Phase 25 对齐的 RED/GREEN evidence。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/25-snapshot-editor/25-01-PLAN.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
Phase-25 test names must be exact:
```text
CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock
CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth
CONS_03_RecordRequestedImportsPreferredRegionViewOnly
CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump
```

Source-audit helper patterns already available in `Tests/TestMain.cpp`:
```cpp
juce::String loadRepositoryTextFile(const juce::String& relativePath);
juce::String extractSection(const juce::String& text,
                           const juce::String& startMarker,
                           const juce::String& endMarker);
int countOccurrences(const juce::String& text, const juce::String& needle);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 按 Test-Driven Spec 写 Phase 25 snapshot-consumer guards</name>
  <files>Tests/TestMain.cpp, .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md</files>
  <behavior>
    - Test 1: `CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock` 证明 renderer 的 `processBlock()` 每个 block 只执行一次 `loadSnapshot()`，并直接按 playback region 解析 view。
    - Test 2: `CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth` 证明 editor sync 逻辑消费 `snapshot->epoch`、`binding.appliedRegion`、`lastConsumedAraSnapshotEpoch_`、`lastConsumedPreferredAraRegion_`，而不是旧 getter。
    - Test 3: `CONS_03_RecordRequestedImportsPreferredRegionViewOnly` 证明 `recordRequested()` 只从 preferred region view 读 copied audio 与 mapping，不触碰 `currentPlaybackAudioSource` 或 retry。
    - Test 4: `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump` 证明同一 source 下切换 preferred region 时，即使 content/mapping revision 不变，snapshot 仍暴露 `appliedRegion != regionIdentity` 的待同步真相。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTunePlaybackRenderer.cpp
    - Source/Plugin/PluginEditor.h
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase25SnapshotConsumerTests()` 并接入主测试流程。测试要求精确如下：
    - `CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock`
      - 读取 `Source/ARA/OpenTunePlaybackRenderer.cpp`
      - 抽取 `bool OpenTunePlaybackRenderer::processBlock` 的函数体
      - 断言函数体里 `loadSnapshot()` 只出现一次
      - 断言函数体包含 `findRenderableRegionView(*snapshot, region)`
      - 断言函数体不包含 `getAudioSourceState`、`getPlaybackRangeForAudioSource`、`getSourceRangeForAudioSource`、`getCurrentPlaybackAudioSource`
    - `CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth`
      - 读取 `Source/Plugin/PluginEditor.cpp` 与 `Source/Plugin/PluginEditor.h`
      - 抽取 `void OpenTuneAudioProcessorEditor::syncImportedAraClipIfNeeded()` 的函数体
      - 断言函数体包含 `snapshot->epoch`、`binding.appliedRegion`、`lastConsumedAraSnapshotEpoch_`、`lastConsumedPreferredAraRegion_`
      - 断言函数体不包含 `getCurrentPlaybackAudioSource`
    - `CONS_03_RecordRequestedImportsPreferredRegionViewOnly`
      - 抽取 `void OpenTuneAudioProcessorEditor::recordRequested()` 的函数体
      - 断言函数体包含 `resolvePreferredAraRegionView(*snapshot)`、`preferredRegionView->copiedAudio`、`prepareClipFromAraRegion`、`registerPlaybackRegionClipBinding`
      - 断言函数体不包含 `getCurrentPlaybackAudioSource`、`retry`
    - `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump`
      - 通过 `OpenTuneDocumentController::buildSnapshotForPublication(...)` 构造同一 source 下两个 region 的 snapshot
      - 两个 region 的 `clipId` 保持相同、`contentRevision` 保持相同、`mappingRevision` 保持相同
      - 把 preferred region 从 A 切到 B，同时让 `binding.appliedRegion` 继续指向 A
      - 断言 snapshot B 的 preferred view 满足：`binding.clipId` 仍可用、`binding.appliedRegion != regionIdentity`、`contentRevision` 不变、`mappingRevision` 不变
    - 写完测试后，先运行 L1/L2/L6；若当前 live tree 为 RED，必须把失败输出写回 `25-TEST-VERIFICATION.md`
    - 当实现进入 GREEN 后，必须再次运行相同 L1/L2/L6，并把 PASS evidence 回写到 `25-TEST-VERIFICATION.md`
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase25SnapshotConsumerTests`
    - `Tests/TestMain.cpp` contains `CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock`
    - `Tests/TestMain.cpp` contains `CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth`
    - `Tests/TestMain.cpp` contains `CONS_03_RecordRequestedImportsPreferredRegionViewOnly`
    - `Tests/TestMain.cpp` contains `CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump`
    - `Tests/TestMain.cpp` contains `runPhase25SnapshotConsumerTests();`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase25-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase25-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase25-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase25SnapshotConsumerTests|CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock|CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth|CONS_03_RecordRequestedImportsPreferredRegionViewOnly|CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase25SnapshotConsumerTests\(\);" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "CONS_01_PlaybackRendererLoadsSnapshotOncePerBlock|CONS_02_EditorTimerUsesSnapshotEpochAndPreferredRegionTruth|CONS_03_RecordRequestedImportsPreferredRegionViewOnly|CONS_04_PreferredRegionSwitchTriggersMappingSyncWithoutRevisionBump" .planning/phases/25-snapshot-editor/25-TEST-VERIFICATION.md; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 25 的 consumer claims 已被可执行测试与源码审计锁住，后续实现不能再绕过 appliedRegion / snapshot epoch 语义。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test contract -> consumer implementation | 如果 Phase 25 直接改 renderer/editor 而没有先补 guards，任何“看起来正确”的 consumer 改动都可能漏掉 region-switch truth |
| snapshot truth -> editor sync decisions | 如果 tests 不锁 `appliedRegion != regionIdentity` 这类 case，same-source preferred switch 会再次依赖 revision coincidence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-11 | R | renderer contract | mitigate | 用 `processBlock` source audit 强制 `loadSnapshot()` 单次读取与 direct region lookup。 |
| T-25-12 | I | editor sync contract | mitigate | 用 Phase 25 tests 锁定 `snapshot->epoch`、`binding.appliedRegion`、local epoch state 的共同存在。 |
| T-25-13 | T | preferred-region switch | mitigate | 构造 same-source / same-revision / appliedRegion-stale snapshot case，强制 tests 在 revision 不变时仍能暴露待同步真相。 |
</threat_model>

<verification>
- `Tests/TestMain.cpp` 已定义 `runPhase25SnapshotConsumerTests()` 并接到主测试流程。
- `build-phase25-docs` 的 L1/L2/L6 命令已在 `25-TEST-VERIFICATION.md` 中记录 RED/GREEN 结果。
- renderer/editor 的 legacy getter 与 `currentPlaybackAudioSource` 继续保持 grep-zero。
</verification>

<success_criteria>
- [ ] Phase 25 已有 executable snapshot-consumer guards。
- [ ] `CONS-04` 已被 same-source / same-revision 的 appliedRegion case 直接锁住。
- [ ] 任何 consumer implementation claim 都必须建立在 fresh build/test evidence 上。
</success_criteria>

<output>
After completion, create `.planning/phases/25-snapshot-editor/25-02-SUMMARY.md`
</output>
