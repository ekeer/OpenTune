---
phase: 09-lifecycle-binding
plan: 02
type: execute
wave: 1
depends_on: [09-01]
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/Plugin/PluginEditor.cpp
  - Source/Inference/RenderCache.h
  - Source/Inference/RenderCache.cpp
autonomous: true
requirements: [READ-03, STAB-01]

must_haves:
  truths:
    - "replace 保留 clipId，复用既有 renderCache，不允许重建 cache"
    - "仅失效受影响 chunk，禁止全量 pending 风暴"
    - "mapping-only 变化仅更新时间与映射，不触发内容重建"
  artifacts:
    - path: "Source/PluginProcessor.cpp"
      provides: "replace 路径的 cache 复用 + 范围失效实现"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "同步路径改为调用核心 replace/映射更新接口"
  key_links:
    - from: "syncImportedAraClipIfNeeded"
      to: "replaceClipAudioById + enqueuePartialRenderById"
      via: "diff range"
      pattern: "partial invalidation only"
---

<objective>
将 replace 与高频编辑路径收敛为“保留 clipId + 复用 cache + 范围失效”，满足 STAB-01。

Purpose: 消除 `replaceClipAudioById` 的 cache 重建和全量失效风险，保障持续编辑期间可回放。
Output: 范围失效替换路径与 revision 去抖协同。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/09-lifecycle-binding/09-CONTEXT.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Plugin/PluginEditor.cpp
@Source/Inference/RenderCache.h
@Source/Inference/RenderCache.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 重构 replaceClipAudioById 为 cache 复用语义</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: replace 后 clipId 不变。
    - Test 2: replace 前后 renderCache 指针保持同一实例。
    - Test 3: replace 不触发全量 chunk 失效。
  </behavior>
  <action>
    重构 `replaceClipAudioById`：
    - 禁止 `it->renderCache = std::make_shared<RenderCache>()`；
    - 保留旧 cache 并仅更新音频、静息区、起始时间等必要字段；
    - 为后续范围失效提供输入（变更区间或 diff 结果）。
  </action>
  <verify>
    <automated>rtk rg -n "replaceClipAudioById|renderCache = std::make_shared<RenderCache>" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>replace 语义符合“clip 身份稳定 + cache 复用”。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 建立统一 diff 区间到 chunk 失效的执行路径</name>
  <files>Source/PluginProcessor.cpp, Source/Plugin/PluginEditor.cpp</files>
  <behavior>
    - Test 1: 仅 source 内容变更时，只对 diff 覆盖区间发起 partial render。
    - Test 2: 仅 mapping 变更时，不发起内容重渲染。
    - Test 3: 高频编辑连续触发时，不出现全量 pending 增长。
  </behavior>
  <action>
    收敛失效逻辑：
    1) 统一生成 changed range（diff 区间 + 编辑区间并集）；
    2) 只调用 `enqueuePartialRenderById` 失效受影响窗口；
    3) mapping-only 更新仅走 `setClipStartSecondsById` 与绑定 revision 更新。

    要求：不得引入全量 invalidate fallback。
  </action>
  <verify>
    <automated>rtk rg -n "enqueuePartialRenderById|setClipStartSecondsById|detectAudioDiffRange" Source/PluginProcessor.cpp Source/Plugin/PluginEditor.cpp</automated>
  </verify>
  <done>失效收敛路径固定，revision 风暴风险受控。</done>
</task>

<task type="auto">
  <name>Task 3: 对齐日志与诊断字段用于 STAB-01 观测</name>
  <files>Source/PluginProcessor.cpp, Source/Plugin/PluginEditor.cpp</files>
  <action>
    为 replace/失效路径统一输出可观测字段（clipId、changedStart、changedEnd、mappingOnly、requestedChunks），用于 Phase 9 验证文档引用。

    仅增加必要日志，不新增并行诊断通道。
  </action>
  <verify>
    <automated>rtk rg -n "clipId=.*changed|mappingOnly|requestedChunks|RenderTrace" Source/PluginProcessor.cpp Source/Plugin/PluginEditor.cpp</automated>
  </verify>
  <done>STAB-01 具备最小可观测证据。</done>
</task>

</tasks>

<verification>
- replace 后 clipId 与 renderCache 实例保持稳定。
- 失效范围由 diff/edit 区间决定，不存在全量 pending。
- mapping-only 更新不触发内容重建。
</verification>

<success_criteria>
- [ ] READ-03: replace 路径无悬空映射与身份漂移。
- [ ] STAB-01: 高频编辑时仅受影响 chunk 失效。
- [ ] 代码中不存在 replace 重建 renderCache 行为。
</success_criteria>

<output>
After completion, create `.planning/phases/09-lifecycle-binding/09-02-SUMMARY.md`
</output>
