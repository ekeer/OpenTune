---
phase: 09-lifecycle-binding
plan: 01
type: execute
wave: 1
depends_on: [08-04]
files_modified:
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
autonomous: true
requirements: [READ-03, STAB-01]

must_haves:
  truths:
    - "AudioSource -> clipId -> renderCache 绑定真相源在核心层维护，UI 不持有权威绑定状态"
    - "导入后绑定一次建档；后续同步只基于 content/mapping revision 驱动"
    - "PluginEditor 不再维护 AraImportedClipBinding 状态机"
  artifacts:
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "生命周期绑定查询与收敛接口"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "绑定状态机与 revision 驱动同步入口"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "仅触发核心同步，不持有绑定真相源"
  key_links:
    - from: "OpenTuneAudioProcessorEditor::recordRequested"
      to: "OpenTuneDocumentController core binding API"
      via: "import + bind"
      pattern: "single source of truth"
---

<objective>
将 ARA source 与 clip 生命周期绑定下沉到核心层，移除 UI 侧权威绑定状态，建立 READ-03 的结构前提。

Purpose: 消除 UI/Core 双真相源，确保导入与后续同步路径都依赖同一个核心绑定索引。
Output: 核心层 binding registry + UI 无状态消费改造。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-lifecycle-binding/09-CONTEXT.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 在 DocumentController 定义 AudioSource 绑定真相源</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp</files>
  <behavior>
    - Test 1: 导入后，给定 audioSource 能查询到绑定 clipId 与已应用 revisions。
    - Test 2: 未绑定 source 返回空绑定，不读取陈旧映射。
  </behavior>
  <action>
    在 DocumentController 新增绑定结构（例如 `AudioSourceClipBinding`）与查询/更新接口：
    - 记录 `clipId`、已应用 `contentRevision`、已应用 `mappingRevision`；
    - 记录最近一次 source/playback range 快照；
    - 绑定状态只在核心层修改，提供只读查询给 UI。

    要求：禁止在 UI 侧保留并行权威状态。
  </action>
  <verify>
    <automated>rtk rg -n "AudioSourceClipBinding|bind.*AudioSource|getBoundClip" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp</automated>
  </verify>
  <done>核心绑定模型已存在，后续导入/同步/删除都可复用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 导入路径改为核心建档并回写 clip 映射</name>
  <files>Source/Plugin/PluginEditor.cpp, Source/ARA/OpenTuneDocumentController.cpp, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: 首次导入完成后，source->clip 映射在核心层可见。
    - Test 2: 同一 source 重复触发同步时，不新建 clip，不产生新绑定记录。
  </behavior>
  <action>
    将 `recordRequested` 从“UI 持有绑定”改为“核心建档”：
    1) 导入成功后由核心层注册 source->clip 绑定；
    2) UI 仅保留当前选中 clip 的展示同步，不维护 binding/revision 状态；
    3) 保持 VST3 单轨语义与 Standalone 分支隔离不变。
  </action>
  <verify>
    <automated>rtk rg -n "recordRequested|bind.*AudioSource|AraImportedClipBinding" Source/Plugin/PluginEditor.cpp Source/ARA/OpenTuneDocumentController.cpp</automated>
  </verify>
  <done>导入后绑定真相源位于核心，UI 只消费。</done>
</task>

<task type="auto">
  <name>Task 3: 移除 PluginEditor 的 AraImportedClipBinding 权威状态</name>
  <files>Source/Plugin/PluginEditor.h, Source/Plugin/PluginEditor.cpp</files>
  <action>
    删除/退役 `AraImportedClipBinding` 结构及其状态写入点，`syncImportedAraClipIfNeeded` 改为调用核心层“按 source 同步到已绑定 clip”接口。

    保留 UI 层职责：
    - 刷新 PianoRoll 显示；
    - 触发后处理与 F0 提取；
    - 不维护 source->clip 权威关系。
  </action>
  <verify>
    <automated>rtk rg -n "AraImportedClipBinding|syncImportedAraClipIfNeeded" Source/Plugin/PluginEditor.h Source/Plugin/PluginEditor.cpp</automated>
  </verify>
  <done>UI 权威绑定状态移除，单真相源成立。</done>
</task>

</tasks>

<verification>
- 核心层可查询 source->clip 绑定与已应用 revision。
- UI 层不再写入绑定真相源，仅调用核心 API。
- Standalone 路径无行为改动（SAFE-01）。
</verification>

<success_criteria>
- [ ] READ-03 前置结构成立：绑定真相源位于核心层。
- [ ] PluginEditor 不再维护 AraImportedClipBinding 权威状态。
- [ ] 导入后同一 source 的后续同步走同一绑定记录。
</success_criteria>

<output>
After completion, create `.planning/phases/09-lifecycle-binding/09-01-SUMMARY.md`
</output>
