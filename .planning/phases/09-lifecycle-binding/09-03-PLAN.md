---
phase: 09-lifecycle-binding
plan: 03
type: verify
wave: 2
depends_on: [09-01, 09-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/09-lifecycle-binding/09-VERIFICATION.md
autonomous: true
requirements: [READ-03, STAB-01]

must_haves:
  truths:
    - "READ-03 必须有导入/replace/删除完整生命周期自动化验证"
    - "STAB-01 必须有局部失效与无全量风暴的证据"
    - "未完成人工 DAW 证据时不可宣告 phase complete"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "生命周期与失效收敛测试"
    - path: ".planning/phases/09-lifecycle-binding/09-VERIFICATION.md"
      provides: "验收矩阵、证据、gate 结论"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/09-lifecycle-binding/09-VERIFICATION.md"
      via: "Traceability"
      pattern: "READ-03|STAB-01"
---

<objective>
建立 Phase 9 验证协议并执行，确认生命周期绑定与失效收敛满足 READ-03 / STAB-01。

Purpose: 在进入 Phase 10 前锁定 source-clip 生命周期一致性和高频编辑稳定性。
Output: 自动化测试 + 可审计验证报告 + gate 结论。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/09-lifecycle-binding/09-CONTEXT.md
@Source/ARA/OpenTuneDocumentController.cpp
@Source/PluginProcessor.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 增加 READ-03 生命周期测试矩阵</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: 导入后 source->clip 映射存在且可命中当前 clip。
    - Test 2: replace 后映射仍命中同一 clipId，未出现悬空引用。
    - Test 3: 删除 source 后映射回收，相关 clip 不再可访问。
  </behavior>
  <action>
    在 TestMain 增加生命周期 section，覆盖 import -> replace -> destroy 全流程，断言绑定一致性与回收语义。
  </action>
  <verify>
    <automated>rtk cmake --build build --config Debug --target OpenTuneTests && rtk test .\build\Debug\OpenTuneTests.exe</automated>
  </verify>
  <done>READ-03 具备自动化回归保护。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 增加 STAB-01 局部失效压力测试</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: 高频局部编辑仅触发局部 chunk pending。
    - Test 2: 连续 replace 不触发全量失效。
    - Test 3: revision 发布可追平最后一次有效编辑。
  </behavior>
  <action>
    增加局部失效压力测试，采集 pending chunk 数量与失效覆盖范围，验证无 revision storm。
  </action>
  <verify>
    <automated>rtk rg -n "READ-03|STAB-01|lifecycle|partial|chunk" Tests/TestMain.cpp</automated>
  </verify>
  <done>STAB-01 在自动化层具备可复验证据。</done>
</task>

<task type="auto">
  <name>Task 3: 形成 Phase 9 验证报告与 Gate 判定</name>
  <files>.planning/phases/09-lifecycle-binding/09-VERIFICATION.md</files>
  <action>
    完成验证文档，至少包含：
    1) READ-03 / STAB-01 Traceability；
    2) 自动化命令、结果与失败判据；
    3) REAPER 人工验证流程（导入/替换/删除场景）；
    4) Gate 结论（PASS / human_needed / gaps_found）。

    人工证据缺失时必须标注 `human_needed`。
  </action>
  <verify>
    <automated>rtk rg -n "READ-03|STAB-01|Traceability|REAPER|Gate|human_needed" .planning/phases/09-lifecycle-binding/09-VERIFICATION.md</automated>
  </verify>
  <done>Phase 9 验证可审计、可复验。</done>
</task>

</tasks>

<verification>
- 自动化：生命周期矩阵 + 局部失效压力测试。
- 手动：REAPER 导入/replace/删除一致性验收。
- Gate：READ-03 与 STAB-01 均有证据链。
</verification>

<success_criteria>
- [ ] READ-03 生命周期全链路测试通过并留档。
- [ ] STAB-01 局部失效压力测试通过并留档。
- [ ] 09-VERIFICATION 文档给出明确 gate 结论。
</success_criteria>

<output>
After completion, create `.planning/phases/09-lifecycle-binding/09-03-SUMMARY.md`
</output>
