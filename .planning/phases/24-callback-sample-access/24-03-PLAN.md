---
phase: 24-callback-sample-access
plan: 03
type: execute
wave: 3
depends_on: [24-02]
files_modified:
  - Tests/TestMain.cpp
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
autonomous: true
requirements: [LIFE-04]

must_haves:
  truths:
    - "host 移除 playback region 或 audio source 后，published snapshot 立即去除对应 stale region/source/binding，不等待后续 timer、retry 或 editor sync。"
    - "removal lifecycle 与 preferredRegion_ reconcile 复用一套 state helper，不会留下 dangling region truth。"
    - "Phase 24 最终以 fresh `build-phase24-docs` L1/L2/L4/L6 evidence 关闭，而不是沿用 Phase 23 或口头验证。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 24 removal lifecycle guards"
      contains: "LIFE_04_RemovalPurgesStaleSnapshotTruth"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "shared removal helpers and immediate removal publish path"
      contains: "willRemovePlaybackRegionFromAudioModification"
    - path: ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"
      provides: "fresh Phase 24 GREEN evidence"
      contains: "[PASS] LIFE_04_RemovalPurgesStaleSnapshotTruth"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "tests 先锁住 removal helper 和 immediate publish，再让 controller wiring 转绿"
      pattern: "willRemovePlaybackRegionFromAudioModification"
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"
      via: "fresh build/test/grep-zero 证据必须回写 verification source of truth，才能关闭 Phase 24"
      pattern: "build-phase24-docs"
---

<objective>
完成 Phase 24 的最后一块：把 playback region / audio source removal 收敛成 immediate stale-truth purge，并用 fresh evidence 关闭整个 phase。

Purpose: 经过 24-02 后，sample access 和 content dirty 已有 callback-driven contract，但 `LIFE-04` 仍要求 host removal 立即从 published snapshot 移除 stale region/source/binding。若 removal 仍等到后续 destroy path 或 editor cleanup 才发生，snapshot 会继续暴露悬挂真相。

Output: removal lifecycle tests、controller removal helpers / wiring、以及写回 `24-TEST-VERIFICATION.md` 的完整 GREEN evidence。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
@.planning/phases/24-callback-sample-access/24-02-PLAN.md
@.planning/phases/23-snapshot-region/23-03-SUMMARY.md
@JUCE-master/modules/juce_audio_processors_headless/utilities/ARA/juce_ARAModelObjects.h
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Tests/TestMain.cpp

<interfaces>
JUCE removal callback contract to implement exactly:
```cpp
void willRemovePlaybackRegionFromAudioModification (ARAAudioModification* audioModification,
                                                    ARAPlaybackRegion* playbackRegion) override;
void willDestroyPlaybackRegion (ARAPlaybackRegion* playbackRegion) override;
void willDestroyAudioSource (ARAAudioSource* audioSource) override;
```

Phase-24 final journey that must appear in tests and verification docs:
```text
LIFE_04_RemovalPurgesStaleSnapshotTruth
```

Removal helpers introduced in 24-01 that this plan must wire for real:
```cpp
bool removePlaybackRegionFromState(juce::ARAPlaybackRegion* playbackRegion);
bool removeAudioSourceFromState(juce::ARAAudioSource* audioSource);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先补 removal lifecycle guards，锁住 stale-truth purge 行为</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `LIFE_04_RemovalPurgesStaleSnapshotTruth` 证明 source audit 中存在 `willRemovePlaybackRegionFromAudioModification`，且 removal helper 会清除 region-level binding truth。
    - Test 2: `LIFE_04_AudioSourceRemovalPurgesDependentRegions` 证明 source removal 后 snapshot 不再保留任何引用该 audio source 的 published region truth。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - JUCE-master/modules/juce_audio_processors_headless/utilities/ARA/juce_ARAModelObjects.h
  </read_first>
  <action>
    扩充 `runPhase24LifecycleCallbackTests()`，要求精确如下：
    - 新增两条精确测试名：
      - `LIFE_04_RemovalPurgesStaleSnapshotTruth`
      - `LIFE_04_AudioSourceRemovalPurgesDependentRegions`
    - 两条测试都必须复用 `loadRepositoryTextFile(...)` 对 controller h/cpp 做 wiring audit，并至少断言：
      - header/cpp 都存在 `willRemovePlaybackRegionFromAudioModification`
      - cpp 存在 `removePlaybackRegionFromState`
      - cpp 存在 `removeAudioSourceFromState`
      - removal callbacks 定义体中出现 `publishSnapshot()`
    - `LIFE_04_RemovalPurgesStaleSnapshotTruth` 还必须直接用 `buildSnapshotForPublication(...)` 构造两个 region 的 snapshot，再模拟从 `regionSlots` 集合删除目标 region 后调用 `publishPendingSnapshot(...)`，断言新的 snapshot 不再能 `findRegion(removedRegion)`
    - `LIFE_04_AudioSourceRemovalPurgesDependentRegions` 必须构造同一 `audioSource` 关联多个 region 的 source/region vectors，再在删除 source 对应 state 后调用 `buildSnapshotForPublication(...)`，断言 `publishedRegions` 中不存在任何 `regionIdentity.audioSource == removedSource` 的 view
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `LIFE_04_RemovalPurgesStaleSnapshotTruth`
    - `Tests/TestMain.cpp` contains `LIFE_04_AudioSourceRemovalPurgesDependentRegions`
    - `Tests/TestMain.cpp` contains `removePlaybackRegionFromState`
    - `Tests/TestMain.cpp` contains `removeAudioSourceFromState`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "LIFE_04_RemovalPurgesStaleSnapshotTruth|LIFE_04_AudioSourceRemovalPurgesDependentRegions" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    `LIFE-04` 已先被 tests 锁住，controller 不能再通过 destroy 顺序或 editor side-effect 侥幸通过验收。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 把 removal callbacks 收敛到 shared helper，并在 callback 内立即发布 stale-truth purge</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Tests/TestMain.cpp
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
  </read_first>
  <action>
    实现 Phase 24 的 removal lifecycle，要求精确如下：
    - `removePlaybackRegionFromState(playbackRegion)` 必须：
      - 从 `regions_` 删除该 `playbackRegion`
      - 如果 `preferredRegion_` 正指向它，则重新 `reconcilePreferredRegion()`
      - 通过删除 `RegionSlot` 一并删除该 region 上的 `AudioSourceClipBinding`
      - 返回是否真的发生状态删除
    - `removeAudioSourceFromState(audioSource)` 必须：
      - 从 `sources_` 删除该 `audioSource`
      - 删除所有 `region.identity.audioSource == audioSource` 的 `RegionSlot`
      - 清理/重算 `preferredRegion_`
      - 返回是否真的发生状态删除
    - `willRemovePlaybackRegionFromAudioModification(...)` 调用 `removePlaybackRegionFromState(playbackRegion)`；若返回 true，立即执行 `publishSnapshot()`
    - `willDestroyPlaybackRegion(...)` 复用同一个 helper；如果 region 还没提前删除，则同样立即 `publishSnapshot()`
    - `willDestroyAudioSource(...)` 调用 `removeAudioSourceFromState(audioSource)`；若返回 true，立即执行 `publishSnapshot()`
    - removal 路径不得依赖 timer、retry、editor sync 或后续 `didEndEditing()` 才清除 stale snapshot truth
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `willRemovePlaybackRegionFromAudioModification`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `removePlaybackRegionFromState`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `removeAudioSourceFromState`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `willDestroyAudioSource`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `publishSnapshot()`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "willRemovePlaybackRegionFromAudioModification|removePlaybackRegionFromState|removeAudioSourceFromState|willDestroyAudioSource" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    host removal 现在直接触发 stale-truth purge，不再留下 dangling region/source/binding 给后续读侧消费。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 用 fresh build/test/grep-zero 证据关闭 Phase 24，并回写 verification source</name>
  <files>.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md, Tests/TestMain.cpp</files>
  <read_first>
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - Tests/TestMain.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
  </read_first>
  <action>
    更新 `24-TEST-VERIFICATION.md` 的 GREEN evidence，并严格使用 fresh Phase 24 build/test 结果。要求精确如下：
    - 记录 L1 命令：`pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests`
    - 记录 L2 / L6 命令：`& ".\build-phase24-docs\OpenTuneTests.exe"`
    - `GREEN Verification` 区必须逐条写出 PASS：
      - `[PASS] LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry`
      - `[PASS] LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks`
      - `[PASS] LIFE_03_ContentDirtyPublishesAtDidEndEditing`
      - `[PASS] LIFE_04_RemovalPurgesStaleSnapshotTruth`
      - `[PASS] LIFE_04_AudioSourceRemovalPurgesDependentRegions`
    - `L4 Static Audit Checklist` 结果必须额外写明：controller h/cpp grep-zero `Timer::callAfterDelay` 与 `sampleAccessRetryCount`，并确认 removal callbacks 使用 immediate `publishSnapshot()`
    - 先跑一次 build，再跑两次 `OpenTuneTests.exe`，最后做一次 grep-zero：`rg -n "Timer::callAfterDelay|sampleAccessRetryCount" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp`
  </action>
  <acceptance_criteria>
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `[PASS] LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry`
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `[PASS] LIFE_04_RemovalPurgesStaleSnapshotTruth`
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `build-phase24-docs`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "Timer::callAfterDelay|sampleAccessRetryCount" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; rg -n "\[PASS\] LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry|\[PASS\] LIFE_04_RemovalPurgesStaleSnapshotTruth|build-phase24-docs" ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 24 已有 fresh build/test/grep-zero 证据，可以把 lifecycle callback 收敛结论绑定到这份 verification source，而不是口头说明。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| host removal callbacks -> controller mutable state | 如果 removal 仍等到 destroy 顺序或 editor cleanup，snapshot 会继续暴露 stale region/source truth |
| fresh verification evidence -> phase closure | 如果继续沿用旧 build 或只做单次运行，Phase 24 无法证明它真正锁住了 callback lifecycle 回归 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-21 | T | removal callback wiring | mitigate | 用 shared helper 统一删除 region/source state，并在 `willRemove...` / `willDestroy...` callback 内立即 `publishSnapshot()`。 |
| T-24-22 | I | stale snapshot truth | mitigate | tests 直接断言 removed region/source 不再出现在 rebuilt snapshot 中。 |
| T-24-23 | R | verification drift | mitigate | 只接受 `build-phase24-docs` 的 fresh L1/L2/L6 evidence，并在 verification doc 记录 grep-zero 结果。 |
</threat_model>

<verification>
- `OpenTuneTests` 必须通过新增的 Phase 24 removal journeys。
- `rg` 确认 removal helpers 和 callbacks 均已落地，且 controller 不再出现 `Timer::callAfterDelay` / `sampleAccessRetryCount`。
- `24-TEST-VERIFICATION.md` 必须记录 fresh `build-phase24-docs` build/test/grep-zero evidence。
</verification>

<success_criteria>
- [ ] host removal 后 published snapshot 立即清除 stale region/source/binding truth。
- [ ] Phase 24 的全部 requirements 都有 direct tests + verification evidence。
- [ ] `24-TEST-VERIFICATION.md` 成为 Phase 24 closure 的唯一 evidence source。
</success_criteria>

<output>
After completion, create `.planning/phases/24-callback-sample-access/24-03-SUMMARY.md`
</output>
