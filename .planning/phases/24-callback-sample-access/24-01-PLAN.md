---
phase: 24-callback-sample-access
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03]

must_haves:
  truths:
    - "Phase 24 先有 verification source of truth；sample access、content dirty 和 publish gate 的验收条件在实现前已经写死。"
    - "`didUpdateAudioSourceProperties()` 不再承担 host audio copy / retry 职责；这些职责只允许出现在官方 sample-access callbacks。"
    - "`OpenTuneDocumentController` 的 source lifecycle state 明确表达为 `sampleAccessEnabled`、`contentDirty`、`pendingCopyFromHost`，而不是 `sampleAccessRetryCount` 和 delayed retry。"
  artifacts:
    - path: ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"
      provides: "Phase 24 verification source of truth"
      contains: "Phase 24: Callback-Driven Sample Access Lifecycle"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "Phase 24 lifecycle callback contract and source-slot state"
      contains: "willEnableAudioSourceSamplesAccess"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "callback-driven lifecycle scaffolding with no delayed retry entry point"
      contains: "copyAudioSourceSamples"
  key_links:
    - from: ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "verification source 固定 callback 名称、测试命令和 grep-zero 审计，再冻结 header contract"
      pattern: "LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks"
    - from: "Source/ARA/OpenTuneDocumentController.h"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "header 先固定 state/helper/callback 名称，cpp 再承接 compile-follow 实现并守住 build gate"
      pattern: "copyAudioSourceSamples"
---

<objective>
先冻结 Phase 24 的 callback-driven lifecycle contract：把 verification source of truth、header lifecycle state、以及 cpp 中的 callback entry points 一次写清，让后续 TDD 和 removal cleanup 都建立在同一套官方 callback 语义上。

Purpose: 当前 live tree 仍在 `didUpdateAudioSourceProperties()` 中直接读全量音频，并通过 `juce::Timer::callAfterDelay(...)` retry 猜 sample access。若不先把 callback ownership 和 source-slot state 写成唯一 contract，后续测试和 cleanup 只能继续围着错误入口打补丁。

Output: Phase 24 verification source、`OpenTuneDocumentController` 的 lifecycle callback declarations / state fields / helper names，以及与之对应的 cpp compile-follow scaffolding。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/TESTING.md
@.planning/phases/23-snapshot-region/23-03-SUMMARY.md
@.planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
@JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.h
@JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.cpp
@JUCE-master/modules/juce_audio_processors_headless/utilities/ARA/juce_ARAModelObjects.h
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Tests/TestMain.cpp

<interfaces>
Current live-tree problem inside `Source/ARA/OpenTuneDocumentController.cpp`:
```cpp
void didUpdateAudioSourceProperties(juce::ARAAudioSource* audioSource)
{
    // metadata update ...
    tryReadAudioData(audioSource);
}

void tryReadAudioData(juce::ARAAudioSource* audioSource)
{
    // HostAudioReader read + juce::Timer::callAfterDelay retry
}
```

JUCE callback names that Phase 24 must adopt exactly:
```cpp
void doUpdateAudioSourceContent (ARAAudioSource* audioSource, ARAContentUpdateScopes scopeFlags) override;
void willEnableAudioSourceSamplesAccess (ARAAudioSource* audioSource, bool enable) override;
void didEnableAudioSourceSamplesAccess (ARAAudioSource* audioSource, bool enable) override;
void willRemovePlaybackRegionFromAudioModification (ARAAudioModification* audioModification,
                                                    ARAPlaybackRegion* playbackRegion) override;
```

Existing repository test helpers available in `Tests/TestMain.cpp` for later waves:
```cpp
juce::String loadRepositoryTextFile(const juce::String& relativePath);
int countOccurrences(const juce::String& text, const juce::String& needle);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 创建 Phase 24 test-verification source of truth</name>
  <files>.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/codebase/TESTING.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/23-snapshot-region/23-TEST-VERIFICATION.md
  </read_first>
  <action>
    创建 `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md`，并把 Phase 24 的 gate 写成唯一 source of truth。要求精确如下：
    - 标题固定为 `Phase 24: Callback-Driven Sample Access Lifecycle`
    - L1 命令固定为 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests`
    - L2 / L6 命令固定为 `& ".\build-phase24-docs\OpenTuneTests.exe"`
    - `Required Journeys` 必须精确列出：
      - `LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry`
      - `LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks`
      - `LIFE_03_ContentDirtyPublishesAtDidEndEditing`
      - `LIFE_04_RemovalPurgesStaleSnapshotTruth`
    - `L4 Static Audit Checklist` 必须点名：`didUpdateAudioSourceProperties`、`doUpdateAudioSourceContent`、`willEnableAudioSourceSamplesAccess`、`didEnableAudioSourceSamplesAccess`、`willRemovePlaybackRegionFromAudioModification`、`willDestroyPlaybackRegion`、`willDestroyAudioSource`、`copyAudioSourceSamples`、`juce::Timer::callAfterDelay`、`sampleAccessRetryCount`
    - `Evidence To Capture` 必须写明：`didUpdateAudioSourceProperties()` 不再直接读音频、sample access enable/disable 才能创建或清除 copied audio、content dirty 直到 `didEndEditing()` 才发布、region/source removal 后 snapshot 立即去除 stale truth
  </action>
  <acceptance_criteria>
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `Phase 24: Callback-Driven Sample Access Lifecycle`
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `build-phase24-docs`
    - `.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md` contains `LIFE_04_RemovalPurgesStaleSnapshotTruth`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 24: Callback-Driven Sample Access Lifecycle" ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "build-phase24-docs" ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "LIFE_04_RemovalPurgesStaleSnapshotTruth" ".planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md"; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 24 的 verification contract 已在实现前存在，后续执行者不能再自己发明 lifecycle gate。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 在 header 冻结 callback-driven source lifecycle contract</name>
  <files>Source/ARA/OpenTuneDocumentController.h</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - JUCE-master/modules/juce_audio_processors_headless/utilities/ARA/juce_ARAModelObjects.h
    - JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.h
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - .planning/phases/23-snapshot-region/23-03-SUMMARY.md
  </read_first>
  <action>
    更新 `Source/ARA/OpenTuneDocumentController.h`，把 Phase 24 需要的 callback/state/helper contract 一次定型，要求精确如下：
    - 在 public override 区新增：
      - `void doUpdateAudioSourceContent(juce::ARAAudioSource* audioSource, juce::ARAContentUpdateScopes scopeFlags) override;`
      - `void willEnableAudioSourceSamplesAccess(juce::ARAAudioSource* audioSource, bool enable) override;`
      - `void didEnableAudioSourceSamplesAccess(juce::ARAAudioSource* audioSource, bool enable) override;`
      - `void willRemovePlaybackRegionFromAudioModification(juce::ARAAudioModification* audioModification, juce::ARAPlaybackRegion* playbackRegion) override;`
    - 把 `SourceSlot` 的 lifecycle state 明确改成以下字段，并删除 `sampleAccessRetryCount`：
      - `bool sampleAccessEnabled{false};`
      - `bool contentDirty{false};`
      - `bool pendingCopyFromHost{false};`
    - 在 private helpers 中声明以下精确 helper：
      - `void copyAudioSourceSamples(juce::ARAAudioSource* audioSource);`
      - `bool removePlaybackRegionFromState(juce::ARAPlaybackRegion* playbackRegion);`
      - `bool removeAudioSourceFromState(juce::ARAAudioSource* audioSource);`
    - 保持 Phase 23 的 `sources_ + regions_ + preferredRegion_ + publishedSnapshot_` snapshot model 不变；不得引入第二套 state 表或 processor API
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.h` contains `doUpdateAudioSourceContent`
    - `Source/ARA/OpenTuneDocumentController.h` contains `willEnableAudioSourceSamplesAccess`
    - `Source/ARA/OpenTuneDocumentController.h` contains `didEnableAudioSourceSamplesAccess`
    - `Source/ARA/OpenTuneDocumentController.h` contains `willRemovePlaybackRegionFromAudioModification`
    - `Source/ARA/OpenTuneDocumentController.h` contains `sampleAccessEnabled`
    - `Source/ARA/OpenTuneDocumentController.h` contains `contentDirty`
    - `Source/ARA/OpenTuneDocumentController.h` contains `pendingCopyFromHost`
    - `Source/ARA/OpenTuneDocumentController.h` contains `copyAudioSourceSamples`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `sampleAccessRetryCount`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "doUpdateAudioSourceContent" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "willEnableAudioSourceSamplesAccess" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "didEnableAudioSourceSamplesAccess" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "willRemovePlaybackRegionFromAudioModification" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "sampleAccessEnabled|contentDirty|pendingCopyFromHost|copyAudioSourceSamples" Source/ARA/OpenTuneDocumentController.h; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "sampleAccessRetryCount" Source/ARA/OpenTuneDocumentController.h) { exit 1 }</automated>
  </verify>
  <done>
    Header 已把 Phase 24 的 callback ownership、state fields 和 helper names 固定下来，后续 tests/cpp 不需要再猜 lifecycle contract。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: 在 cpp 建立 callback-driven lifecycle scaffolding，并切断 eager read/retry 入口</name>
  <files>Source/ARA/OpenTuneDocumentController.cpp</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.cpp
    - Source/ARA/OpenTuneDocumentController.h
    - JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.cpp
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - .planning/phases/23-snapshot-region/23-03-SUMMARY.md
  </read_first>
  <action>
    更新 `Source/ARA/OpenTuneDocumentController.cpp`，建立 Phase 24 的 compile-follow lifecycle scaffolding，并立即切断错误入口。要求精确如下：
    - 把 `tryReadAudioData(...)` 完整替换成 `copyAudioSourceSamples(...)`；cpp 中不得再保留 `tryReadAudioData` 或 `juce::Timer::callAfterDelay(...)`
    - `didUpdateAudioSourceProperties()` 只更新 `name`、`sampleRate`、`numChannels`、`numSamples` 和 `markSnapshotDirty()`；它不得调用 `HostAudioReader`、`copyAudioSourceSamples(audioSource)`、`publishSnapshot()` 或任何 retry 逻辑
    - `doUpdateAudioSourceContent()` 只在 `scopeFlags.affectSamples()` 时执行：`bumpSourceContentRevision(audioSource)`、`sourceSlot.contentDirty = true`、`sourceSlot.pendingCopyFromHost = true`、`markSnapshotDirty()`；它不得直接 copy 音频或发布 snapshot
    - `willEnableAudioSourceSamplesAccess(audioSource, false)` 必须清除 `copiedAudio`、`sampleAccessEnabled`、`pendingCopyFromHost`，并 `markSnapshotDirty()`；`didEnableAudioSourceSamplesAccess(audioSource, true)` 才允许调用 `copyAudioSourceSamples(audioSource)` 读取 host samples
    - `didEndEditing()` 继续作为 content-driven publish gate；本 task 不把 content change、metadata change 或 sample-access enable 直接升级成即时 `publishSnapshot()`
    - Task 结束时必须通过 `build-phase24-docs` 的 L1 build gate，保证 tree 仍可编译
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `doUpdateAudioSourceContent`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `willEnableAudioSourceSamplesAccess`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `didEnableAudioSourceSamplesAccess`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `copyAudioSourceSamples`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `tryReadAudioData`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `Timer::callAfterDelay`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `sampleAccessRetryCount`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "doUpdateAudioSourceContent|willEnableAudioSourceSamplesAccess|didEnableAudioSourceSamplesAccess|copyAudioSourceSamples" Source/ARA/OpenTuneDocumentController.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "tryReadAudioData|Timer::callAfterDelay|sampleAccessRetryCount" Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }; pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    cpp 已经具备 callback-driven lifecycle scaffolding，错误的 eager read/retry 入口被切断，而且 tree 继续可编译。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ARA audio-source callbacks -> mutable source slot state | 如果 `didUpdateAudioSourceProperties()` 继续直接读音频，Phase 24 会保留错误的 callback ownership |
| mutable lifecycle state -> published snapshot | 如果 content-dirty / sample-access state 没有统一 gate，snapshot 会在错误时间点暴露半更新 truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-01 | T | `didUpdateAudioSourceProperties()` | mitigate | 立即切断 `HostAudioReader` / retry / delayed timer 路径，只保留 metadata update + dirty flag。 |
| T-24-02 | I | source-slot lifecycle state | mitigate | 在 header 明确 `sampleAccessEnabled`、`contentDirty`、`pendingCopyFromHost`，删除 `sampleAccessRetryCount`。 |
| T-24-03 | D | build integration | mitigate | Wave 1 结束时强制通过 `build-phase24-docs` 的 L1 gate，防止 contract 改造把 tree 改断。 |
</threat_model>

<verification>
- `rg` 确认 `24-TEST-VERIFICATION.md`、header callback declarations、source-slot state fields、cpp lifecycle callback definitions 已落地。
- `rg` 确认 `tryReadAudioData`、`sampleAccessRetryCount`、`Timer::callAfterDelay` 已从 controller 生命周期路径消失。
- 执行 `pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests`，证明 Wave 1 结束时 tree 仍可编译。
</verification>

<success_criteria>
- [ ] Phase 24 verification source of truth 已创建。
- [ ] controller lifecycle contract 已改成 official callback names + explicit state fields。
- [ ] eager read / retry 入口已被切断，后续 wave 只需要继续补 tests 和 removal cleanup。
</success_criteria>

<output>
After completion, create `.planning/phases/24-callback-sample-access/24-01-SUMMARY.md`
</output>
