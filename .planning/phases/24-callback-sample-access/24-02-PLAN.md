---
phase: 24-callback-sample-access
plan: 02
type: execute
wave: 2
depends_on: [24-01]
files_modified:
  - Tests/TestMain.cpp
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
autonomous: true
requirements: [LIFE-01, LIFE-02, LIFE-03]

must_haves:
  truths:
    - "`OpenTuneTests` 直接证明 `didUpdateAudioSourceProperties()` 已停止 eager read / retry，且 sample access enable callbacks 是唯一允许触发 host audio copy 的入口。"
    - "`doUpdateAudioSourceContent()` 只标记 content dirty / pending copy / revision，不在 callback 内直接发布 snapshot。"
    - "新的 audio content 只有在 `didEndEditing()` batch-end gate 后才进入 published snapshot。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 24 lifecycle callback guards"
      contains: "runPhase24LifecycleCallbackTests"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "sample-access callback-driven copy lifecycle and content-dirty publish semantics"
      contains: "didEnableAudioSourceSamplesAccess"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "no eager read / retry in audio-source properties callback"
      contains: "didUpdateAudioSourceProperties"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/ARA/OpenTuneDocumentController.cpp"
      via: "tests 先锁住 no-retry / enable-only copy / didEndEditing publish gate，再让 controller 实现转绿"
      pattern: "LIFE_03_ContentDirtyPublishesAtDidEndEditing"
    - from: "Source/ARA/OpenTuneDocumentController.cpp"
      to: "Source/ARA/OpenTuneDocumentController.h"
      via: "cpp 实现必须只消费 24-01 已冻结的 callback names 和 source-slot fields"
      pattern: "pendingCopyFromHost"
---

<objective>
把 sample access、content dirty 和 batch-end publish 语义真正写成可执行守护：先补 `OpenTuneTests` 的 Phase 24 lifecycle tests，再让 controller 实现以这些 tests 为准转绿。

Purpose: Wave 1 只切断了错误入口并冻结 contract，但还缺少直接证明 `LIFE-01` / `LIFE-02` / `LIFE-03` 的 automated guard。没有这些 tests，Phase 24 仍可能把“代码看起来像 callback-driven”误当成已经满足官方生命周期契约。

Output: `runPhase24LifecycleCallbackTests()`、`OpenTuneDocumentController` 的 sample-access callback-driven copy 语义，以及 content dirty -> `didEndEditing()` publish gate 的直接证据。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
@.planning/phases/24-callback-sample-access/24-01-PLAN.md
@.planning/phases/23-snapshot-region/23-03-SUMMARY.md
@JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.cpp
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Tests/TestMain.cpp

<interfaces>
Repository helpers already available to support source-audit tests:
```cpp
juce::String loadRepositoryTextFile(const juce::String& relativePath);
int countOccurrences(const juce::String& text, const juce::String& needle);
```

Phase-24 journey names that tests must lock exactly:
```text
LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry
LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks
LIFE_03_ContentDirtyPublishesAtDidEndEditing
```

Existing publication helpers that can be reused to prove batch-end publish behavior:
```cpp
OpenTuneDocumentController::SnapshotHandle buildSnapshotForPublication(...);
OpenTuneDocumentController::SnapshotHandle publishPendingSnapshot(...);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 先写 Phase 24 lifecycle guards，再把主测试流程接到新 section</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry` 证明 controller 源码里已经没有 `sampleAccessRetryCount`、`Timer::callAfterDelay`，且 `didUpdateAudioSourceProperties()` 不再承担 host audio copy。
    - Test 2: `LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks` 证明 `willEnableAudioSourceSamplesAccess()` / `didEnableAudioSourceSamplesAccess()` 是唯一 sample-access lifecycle callback，且 `didEnable...` 包含 `copyAudioSourceSamples(`。
    - Test 3: `LIFE_03_ContentDirtyPublishesAtDidEndEditing` 证明 content 变化在 `pendingSnapshotPublication=false` 时不会替换 snapshot，在 `pendingSnapshotPublication=true` 时才会发布新 truth。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - .planning/phases/23-snapshot-region/23-03-SUMMARY.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 新增 `runPhase24LifecycleCallbackTests()` 并接到 `main()`。实现要求精确如下：
    - 使用已有 `loadRepositoryTextFile(...)` helper 读取 `Source/ARA/OpenTuneDocumentController.h/.cpp` 做 source audit；不要再写新的仓库定位逻辑
    - 精确新增以下测试名：
      - `LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry`
      - `LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks`
      - `LIFE_03_ContentDirtyPublishesAtDidEndEditing`
    - `LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry` 必须同时断言：
      - controller cpp 不包含 `Timer::callAfterDelay`
      - controller h/cpp 都不包含 `sampleAccessRetryCount`
      - `didUpdateAudioSourceProperties` 定义存在，且 `copyAudioSourceSamples(` 的唯一调用位置不来自 `didUpdateAudioSourceProperties` body
    - `LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks` 必须同时断言：
      - controller h/cpp 都包含 `willEnableAudioSourceSamplesAccess` 与 `didEnableAudioSourceSamplesAccess`
      - controller cpp 中 `didEnableAudioSourceSamplesAccess` 定义体包含 `copyAudioSourceSamples(`
      - controller cpp 中 `doUpdateAudioSourceContent` 定义体不包含 `publishSnapshot()`
    - `LIFE_03_ContentDirtyPublishesAtDidEndEditing` 必须复用 `buildSnapshotForPublication(...)` / `publishPendingSnapshot(...)`：
      - 先构造初始 source/region state 和旧 snapshot
      - 再把 `SourceSlot::contentRevision` 与 copied audio 改成新值
      - `pendingSnapshotPublication=false` 时断言返回旧 snapshot handle
      - `pendingSnapshotPublication=true` 时断言返回新的 snapshot handle，且新 view 的 `contentRevision` 与 copied audio 已更新
    - build/test 命令固定使用 `build-phase24-docs` 与 `OpenTuneTests.exe`
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase24LifecycleCallbackTests`
    - `Tests/TestMain.cpp` contains `LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry`
    - `Tests/TestMain.cpp` contains `LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks`
    - `Tests/TestMain.cpp` contains `LIFE_03_ContentDirtyPublishesAtDidEndEditing`
    - `Tests/TestMain.cpp` contains `runPhase24LifecycleCallbackTests();`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; rg -n "runPhase24LifecycleCallbackTests|LIFE_01_DidUpdateAudioSourcePropertiesStopsEagerReadAndRetry|LIFE_02_SampleAccessLifecycleOnlyUsesEnableCallbacks|LIFE_03_ContentDirtyPublishesAtDidEndEditing" Tests/TestMain.cpp; if ($LASTEXITCODE -ne 0) { exit 1 }</automated>
  </verify>
  <done>
    Phase 24 的 sample-access / content-dirty publish 语义已变成可执行 tests，而不是靠日志或口头解释验收。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 让 controller 以 tests 为准转绿，完成 sample-access 与 content-dirty publish 语义</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp</files>
  <read_first>
    - Source/ARA/OpenTuneDocumentController.h
    - Source/ARA/OpenTuneDocumentController.cpp
    - Tests/TestMain.cpp
    - JUCE-master/modules/juce_audio_formats/format/juce_ARAAudioReaders.cpp
    - .planning/phases/24-callback-sample-access/24-TEST-VERIFICATION.md
  </read_first>
  <action>
    按 Task 1 的 tests 调整 controller，实现要求精确如下：
    - `copyAudioSourceSamples(juce::ARAAudioSource* audioSource)` 使用一次性 `ARA::PlugIn::HostAudioReader` 读取所有 channel/sample；读成功后：
      - `sourceSlot.copiedAudio = copiedAudio`
      - `sourceSlot.sampleAccessEnabled = true`
      - `sourceSlot.contentDirty = false`
      - `sourceSlot.pendingCopyFromHost = false`
      - `markSnapshotDirty()`
      - 不做 delayed retry
    - `willEnableAudioSourceSamplesAccess(audioSource, false)` 必须清空 copied audio，并把 `sampleAccessEnabled=false`、`contentDirty=false`、`pendingCopyFromHost=false`；`didEnableAudioSourceSamplesAccess(audioSource, true)` 才允许根据 `pendingCopyFromHost || copiedAudio == nullptr` 调 `copyAudioSourceSamples(audioSource)`
    - `doUpdateAudioSourceContent(audioSource, scopeFlags)` 仅在 `scopeFlags.affectSamples()` 时做 `bumpSourceContentRevision(audioSource)`、`contentDirty=true`、`pendingCopyFromHost=true`、`markSnapshotDirty()`；绝不直接 `publishSnapshot()` 或直接读 host audio
    - `didUpdateAudioSourceProperties(audioSource)` 继续只负责 metadata；它不得调用 `copyAudioSourceSamples(audioSource)`、`HostAudioReader`、`publishSnapshot()` 或 retry 逻辑
    - `didEndEditing()` 继续是 content-driven publish gate：content/sample-access 变化只设置 dirty state，真正的 snapshot replacement 仍由 batch-end 决定
    - 通过 `build-phase24-docs` 的 build + test gate
  </action>
  <acceptance_criteria>
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `copyAudioSourceSamples`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `didEnableAudioSourceSamplesAccess`
    - `Source/ARA/OpenTuneDocumentController.cpp` contains `doUpdateAudioSourceContent`
    - `Source/ARA/OpenTuneDocumentController.cpp` does not contain `Timer::callAfterDelay`
    - `Source/ARA/OpenTuneDocumentController.h` does not contain `sampleAccessRetryCount`
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase24-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit 1 }; & ".\build-phase24-docs\OpenTuneTests.exe"; if ($LASTEXITCODE -ne 0) { exit 1 }; if (rg -n "Timer::callAfterDelay|sampleAccessRetryCount" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp) { exit 1 }</automated>
  </verify>
  <done>
    `LIFE-01` / `LIFE-02` / `LIFE-03` 已由 `OpenTuneTests` 和 source audit 直接守住，controller 不再依赖 eager read / retry 或 callback 内即时 publish。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source-audit tests -> controller lifecycle code | 如果 tests 不直接锁住 no-retry 与 enable-only copy，Phase 24 容易在表面改名后继续保留旧行为 |
| content-dirty mutable state -> `didEndEditing()` publish gate | 如果 content change 仍在 callback 内即时 publish，snapshot 会重新暴露半更新 truth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-24-11 | T | sample-access callback ownership | mitigate | 在 `Tests/TestMain.cpp` 明确锁住 `didEnableAudioSourceSamplesAccess` 的 copy responsibility，并禁止 `didUpdateAudioSourceProperties` 再次介入。 |
| T-24-12 | I | content-dirty publish semantics | mitigate | 用 `publishPendingSnapshot(...)` journey 证明 dirty state 只有在 batch-end gate 打开时才发布。 |
| T-24-13 | D | test drift | mitigate | Build 与 test 全部固定在 `build-phase24-docs` / `OpenTuneTests.exe`，避免复用旧 Phase 23 binary。 |
</threat_model>

<verification>
- 构建 `build-phase24-docs` 的 `OpenTuneTests` 并执行 `OpenTuneTests.exe`，确认 `runPhase24LifecycleCallbackTests()` 通过。
- `rg` 确认 controller 中不再出现 `Timer::callAfterDelay` 和 `sampleAccessRetryCount`。
- `Tests/TestMain.cpp` 必须包含三条 Phase 24 journey 名称和 `runPhase24LifecycleCallbackTests();` 主流程接线。
</verification>

<success_criteria>
- [ ] `OpenTuneTests` 已直接证明 no-eager-read / enable-only-copy / batch-end publish gate。
- [ ] controller cpp 已按 tests 转绿，没有回退到 retry 或 callback 内即时 publish。
- [ ] Phase 24 的前 3 条 requirements 拿到可执行自动化证据。
</success_criteria>

<output>
After completion, create `.planning/phases/24-callback-sample-access/24-02-SUMMARY.md`
</output>
