---
phase: 01-双格式构建基础
plan: '04'
type: execute
wave: 2
depends_on:
  - '01'
  - '02'
files_modified: []
autonomous: true
requirements:
  - BLD-02
  - BLD-05

must_haves:
  truths:
    - "cmake -DFORMATS=\"Standalone\" -B build-standalone 成功配置"
    - "cmake --build build-standalone --config Release 成功编译"
    - "build-standalone 产物包含 OpenTune_Standalone 可执行文件"
    - "Standalone 输出目录包含 onnxruntime.dll, DirectML.dll, hifigan.onnx, rmvpe.onnx"
  artifacts:
    - path: build-standalone/OpenTune_Standalone.exe (生成)
      provides: Standalone 可执行文件
  key_links:
    - from: CMakeLists.txt (dual FORMAT)
      to: OpenTune_Standalone target
      via: cmake -DFORMATS="Standalone"
    - from: CMakeLists.txt (POST_BUILD)
      to: Standalone output dir
      via: $<TARGET_FILE_DIR:OpenTune_Standalone>
---

<objective>
验证 Standalone 目标能够单独编译成功，且产物正确。
</objective>

Purpose: 确认双格式改造后，Standalone 目标独立编译通过，POST_BUILD 正确复制 DLL 和模型文件。

Output: 编译验证结果

<context>
@CMakeLists.txt

# 验证目标
- BLD-02: Standalone 编译目标生成 OpenTune_Standalone 可执行文件
- BLD-05: POST_BUILD 脚本正确复制 DLL 和模型文件到 Standalone 输出目录

# 验证步骤
1. cmake -DFORMATS="Standalone" -B build-standalone
2. cmake --build build-standalone --config Release
3. 检查产物

# 重要约束
- 只需验证 Standalone（VST3 验证在 Phase 6）
- Phase 6 才验证 VST3 编译
</context>

<tasks>

<task type="auto">
  <name>Task 1: 验证 Standalone 编译</name>
  <files>CMakeLists.txt</files>
  <read_first>
    CMakeLists.txt (complete file for understanding post-modification state)
  </read_first>
  <action>
执行以下验证步骤:

1. 配置 Standalone 目标:
    cmake -DFORMATS="Standalone" -B build-standalone -S . -G "Visual Studio 17 2022" -A x64

2. 编译 Standalone 目标:
    cmake --build build-standalone --config Release --parallel

3. 验证产物存在:
    - build-standalone/OpenTune_Standalone.dir/Release/OpenTune_Standalone.exe
    - build-standalone/OpenTune_Standalone.exe（如 CMake 重新组织了输出）

4. 验证 DLL 和模型文件复制:
    检查 build-standalone 目录结构:
    - onnxruntime.dll 在 OpenTune_Standalone 输出目录
    - DirectML.dll 在 OpenTune_Standalone 输出目录
    - D3D12/D3D12Core.dll 和 D3D12/D3D12SDKLayers.dll 在 OpenTune_Standalone 输出目录
    - models/hifigan.onnx 在 OpenTune_Standalone 输出目录
    - models/rmvpe.onnx 在 OpenTune_Standalone 输出目录

注意: Windows 上的具体输出目录取决于 CMake Generator。
MSVC 生成器会产生 build-standalone/OpenTune_Standalone.dir/... 结构。
使用 --trace-format=json-v1 和 grep 查找实际输出路径。

如果编译失败:
1. 检查 CMake configure 输出中的警告和错误
2. 检查是否有 ARA SDK 相关错误（Standalone 目标不应依赖 ARA SDK）
3. 报告具体的编译错误信息
  </action>
  <acceptance_criteria>
    - cmake -DFORMATS="Standalone" configure 成功（无 fatal errors）
    - cmake --build build-standalone 成功（无 link errors）
    - OpenTune_Standalone.exe 存在于构建产物中
    - onnxruntime.dll 复制到 Standalone 输出目录
    - DirectML.dll 复制到 Standalone 输出目录
    - models/hifigan.onnx 和 models/rmvpe.onnx 复制到 Standalone 输出目录
  </acceptance_criteria>
  <verify>
    <automated>
      # 配置验证
      test -d build-standalone && echo "CONFIGURE_SUCCESS"
      # 编译验证
      test -f build-standalone/OpenTune_Standalone.exe || test -f build-standalone/OpenTune_Standalone.dir/Release/OpenTune_Standalone.exe && echo "BUILD_SUCCESS"
      # DLL 验证
      ls build-standalone/*/onnxruntime.dll 2>/dev/null | head -1 && echo "ONNXRUNTIME_COPIED"
    </automated>
  </verify>
  <done>Standalone 编译目标成功生成 OpenTune_Standalone 可执行文件，POST_BUILD 正确复制 DLL 和模型文件</done>
</task>

</tasks>

<verification>
- `cmake -DFORMATS="Standalone" -B build-standalone` 成功（BLD-02）
- `cmake --build build-standalone` 成功（BLD-02）
- Standalone 产物包含 OpenTune_Standalone.exe（BLD-02）
- POST_BUILD 正确复制所有 DLL 和模型文件（BLD-05）
</verification>

<success_criteria>
- BLD-02: Standalone 编译目标成功生成 OpenTune_Standalone 可执行文件
- BLD-05: POST_BUILD 脚本正确复制 DLL 和模型文件到 Standalone 输出目录
</success_criteria>

<output>
After completion, create `.planning/phases/01-双格式构建基础/04-SUMMARY.md`
</output>
