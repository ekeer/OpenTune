---
phase: 01-双格式构建基础
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
autonomous: true
requirements:
  - BLD-01
  - BLD-03

must_haves:
  truths:
    - "CMake accepts -DFORMATS=\"Standalone\" to generate OpenTune_Standalone target"
    - "CMake accepts -DFORMATS=\"VST3\" to generate OpenTune_VST3 target"
    - "CMake accepts -DFORMATS=\"Standalone VST3\" to generate both targets"
    - "IS_ARA_EFFECT is set to TRUE for ARA extension support"
  artifacts:
    - path: CMakeLists.txt
      provides: juce_add_plugin with dual FORMAT support
      contains: "FORMATS Standalone VST3"
    - path: CMakeLists.txt
      provides: IS_ARA_EFFECT TRUE for VST3 target
      contains: "IS_ARA_EFFECT TRUE"
  key_links:
    - from: CMakeLists.txt (juce_add_plugin)
      to: VST3 target
      via: FORMATS argument
    - from: CMakeLists.txt
      to: ARA SDK
      via: IS_ARA_EFFECT flag
---

<objective>
将 CMakeLists.txt 的 `juce_add_plugin` 从 `FORMATS Standalone` 改为 `FORMATS Standalone VST3`，并添加 `IS_ARA_EFFECT TRUE`。
</objective>

Purpose: 让 CMake 生成两个独立的编译目标（OpenTune_Standalone 和 OpenTune_VST3），VST3 目标启用 ARA 扩展支持。

Output: 修改后的 CMakeLists.txt

<context>
@CMakeLists.txt

# 主工作区当前配置（需要修改的部分）
- juce_add_plugin (line 193-226): FORMATS Standalone, IS_ARA_EFFECT 未设置
- 插件标识信息保持不变: COMPANY_NAME DAYA, PLUGIN_MANUFACTURER_CODE DAYA, PLUGIN_CODE De77

# VST3 References 参考配置（正确的目标状态）
- juce_add_plugin (VST3 References line 197-234): FORMATS VST3, IS_ARA_EFFECT TRUE
- VST3 References 的完整 juce_add_plugin 配置:
    FORMATS VST3
    IS_ARA_EFFECT TRUE
    IS_SYNTH FALSE
    NEEDS_MIDI_INPUT FALSE
    NEEDS_MIDI_OUTPUT FALSE
    IS_MIDI_EFFECT FALSE
    EDITOR_WANTS_KEYBOARD_FOCUS TRUE
    COPY_PLUGIN_AFTER_BUILD FALSE

# 关键要求
- BLD-01: 双目标编译
- BLD-03: VST3 目标生成 OpenTune_VST3 插件包
- IS_ARA_EFFECT=TRUE 启用 JUCE 的 ARA 扩展支持（JucePlugin_Enable_ARA 宏）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 修改 juce_add_plugin 支持双格式</name>
  <files>CMakeLists.txt</files>
  <read_first>
    CMakeLists.txt (lines 193-226)
  </read_first>
  <action>
将 CMakeLists.txt 中的 juce_add_plugin 定义从 `FORMATS Standalone` 改为 `FORMATS Standalone VST3`，并添加 `IS_ARA_EFFECT TRUE`。

具体修改:
1. 将第 209 行的 `FORMATS Standalone` 改为 `FORMATS Standalone VST3`
2. 在 FORMATS 行之后、Plugin Type 行之前添加 `IS_ARA_EFFECT TRUE`

参考 VST3 References/CMakeLists.txt 的 juce_add_plugin 配置（line 197-234），主工作区的配置应改为:

    juce_add_plugin(OpenTune
        COMPANY_NAME "DAYA"
        COMPANY_WEBSITE "https://daya.audio"
        COMPANY_EMAIL "support@daya.audio"
        PLUGIN_MANUFACTURER_CODE DAYA
        PLUGIN_CODE De77
        PRODUCT_NAME "OpenTune"
        BUNDLE_ID "com.daya.opentune"
        VERSION "1.0.0"
        FORMATS Standalone VST3
        IS_ARA_EFFECT TRUE
        IS_SYNTH FALSE
        NEEDS_MIDI_INPUT FALSE
        NEEDS_MIDI_OUTPUT FALSE
        IS_MIDI_EFFECT FALSE
        EDITOR_WANTS_KEYBOARD_FOCUS TRUE
        COPY_PLUGIN_AFTER_BUILD FALSE
    )

注意: PLUGIN_DESCRIPTION 和 PLUGIN_CATEGORY 行在主工作区不存在，不需要添加。保持主工作区现有配置不变。
  </action>
  <acceptance_criteria>
    - CMakeLists.txt contains `FORMATS Standalone VST3` (not just one format)
    - CMakeLists.txt contains `IS_ARA_EFFECT TRUE`
    - CMakeLists.txt does NOT contain any standalone-only flags that would conflict
  </acceptance_criteria>
  <verify>
    <automated>grep -c "FORMATS Standalone VST3" CMakeLists.txt</automated>
  </verify>
  <done>juce_add_plugin 配置为双格式 + ARA 启用</done>
</task>

</tasks>

<verification>
- `grep "FORMATS Standalone VST3" CMakeLists.txt` 返回匹配（BLD-01）
- `grep "IS_ARA_EFFECT TRUE" CMakeLists.txt` 返回匹配（ARA 支持）
- CMake configure 不报错
</verification>

<success_criteria>
- BLD-01: CMake 支持双目标编译（-DFORMATS="Standalone VST3"）
- BLD-03: VST3 目标已启用（IS_ARA_EFFECT TRUE）
</success_criteria>

<output>
After completion, create `.planning/phases/01-双格式构建基础/01-SUMMARY.md`
</output>
