---
phase: 01-双格式构建基础
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - CMakeLists.txt
autonomous: true
requirements:
  - BLD-04

must_haves:
  truths:
    - "ARA_SDK_PATH configurable via CMake cache variable"
    - "ARA SDK path is passed to JUCE via juce_set_ara_sdk_path()"
    - "CMake warns if ARA SDK is not found"
  artifacts:
    - path: CMakeLists.txt
      provides: ARA SDK path configuration
      contains: "ARA_SDK_PATH"
    - path: CMakeLists.txt
      provides: JUCE ARA SDK integration
      contains: "juce_set_ara_sdk_path"
  key_links:
    - from: CMakeLists.txt (ARA_SDK_PATH)
      to: ARA SDK directory
      via: juce_set_ara_sdk_path
---

<objective>
将 ARA SDK 路径配置集成到主工作区的 CMakeLists.txt 中，使 VST3 目标能正确引用 ARA SDK。
</objective>

Purpose: 为 VST3 编译目标提供 ARA SDK 支持路径配置。

Output: 修改后的 CMakeLists.txt

<context>
@CMakeLists.txt

# 主工作区当前状态
- 无 ARA SDK 配置（VST3 References 在 line 35 有 ARA_SDK_PATH 设置）
- JUCE 配置在 line 35-36: set(OPENTUNE_JUCE_DIR + add_subdirectory)

# VST3 References 的 ARA SDK 配置（正确的目标状态）
# VST3 References/CMakeLists.txt line 32-50:
set(ARA_SDK_PATH "${CMAKE_CURRENT_SOURCE_DIR}/ARA_SDK-releases-2.2.0" CACHE PATH "Path to ARA SDK")

set(OPENTUNE_JUCE_DIR "${CMAKE_CURRENT_SOURCE_DIR}/JUCE-master")
add_subdirectory("${OPENTUNE_JUCE_DIR}")

# Set ARA SDK path for JUCE
if(EXISTS "${ARA_SDK_PATH}")
    juce_set_ara_sdk_path("${ARA_SDK_PATH}")
    message(STATUS "ARA SDK found at: ${ARA_SDK_PATH}")
else()
    message(WARNING "ARA SDK not found at ${ARA_SDK_PATH}. ARA features will be disabled.")
    message(STATUS "To enable ARA, clone the SDK: git clone --recursive --branch releases/2.2.0 https://github.com/Celemony/ARA_SDK")
endif()

# 关键要求
- BLD-04: ARA SDK 路径配置在 CMakeLists.txt 中（ARA_SDK_PATH）
</context>

<tasks>

<task type="auto">
  <name>Task 1: 添加 ARA SDK 路径配置</name>
  <files>CMakeLists.txt</files>
  <read_first>
    CMakeLists.txt (lines 32-50, JUCE configuration section)
  </read_first>
  <action>
在 CMakeLists.txt 的 JUCE Configuration section（第 33-36 行）之前，添加 ARA SDK 配置。

在 `# ==============================================================================` 和 `# JUCE Configuration` 之间插入:

# ==============================================================================
# ARA SDK Configuration (for VST3 ARA Extension support)
# ==============================================================================
set(ARA_SDK_PATH "${CMAKE_CURRENT_SOURCE_DIR}/ARA_SDK-releases-2.2.0" CACHE PATH "Path to ARA SDK")

具体操作:
1. 在 line 32（`# ==============================================================================`）后添加 ARA SDK 配置 section
2. 将 ARA_SDK_PATH 设置放在 `# ==============================================================================
# JUCE Configuration` section 之前
3. 在 add_subdirectory(JUCE) 之后添加 juce_set_ara_sdk_path 调用

注意: 不要修改现有的 OPENTUNE_JUCE_DIR 和 add_subdirectory 行。只在它们之前添加 ARA_SDK_PATH，并在它们之后添加 juce_set_ara_sdk_path。
  </action>
  <acceptance_criteria>
    - CMakeLists.txt contains `set(ARA_SDK_PATH` with CACHE PATH
    - CMakeLists.txt contains `juce_set_ara_sdk_path("${ARA_SDK_PATH}")`
    - CMakeLists.txt contains warning message for missing ARA SDK
  </acceptance_criteria>
  <verify>
    <automated>grep -c "ARA_SDK_PATH" CMakeLists.txt && grep -c "juce_set_ara_sdk_path" CMakeLists.txt</automated>
  </verify>
  <done>ARA SDK 路径配置已集成</done>
</task>

</tasks>

<verification>
- `grep "set(ARA_SDK_PATH" CMakeLists.txt` 返回匹配
- `grep "juce_set_ara_sdk_path" CMakeLists.txt` 返回匹配
- CMake configure 正常执行（即使 ARA SDK 不存在也应继续）
</verification>

<success_criteria>
- BLD-04: ARA SDK 路径配置在 CMakeLists.txt 中
</success_criteria>

<output>
After completion, create `.planning/phases/01-双格式构建基础/02-SUMMARY.md`
</output>
