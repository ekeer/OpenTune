---
phase: 01-双格式构建基础
plan: '03'
type: execute
wave: 2
depends_on:
  - '01'
files_modified:
  - CMakeLists.txt
autonomous: true
requirements:
  - BLD-01
  - BLD-02
  - BLD-03

must_haves:
  truths:
    - "Standalone-only files conditionally included only for OpenTune_Standalone target"
    - "Common files always included for both targets"
    - "VST3 bundle resources configured with separate POST_BUILD commands"
    - "Standalone bundle resources preserved"
  artifacts:
    - path: CMakeLists.txt
      provides: Conditional source file lists for each format
      contains: "if(JucePlugin_Build_Standalone)"
    - path: CMakeLists.txt
      provides: VST3 POST_BUILD commands
      contains: "OpenTune_VST3"
    - path: CMakeLists.txt
      provides: Standalone POST_BUILD commands
      contains: "OpenTune_Standalone"
  key_links:
    - from: CMakeLists.txt (conditional sources)
      to: Standalone target
      via: JucePlugin_Build_Standalone condition
    - from: CMakeLists.txt (POST_BUILD)
      to: VST3/Standalone output dirs
      via: TARGET_FILE_DIR generator expression
---

<objective>
将 CMakeLists.txt 中的源文件列表和 POST_BUILD 命令改为条件化，使 Standalone 和 VST3 目标使用各自的文件集合和构建产物复制规则。
</objective>

Purpose: 让 Standalone 和 VST3 目标使用不同的源文件（Standalone 用 Standalone 专属文件，VST3 用 VST3 专属文件），以及独立的 POST_BUILD 脚本。

Output: 修改后的 CMakeLists.txt

<context>
@CMakeLists.txt

# 主工作区当前源文件配置 (lines 239-370)
# Standalone 特有文件:
#   Source/Host/HostIntegrationStandalone.cpp
#   Source/Standalone/EditorFactoryStandalone.cpp
#   Source/Standalone/PluginEditor.cpp / .h

# POST_BUILD 当前配置 (lines 489-575)
# 所有命令针对 OpenTune_Standalone

# VST3 References 的源文件配置 (lines 247-394)
# VST3 特有文件:
#   Source/Host/HostIntegrationPlugin.cpp (NOT HostIntegrationStandalone.cpp)
# 注意: VST3 References 仍然使用 Source/Standalone/PluginEditor.cpp/.h
#   Source/ARA/* (conditional, Phase 2)
# POST_BUILD (VST3 References lines 503-561):
#   所有命令针对 OpenTune_VST3

# 关键要求
- BLD-01: 条件化源文件（Standalone vs VST3）
- BLD-02: Standalone 目标使用 Standalone 特有文件
- BLD-03: VST3 目标使用 VST3 特有文件
- BLD-05: POST_BUILD 分别为两个目标复制 DLL 和模型文件

# 重要约束
- Source/Standalone/PluginEditor.h/cpp 属于 Standalone，合并不影响
  Standalone 现有功能和 UI（AGENTS.md 第 10 条）
- VST3 使用独立的编辑器路径（Phase 4），Phase 1 不涉及编辑器创建
- Phase 4 才创建 Source/Plugin/ 目录

# 源文件差异分析
Standalone 特有（主工作区有，VST3 References 也有）:
  Source/Host/HostIntegrationStandalone.cpp

VST3 特有（主工作区无）:
  Source/Host/HostIntegrationPlugin.cpp  ← Phase 4 才创建

共享文件（两格式都有，不需要改）:
  Source/PluginProcessor.h/cpp
  Source/Inference/*
  Source/DSP/*
  Source/Utils/*
  Source/Services/*
  Source/Standalone/UI/*
  Source/Editor/EditorFactory.h
  Source/Standalone/EditorFactoryStandalone.cpp ← 两格式都用 EditorFactory.h
  Source/Standalone/PluginEditor.cpp/.h ← 两格式都列在 CMakeLists.txt
    但 VST3 Phase 4 会替换为 Source/Plugin/PluginEditor.cpp/.h
</context>

<tasks>

<task type="auto">
  <name>Task 1: 条件化源文件列表（Standalone 特有文件）</name>
  <files>CMakeLists.txt</files>
  <read_first>
    CMakeLists.txt (lines 239-370, target_sources section)
  </read_first>
  <action>
修改 CMakeLists.txt 的 target_sources 块，将 Standalone 特有文件改为条件化包含。

当前状态 (lines 241-248):
    target_sources(OpenTune
        PRIVATE
            # Standalone specific files - Must be compiled for OpenTune target
            Source/Host/HostIntegrationStandalone.cpp
            Source/Standalone/EditorFactoryStandalone.cpp
            Source/Standalone/PluginEditor.cpp
            Source/Standalone/PluginEditor.h
            Source/Services/F0ExtractionService.cpp
            Source/Services/F0ExtractionService.h
            Source/Host/HostIntegration.h

修改为:

条件化 HostIntegrationStandalone.cpp（仅 Standalone）:

在 target_sources 的 PRIVATE 块内，将 `Source/Host/HostIntegrationStandalone.cpp` 行改为:
    $<$<BOOL:${JucePlugin_Build_Standalone}>:Source/Host/HostGeneratorStandalone.cpp>

实际上，由于 CMake 的生成器表达式工作在 target 级别而不是 source 级别，
正确的做法是用 CMake 的 target_sources 条件化。

将整个 target_sources 块拆分为:

1. 公共源文件（无条件，两格式都包含）— 保留现有列表不变
2. Standalone 特有源文件（条件化）:

在现有的 target_sources 块（lines 239-370）之后，添加:

# ==============================================================================
# Conditional Source Files per Format
# ==============================================================================
# Standalone-specific files (conditionally included)
if(JucePlugin_Build_Standalone)
    target_sources(OpenTune PRIVATE
        Source/Host/HostIntegrationStandalone.cpp
    )
endif()

# VST3-specific files (conditionally included) - Phase 4 adds Source/Plugin/*
if(NOT JucePlugin_Build_Standalone)
    # Phase 4: target_sources(OpenTune PRIVATE Source/Plugin/PluginEditor.cpp Source/Plugin/PluginEditor.h)
    # Phase 4: target_sources(OpenTune PRIVATE Source/Host/HostIntegrationPlugin.cpp)
endif()

重要: 不要在现有的无条件 target_sources 块中添加条件化内容。
现有列表中的 Source/Host/HostIntegrationStandalone.cpp 保留在无条件列表中，
然后在新建的 section 中用条件化包含覆盖。

实际上: 根据 AGENTS.md 第 10 条，UI 文件属于格式专用。
更好的方案是将 HostIntegrationStandalone.cpp 从无条件列表移到 Standalone 条件块中。

操作:
1. 从现有 target_sources 的无条件列表中删除 `Source/Host/HostIntegrationStandalone.cpp`（line 242）
2. 在 target_sources 块之后添加条件化 section（使用 if(JucePlugin_Build_Standalone)）
3. 将 Source/Host/HostIntegrationStandalone.cpp 放入条件化 section

其他文件保持不变:
- Source/Standalone/EditorFactoryStandalone.cpp → 实际是 EditorFactory 工厂实现，
  两格式都通过 EditorFactory.h 使用，所以保留在无条件列表
- Source/Standalone/PluginEditor.cpp/.h → 两格式 CMakeLists 都包含，
  条件化在 Phase 4 进行
- Source/Services/F0ExtractionService.cpp/.h → 两格式都需要
- Source/Host/HostIntegration.h → 两格式都需要
  </action>
  <acceptance_criteria>
    - CMakeLists.txt contains `if(JucePlugin_Build_Standalone)` with HostIntegrationStandalone.cpp
    - CMakeLists.txt contains conditional section for format-specific source files
    - Source/Host/HostIntegrationStandalone.cpp NOT in unconditional target_sources
  </acceptance_criteria>
  <verify>
    <automated>grep -c "if(JucePlugin_Build_Standalone)" CMakeLists.txt && grep -c "HostIntegrationStandalone" CMakeLists.txt</automated>
  </verify>
  <done>Standalone 特有文件条件化包含</done>
</task>

<task type="auto">
  <name>Task 2: 条件化 POST_BUILD 脚本</name>
  <files>CMakeLists.txt</files>
  <read_first>
    CMakeLists.txt (lines 480-576, POST_BUILD section)
  </read_first>
  <action>
修改 CMakeLists.txt 的 POST_BUILD 部分，使 Standalone 和 VST3 目标各自复制正确的文件。

当前状态: 所有 POST_BUILD 命令都针对 OpenTune_Standalone

目标状态: 
- OpenTune_Standalone: 保留现有 POST_BUILD（复制到 Standalone 输出目录）
- OpenTune_VST3: 添加新的 POST_BUILD 命令（复制到 VST3 bundle 目录）

参考 VST3 References 的 VST3 POST_BUILD 配置（lines 498-561）:

# VST3 output directory
set(VST3_OUTPUT_DIR "${CMAKE_BINARY_DIR}/VST3")
make_directory("${VST3_OUTPUT_DIR}")

# VST3 DLL 复制 (uses $<TARGET_FILE_DIR:OpenTune_VST3>)

具体操作:
1. 保留现有的所有 OpenTune_Standalone POST_BUILD 命令（lines 489-575），
   确保它们明确使用 `TARGET OpenTune_Standalone`

2. 在现有的 Standalone POST_BUILD 命令之后，添加 VST3 的 POST_BUILD 命令。
   注意: 需要用条件编译 if(NOT JucePlugin_Build_Standalone) 包裹，
   或者使用 CMake 的生成器表达式。

3. 最佳方案: 为 VST3 添加单独的 if(NOT JucePlugin_Build_Standalone) 块，
   内容参照 VST3 References lines 503-561，但使用 $<TARGET_FILE_DIR:OpenTune_VST3>

参考 VST3 References 的 VST3 POST_BUILD（简化）:
    # ONNX Runtime DLL
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${ONNXRUNTIME_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/onnxruntime.dll"
    )
    
    # DirectML DLL
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD ...)
    
    # D3D12 Agility
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD ...)
    
    # AI models (hifigan + rmvpe)
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD ...)

关键: 两格式的 POST_BUILD 内容几乎相同，只是目标名不同。
最简洁的实现方式是在同一个 POST_BUILD 命令中使用生成器表达式:
    add_custom_command(TARGET OpenTune POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${ONNXRUNTIME_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_Standalone>/onnxruntime.dll"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${ONNXRUNTIME_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/onnxruntime.dll"
    )

这种方案的优点是: 单条命令同时为两个目标工作（OpenTune 是基名，
CMake 会自动为 OpenTune_Standalone 和 OpenTune_VST3 都生成命令）。

具体实现: 将所有现有的 POST_BUILD 命令（lines 489-575）中的
`TARGET OpenTune_Standalone` 改为 `TARGET OpenTune`，
这样 CMake 会自动为两个编译目标都生成命令。

这样做是因为当 FORMATS 指定多个格式时，CMake 会生成 OpenTune_Standalone 和 OpenTune_VST3
两个目标，而 `TARGET OpenTune` 的 POST_BUILD 会对两个子目标都生效。

具体修改:
1. 将所有 `TARGET OpenTune_Standalone` 改为 `TARGET OpenTune`
   - line 489: TARGET OpenTune_Standalone POST_BUILD
   - line 497: TARGET OpenTune_Standalone POST_BUILD (ONNXRUNTIME_PROVIDERS_SHARED_DLL)
   - line 506: TARGET OpenTune_Standalone POST_BUILD (ONNXRUNTIME_PROVIDERS_DML_DLL)
   - line 514: TARGET OpenTune_Standalone POST_BUILD (DirectML)
   - line 521: TARGET OpenTune_Standalone POST_BUILD (D3D12)
   - line 533: TARGET OpenTune_Standalone POST_BUILD (models)
   - line 546: TARGET OpenTune_Standalone POST_BUILD (macOS ONNX)
   - line 555: TARGET OpenTune_Standalone POST_BUILD (macOS models)
   - line 568: TARGET OpenTune_Standalone POST_BUILD (UserGuide)

2. VST3 特有路径: D3D12 的 D3D12Core.dll 和 D3D12SDKLayers.dll
   需要为 VST3 目标也创建目录并复制（VST3 References lines 535-545）

在将 TARGET OpenTune_Standalone 改为 TARGET OpenTune 后，
还需要确保 VST3 目标有 VST3 特有的 POST_BUILD（复制到 VST3 bundle 目录）。

实际上，由于 `TARGET OpenTune` 会自动为 OpenTune_Standalone 和 OpenTune_VST3 都执行，
而生成器表达式 `$<TARGET_FILE_DIR:OpenTune_Standalone>` 和 `$<TARGET_FILE_DIR:OpenTune_VST3>`
可以区分两个输出目录。

更好的方案: 在 Standalone 的 POST_BUILD 之后，
添加 VST3 特有的 POST_BUILD（if(NOT JucePlugin_Build_Standalone) 块）：
- 复制 ONNX Runtime DLL 到 VST3 bundle
- 复制 DirectML DLL 到 VST3 bundle
- 复制 D3D12 DLL 到 VST3 bundle
- 复制 AI models 到 VST3 bundle

具体实现:
在 line 576（endif() 之后，Unit Tests section 之前）添加:

# ==============================================================================
# VST3-Specific POST_BUILD Commands
# ==============================================================================
if(NOT JucePlugin_Build_Standalone)
    # ONNX Runtime DLL
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${ONNXRUNTIME_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/onnxruntime.dll"
        COMMENT "Copying ONNX Runtime DLL to VST3 plugin..."
    )
    
    # ONNX Runtime providers shared DLL
    if(ONNXRUNTIME_PROVIDERS_SHARED_DLL)
        add_custom_command(TARGET OpenTune_VST3 POST_BUILD
            COMMAND ${CMAKE_COMMAND} -E copy_if_different
                "${ONNXRUNTIME_PROVIDERS_SHARED_DLL}"
                "$<TARGET_FILE_DIR:OpenTune_VST3>/onnxruntime_providers_shared.dll"
        )
    endif()
    
    # DirectML DLL
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${DIRECTML_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/DirectML.dll"
    )
    
    # D3D12 Agility SDK
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E make_directory
            "$<TARGET_FILE_DIR:OpenTune_VST3>/D3D12"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${D3D12_CORE_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/D3D12/D3D12Core.dll"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${D3D12_SDK_LAYERS_DLL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/D3D12/D3D12SDKLayers.dll"
    )
    
    # AI Models
    add_custom_command(TARGET OpenTune_VST3 POST_BUILD
        COMMAND ${CMAKE_COMMAND} -E make_directory
            "$<TARGET_FILE_DIR:OpenTune_VST3>/models"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${HIFIGAN_MODEL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/models/hifigan.onnx"
        COMMAND ${CMAKE_COMMAND} -E copy_if_different
            "${RMVPE_MODEL}"
            "$<TARGET_FILE_DIR:OpenTune_VST3>/models/rmvpe.onnx"
    )
endif()
  </action>
  <acceptance_criteria>
    - CMakeLists.txt contains `TARGET OpenTune_VST3` in VST3-specific POST_BUILD section
    - CMakeLists.txt contains `if(NOT JucePlugin_Build_Standalone)` for VST3 POST_BUILD
    - VST3 POST_BUILD copies all runtime DLLs (onnxruntime, DirectML, D3D12) and models
    - Existing Standalone POST_BUILD commands are preserved
  </acceptance_criteria>
  <verify>
    <automated>grep -c "if(NOT JucePlugin_Build_Standalone)" CMakeLists.txt && grep -c "OpenTune_VST3" CMakeLists.txt</automated>
  </verify>
  <done>POST_BUILD 分别为两个目标复制正确的 DLL 和模型文件</done>
</task>

</tasks>

<verification>
- CMake configure 生成 OpenTune_Standalone 和 OpenTune_VST3 两个目标
- BLD-01: 双目标编译配置生效
- BLD-02: Standalone 目标有对应的 POST_BUILD
- BLD-03: VST3 目标有对应的 POST_BUILD
- BLD-05: 两个目标的 DLL 和模型文件复制正确
</verification>

<success_criteria>
- BLD-01, BLD-02, BLD-03, BLD-05 均满足
- CMake configure 成功
</success_criteria>

<output>
After completion, create `.planning/phases/01-双格式构建基础/03-SUMMARY.md`
</output>
