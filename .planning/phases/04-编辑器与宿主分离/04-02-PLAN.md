---
phase: 04-编辑器与宿主分离
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Source/Editor/EditorFactoryPlugin.cpp
  - Source/Standalone/EditorFactoryStandalone.cpp
autonomous: true
requirements:
  - EDTR-04
  - EDTR-05
  - EDTR-06
  - EDTR-07
must_haves:
  truths:
    - "编译格式可在 createOpenTuneEditor() 处选择正确编辑器实现"
    - "VST3 模式不会链接到 Standalone 编辑器工厂实现"
    - "Standalone 编辑器文件保持原路径与原实现，不被插件工厂替代"
  artifacts:
    - path: "Source/Editor/EditorFactoryPlugin.cpp"
      provides: "VST3 编辑器工厂实现"
    - path: "Source/Standalone/EditorFactoryStandalone.cpp"
      provides: "Standalone 工厂条件编译守卫"
  key_links:
    - from: "Source/Editor/EditorFactoryPlugin.cpp"
      to: "Source/Plugin/PluginEditor.h"
      via: "createOpenTuneEditor"
      pattern: "#include \"Plugin/PluginEditor.h\""
    - from: "Source/Standalone/EditorFactoryStandalone.cpp"
      to: "JucePlugin_Build_Standalone"
      via: "预处理条件"
      pattern: "#if JucePlugin_Build_Standalone"
---

<objective>
建立格式选择工厂的双实现：Standalone 与 VST3 各自由条件编译守卫控制，避免符号冲突与交叉引用。

Purpose: 满足 EditorFactory 条件化选择要求，保持 Standalone 编辑器稳定。
Output: EditorFactoryPlugin + Standalone 工厂守卫，形成按格式分流的编辑器构建路径。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Source/Editor/EditorFactory.h
@Source/Standalone/EditorFactoryStandalone.cpp
@VST3 References/Source/Editor/EditorFactoryPlugin.cpp

<interfaces>
From Source/Editor/EditorFactory.h:
```cpp
juce::AudioProcessorEditor* createOpenTuneEditor(OpenTuneAudioProcessor& processor);
```

From Source/Standalone/EditorFactoryStandalone.cpp (current):
```cpp
juce::AudioProcessorEditor* createOpenTuneEditor(OpenTuneAudioProcessor& processor)
{
    return new OpenTuneAudioProcessorEditor(processor);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 VST3 工厂实现 EditorFactoryPlugin.cpp</name>
  <files>Source/Editor/EditorFactoryPlugin.cpp</files>
  <read_first>
    - Source/Editor/EditorFactory.h
    - Source/Plugin/PluginEditor.h
    - VST3 References/Source/Editor/EditorFactoryPlugin.cpp
  </read_first>
  <action>创建 Source/Editor/EditorFactoryPlugin.cpp，实现 createOpenTuneEditor(OpenTuneAudioProcessor&) 并返回 Source/Plugin/PluginEditor 的编辑器实例（满足 EDTR-04/EDTR-05）。文件整体使用 `#if !JucePlugin_Build_Standalone` 守卫，避免与 Standalone 工厂重定义。</action>
  <acceptance_criteria>
    - 文件存在：Source/Editor/EditorFactoryPlugin.cpp
    - 包含字符串：#if !JucePlugin_Build_Standalone
    - 包含字符串：#include "Plugin/PluginEditor.h"
    - 包含字符串：return new OpenTuneAudioProcessorEditor(processor)
  </acceptance_criteria>
  <verify>
    <automated>rg -n "#if !JucePlugin_Build_Standalone|Plugin/PluginEditor.h|return new OpenTuneAudioProcessorEditor\(processor\)" Source/Editor/EditorFactoryPlugin.cpp</automated>
  </verify>
  <done>VST3 工厂实现可用且仅在插件构建路径编译。</done>
</task>

<task type="auto">
  <name>Task 2: 为 Standalone 工厂补充格式守卫并保持 Standalone 编辑器不变</name>
  <files>Source/Standalone/EditorFactoryStandalone.cpp</files>
  <read_first>
    - Source/Standalone/EditorFactoryStandalone.cpp
    - Source/Standalone/PluginEditor.h
    - Source/Standalone/PluginEditor.cpp
  </read_first>
  <action>在 Source/Standalone/EditorFactoryStandalone.cpp 外层增加 `#if JucePlugin_Build_Standalone` / `#endif`，保持现有 createOpenTuneEditor 行为不变（满足 EDTR-06）。同时保证本任务不修改 Source/Standalone/PluginEditor.h/cpp 内容（满足 EDTR-07）。</action>
  <acceptance_criteria>
    - Standalone 工厂包含字符串：#if JucePlugin_Build_Standalone
    - Standalone 工厂包含字符串：#endif // JucePlugin_Build_Standalone
    - `git diff --name-only -- Source/Standalone/PluginEditor.h Source/Standalone/PluginEditor.cpp` 输出为空
  </acceptance_criteria>
  <verify>
    <automated>rg -n "#if JucePlugin_Build_Standalone|#endif // JucePlugin_Build_Standalone" Source/Standalone/EditorFactoryStandalone.cpp; git diff --name-only -- Source/Standalone/PluginEditor.h Source/Standalone/PluginEditor.cpp</automated>
  </verify>
  <done>格式守卫完整，且 Standalone 编辑器实现零改动。</done>
</task>

</tasks>

<verification>
通过静态检查确认：同名工厂函数在不同编译格式下仅有一个实现参与编译，Standalone 编辑器文件未被改写。
</verification>

<success_criteria>
- `Source/Editor/EditorFactoryPlugin.cpp` 创建完成并使用 `#if !JucePlugin_Build_Standalone`
- `Source/Standalone/EditorFactoryStandalone.cpp` 增加 `#if JucePlugin_Build_Standalone`
- `Source/Standalone/PluginEditor.h/.cpp` 无改动
</success_criteria>

<output>
After completion, create `.planning/phases/04-编辑器与宿主分离/04-02-SUMMARY.md`
</output>
