---
phase: 04-编辑器与宿主分离
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - Source/Host/HostIntegrationPlugin.cpp
  - CMakeLists.txt
autonomous: true
requirements:
  - HOST-01
  - HOST-02
  - HOST-03
  - HOST-04
must_haves:
  truths:
    - "VST3 模式拥有独立 HostIntegrationPlugin 实现"
    - "createHostIntegration() 会按编译格式返回正确实现"
    - "新增 Plugin/Editor 工程文件已被 CMake 纳入目标"
  artifacts:
    - path: "Source/Host/HostIntegrationPlugin.cpp"
      provides: "VST3 宿主集成实现与工厂"
    - path: "CMakeLists.txt"
      provides: "Plugin Editor / Plugin Host 源文件接入"
  key_links:
    - from: "Source/Host/HostIntegrationPlugin.cpp"
      to: "Source/Host/HostIntegration.h"
      via: "HostIntegration 继承实现"
      pattern: "class HostIntegrationPlugin final : public HostIntegration"
    - from: "CMakeLists.txt"
      to: "Source/Host/HostIntegrationPlugin.cpp"
      via: "target_sources(OpenTune PRIVATE ...)"
      pattern: "Source/Host/HostIntegrationPlugin.cpp"
---

<objective>
创建插件模式宿主集成实现，并将 Phase 4 新增源文件接入构建系统，完成 HostIntegration 分离。

Purpose: 让 VST3 与 Standalone 宿主逻辑彻底按编译格式分流。
Output: HostIntegrationPlugin.cpp + CMake 源文件清单更新。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Source/Host/HostIntegration.h
@Source/Host/HostIntegrationStandalone.cpp
@VST3 References/Source/Host/HostIntegrationPlugin.cpp
@CMakeLists.txt

<interfaces>
From Source/Host/HostIntegration.h:
```cpp
class HostIntegration {
public:
    virtual ~HostIntegration() = default;
    virtual void configureInitialState(OpenTuneAudioProcessor& processor) = 0;
    virtual bool processIfApplicable(OpenTuneAudioProcessor& processor,
                                     juce::AudioBuffer<float>& buffer,
                                     int totalNumInputChannels,
                                     int totalNumOutputChannels,
                                     int numSamples) = 0;
    virtual void audioSettingsRequested(juce::AudioProcessorEditor& editor) = 0;
};

std::unique_ptr<HostIntegration> createHostIntegration();
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 HostIntegrationPlugin.cpp 并限定插件编译路径</name>
  <files>Source/Host/HostIntegrationPlugin.cpp</files>
  <read_first>
    - Source/Host/HostIntegration.h
    - Source/Host/HostIntegrationStandalone.cpp
    - VST3 References/Source/Host/HostIntegrationPlugin.cpp
  </read_first>
  <action>创建 Source/Host/HostIntegrationPlugin.cpp，实现 HostIntegrationPlugin（满足 HOST-01）。文件外层使用 `#if !JucePlugin_Build_Standalone` 守卫（满足 HOST-03），并实现 createHostIntegration() 返回 HostIntegrationPlugin（满足 HOST-04）。保持 Source/Host/HostIntegrationStandalone.cpp 的 `#if JucePlugin_Build_Standalone` 结构不变（满足 HOST-02）。</action>
  <acceptance_criteria>
    - 文件存在：Source/Host/HostIntegrationPlugin.cpp
    - 包含字符串：#if !JucePlugin_Build_Standalone
    - 包含字符串：class HostIntegrationPlugin final : public HostIntegration
    - 包含字符串：return std::make_unique<HostIntegrationPlugin>()
    - `Source/Host/HostIntegrationStandalone.cpp` 仍包含 `#if JucePlugin_Build_Standalone`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "#if !JucePlugin_Build_Standalone|HostIntegrationPlugin final|return std::make_unique<HostIntegrationPlugin>" Source/Host/HostIntegrationPlugin.cpp; rg -n "#if JucePlugin_Build_Standalone" Source/Host/HostIntegrationStandalone.cpp</automated>
  </verify>
  <done>HostIntegration 双实现边界完整，工厂按格式返回正确实例。</done>
</task>

<task type="auto">
  <name>Task 2: 更新 CMake 源文件清单接入插件专用编辑器与宿主实现</name>
  <files>CMakeLists.txt</files>
  <read_first>
    - CMakeLists.txt
    - Source/Plugin/PluginEditor.h
    - Source/Plugin/PluginEditor.cpp
    - Source/Host/HostIntegrationPlugin.cpp
    - Source/Editor/EditorFactoryPlugin.cpp
  </read_first>
  <action>在 CMakeLists.txt 的 target_sources(OpenTune PRIVATE ...) 区域新增以下条目：`Source/Plugin/PluginEditor.cpp`、`Source/Plugin/PluginEditor.h`、`Source/Editor/EditorFactoryPlugin.cpp`、`Source/Host/HostIntegrationPlugin.cpp`。不要移除现有 Standalone 相关条目，避免破坏双格式并行构建路径。</action>
  <acceptance_criteria>
    - CMakeLists.txt 包含 `Source/Plugin/PluginEditor.cpp`
    - CMakeLists.txt 包含 `Source/Plugin/PluginEditor.h`
    - CMakeLists.txt 包含 `Source/Editor/EditorFactoryPlugin.cpp`
    - CMakeLists.txt 包含 `Source/Host/HostIntegrationPlugin.cpp`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Source/Plugin/PluginEditor\\.cpp|Source/Plugin/PluginEditor\\.h|Source/Editor/EditorFactoryPlugin\\.cpp|Source/Host/HostIntegrationPlugin\\.cpp" CMakeLists.txt</automated>
  </verify>
  <done>Phase 4 新增文件已全部接入 OpenTune 目标源列表。</done>
</task>

</tasks>

<verification>
静态验证 HostIntegration 与 EditorFactory 的双实现均有明确编译守卫，且 CMake 已接入新增文件。
</verification>

<success_criteria>
- `Source/Host/HostIntegrationPlugin.cpp` 创建完成
- Host 双实现守卫分别为 `JucePlugin_Build_Standalone` 与 `!JucePlugin_Build_Standalone`
- CMake 包含 PluginEditor / EditorFactoryPlugin / HostIntegrationPlugin 源文件
</success_criteria>

<output>
After completion, create `.planning/phases/04-编辑器与宿主分离/04-03-SUMMARY.md`
</output>
