---
phase: 04-编辑器与宿主分离
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
autonomous: true
requirements:
  - EDTR-01
  - EDTR-02
  - EDTR-03
must_haves:
  truths:
    - "VST3 构建使用独立的 Plugin 编辑器入口，不再复用 Standalone 编辑器文件"
    - "VST3 编辑器界面不包含 TrackPanel/ArrangementView"
    - "VST3 编辑器中的 MenuBarComponent 默认不可见"
  artifacts:
    - path: "Source/Plugin/PluginEditor.h"
      provides: "VST3 专用编辑器声明（精简组件集合）"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "VST3 专用编辑器实现（菜单隐藏 + 精简布局）"
  key_links:
    - from: "Source/Plugin/PluginEditor.h"
      to: "Source/Standalone/UI/PianoRollComponent.h"
      via: "组件成员与监听器"
      pattern: "PianoRollComponent"
    - from: "Source/Plugin/PluginEditor.cpp"
      to: "MenuBarComponent"
      via: "构造函数初始化"
      pattern: "menuBar_\\.setVisible\\(false\\)"
---

<objective>
建立 VST3 专用编辑器文件（Source/Plugin/PluginEditor.h/.cpp），形成与 Standalone UI 完全隔离的插件编辑器入口。

Purpose: 落地 UI 隔离原则，确保 VST3 与 Standalone 的编辑器职责分离。
Output: 可被后续 EditorFactoryPlugin 直接实例化的 VST3 编辑器实现。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/VST3Merge.md
@VST3 References/Source/Standalone/PluginEditor.h
@VST3 References/Source/Standalone/PluginEditor.cpp

<interfaces>
From Source/Editor/EditorFactory.h:
```cpp
juce::AudioProcessorEditor* createOpenTuneEditor(OpenTuneAudioProcessor& processor);
```

From Source/PluginProcessor.cpp:
```cpp
juce::AudioProcessorEditor* OpenTuneAudioProcessor::createEditor() {
    return createOpenTuneEditor(*this);
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 创建 VST3 专用 PluginEditor 头文件契约</name>
  <files>Source/Plugin/PluginEditor.h</files>
  <read_first>
    - VST3 References/Source/Standalone/PluginEditor.h
    - Source/Standalone/PluginEditor.h
    - Source/Editor/EditorFactory.h
  </read_first>
  <action>创建 Source/Plugin/PluginEditor.h，定义 OpenTuneAudioProcessorEditor（VST3 专用）。保留 PianoRoll/TransportBar/TopBar/ParameterPanel/MenuBar 相关接口，明确不声明 TrackPanelComponent 与 ArrangementViewComponent（满足 EDTR-02）。保持命名与命名空间风格与现有代码一致。</action>
  <acceptance_criteria>
    - 文件存在：Source/Plugin/PluginEditor.h
    - 文件包含类声明：class OpenTuneAudioProcessorEditor
    - 文件不包含字符串：TrackPanelComponent
    - 文件不包含字符串：ArrangementViewComponent
    - 文件包含字符串：MenuBarComponent
  </acceptance_criteria>
  <verify>
    <automated>rg -n "OpenTuneAudioProcessorEditor|MenuBarComponent|TrackPanelComponent|ArrangementViewComponent" Source/Plugin/PluginEditor.h</automated>
  </verify>
  <done>VST3 编辑器头文件完成，组件边界明确。</done>
</task>

<task type="auto">
  <name>Task 2: 创建 VST3 专用 PluginEditor 实现并隐藏菜单栏</name>
  <files>Source/Plugin/PluginEditor.cpp</files>
  <read_first>
    - VST3 References/Source/Standalone/PluginEditor.cpp
    - Source/Standalone/PluginEditor.cpp
    - Source/Plugin/PluginEditor.h
  </read_first>
  <action>创建 Source/Plugin/PluginEditor.cpp，按 VST3 References 的精简编辑器实现迁移到 Plugin 目录；构造函数必须调用 menuBar_.setVisible(false)（满足 EDTR-03），布局仅保留 TopBar/TransportBar/PianoRoll/ParameterPanel 相关区域，不引入 TrackPanel/ArrangementView 代码路径。</action>
  <acceptance_criteria>
    - 文件存在：Source/Plugin/PluginEditor.cpp
    - 包含字符串：menuBar_.setVisible(false)
    - 不包含字符串：trackPanel_
    - 不包含字符串：arrangementView_
    - 包含构造函数实现：OpenTuneAudioProcessorEditor::OpenTuneAudioProcessorEditor
  </acceptance_criteria>
  <verify>
    <automated>rg -n "menuBar_\.setVisible\(false\)|trackPanel_|arrangementView_|OpenTuneAudioProcessorEditor::OpenTuneAudioProcessorEditor" Source/Plugin/PluginEditor.cpp</automated>
  </verify>
  <done>VST3 编辑器实现可独立编译，MenuBar 默认隐藏，且无多轨道 UI 依赖。</done>
</task>

</tasks>

<verification>
执行后可静态确认 VST3 编辑器与 Standalone 编辑器文件路径已分离，且 VST3 不包含 TrackPanel/ArrangementView 引用。
</verification>

<success_criteria>
- `Source/Plugin/PluginEditor.h/.cpp` 创建完成
- VST3 编辑器代码中不存在 `TrackPanelComponent` / `ArrangementViewComponent`
- `menuBar_.setVisible(false)` 存在于 VST3 编辑器构造流程
</success_criteria>

<output>
After completion, create `.planning/phases/04-编辑器与宿主分离/04-01-SUMMARY.md`
</output>
