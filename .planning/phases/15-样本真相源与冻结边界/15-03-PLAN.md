---
phase: 15-样本真相源与冻结边界
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md
  - .planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md
autonomous: true
requirements: [SAMP-01, SAMP-02, TASK-01, TASK-02, TASK-03]

must_haves:
  truths:
    - "自动化测试直接证明 imported clip sample span、frozen boundary 公式、以及 synth-length mismatch 拒绝路径。"
    - "Phase 15 verification 文档把 5 个 requirement 全部映射到具体代码证据与命令，不留‘看起来通过’的空口结论。"
    - "执行者能用单一 `OpenTuneTests` 命令复核本 phase 的核心 contract，而不是只靠手读代码。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 15 frozen-boundary regression tests"
      contains: "runPhase15FrozenBoundaryTests"
    - path: ".planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md"
      provides: "L1/L2/L4/L6 verification commands for Phase 15"
      contains: "TASK-03"
    - path: ".planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md"
      provides: "phase-level requirement traceability and gate decision"
      contains: "SAMP-01"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/PluginProcessor.h"
      via: "ClipSampleRange / FrozenRenderBoundaries helper tests"
      pattern: "freezeRenderBoundaries"
    - from: ".planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md"
      to: ".planning/REQUIREMENTS.md"
      via: "requirement traceability table"
      pattern: "TASK-03"
---

<objective>
为 Phase 15 建立自动化证据闭环，证明 sample truth contract、frozen boundary math 和 mismatch failure path 都真实可测。

Purpose: Phase 15 如果只有代码没有验证，很容易在后续 Phase 16/17 再次被 seconds-based 逻辑侵蚀；这一计划把回归测试和 phase gate 一次性补齐。

Output: Phase 15 测试用例、`15-TEST-VERIFICATION.md`、`15-VERIFICATION.md`。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/codebase/TESTING.md
@.planning/phases/11-最小状态模型定稿/11-03-SUMMARY.md
@.planning/phases/10-state-visualization-consistency-guard/10-03-SUMMARY.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 sample truth 与 frozen-boundary helper 添加回归测试</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: imported clip 在 48kHz -> 44.1kHz 导入后，`getClipSampleRangeById(...)` 返回 `0..44100`
    - Test 2: `freezeRenderBoundaries(...)` 对 `0.5s` / `hopSize=512` 返回 `publishSampleCount=22050`、`frameCount=44`、`synthSampleCount=22528`
    - Test 3: `preparePublishedAudioFromSynthesis(...)` 遇到 `actual != synthSampleCount` 返回 `false`，长度匹配时只产出 `publishSampleCount` 个 sample
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - .planning/codebase/TESTING.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 新增 `runPhase15FrozenBoundaryTests()`，并把它接到 `main()` 的执行序列里。

    新增至少 3 个测试用例，名称与断言必须足够具体：

    1. `SAMP_01_ImportedClipSampleRangeUsesStored44k1Length`
       - 创建 `48000` sample、`48000Hz` 的单声道输入 buffer
       - 调用 `prepareImportClip(...)` 与 `commitPreparedImportClip(...)`
       - 用 `const uint64_t clipId = processor.getCurrentClipIdForPlugin();` 获取新 clip 的 `clipId`
       - 调用 `getClipSampleRangeById(0, clipId, range)`
       - 断言：`range.startSample == 0`
       - 断言：`range.endSampleExclusive == 44100`

    2. `TASK_01_FreezeRenderBoundariesProducesPublishAndSynthCounts`
       - 直接构造 `ClipSampleRange{0, 44100}`
       - 调用 `freezeRenderBoundaries(range, 0.0, 0.5, 512, boundaries)`
       - 断言：
         - `boundaries.trueStartSample == 0`
         - `boundaries.trueEndSample == 22050`
         - `boundaries.publishSampleCount == 22050`
         - `boundaries.frameCount == 44`
         - `boundaries.synthSampleCount == 22528`
         - `boundaries.synthEndSample == 22528`

    3. `TASK_03_PreparePublishedAudioRejectsSynthLengthMismatch`
       - 复用上个测试得到的 `boundaries`
       - 用 `22527` sample 的 `synthesizedAudio` 调用 `preparePublishedAudioFromSynthesis(...)`，断言返回 `false`
       - 再用 `22528` sample 的 `synthesizedAudio` 调用，断言返回 `true`
       - 断言产出的 `publishedAudio.size() == 22050`

    `main()` 中必须显式追加 `runPhase15FrozenBoundaryTests();`，不要只定义函数不接执行入口。

    测试写法必须沿用当前 harness 习惯：`logSection(...)`、`const char* test = ...`、`logFail(...)` / `logPass(...)`。
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase15FrozenBoundaryTests`
    - `Tests/TestMain.cpp` contains `SAMP_01_ImportedClipSampleRangeUsesStored44k1Length`
    - `Tests/TestMain.cpp` contains `TASK_01_FreezeRenderBoundariesProducesPublishAndSynthCounts`
    - `Tests/TestMain.cpp` contains `TASK_03_PreparePublishedAudioRejectsSynthLengthMismatch`
    - `Tests/TestMain.cpp` contains `preparePublishedAudioFromSynthesis`
    - `Tests/TestMain.cpp` contains `freezeRenderBoundaries`
    - `Tests/TestMain.cpp` contains `runPhase15FrozenBoundaryTests();`
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target OpenTuneTests --config Release && .\build\Release\OpenTuneTests.exe</automated>
  </verify>
  <done>
    `OpenTuneTests` 中已经有直接命中 Phase 15 contract 的回归测试，后续 phase 如果破坏 sample truth 或 mismatch 拒绝路径，会立即暴露。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 生成 Phase 15 的测试验证文档与 requirement traceability</name>
  <files>.planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md, .planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/phases/11-最小状态模型定稿/11-03-SUMMARY.md
    - .planning/phases/10-state-visualization-consistency-guard/10-03-SUMMARY.md
    - Tests/TestMain.cpp
    - Source/PluginProcessor.cpp
  </read_first>
  <action>
    创建两个 phase 文档：

    1. `15-TEST-VERIFICATION.md`
       - 明确列出本 phase 的 L1 / L2 / L4 / L6 命令：
         - L1: `cmake --build build --target OpenTuneTests --config Release`
         - L2/L6: `.\build\Release\OpenTuneTests.exe`
         - L4: `rg "FrozenRenderBoundaries|preparePublishedAudioFromSynthesis|synthSampleCountMismatch" Source/PluginProcessor.h Source/PluginProcessor.cpp`
       - 不写 UI/E2E 旅程；直接写明本 phase 是 shared core contract phase，L5 不适用

    2. `15-VERIFICATION.md`
       - 写出 5 个 requirement 的 traceability 表：`SAMP-01`, `SAMP-02`, `TASK-01`, `TASK-02`, `TASK-03`
       - 每一行都要给出：目标文件、测试/grep 命令、通过标准
       - `TASK-02` 的证据必须明确写到“callback 不再使用 `expectedSamples` / `resize()`”
       - `TASK-03` 的证据必须明确写到“`preparePublishedAudioFromSynthesis` mismatch 返回 false，worker callback 走 TerminalFailure”
       - 给出最终 gate 模板：`PASS / FAIL / BLOCKED`

    约束：
    - 文档用 Phase 15 的真实文件名与命令，不允许抄旧 phase 的 target 名称或目录
    - requirement traceability 不能写泛化描述，必须逐项落到文件与命令
  </action>
  <acceptance_criteria>
    - `.planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md` exists
    - `.planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md` contains `cmake --build build --target OpenTuneTests --config Release`
    - `.planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md` contains `.\build\Release\OpenTuneTests.exe`
    - `.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md` exists
    - `.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md` contains `SAMP-01`
    - `.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md` contains `TASK-03`
    - `.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md` contains `expectedSamples`
    - `.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md` contains `TerminalFailure`
  </acceptance_criteria>
  <verify>
    <automated>rg "SAMP-01|SAMP-02|TASK-01|TASK-02|TASK-03|OpenTuneTests|expectedSamples|TerminalFailure" ".planning/phases/15-样本真相源与冻结边界/15-TEST-VERIFICATION.md" ".planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 15 有了可以直接执行的测试/验证手册，5 个 requirement 都能追溯到具体代码与命令。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code -> test harness | shared core contract 通过本地自动化测试被验证 |
| verification docs -> phase gate decision | requirement 证据从文档映射到最终 PASS/FAIL 结论 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-07 | T | `Tests/TestMain.cpp` | mitigate | 用精确 sample 数值断言 `ClipSampleRange` / `FrozenRenderBoundaries` / mismatch helper，防止测试只验证模糊行为。 |
| T-15-08 | R | `15-VERIFICATION.md` | mitigate | requirement traceability 逐项绑定命令、文件和通过标准，避免执行结束后出现“做了很多但没有 requirement 证据”的否认空间。 |
| T-15-09 | D | `OpenTuneTests` full run | accept | 当前仓库只有单一测试可执行文件，Phase 15 接受全量跑一次的成本，不为局部过滤额外引入第二套测试入口。 |
</threat_model>

<verification>
- 构建并运行 `OpenTuneTests`，确认新增 Phase 15 tests 随主测试入口执行。
- `rg` 检查 `15-TEST-VERIFICATION.md` 与 `15-VERIFICATION.md` 已覆盖全部 5 个 requirement。
- 人工复核 `15-VERIFICATION.md`，确认 `TASK-02` 与 `TASK-03` 的证据不是泛化描述，而是实际代码符号与命令。
</verification>

<success_criteria>
- [ ] `OpenTuneTests` 能自动覆盖 Phase 15 的 sample truth、frozen boundary 和 mismatch helper。
- [ ] `15-TEST-VERIFICATION.md` 列出了真实可执行的验证命令。
- [ ] `15-VERIFICATION.md` 对 5 个 requirement 做到 100% traceability。
</success_criteria>

<output>
After completion, create `.planning/phases/15-样本真相源与冻结边界/15-03-SUMMARY.md`
</output>
