---
phase: 15-样本真相源与冻结边界
plan: 02
type: execute
wave: 2
depends_on: [15-01]
files_modified:
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [SAMP-02, TASK-01, TASK-02, TASK-03]

must_haves:
  truths:
    - "worker 从 chunk 请求进入 mel/vocoder 之前，只冻结一次 sample-domain 边界，并把同一组字段传到 callback 与 cache publish。"
    - "callback 不再根据 `jobStartSeconds / jobEndSeconds` 重新算 expectedSamples，也不再 `resize()` 结果掩盖契约错误。"
    - "cache 发布与重采样派生都只消费冻结 boundary helper 产出的 `publishedAudio`。"
  artifacts:
    - path: "Source/PluginProcessor.cpp"
      provides: "worker-side frozen-boundary propagation"
      contains: "FrozenRenderBoundaries boundaries"
    - path: "Source/PluginProcessor.cpp"
      provides: "strict synth-length validation"
      contains: "synthSampleCountMismatch"
    - path: "Source/PluginProcessor.cpp"
      provides: "published-audio preparation before cache publish"
      contains: "preparePublishedAudioFromSynthesis"
  key_links:
    - from: "Source/PluginProcessor.cpp"
      to: "Source/PluginProcessor.h"
      via: "worker calls freezeRenderBoundaries before mel/vocoder"
      pattern: "freezeRenderBoundaries"
    - from: "Source/PluginProcessor.cpp"
      to: "Source/Inference/RenderCache.h"
      via: "publishedAudio -> addChunk/addResampledChunk"
      pattern: "addChunk"
---

<objective>
把 `chunkRenderWorkerLoop()` 和 callback 改成只消费冻结 sample 边界，彻底拿掉 callback 阶段用 seconds 事后修形的旧路径。

Purpose: 满足 Phase 15 最核心的 TASK-01 / TASK-02 / TASK-03，让 worker、callback、cache 流程共享同一组冻结字段，而不是各自再把 seconds 换算成长度。

Output: worker frozen-boundary 流、严格长度校验、publishedAudio 驱动的 base/resampled cache 发布。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Inference/RenderCache.h
@Source/Utils/TimeCoordinate.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 在 worker 中冻结并传递 sample-domain render boundaries</name>
  <files>Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.cpp
    - Source/PluginProcessor.h
    - Source/Inference/RenderCache.h
    - Source/Utils/TimeCoordinate.h
    - .planning/phases/15-样本真相源与冻结边界/15-01-PLAN.md
  </read_first>
  <action>
    在 `chunkRenderWorkerLoop()` 中把当前“边读音频边散落计算长度”的流程改成显式冻结边界后再继续执行，具体要求如下：

    - `WorkerRenderJob` 或 worker 局部状态必须持有 `FrozenRenderBoundaries boundaries`
    - 在找到目标 `clip` 后，先构造 `ClipSampleRange clipRange{0, clip.audioBuffer->getNumSamples()}`，然后调用：
      `freezeRenderBoundaries(clipRange, relChunkStartSec, relChunkEndSec, 512, boundaries)`
    - 后续音频读取、mel frame 计算、日志都只使用 `boundaries` 字段：
      - 读取音频区间：`[boundaries.trueStartSample, boundaries.trueEndSample)`
      - `numFrames = boundaries.frameCount`
      - 不再保留独立的 `targetSamples` 推导链
    - `RenderTrace` 日志必须输出以下字段，便于 Phase 15 验证：
      - `trueStartSample`
      - `trueEndSample`
      - `synthEndSample`
      - `publishSampleCount`
      - `synthSampleCount`
      - `frameCount`

    约束：
    - 仍允许 `RenderCache` 用 `startSeconds/endSeconds` 做 chunk identity，因为本 phase 只冻结长度契约，不改写 cache key 体系
    - 但长度、frame 和 callback 发布判断全部必须从 `boundaries` 读取，不能再用 `relChunkEndSec - relChunkStartSec` 或二次 `secondsToSamples(...)`
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.cpp` contains `FrozenRenderBoundaries boundaries`
    - `Source/PluginProcessor.cpp` contains `freezeRenderBoundaries(clipRange, relChunkStartSec, relChunkEndSec, 512, boundaries)`
    - `Source/PluginProcessor.cpp` contains `boundaries.trueStartSample`
    - `Source/PluginProcessor.cpp` contains `boundaries.trueEndSample`
    - `Source/PluginProcessor.cpp` contains `boundaries.publishSampleCount`
    - `Source/PluginProcessor.cpp` contains `boundaries.synthSampleCount`
    - `Source/PluginProcessor.cpp` contains `numFrames = boundaries.frameCount`
  </acceptance_criteria>
  <verify>
    <automated>rg "FrozenRenderBoundaries boundaries|freezeRenderBoundaries\(clipRange, relChunkStartSec, relChunkEndSec, 512, boundaries\)|boundaries\.trueStartSample|boundaries\.trueEndSample|boundaries\.publishSampleCount|boundaries\.synthSampleCount|numFrames = boundaries\.frameCount" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>
    worker 已经在进入 mel/vocoder 之前冻结唯一 boundary contract；从该点开始，render 流程不再依赖散落的 seconds-based 长度变量。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 用严格 synth-length 校验替换 callback 的 resize 修形</name>
  <files>Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.cpp
    - Source/PluginProcessor.h
    - Source/Inference/RenderCache.h
    - .planning/phases/15-样本真相源与冻结边界/15-01-PLAN.md
  </read_first>
  <action>
    重写 vocoder callback 的发布路径，禁止继续通过 `expectedSamples` 和 `resize()` 掩盖边界错配。

    必做改动：
    - 删除现有 `expectedSamples` / `normalizeChunkLength` / `renderedAudio.resize(...)` 逻辑
    - callback 收到 `audio` 后，第一步调用：
      `preparePublishedAudioFromSynthesis(boundaries, audio, publishedAudio)`
    - 如果 helper 返回 `false`：
      - 记录 `synthSampleCountMismatch` 日志
      - 日志里至少打印：`jobStartSeconds`, `boundaries.synthSampleCount`, `boundaries.publishSampleCount`, `audio.size()`
      - 立即 `completeChunkRender(..., RenderCache::CompletionResult::TerminalFailure)`
      - 不允许调用 `addChunk(...)` 或 `addResampledChunk(...)`
    - 如果 helper 返回 `true`：
      - 用 `publishedAudio` 调用 `addChunk(...)`
      - resampled cache 也必须从同一个 `publishedAudio` 派生，不允许再从原始 `audio` 派生
      - 成功后才 `completeChunkRender(...Succeeded)`

    约束：
    - base cache 和 resampled cache 在本 phase 都继续沿用现有 seconds key，但音频内容必须来自相同 `publishedAudio`
    - callback 里不得再出现第二次 `TimeCoordinate::secondsToSamples(jobEndSeconds...) - ...` 这种重新推导长度的表达式
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.cpp` contains `preparePublishedAudioFromSynthesis(boundaries, audio, publishedAudio)`
    - `Source/PluginProcessor.cpp` contains `synthSampleCountMismatch`
    - `Source/PluginProcessor.cpp` contains `RenderCache::CompletionResult::TerminalFailure`
    - `Source/PluginProcessor.cpp` contains `addChunk(`
    - `Source/PluginProcessor.cpp` contains `addResampledChunk(`
    - `Source/PluginProcessor.cpp` does not contain `normalizeChunkLength`
    - `Source/PluginProcessor.cpp` does not contain `renderedAudio.resize(`
    - `Source/PluginProcessor.cpp` does not contain `expectedSamples = juce::jmax<int64_t>`
  </acceptance_criteria>
  <verify>
    <automated>if (rg "normalizeChunkLength|renderedAudio\.resize\(|expectedSamples = juce::jmax<int64_t>" Source/PluginProcessor.cpp) { exit 1 }; rg "preparePublishedAudioFromSynthesis\(boundaries, audio, publishedAudio\)|synthSampleCountMismatch|RenderCache::CompletionResult::TerminalFailure|addChunk\(|addResampledChunk\(" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>
    callback 已经改成“对齐 frozen synth contract 才发布，否则失败退出”；旧的 seconds-based 长度修形路径被完全移除。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| worker thread -> vocoder callback | synthesis 输出从推理域返回到共享 render/cache 域 |
| callback -> render cache publish | 只有合法长度的音频允许进入 cache 与后续 playback 读取 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-04 | T | `chunkRenderWorkerLoop` | mitigate | 通过 `FrozenRenderBoundaries` 把长度、frame、publish 约束冻结一次，阻止 worker/callback 各自再从 seconds 重建边界。 |
| T-15-05 | D | vocoder callback publish path | mitigate | 若 `audio.size() != synthSampleCount` 立即 `TerminalFailure` 且不发布 cache，避免错长缓冲被持久化后持续污染播放路径。 |
| T-15-06 | I | resampled cache derivation | accept | 本 phase 处理的是本地音频长度一致性，不涉及凭据或跨用户数据；信息泄露风险低，重点放在长度完整性与发布一致性。 |
</threat_model>

<verification>
- `rg` 确认 worker 使用 `FrozenRenderBoundaries`，callback 使用 `preparePublishedAudioFromSynthesis`。
- `rg` 确认 `normalizeChunkLength`、`expectedSamples`、`renderedAudio.resize(...)` 已消失。
- 执行者做完 15-02 后，进入 15-03 前必须能清楚指出 synth mismatch 走 `TerminalFailure` 而不是静默修形。
</verification>

<success_criteria>
- [ ] worker、callback、cache 流程共享同一组 frozen sample fields。
- [ ] callback 不再通过 seconds-based expected length 进行修形。
- [ ] base cache 与 resampled cache 都从同一个 `publishedAudio` 结果派生。
</success_criteria>

<output>
After completion, create `.planning/phases/15-样本真相源与冻结边界/15-02-SUMMARY.md`
</output>
