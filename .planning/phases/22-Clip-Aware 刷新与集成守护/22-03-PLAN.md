---
phase: 22-Clip-Aware 刷新与集成守护
plan: 03
type: execute
wave: 3
depends_on: [22-02]
files_modified:
  - Source/Standalone/UI/PianoRollComponent.cpp
  - Tests/TestMain.cpp
autonomous: true
requirements: [PAINT-01, FLOW-01]

must_haves:
  truths:
    - "strip dirty contract 仍由 `getPlayheadDirtyBounds()` + `updateMainLayerPlayhead()` + `invalidateVisual(...)` + `flushPendingVisualInvalidation()` 组成，Phase 22 只补守护不改架构。"
    - "playhead projection、continuous/page scroll、fit-to-screen 必须继续命中 sample-authoritative clip timeline 语义。"
    - "Phase 18-21 retained guards 要和新的 PAINT/FLOW 守护一起常驻，防止 clip-aware 优化偷破坏单层、单入口、单 VBlank 闭环。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "Phase 22 integration-level behavioral fixes if tests暴露缺口"
      contains: "getPlayheadDirtyBounds"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 22 strip-dirty / fit-to-screen / retained regressions"
      contains: "PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge"
  key_links:
    - from: "Tests/TestMain.cpp"
      to: "Source/Standalone/UI/PianoRollComponent.cpp"
      via: "strip dirty、fit-to-screen 与 projected playhead 集成行为必须直接由组件层守护"
      pattern: "FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection"
---

<objective>
补齐 Phase 22 的集成守护与 retained regressions，覆盖 strip dirty、clip-aware、fit-to-screen，以及 Phase 18-21 的关键守护组合。

Purpose: renderer helper 落地后，真正的收口风险转移到 integration 层——如果 strip dirty、fit-to-screen 或 projected playhead 语义漂移，Phase 22 仍然不算完成。这个计划负责把这些行为写成常驻 gate，并仅在 `PianoRollComponent` 里做必要修正。

Output: 更新后的 integration-level regressions，以及必要时修正后的 `PianoRollComponent` 行为。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp

<interfaces>
From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
double readProjectedPlayheadTime() const;
juce::Rectangle<int> getPlayheadDirtyBounds(double playheadTimeSeconds) const;
void updateMainLayerPlayhead(double playheadTimeSeconds);
void onVisualVBlankCallback(double timestampSec);
void fitToScreen();
```

From `Tests/TestMain.cpp` existing probes:
```cpp
struct PianoRollComponentTestProbe {
    static bool hasPendingVisualInvalidation(const PianoRollComponent& pianoRoll);
    static double lastVisualFlushMs(const PianoRollComponent& pianoRoll);
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 补齐 strip dirty / fit-to-screen / retained guards</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge` 期望 playhead dirty 仍由 strip dirty + single flush bridge 驱动。
    - Test 2: `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection` 期望 fit-to-screen 优先命中 clip timeline projection。
    - Test 3: retained `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*` 与新 PAINT/FLOW 守护组合执行。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Source/Standalone/PluginEditor.cpp
    - Source/Plugin/PluginEditor.cpp
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中补齐 integration-level Phase 22 守护，要求精确如下：
    - 新增 `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge`
      - 使用桌面 host + `PianoRollComponent`，设置 projected playhead source 并触发一次 `onVisualVBlankCallback(...)`
      - 通过 `PianoRollComponentTestProbe` 或新增 probe 断言 pending invalidation 被 drain、flush 时间戳前进、playhead dirty 不是 full repaint-only 退化
      - 继续围绕 `getPlayheadDirtyBounds()` / `updateMainLayerPlayhead()` / `invalidateVisual(...)` / `flushPendingVisualInvalidation()` 现有 contract，不允许把测试写成接受 direct `repaint()`
    - 强化 `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
      - 明确覆盖 projected start != trackOffsetSeconds_ 默认值的场景
      - 断言 fit-to-screen 后 scroll/zoom 命中 processor timeline projection
    - 在 Phase 22 的 test runner 中把 `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*` retained guards 一并执行，形成常驻组合 gate
    - 对 `Source/Standalone/PluginEditor.cpp` / `Source/Plugin/PluginEditor.cpp` 只做审计型 retained checks：不得把少量现有 `pianoRoll_.repaint()` 升格为“需要全面重构”；Phase 22 只要求不新增 cadence / direct invalidate residue
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge`
    - `Tests/TestMain.cpp` contains `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
    - `Tests/TestMain.cpp` Phase 22 runner includes retained `TIME_01_*`, `LAYER_01_*`, `INVAL_01_*`, `CLOCK_01_*`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge|FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection|TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead|CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll" "Tests/TestMain.cpp"</automated>
  </verify>
  <done>
    Phase 22 已有 strip dirty、fit-to-screen 与 retained phase guards 组合 gate，不再只靠 renderer-level 事实自证完成。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 只在 `PianoRollComponent` 内修正集成缺口并转绿 Phase 22 guards</name>
  <files>Source/Standalone/UI/PianoRollComponent.cpp, Tests/TestMain.cpp</files>
  <read_first>
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Tests/TestMain.cpp
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
  </read_first>
  <action>
    若 Plan 03 新增 guards 暴露行为缺口，只允许在 `PianoRollComponent.cpp` 内做必要修正，使以下事实转绿：
    - `getPlayheadDirtyBounds()` / `updateMainLayerPlayhead()` 继续只生成 strip dirty，不回退 full repaint / overlay
    - `onVisualVBlankCallback(...)` 仍是唯一 visual tick；任何修复都不得恢复 editor cadence、direct invalidate 或第二条 flush bridge
    - `fitToScreen()` 继续优先走 `getActiveClipTimelineProjection(...)`，并正确落到 projected start/duration
    - retained `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*` 与新的 `PAINT_01_*` / `FLOW_01_*` 全部转绿
    - 在运行 integration green verify 前，必须先读取 `22-TEST-VERIFICATION.md` 中已确认的 `Confirmed Toolchain Setup Command:`，用该命令 fresh build `build-phase22-docs`，然后再运行 `OpenTuneTests.exe`；禁止直接依赖旧 exe
    如果 guards 已全绿，则本任务只验证并保持 component 无额外架构改动。
  </action>
  <acceptance_criteria>
    - Phase 22 integration-level journeys are green
    - Phase 18-21 retained guards remain green
    - `Source/Standalone/UI/PianoRollComponent.cpp` does not introduce overlay/editor cadence/direct invalidate side paths
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase22-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\build-phase22-docs\OpenTuneTests.exe'</automated>
  </verify>
  <done>
    Phase 22 的 integration 行为已闭环：strip dirty、projected playhead、continuous/page scroll、fit-to-screen 都在单层/单入口/单 VBlank 结构下继续成立。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| component integration -> renderer optimization | 如果 integration guards 缺失，renderer clip-aware 优化可能静默破坏 playhead/scroll/fit-to-screen 语义 |
| retained phases -> phase 22 closure | 如果 retained tests 不被纳入同一 gate，Phase 22 会在“局部通过、整体回归”的状态下假阳性收口 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-20 | T | strip dirty contract | mitigate | 用 `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge` 直接锁定 playhead dirty + single flush bridge。 |
| T-22-21 | D | timeline behavior regressions | mitigate | 用 `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection` + retained `TIME_01_*` 证明 timeline 行为未漂移。 |
| T-22-22 | E | editor shell scope expansion | accept | Phase 22 接受 editor shell 只做 retained 审计，不把现有 repaint residue 升级为全面重构任务。 |
</threat_model>

<verification>
- Phase 22 integration-level regressions 已加入并执行。
- `PianoRollComponent` 如有必要已被修正到通过 strip dirty / fit-to-screen / retained guards。
- Phase 18-21 retained guards 与新的 Phase 22 guards 一起转绿。
</verification>

<success_criteria>
- [ ] strip dirty / single flush bridge 行为被自动化守住。
- [ ] fit-to-screen 继续命中 clip timeline projection。
- [ ] Phase 18-21 retained guards 与 Phase 22 新 guards 同时通过。
</success_criteria>

<output>
After completion, create `.planning/phases/22-Clip-Aware 刷新与集成守护/22-03-SUMMARY.md`
</output>
