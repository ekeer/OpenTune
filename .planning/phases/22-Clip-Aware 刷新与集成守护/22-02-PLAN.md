---
phase: 22-Clip-Aware 刷新与集成守护
plan: 02
type: execute
wave: 2
depends_on: [22-01]
files_modified:
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
  - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
  - Tests/TestMain.cpp
autonomous: true
requirements: [PAINT-02]

must_haves:
  truths:
    - "renderer 内部必须存在统一 visible time window 语义，notes、waveform、F0 不再各算各的时间窗。"
    - "drawNotes() 必须改成 visible note window 遍历，而不是在 full scan 里加条件分支。"
    - "Phase 22 只能优化主层 renderer 成本，不能恢复 overlay、editor cadence 或第二条 flush 路径。"
  artifacts:
    - path: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.h"
      provides: "visible window helper contract"
      contains: "PianoRollRenderer"
    - path: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"
      provides: "unified renderer visible-range implementation"
      contains: "drawNotes"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 22 renderer-level tests turned green"
      contains: "PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow"
  key_links:
    - from: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"
      to: "Tests/TestMain.cpp"
      via: "renderer helper 与 note iterator window 必须直接满足 Plan 01 RED regressions"
      pattern: "PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0"
---

<objective>
在 `PianoRollRenderer` 内建立统一 visible time window helper，并让 `drawNotes()` 彻底切换到 visible note window 遍历，兑现 Phase 22 的 clip-aware 核心。

Purpose: 当前 live tree 的瓶颈不是 dirty rect 太宽，而是 `drawNotes()` 仍然 full scan。这个计划只处理 renderer 内部的正确结构：统一时间窗、note iterator window、保持 waveform/F0 既有 visible-range 语义一致。

Output: 更新后的 `PianoRollRenderer` visible-range helper / note window 实现，以及转绿的 Phase 22 renderer-level regressions。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
@Source/Utils/Note.h
@Source/PluginProcessor.cpp
@Tests/TestMain.cpp

<interfaces>
From `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h`:
```cpp
struct RenderContext
{
    int pianoKeyWidth = 60;
    int width = 0;
    double trackOffsetSeconds = 0.0;
    std::function<double(int)> xToTime;
    std::function<int(double)> timeToX;
};
```

From `Source/Utils/Note.h` / current storage contract referenced by research:
```cpp
// Notes are normalized to non-overlapping, startTime-sorted storage before renderer reads them.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 抽出 renderer 统一 visible time window，并让 drawNotes 改成 visible note window 遍历</name>
  <files>Source/Standalone/UI/PianoRoll/PianoRollRenderer.h, Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp</files>
  <behavior>
    - Test 1: `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0` 期望 notes/waveform/F0 共用同一 visible time window 语义。
    - Test 2: `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow` 期望 drawNotes 先求可见 note window，再只遍历 window 内 notes。
  </behavior>
  <read_first>
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Utils/Note.h
    - Tests/TestMain.cpp
  </read_first>
  <action>
    在 `PianoRollRenderer` 内完成以下结构收敛：
    - 抽出 renderer 私有 visible time window helper，统一给 waveform / F0 / notes 三条主内容路径使用；helper 必须以 `ctx.pianoKeyWidth -> ctx.width` 的内容区为基线，并显式扣除 `trackOffsetSeconds`
    - `drawWaveform()` 与 `drawF0Curve()` 改为调用该 helper，保持当前 visible-range 行为不变，不重写它们的业务语义
    - `drawNotes()` 改成基于 notes 已排序且 non-overlapping contract 的 visible iterator window：先找第一个可能命中可见区的 note，再迭代到 `note.startTime >= visibleClipEnd` 为止
    - 禁止保留 `for (const auto& note : notes)` full scan 主循环；如果需要处理 overlap 边界，只允许回退最多一个前驱 note，而不是整表扫描
    - 不要把 visible-range 逻辑抬回 `PianoRollComponent`，也不要新增 editor / VBlank / invalidation 旁路
  </action>
  <acceptance_criteria>
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp` contains unified visible time window helper usage in `drawWaveform`, `drawF0Curve`, and `drawNotes`
    - `Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp` no longer relies on `for (const auto& note : notes)` as the primary `drawNotes()` traversal
    - `drawNotes()` iterates only the visible note window determined from clip-visible start/end
  </acceptance_criteria>
  <verify>
    <automated>rg -n "drawWaveform|drawF0Curve|drawNotes|lower_bound|visible.*time|visible.*clip" "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"</automated>
  </verify>
  <done>
    renderer 内的 visible time window 与 visible note window 已成为唯一正确解释，Phase 22 的核心缺口被结构性填平。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 跑 Phase 22 renderer-level regressions 并转绿</name>
  <files>Tests/TestMain.cpp</files>
  <read_first>
    - Tests/TestMain.cpp
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
  </read_first>
  <action>
    让 Plan 01 定义的 renderer-level Phase 22 journeys 在 live tree 上转绿，至少覆盖：
    - `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow`
    - `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0`
    同时保持 `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*` 不回归。若测试需要 probe，可只在 `Tests/TestMain.cpp` 中扩展 `PianoRollComponentTestProbe` / renderer probe，不在 production code 新增兼容层。
    在运行 green verify 前，必须先读取 `22-TEST-VERIFICATION.md` 中已确认的 `Confirmed Toolchain Setup Command:`，用该命令 fresh build `build-phase22-docs`，然后再运行 `OpenTuneTests.exe`；禁止直接运行可能不存在或 stale 的 exe。
  </action>
  <acceptance_criteria>
    - Phase 22 renderer-level journeys pass on the modified renderer
    - Phase 18-21 retained guards remain green
  </acceptance_criteria>
  <verify>
    <automated>pwsh -NoProfile -File ".planning/scripts/invoke-msvc-cmake.ps1" -BuildDir build-phase22-docs -Target OpenTuneTests; if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }; & '.\build-phase22-docs\OpenTuneTests.exe'</automated>
  </verify>
  <done>
    Phase 22 renderer-level clip-aware 事实已经被自动化守护覆盖，notes 不再拖着主层 strip repaint 做全量遍历。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| renderer helper -> paint correctness | 如果 visible time window 语义分裂，notes / waveform / F0 会重新产生不同步的裁剪边界 |
| note iterator window -> performance contract | 如果 notes 仍 full scan，strip dirty 再窄也只是表面优化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-10 | T | renderer visible-window contract | mitigate | 让 waveform / F0 / notes 共用一个 renderer helper，禁止 component 侧复制时间窗算法。 |
| T-22-11 | D | note traversal cost | mitigate | `drawNotes()` 改成 binary-search + visible iterator window，只迭代可见 notes。 |
| T-22-12 | E | phase scope drift into cadence/overlay | accept | Phase 22 接受 cadence 与 flush 架构保持现状，只在 renderer 内优化可见区遍历，不触碰 editor shell 架构。 |
</threat_model>

<verification>
- `PianoRollRenderer` 已形成统一 visible time window helper。
- `drawNotes()` 已切换为 visible note window 遍历。
- Phase 22 renderer-level regressions 已转绿。
</verification>

<success_criteria>
- [ ] notes / waveform / F0 共用同一 renderer visible time window 语义。
- [ ] `drawNotes()` 不再对整个 `notes` 全量遍历。
- [ ] Phase 18-21 retained guards 继续通过。
</success_criteria>

<output>
After completion, create `.planning/phases/22-Clip-Aware 刷新与集成守护/22-02-SUMMARY.md`
</output>
