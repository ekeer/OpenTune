---
phase: 22-Clip-Aware 刷新与集成守护
plan: 04
type: execute
wave: 4
depends_on: [22-03]
files_modified:
  - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
  - .planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/PROJECT.md
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [PAINT-01, PAINT-02, FLOW-01]

must_haves:
  truths:
    - "Phase 22 closure 必须以 fresh toolchain 确认 + fresh build/test 为准，不允许沿用 Phase 20/21 的过时 `VsDevCmd.bat` 路径假设。"
    - "只有 gate 真正通过，`22-TEST-VERIFICATION.md` / `22-VERIFICATION.md` / roadmap / state 才能写 PASS；否则必须忠实记录 blocker。"
    - "只有在 gate PASS 时才同步 `.planning/PROJECT.md` / `.planning/REQUIREMENTS.md`，把 `PAINT-01` / `PAINT-02` / `FLOW-01` 标成完成。"
  artifacts:
    - path: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"
      provides: "fresh L1/L2/L4/L6 evidence for Phase 22"
      contains: "build-phase22-docs"
    - path: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md"
      provides: "requirement-level traceability for PAINT-01, PAINT-02, FLOW-01"
      contains: "Gate status:"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 22 status aligned to real gate result"
      contains: "22-04-PLAN.md"
    - path: ".planning/STATE.md"
      provides: "project state aligned to Phase 22 gate result"
      contains: "Phase 22"
  key_links:
    - from: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"
      to: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md"
      via: "verification 报告直接消费 fresh gate 结果，不再靠旧 phase 命令假定"
      pattern: "Gate status:"
    - from: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md"
      to: ".planning/PROJECT.md"
      via: "只有 Gate status: PASS 才允许回写 milestone 文档中的 requirement 完成态"
      pattern: "Gate status: PASS"
---

<objective>
在 Phase 22 代码 closure 之后，先确认/修正真实 toolchain 命令，再执行 fresh `build-phase22-docs` gates，并把 verification / roadmap / state / 条件性 project docs 全部对齐到真实结果。

Purpose: 当前 research 已确认旧 `VsDevCmd.bat` 路径可能无效。Phase 22 最大的 closure 风险不是代码，而是沿用失效工具链命令制造假阳性。因此这个计划把“先确认真实 toolchain，再做 fresh gate”写成硬性步骤。

Output: fresh Phase 22 gate 证据闭环，以及与证据一致的 verification / roadmap / state / 条件性 project docs。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.cpp
@Tests/TestMain.cpp
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 先确认/修正真实 toolchain 命令，再执行 fresh L1/L2/L4/L6 gate 并回写 22-TEST-VERIFICATION</name>
  <files>.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Tests/TestMain.cpp
  </read_first>
  <action>
    先做 toolchain closure gate，再做 fresh Phase 22 gates，要求精确如下：
    - 第一步不是直接 build，而是**确认真实可用的 MSVC developer shell 命令**。如果 research 里的旧 `VsDevCmd.bat` 路径无效，必须在 `22-TEST-VERIFICATION.md` 里记录新的真实命令，或记录“未找到可用 toolchain，Phase 22 closure blocked”
    - 只有 toolchain 命令确认后，才允许执行 L1：fresh configure/build `build-phase22-docs` 并构建 `OpenTuneTests`
    - L2：运行 `OpenTuneTests.exe`，必须记录 `=== Phase 22: Clip-Aware Paint Tests ===`、`PAINT_01_*`、`PAINT_02_*`、`FLOW_01_*`，以及 retained `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*`
    - L4：静态审计必须写回文档，至少覆盖：
      - `drawNotes(` 不再以全表 full scan 为主循环
      - `drawWaveform(` / `drawF0Curve(` / `drawNotes(` 共享 visible time window 语义
      - `getPlayheadDirtyBounds(` / `updateMainLayerPlayhead(` / `invalidateVisual(` / `flushPendingVisualInvalidation(` 仍是单入口单 flush bridge
      - `onVisualVBlankCallback(` 在 `PianoRollComponent.cpp` 仍恰好 1 处，`Source/Standalone/PluginEditor.cpp` / `Source/Plugin/PluginEditor.cpp` 中不新增 `pianoRoll_.invalidateVisual` / `pianoRoll_.onVisualVBlankCallback` / `requestInvalidate(pianoRoll_)`
    - L6：再次运行 `OpenTuneTests.exe`，记录完整回归是否仍全部通过
    - `22-TEST-VERIFICATION.md` 必须写清每一级命令、结果、关键输出，以及 toolchain 命令最终版本；禁止只写“Passed/Failed”
  </action>
  <acceptance_criteria>
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `build-phase22-docs`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `Phase 22: Clip-Aware Paint And Integration Guards`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "build-phase22-docs|Phase 22: Clip-Aware Paint And Integration Guards|PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge|FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection" ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 22 的 gate 证据已建立在真实 toolchain 命令之上，不再依赖失效路径假设或 stale build 产物。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 写 verification traceability，并按真实 gate 结果回写 roadmap/state/条件性 project docs</name>
  <files>.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md, .planning/ROADMAP.md, .planning/STATE.md, .planning/PROJECT.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
    - .planning/PROJECT.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - .planning/STATE.md
  </read_first>
  <action>
    基于 fresh gate 结果生成 `.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md`，并按结果更新状态文档。要求精确如下：
    - `22-VERIFICATION.md` 必须包含 `Observable Truths`、`Requirements Coverage`、`Artifact Verification`、`Key Link Verification`、`Behavioral Spot-Checks`、`Remaining Risks`
    - `Requirements Coverage` 只映射 `PAINT-01`、`PAINT-02`、`FLOW-01`
    - `Observable Truths` 必须至少覆盖：
      1. `drawNotes()` 已改成 visible note window 遍历，waveform / F0 / notes 共用 renderer visible time window
      2. `getPlayheadDirtyBounds()` + `updateMainLayerPlayhead()` + `invalidateVisual(...)` + `flushPendingVisualInvalidation()` 仍维持 strip dirty / 单入口 / 单 flush bridge
      3. `fitToScreen()`、continuous/page scroll、playhead projection 继续命中 sample-authoritative projection
    - 如果 L1/L2/L4/L6 全部通过，则：
      - `22-VERIFICATION.md` 写 `Verdict: passed` 与 `Gate status: PASS`
      - `.planning/ROADMAP.md` 把 Phase 22 plan list 勾满、progress 改成 completed
      - `.planning/STATE.md` 更新为 v2.1 milestone complete 或明确下一步完成收口状态
      - 同步 `.planning/PROJECT.md` / `.planning/REQUIREMENTS.md`：把 `PAINT-01` / `PAINT-02` / `FLOW-01` 从 active/pending 回写为完成态
    - 如果任一级失败，则：
      - `22-VERIFICATION.md` 写真实 blocker 与 `Gate status: BLOCKED` 或 `FAIL`
      - `.planning/ROADMAP.md` / `.planning/STATE.md` 只能反映“Phase 22 verification blocked / gaps found”
      - `.planning/PROJECT.md` / `.planning/REQUIREMENTS.md` **不得** 写完成
  </action>
  <acceptance_criteria>
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md` contains `Gate status:`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md` contains `PAINT-01`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md` contains `PAINT-02`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md` contains `FLOW-01`
    - `.planning/ROADMAP.md` contains `22-04-PLAN.md`
    - `.planning/STATE.md` contains `Phase 22`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Gate status:|PAINT-01|PAINT-02|FLOW-01|22-04-PLAN.md|Phase 22" ".planning/phases/22-Clip-Aware 刷新与集成守护/22-VERIFICATION.md" ".planning/ROADMAP.md" ".planning/STATE.md"</automated>
  </verify>
  <done>
    Phase 22 的 requirements、roadmap、state 与条件性 milestone 文档都重新对齐到同一份真实 gate 证据，不再出现“文档已完成、fresh gate 未证实”的分裂状态。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| toolchain discovery -> fresh build evidence | 如果 closure 继续假定失效的 `VsDevCmd.bat` 路径，Phase 22 会出现“命令写了但从未真实执行”的假阳性 |
| verification verdict -> project state docs | 只有真实 gate 结果才能更新 roadmap/state/project docs，否则 v2.1 baseline 会被污染 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-30 | R | Phase 22 evidence trail | mitigate | 把“先确认真实 toolchain 命令”写成 closure 第一步，并把最终命令与输出原样写回 `22-TEST-VERIFICATION.md`。 |
| T-22-31 | T | verification / state consistency | mitigate | `22-VERIFICATION.md` 直接消费 fresh gate 结果；只有 PASS 才允许 roadmap/state/project docs 标完成。 |
| T-22-32 | D | stale build reuse | mitigate | Phase 22 明确禁止复用旧 `build/` 或旧 `VsDevCmd` 路径；所有证据都必须带 `build-phase22-docs` 与真实 toolchain 命令。 |
</threat_model>

<verification>
- `22-TEST-VERIFICATION.md` 已记录真实 toolchain 命令和 fresh L1/L2/L4/L6 结果。
- `22-VERIFICATION.md` 已完成 requirement 级 traceability。
- roadmap/state 与条件性 project docs 已按真实 gate 结果同步。
</verification>

<success_criteria>
- [ ] Phase 22 closure 只依赖真实 toolchain + fresh build/test 证据。
- [ ] `PAINT-01` / `PAINT-02` / `FLOW-01` 都在 verification 文档里有直接证据。
- [ ] roadmap/state/project docs 与真实 gate 结果完全一致。
</success_criteria>

<output>
After completion, create `.planning/phases/22-Clip-Aware 刷新与集成守护/22-04-SUMMARY.md`
</output>
