---
phase: 22-Clip-Aware 刷新与集成守护
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
  - Tests/TestMain.cpp
autonomous: true
requirements: [PAINT-01, PAINT-02, FLOW-01]

must_haves:
  truths:
    - "Phase 22 在动 renderer 之前先有 clip-aware 验证契约，执行者不需要猜什么叫 visible note window、strip dirty 和 fit-to-screen 守护。"
    - "RED regressions 先把 notes 不得 full scan、visible time window 统一、fit-to-screen 继续命中 clip timeline projection 写成明确测试事实。"
    - "Phase 18-21 的 projected playhead、single-host scene、unified invalidation、single VBlank 都继续作为 Phase 22 的 retained baseline。"
  artifacts:
    - path: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"
      provides: "Phase 22 verification source of truth"
      contains: "Phase 22: Clip-Aware Paint And Integration Guards"
    - path: "Tests/TestMain.cpp"
      provides: "Phase 22 RED regressions and retained guard entrypoints"
      contains: "runPhase22ClipAwarePaintTests"
  key_links:
    - from: ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"
      to: "Tests/TestMain.cpp"
      via: "验证文档先写死必须出现的 Phase 22 journey 名称、gate 与 retained guards"
      pattern: "runPhase22ClipAwarePaintTests"
    - from: "Tests/TestMain.cpp"
      to: "Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp"
      via: "RED 测试先锁定 drawNotes 必须脱离 full scan，转向 visible note window"
      pattern: "drawNotes"
---

<objective>
先把 Phase 22 的 test-verification 契约和 RED regressions 固定下来，再进入 renderer clip-aware 收敛与集成守护实现。Phase 22 继续遵守 test-driven-spec：先定义验证，再允许修改 `PianoRollRenderer` / `PianoRollComponent`。

Purpose: 如果不先把 `PAINT-01` / `PAINT-02` / `FLOW-01` 的测试事实写死，执行阶段很容易把“dirty rect 已经很窄”误判成 clip-aware 完成，或者遗漏 fit-to-screen 与 retained phase guards。

Output: 更新后的 `22-TEST-VERIFICATION.md` 与 `Tests/TestMain.cpp` 中的 Phase 22 RED regressions。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@.planning/codebase/ARCHITECTURE.md
@.planning/phases/20-统一失效入口/20-01-PLAN.md
@.planning/phases/21-单 VBlank 视觉循环/21-01-PLAN.md
@.planning/phases/21-单 VBlank 视觉循环/21-02-PLAN.md
@.planning/phases/21-单 VBlank 视觉循环/21-04-PLAN.md
@.planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
@Tests/TestMain.cpp
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
@Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/Standalone/PluginEditor.cpp
@Source/Plugin/PluginEditor.cpp

<interfaces>
From `Source/Standalone/UI/PianoRoll/PianoRollRenderer.h`:
```cpp
void drawWaveform(juce::Graphics& g, const RenderContext& ctx);
void drawNotes(juce::Graphics& g, const RenderContext& ctx,
               const std::vector<Note>& notes,
               double trackOffsetSeconds);
void drawF0Curve(juce::Graphics& g,
                 const std::vector<float>& f0,
                 juce::Colour colour,
                 float alpha,
                 bool isThinLine,
                 const RenderContext& ctx,
                 std::shared_ptr<PitchCurve> currentCurve,
                 const std::vector<uint8_t>* visibleMask = nullptr);
```

From `Source/Standalone/UI/PianoRollComponent.h`:
```cpp
void onVisualVBlankCallback(double timestampSec);
void invalidateVisual(const PianoRollVisualInvalidationRequest& request);
void flushPendingVisualInvalidation();
void fitToScreen();
```

Retained guards that must stay green:
```cpp
TIME_01_PianoRollContinuousScrollUsesProjectedClipPlayhead
TIME_01_PianoRollPageScrollUsesProjectedClipPlayhead
LAYER_01_PianoRollHasNoPlayheadOverlayChild
INVAL_01_PianoRollVisualVBlankFlushesPendingInteractiveInvalidation
CLOCK_01_PianoRollSingleVisualTickFlushesPlayheadAndScroll
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 固化 Phase 22 test-verification 契约</name>
  <files>.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-RESEARCH.md
    - .planning/phases/21-单 VBlank 视觉循环/21-TEST-VERIFICATION.md
    - .planning/phases/21-单 VBlank 视觉循环/21-VERIFICATION.md
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Standalone/UI/PianoRollComponent.cpp
    - Tests/TestMain.cpp
  </read_first>
  <action>
    创建 `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md`，把 Phase 22 的验证源固定成明确可执行的四级 gate，要求逐条写死：
    - 标题固定为 `Phase 22: Clip-Aware Paint And Integration Guards`
    - L1 先写成“toolchain command must be confirmed before fresh build”，并在命令区显式占位 `build-phase22-docs`；不得直接假定旧 `VsDevCmd.bat` 路径仍有效
    - 文档中必须预留 `Confirmed Toolchain Setup Command:` 字段，供后续计划复用；如果尚未确认，先写 `TBD`，但字段名必须固定
    - L2 / L6 命令目标固定为 `OpenTuneTests.exe`，并要求输出中必须出现 `=== Phase 22: Clip-Aware Paint Tests ===`
    - L4 静态审计必须点名 `drawNotes(`、`drawWaveform(`、`drawF0Curve(`、`getPlayheadDirtyBounds(`、`updateMainLayerPlayhead(`、`invalidateVisual(`、`flushPendingVisualInvalidation(`、`onVisualVBlankCallback(`、`fitToScreen(`、`pianoRoll_.onVisualVBlankCallback`、`pianoRoll_.invalidateVisual`、`requestInvalidate(pianoRoll_`，并明确要求：
      - `drawNotes()` 不能停留在 `for (const auto& note : notes)` full scan
      - waveform / F0 / notes 必须共享同一 renderer visible time window 语义
      - `getPlayheadDirtyBounds()` + `updateMainLayerPlayhead()` + `invalidateVisual(...)` + `flushPendingVisualInvalidation()` 继续是 strip dirty / 单入口 / 单 flush bridge
      - `onVisualVBlankCallback(...)` 继续是唯一视觉 tick，editor shell 不得新增 cadence / direct invalidate residue
    - `Required Journeys` 必须精确列出：
      - `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow`
      - `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0`
      - `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge`
      - `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
      - retained `TIME_01_*` / `LAYER_01_*` / `INVAL_01_*` / `CLOCK_01_*`
    - `Evidence To Capture` 必须写清：visible note window 不等于 full scan 内加 if；drawWaveform/drawF0Curve 已是 visible-range 驱动；Phase 22 不恢复 overlay、editor cadence、direct invalidate
  </action>
  <acceptance_criteria>
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `Phase 22: Clip-Aware Paint And Integration Guards`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `build-phase22-docs`
  </acceptance_criteria>
  <verify>
    <automated>rg -n "Phase 22: Clip-Aware Paint And Integration Guards|PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow|PAINT_01_PianoRollPlayheadDirtyStripStillUsesSingleFlushBridge|FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection|build-phase22-docs" ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 22 已有先于实现存在的验证契约，执行者不需要再猜应该证明什么。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 先写 Phase 22 RED regressions</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow` 证明 notes 绘制必须建立 visible note window，而不是 full scan 内 continue。
    - Test 2: `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0` 证明三条主内容路径共用一个 renderer visible time window 语义。
    - Test 3: `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection` 证明 fit-to-screen 继续优先命中 clip timeline projection，而不是 UI 层自行估时长。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - .planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.h
    - Source/Standalone/UI/PianoRoll/PianoRollRenderer.cpp
    - Source/Standalone/UI/PianoRollComponent.h
    - Source/Standalone/UI/PianoRollComponent.cpp
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 中新增 `runPhase22ClipAwarePaintTests()` 并接入主测试流程。先写 RED baseline，精确锁定下列未来 contract 与 journey 名称，不允许临时改名：
    - `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow`
      - 通过 test probe 或最小 helper contract 断言 renderer 能求出 visible note iterator window，而不是只能把整个 `notes` 全向量交给 `drawNotes()` 遍历
      - 必须让测试在当前 live tree 上因 `drawNotes()` 仍 full scan 而失败
    - `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0`
      - 锁定 renderer 内必须出现单一 visible time window helper / contract，notes、waveform、F0 都消费它
      - 当前 live tree 因 notes 尚未接入统一 helper 而失败
    - `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
      - 使用 processor clip timeline projection 构造 host + `PianoRollComponent`，断言 `fitToScreen()` 后 zoom/scroll 命中 projected start 和 projected duration
      - 现有 `TIME_01_*` 行为保持原断言，不能放宽
    - 主测试入口必须显式调用 `runPhase22ClipAwarePaintTests();`
    - 在写完 RED baseline 后，必须先确认真实 toolchain setup command，把它回写到 `22-TEST-VERIFICATION.md` 的 `Confirmed Toolchain Setup Command:` 字段，再 fresh build `build-phase22-docs` 并运行当前测试二进制
    - 运行结果必须在当前 live tree 上对至少一个 `PAINT_02_*` journey 给出明确 FAIL / 非零退出，把 RED 证据与失败输出写回 `22-TEST-VERIFICATION.md`；禁止只用 `rg` 证明“测试名字出现了”
    - 先不要在 Plan 01 偷做 editor shell 全面 cleanup、host runtime 修复或安全专项测试；Phase 22 只围绕 `PAINT-01` / `PAINT-02` / `FLOW-01`
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase22ClipAwarePaintTests`
    - `Tests/TestMain.cpp` contains `PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow`
    - `Tests/TestMain.cpp` contains `PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0`
    - `Tests/TestMain.cpp` contains `FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection`
    - `Tests/TestMain.cpp` contains `runPhase22ClipAwarePaintTests();`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` contains `Confirmed Toolchain Setup Command:`
    - `.planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md` records explicit RED failure evidence for Phase 22 journeys
  </acceptance_criteria>
  <verify>
    <automated>rg -n "runPhase22ClipAwarePaintTests|PAINT_02_PianoRollDrawNotesUsesVisibleNoteWindow|PAINT_02_PianoRollRendererUsesUnifiedVisibleTimeWindowForNotesWaveformAndF0|FLOW_01_PianoRollFitToScreenUsesClipTimelineProjection" "Tests/TestMain.cpp"; if ($?) { rg -n "Confirmed Toolchain Setup Command:|Result: FAIL|Result: BLOCKED|=== Phase 22: Clip-Aware Paint Tests ===" ".planning/phases/22-Clip-Aware 刷新与集成守护/22-TEST-VERIFICATION.md" }</automated>
  </verify>
  <done>
    Phase 22 的 RED 阶段已完成：visible note window、统一 visible time window、fit-to-screen projection 三条事实先于实现被写死，并且当前 live tree 的失败证据已被 fresh build/run 真正记录。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| verification contract -> implementation | 如果 Phase 22 没有先写死验证契约，执行者会把“dirty rect 已存在”误判成 clip-aware 完成 |
| regression suite -> renderer contract | 如果 RED baseline 不先锁定 visible note window 与 unified time window，后续极易退化为 full scan + if 的伪优化 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01 | R | Phase 22 verification contract | mitigate | 在 `22-TEST-VERIFICATION.md` 中写死 Phase 22 gate、journey 与 retained guards，防止执行阶段口头补定义。 |
| T-22-02 | T | renderer clip-aware regression coverage | mitigate | 在 `Tests/TestMain.cpp` 先加入 `runPhase22ClipAwarePaintTests()`，并用 fresh build/run 记录当前 live tree 的 RED failure，直接约束 visible note window、统一 visible time window、fit-to-screen projection。 |
| T-22-03 | D | phase scope drift | accept | 本计划接受 editor shell 现有少量 `pianoRoll_.repaint()` residue 仍存在，因为 Phase 22 不做 editor shell 全面重构，只把它们纳入 retained 审计边界。 |
</threat_model>

<verification>
- `22-TEST-VERIFICATION.md` 已成为 Phase 22 的验证源文件。
- `Tests/TestMain.cpp` 已定义 Phase 22 RED 回归入口。
- Phase 18-21 的 projected playhead、single-host、single-entry、single-VBlank 守护继续被列为 Phase 22 retained baseline。
</verification>

<success_criteria>
- [ ] Phase 22 已有先行 test-verification 契约。
- [ ] Phase 22 已有覆盖 visible note window / unified visible time window / fit-to-screen projection 的 RED baseline。
- [ ] 执行者可以在不猜测需求的前提下进入 renderer 与 integration guard 实现。
</success_criteria>

<output>
After completion, create `.planning/phases/22-Clip-Aware 刷新与集成守护/22-01-SUMMARY.md`
</output>
