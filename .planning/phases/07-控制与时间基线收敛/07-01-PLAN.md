---
phase: 07-控制与时间基线收敛
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [BASE-01, BASE-02]

must_haves:
  truths:
    - "插件内可见播放状态与位置只以宿主回读为事实源"
    - "插件侧播放控制请求失败时不会伪造本地播放状态"
    - "ARA 分支下不再存在与宿主并行的传输真相"
  artifacts:
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "播放/暂停/定位请求与宿主回读状态对齐"
    - path: "Source/PluginProcessor.cpp"
      provides: "host transport snapshot 回读与暴露"
  key_links:
    - from: "OpenTuneAudioProcessorEditor::playRequested/pauseRequested/stopRequested"
      to: "OpenTuneDocumentController::requestStartPlayback/requestStopPlayback/requestSetPlaybackPosition"
      via: "ARA HostPlaybackController"
      pattern: "request*Playback + host-readback-only UI update"
---

<objective>
收敛 VST3 ARA 传输控制为“请求 -> 宿主回读 -> UI 更新”的单向闭环，消除插件本地状态与宿主状态双轨并行。

Purpose: 满足 BASE-01/BASE-02，避免出现“按钮已播放但宿主未播放”或“宿主已暂停但插件仍显示播放”。
Output: ARA 分支传输控制链路重构，状态回读路径清晰可验证。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@VST3 References/docs/adr/ADR-0003-ara-playback-controller-interface.md
@Source/Plugin/PluginEditor.h
@Source/Plugin/PluginEditor.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
</context>

<interfaces>
From Source/Plugin/PluginEditor.cpp:
```cpp
void playRequested() override;
void pauseRequested() override;
void stopRequested() override;
void playheadPositionChangeRequested(double timeSeconds) override;
```

From Source/PluginProcessor.h:
```cpp
void setPlaying(bool playing);
bool isPlaying() const;
void setPosition(double seconds);
double getPosition() const;
```

From Source/ARA/OpenTuneDocumentController.h:
```cpp
bool requestSetPlaybackPosition(double timeInSeconds);
bool requestStartPlayback();
bool requestStopPlayback();
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 定义宿主权威传输快照并暴露只读接口</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: ARA 绑定时，processBlock 每次回调都刷新宿主 isPlaying/time/loop/ppq 快照。
    - Test 2: 非 ARA 路径不覆盖 standalone 现有传输行为。
  </behavior>
  <action>
    在 Processor 层定义 HostTransportSnapshot（至少含 isPlaying、timeSeconds、bpm、ppq、loop），并提供线程安全只读访问。

    将现有分散的 host* 原子更新统一到单入口（processBlock ARA 分支读取 playHead 后），明确“宿主回读即事实”。

    保留 Standalone 既有 API 行为；仅在 !JucePlugin_Build_Standalone + JucePlugin_Enable_ARA 路径启用宿主快照语义。
  </action>
  <verify>
    <automated>rg -n "HostTransportSnapshot|host.*Snapshot|getHost.*Transport|getPosition\(\) const" Source/PluginProcessor.h Source/PluginProcessor.cpp</automated>
  </verify>
  <done>Processor 形成单一宿主传输快照真相源，供编辑器只读消费。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 编辑器改为请求式控制，移除 ARA 分支本地状态写入</name>
  <files>Source/Plugin/PluginEditor.h, Source/Plugin/PluginEditor.cpp</files>
  <behavior>
    - Test 1: play/pause/stop 触发后，UI 状态以宿主回读变更为准，而非立即本地置位。
    - Test 2: requestStartPlayback/requestStopPlayback/requestSetPlaybackPosition 失败时，UI 不进入伪状态。
  </behavior>
  <action>
    重构 playRequested/pauseRequested/stopRequested/playheadPositionChangeRequested：
    - ARA 分支仅发送请求 + 记录最后一次控制命令（用于诊断日志）。
    - 删除/禁止 ARA 分支直接调用 setPlaying 或 transportBar_.setPlaying 的本地强制写入。
    - UI 定时刷新统一从 Processor 宿主快照读取并渲染。

    非 ARA（Standalone）分支保持当前行为不变，严格由条件编译隔离。
  </action>
  <verify>
    <automated>rg -n "playRequested\(|pauseRequested\(|stopRequested\(|requestStartPlayback|requestStopPlayback|requestSetPlaybackPosition|setPlaying\(" Source/Plugin/PluginEditor.cpp</automated>
  </verify>
  <done>编辑器传输控制闭环改为 request->readback，ARA 下无并行本地真相。</done>
</task>

<task type="auto">
  <name>Task 3: 增加最小诊断日志并完成回归检查</name>
  <files>Source/Plugin/PluginEditor.cpp, Source/PluginProcessor.cpp</files>
  <action>
    增加关键日志点（最后控制请求、宿主回读状态变化、失败原因），用于验证 BASE-01/02。

    执行回归：
    1) Standalone Debug 构建通过；
    2) VST3 Debug 构建通过；
    3) 不引入新的编译警告（重点检查条件编译分支）。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_Standalone; if ($?) { cmake --build build --config Debug --target OpenTune_VST3 }</automated>
  </verify>
  <done>可观测日志与双格式构建回归通过，Phase 7 控制基线具备验收证据。</done>
</task>

</tasks>

<verification>
- DAW 手动验证：REAPER 中分别执行宿主播放/暂停/定位与插件按钮操作，检查双方状态一致。
- 异常验证：HostPlaybackController 不可用时，插件显示告警但不进入错误播放状态。
- 守护验证：Standalone 传输行为与交互语义不变。
</verification>

<success_criteria>
- [ ] BASE-01：宿主驱动操作后，插件状态与位置始终与宿主一致。
- [ ] BASE-02：插件触发控制后，最终状态以宿主回读结果为准。
- [ ] ARA 路径不再存在 setPlaying 等本地并行真相写入。
</success_criteria>

<output>
After completion, create `.planning/phases/07-控制与时间基线收敛/07-01-SUMMARY.md`
</output>
