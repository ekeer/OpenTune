---
phase: 07-控制与时间基线收敛
plan: 02
type: execute
wave: 1
depends_on: [07-01]
files_modified:
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - Source/ARA/OpenTunePlaybackRenderer.h
  - Source/ARA/OpenTunePlaybackRenderer.cpp
  - Source/Utils/TimeCoordinate.h
autonomous: true
requirements: [BASE-03]

must_haves:
  truths:
    - "ARA PlaybackRenderer 使用 playbackRange + sourceRange 的一致映射"
    - "不同工程采样率下读取起点与长度换算一致"
    - "seek/loop 场景不出现可感知漂移或突发错位"
  artifacts:
    - path: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      provides: "统一时间映射与采样率换算实现"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "播放区间与源区间读取契约"
  key_links:
    - from: "OpenTuneDocumentController::getPlaybackRangeForAudioSource/getSourceRangeForAudioSource"
      to: "OpenTunePlaybackRenderer::processBlock"
      via: "时间映射函数"
      pattern: "playbackTime -> sourceTime -> sourceSample"
---

<objective>
建立 ARA 回放时间映射与采样率转换的单一算法，实现 seek/loop/跨采样率场景稳定一致。

Purpose: 满足 BASE-03，消除当前 PlaybackRenderer 仅按 regionStart 计算 srcOffset 带来的映射误差。
Output: 显式映射函数与边界处理策略，跨场景可复现一致行为。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Source/ARA/OpenTuneDocumentController.h
@Source/ARA/OpenTuneDocumentController.cpp
@Source/ARA/OpenTunePlaybackRenderer.h
@Source/ARA/OpenTunePlaybackRenderer.cpp
@Source/Utils/TimeCoordinate.h
</context>

<interfaces>
From Source/ARA/OpenTuneDocumentController.h:
```cpp
bool getPlaybackRangeForAudioSource(juce::ARAAudioSource* source,
                                    double& outStartSeconds,
                                    double& outEndSeconds) const;
bool getSourceRangeForAudioSource(juce::ARAAudioSource* source,
                                  double& outStartSeconds,
                                  double& outEndSeconds) const;
```

From Source/ARA/OpenTunePlaybackRenderer.cpp:
```cpp
bool processBlock(juce::AudioBuffer<float>& buffer,
                  juce::AudioProcessor::Realtime realtime,
                  const juce::AudioPlayHead::PositionInfo& positionInfo) noexcept override;
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 提炼时间映射函数并覆盖边界样例</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.h, Source/ARA/OpenTunePlaybackRenderer.cpp, Source/Utils/TimeCoordinate.h</files>
  <behavior>
    - Test 1: 播放时间位于 playbackRange 起点时，映射到 sourceRange 起点。
    - Test 2: playbackRange/sourceRange 长度不等时，映射比例正确。
    - Test 3: seek 到边界点时，映射 sample 不越界。
  </behavior>
  <action>
    在 PlaybackRenderer 中引入纯函数（或私有静态函数）实现：
    playbackTime -> sourceTime -> sourceSample。

    函数输入显式包含 playbackStart/end、sourceStart/end、sourceSampleRate，统一处理：
    - 时间归一化比例
    - clamp 与 epsilon 边界
    - sample 索引上/下界

    复用 TimeCoordinate 转换函数，避免散落的手写 static_cast 逻辑。
  </action>
  <verify>
    <automated>rg -n "map.*Playback|playback.*source|sourceSample|clamp|TimeCoordinate" Source/ARA/OpenTunePlaybackRenderer.cpp</automated>
  </verify>
  <done>时间映射算法单点实现，覆盖边界样例并具备可读契约。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 用映射函数重写 ARA processBlock 读取路径</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp, Source/ARA/OpenTuneDocumentController.cpp</files>
  <behavior>
    - Test 1: region 内任意播放点可正确读取 source 对应片段。
    - Test 2: sourceRange 非零偏移场景，读取不再从 0 错位。
    - Test 3: hostSampleRate != sourceSampleRate 时，重采样后 block 长度与输出一致。
  </behavior>
  <action>
    在 processBlock 中使用 DocumentController 提供的 playback/source range；禁止继续使用
    `(playheadTime - regionStartTime) * sampleRate` 的简化近似。

    重写读取流程：
    1) 先计算 source 起点 sample；
    2) 再按 host block 目标长度读取；
    3) 最后统一走重采样/直拷贝分支。

    同时审查 addFrom/copyFrom 语义，确保多 region 混音不被后写覆盖。
  </action>
  <verify>
    <automated>rg -n "getPlaybackRangeForAudioSource|getSourceRangeForAudioSource|upsampleForHost|addFrom|copyFrom" Source/ARA/OpenTunePlaybackRenderer.cpp</automated>
  </verify>
  <done>ARA 回放读取改为基于显式 range mapping，seek/offset 行为一致。</done>
</task>

<task type="auto">
  <name>Task 3: 采样率与 loop 回归验证</name>
  <files>Source/ARA/OpenTunePlaybackRenderer.cpp</files>
  <action>
    增加必要日志（仅关键路径）记录：playbackTime、mappedSourceTime、srcOffset、host/source sampleRate。

    在 REAPER 中做三组验证：
    - 44.1k 工程 + 循环播放；
    - 48k 工程 + 高频 seek；
    - 修改 region 起止后重复播放。

    对比日志确保映射连续，不出现跳变与越界。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_VST3</automated>
  </verify>
  <done>BASE-03 验证闭环建立，跨采样率与 loop 行为可重复通过。</done>
</task>

</tasks>

<verification>
- 手动听感验证：loop 边界无明显错位/抽动。
- 日志验证：playback->source 映射单调连续（除 loop 回绕外）。
- 结构验证：时间换算入口唯一，不存在第二套映射实现。
</verification>

<success_criteria>
- [ ] seek/loop/跨采样率场景下，时间映射稳定且无可感知漂移。
- [ ] PlaybackRenderer 完整使用 playback/source range 契约读取。
- [ ] 映射算法只有一个实现，后续可复用于 Phase 8 统一读路径。
</success_criteria>

<output>
After completion, create `.planning/phases/07-控制与时间基线收敛/07-02-SUMMARY.md`
</output>
