---
phase: 07-控制与时间基线收敛
plan: 03
type: verify
wave: 2
depends_on: [07-01, 07-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md
autonomous: true
requirements: [BASE-01, BASE-02, BASE-03]

must_haves:
  truths:
    - "Phase 7 三项 requirement 均有自动化或可执行的人工验证步骤"
    - "验证过程覆盖宿主驱动与插件驱动双向传输控制"
    - "验证结论可追溯到日志与测试输出"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "时间映射与边界转换单元测试"
    - path: ".planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md"
      provides: "Phase 7 验证清单与结果记录模板"
  key_links:
    - from: ".planning/REQUIREMENTS.md"
      to: ".planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md"
      via: "Requirement -> Test/Scenario Traceability"
      pattern: "BASE-01|BASE-02|BASE-03"
---

<objective>
固化 Phase 7 验证协议，补齐可自动执行的映射测试与可复现的 DAW 手动验证清单。

Purpose: 在进入 Phase 8 前，确保控制/时间基线已闭环，避免把不确定性带入统一读路径重构。
Output: Phase 7 验证文档与测试证据。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/TESTING.md
@Tests/TestMain.cpp
@Source/Plugin/PluginEditor.cpp
@Source/ARA/OpenTunePlaybackRenderer.cpp
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为时间映射与边界行为补充单元测试</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: playback/source 同长映射下 sample 精确对齐。
    - Test 2: playback/source 异长映射下比例正确。
    - Test 3: 超界输入被 clamp 到合法范围。
  </behavior>
  <action>
    将 07-02 提炼出的映射算法抽象为可测试接口（或同算法测试副本），在 TestMain 增加独立 section。

    覆盖正常/边界/异常输入，输出 PASS/FAIL 可直接关联 BASE-03。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests; if ($?) { .\build\Debug\OpenTuneTests.exe }</automated>
  </verify>
  <done>映射算法具备自动化回归保护，避免后续 Phase 8 改动回退。</done>
</task>

<task type="auto">
  <name>Task 2: 编写 Phase 7 验证清单与证据模板</name>
  <files>.planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md</files>
  <action>
    创建验证文档并包含：
    1) Requirement -> Test/Scenario Traceability；
    2) 自动化命令、预期输出、失败判据；
    3) REAPER 手动步骤（宿主驱动 + 插件驱动 + seek/loop + 跨采样率）；
    4) 日志字段清单与采集方法。

    文档必须可由他人按步骤复现，不依赖隐性上下文。
  </action>
  <verify>
    <automated>rg -n "BASE-01|BASE-02|BASE-03|Traceability|REAPER|自动化|手动" .planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md</automated>
  </verify>
  <done>Phase 7 验证协议完成，具备可重复执行与审计能力。</done>
</task>

<task type="auto">
  <name>Task 3: 完整执行验证并出具结论</name>
  <files>.planning/phases/07-控制与时间基线收敛/07-VERIFICATION.md</files>
  <action>
    逐项执行验证清单，填充实际结果（PASS/FAIL + 证据路径）。

    若失败，回链到 07-01/07-02 修复，不允许通过“临时兜底”绕过。

    验证通过后在文档末尾给出 Phase 7 Gate 结论与进入 Phase 8 建议。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_VST3; if ($?) { ctest -C Debug --output-on-failure --test-dir build }</automated>
  </verify>
  <done>Phase 7 gate 明确：通过则可进入 Phase 8；失败则阻断推进。</done>
</task>

</tasks>

<verification>
- 自动化：OpenTuneTests + CTest 结果留档。
- 手动：REAPER 场景截图/日志摘要留档。
- Gate：三项 requirement 均有正向证据且无 P0/P1 回归。
</verification>

<success_criteria>
- [ ] BASE-01/02/03 均具备可复验的证据条目。
- [ ] 验证文档可由新成员独立执行并得出同结论。
- [ ] 通过 Gate 后 Phase 7 状态可切换为 Completed。
</success_criteria>

<output>
After completion, create `.planning/phases/07-控制与时间基线收敛/07-03-SUMMARY.md`
</output>
