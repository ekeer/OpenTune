---
phase: 16-hop-对齐切分与末块语义
plan: 03
type: execute
wave: 3
depends_on: [16-02]
files_modified:
  - Tests/TestMain.cpp
  - .planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md
  - .planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md
autonomous: true
requirements: [CHNK-01, CHNK-02, CHNK-03, CHNK-04, TASK-04]

must_haves:
  truths:
    - "自动化测试必须直接证明 hop-aligned chunk 边界、gap skip、only-last-chunk padding、以及 padded tail 不再触发 trailing-F0 extension。"
    - "verification 文档要把 5 个 requirement 全部映射到具体文件、测试名与命令，不留模糊描述。"
    - "Phase 16 做完后可以靠单一 `OpenTuneTests` 入口回归，不依赖手工读代码猜行为。"
  artifacts:
    - path: "Tests/TestMain.cpp"
      provides: "Phase 16 hop-alignment and padding regression tests"
      contains: "runPhase16HopAlignedChunkTests"
    - path: ".planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md"
      provides: "Phase 16 test verification commands"
      contains: "CHNK-04"
    - path: ".planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md"
      provides: "requirement traceability for all Phase 16 requirements"
      contains: "TASK-04"
---

<objective>
为 Phase 16 建立自动化回归与 requirement traceability，证明 hop 对齐切分、末块 padding 和 padded-tail F0 语义都已经落地。

Purpose: Phase 16 一旦缺少自动化证据，后续 Phase 17/18 很容易又把 seconds-based chunk boundary 或 pad-tail voiced extension 带回来；这个 plan 负责把回归测试和 gate 文档一次性补齐。

Output: `runPhase16HopAlignedChunkTests()`、`16-TEST-VERIFICATION.md`、`16-VERIFICATION.md`。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/15-样本真相源与冻结边界/15-VERIFICATION.md
@.planning/phases/16-hop-对齐切分与末块语义/16-01-PLAN.md
@.planning/phases/16-hop-对齐切分与末块语义/16-02-PLAN.md
@Tests/TestMain.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/Inference/RenderCache.h
@Source/Inference/RenderCache.cpp
@Source/Utils/SilentGapDetector.h
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 为 hop 对齐切分与末块 padding 语义添加回归测试</name>
  <files>Tests/TestMain.cpp</files>
  <behavior>
    - Test 1: sample-domain silent gap builder 只产出位于静息处内部的 hop-aligned chunk boundary。
    - Test 2: 当前静息处内没有合法 hop 点时，该静息处被跳过，后续合法静息处仍然参与切分。
    - Test 3: 非末块 `freezeRenderBoundaries(...)` 不允许 padding，末块保留真实 `trueEndSample` 并生成更大的 `synthSampleCount`。
    - Test 4: padded last chunk 的 trailing-zero tail 在 `fillF0GapsForVocoder()` 后仍保持 0，不会被 voiced 延伸。
  </behavior>
  <read_first>
    - Tests/TestMain.cpp
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - Source/Inference/RenderCache.h
    - Source/Inference/RenderCache.cpp
    - Source/Utils/SilentGapDetector.h
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    在 `Tests/TestMain.cpp` 新增 `runPhase16HopAlignedChunkTests()`，并接到 `main()` 的执行序列。

    至少补齐以下测试：

    1. `CHNK_01_CHNK_02_EnqueuePartialRenderUsesHopAlignedSampleBoundaries`
       - 构造带 sample-domain `silentGaps` 的 clip snapshot
       - 调用 `enqueuePartialRender(...)`
       - 从 `RenderCache::PendingJob` 验证 `startSample/endSampleExclusive` 命中 hop-aligned 内部边界

    2. `CHNK_02_GapWithoutAlignedCandidateIsSkipped`
       - 构造一个没有合法 hop 点的静息处和一个后续合法静息处
       - 验证最终只生成后者对应的 chunk split

    3. `CHNK_03_CHNK_04_FreezeRenderBoundariesPadsOnlyLastChunk`
       - 非末块不整 hop 时返回 `false`
       - 末块保留真实 `trueEndSample`，同时 `synthSampleCount > publishSampleCount`

    4. `TASK_04_PaddedTailDoesNotExtendVoicedF0`
       - 构造带 padding tail 的 F0 向量
       - 调用新的 `fillF0GapsForVocoder(...)` 契约
       - 验证 padding tail 仍为 0

    测试写法继续沿用当前 harness：`logSection(...)`、`const char* test = ...`、`logFail(...)` / `logPass(...)`。
  </action>
  <acceptance_criteria>
    - `Tests/TestMain.cpp` contains `runPhase16HopAlignedChunkTests`
    - `Tests/TestMain.cpp` contains `CHNK_01_CHNK_02_EnqueuePartialRenderUsesHopAlignedSampleBoundaries`
    - `Tests/TestMain.cpp` contains `CHNK_02_GapWithoutAlignedCandidateIsSkipped`
    - `Tests/TestMain.cpp` contains `CHNK_03_CHNK_04_FreezeRenderBoundariesPadsOnlyLastChunk`
    - `Tests/TestMain.cpp` contains `TASK_04_PaddedTailDoesNotExtendVoicedF0`
    - `Tests/TestMain.cpp` contains `runPhase16HopAlignedChunkTests();`
  </acceptance_criteria>
  <verify>
    <automated>cmake --build build --target OpenTuneTests --config Release && .\build\OpenTuneTests.exe</automated>
  </verify>
  <done>
    `OpenTuneTests` 现在可以直接打到 Phase 16 的 hop 对齐切分、skip rule、末块 padding 和 pad-tail F0 语义。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 生成 Phase 16 的测试验证文档与 requirement traceability</name>
  <files>.planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md, .planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md
    - .planning/ROADMAP.md
    - Tests/TestMain.cpp
    - Source/PluginProcessor.cpp
    - Source/Inference/RenderCache.cpp
  </read_first>
  <action>
    创建两个文档：

    1. `16-TEST-VERIFICATION.md`
       - 至少列出 L1 / L2 / L4 / L6 命令
       - L1: `cmake --build build --target OpenTuneTests --config Release`
       - L2/L6: `./build/OpenTuneTests.exe` 或当前工作树真实可执行路径
       - L4: `rg` 检查 hop-aligned chunk boundary、sample pending job、only-last-chunk padding、`allowTrailingExtension`
       - 写明本 phase 是 shared core / render phase，L5 不适用

    2. `16-VERIFICATION.md`
       - 对 `CHNK-01`, `CHNK-02`, `CHNK-03`, `CHNK-04`, `TASK-04` 做 100% traceability
       - 每项都要给出：目标文件、测试/grep 命令、通过标准
       - `CHNK-02` 必须明确写到“无合法 hop 点时跳过该静息处”
       - `CHNK-04` / `TASK-04` 必须明确写到“padding 只存在于推理输入，padded tail 不被 voiced 延伸”
       - 给出最终 gate 模板：`PASS / FAIL / BLOCKED`
  </action>
  <acceptance_criteria>
    - `.planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md` exists
    - `.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md` exists
    - `.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md` contains `CHNK-01`
    - `.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md` contains `CHNK-04`
    - `.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md` contains `TASK-04`
    - `.planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md` contains `allowTrailingExtension`
  </acceptance_criteria>
  <verify>
    <automated>rg "CHNK-01|CHNK-02|CHNK-03|CHNK-04|TASK-04|allowTrailingExtension|OpenTuneTests" ".planning/phases/16-hop-对齐切分与末块语义/16-TEST-VERIFICATION.md" ".planning/phases/16-hop-对齐切分与末块语义/16-VERIFICATION.md"</automated>
  </verify>
  <done>
    Phase 16 已经有测试入口与 requirement 级别的验证手册，后续 phase 可以直接复核本阶段的不变量。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source code -> regression tests | Phase 16 的 sample-boundary 与 padding 语义必须由自动化直接证明 |
| verification docs -> phase gate decision | requirement 证据需要直接映射到 PASS/FAIL 结论 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | T | `Tests/TestMain.cpp` | mitigate | 直接断言 sample boundary、skip rule、last-chunk padding 和 padded-tail F0 结果，避免模糊测试。 |
| T-16-08 | R | `16-VERIFICATION.md` | mitigate | requirement traceability 逐项绑定文件、命令和通过标准，防止 phase 结束后只有口头结论。 |
| T-16-09 | D | `OpenTuneTests` full run | accept | 当前仓库沿用单一测试入口，接受全量运行成本，不引入第二套测试二进制。 |
</threat_model>

<verification>
- 构建并运行 `OpenTuneTests`，确认 Phase 16 tests 随主入口执行。
- `rg` 检查 `16-TEST-VERIFICATION.md` 与 `16-VERIFICATION.md` 覆盖全部 5 个 requirement。
- 人工复核 `16-VERIFICATION.md`，确认 `CHNK-02` 与 `TASK-04` 的证据是具体文件和命令，而不是泛化描述。
</verification>

<success_criteria>
- [ ] `OpenTuneTests` 自动覆盖 Phase 16 的 hop 对齐切分、末块 padding 和 pad-tail F0 语义。
- [ ] `16-TEST-VERIFICATION.md` 列出真实可执行的验证命令。
- [ ] `16-VERIFICATION.md` 对 5 个 requirement 做到 100% traceability。
</success_criteria>

<output>
After completion, create `.planning/phases/16-hop-对齐切分与末块语义/16-03-SUMMARY.md`
</output>
