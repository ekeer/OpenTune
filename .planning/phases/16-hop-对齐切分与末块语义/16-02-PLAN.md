---
phase: 16-hop-对齐切分与末块语义
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [CHNK-03, CHNK-04, TASK-04]

must_haves:
  truths:
    - "真实 `clipEndSample` 必须保持不变；只有最后一个 chunk 才允许从 `publishSampleCount` 扩到 `synthSampleCount`。"
    - "padding 只能存在于 mel/vocoder 输入准备阶段，worker 发布前仍然只认真实 sample span。"
    - "最后一个 padded chunk 产生的尾部 frame 不能再触发 trailing-F0 extension。"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "sample-input frozen boundary helper contract"
      contains: "freezeRenderBoundaries"
    - path: "Source/PluginProcessor.cpp"
      provides: "only-last-chunk synth padding"
      contains: "boundaries.synthSampleCount > boundaries.publishSampleCount"
    - path: "Source/PluginProcessor.cpp"
      provides: "tail-padding-safe F0 gap fill"
      contains: "allowTrailingExtension"
  key_links:
    - from: "Source/PluginProcessor.cpp"
      to: "Source/PluginProcessor.h"
      via: "worker freezes boundaries from exact sample span"
      pattern: "freezeRenderBoundaries"
---

<objective>
把 worker 的边界冻结、mel/vocoder 输入准备和 F0 gap fill 全部收紧到“只有最后一个 chunk 允许 inference-local padding，且 padding 尾帧绝不被当成 voiced 延伸”。

Purpose: Phase 16 的 chunk 切分一旦 hop 对齐，worker 就必须严格区分真实末端与推理末端：中间 chunk 天然整 hop，最后一个 chunk 才能补零，且补零尾巴只能留在推理输入内部。

Output: sample-input `freezeRenderBoundaries(...)`、only-last-chunk zero padding、padding-safe trailing-F0 behavior。
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/16-hop-对齐切分与末块语义/16-01-PLAN.md
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp
@Source/DSP/MelSpectrogram.h
@Source/Utils/PitchCurve.h
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: 用 sample-span 冻结边界并把 zero padding 限制在最后一个 chunk 的输入准备阶段</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.h
    - Source/PluginProcessor.cpp
    - Source/Inference/RenderCache.h
    - .planning/phases/16-hop-对齐切分与末块语义/16-01-PLAN.md
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    改造 `freezeRenderBoundaries(...)` 与 worker 音频准备，让 padding 语义只在最后一个 chunk 存在：

    - `freezeRenderBoundaries(...)` 改成从精确 sample 输入构造 `FrozenRenderBoundaries`，不再接受 seconds 作为最终边界输入。
    - 规则必须写死：
      - `trueStartSample` / `trueEndSample` 直接来自 sample input（经 clip range clamp）
      - 若当前 chunk 不是最后一个 chunk，`publishSampleCount` 必须能整除 `hopSize`，否则返回 `false`
      - 只有 `trueEndSample == clipRange.endSampleExclusive` 时才允许 `synthSampleCount > publishSampleCount`
      - `synthEndSample` 只能由最后一个 chunk 的 zero padding 推导，不允许改写 `trueEndSample`
    - `chunkRenderWorkerLoop()` 中准备 mel/vocoder 输入时：
      - `monoAudio` 至少扩到 `boundaries.synthSampleCount`
      - 真实音频只填充前 `publishSampleCount` 个 sample
      - 若最后一个 chunk 需要 padding，剩余尾巴必须显式补零，不允许继续依赖 `computeLogMelSpectrogram()` 的反射边界当成真实语义

    约束：
    - 不允许中间 chunk 继续通过 ceil frameCount 隐式 padding。
    - 不允许修改 `RenderCache` 发布长度；发布链仍只认 `publishSampleCount`。
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.h` contains `freezeRenderBoundaries`
    - `Source/PluginProcessor.cpp` does not contain `freezeRenderBoundaries(clipRange, relChunkStartSec, relChunkEndSec, 512, boundaries)`
    - `Source/PluginProcessor.cpp` contains `boundaries.synthSampleCount > boundaries.publishSampleCount`
    - `Source/PluginProcessor.cpp` contains `monoAudio.resize`
    - `Source/PluginProcessor.cpp` contains `boundaries.trueEndSample == clipRange.endSampleExclusive`
  </acceptance_criteria>
  <verify>
    <automated>rg "freezeRenderBoundaries|synthSampleCount > boundaries.publishSampleCount|monoAudio.resize|clipRange.endSampleExclusive" Source/PluginProcessor.h Source/PluginProcessor.cpp</automated>
  </verify>
  <done>
    worker 已经只在最后一个 chunk 的 mel/vocoder 输入准备阶段补零；真实 `clipEndSample` 和发布长度完全保持不变。
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: 禁止 padded tail 触发 trailing-F0 extension</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <read_first>
    - Source/PluginProcessor.cpp
    - Source/PluginProcessor.h
    - Source/Utils/PitchCurve.h
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    重构 `fillF0GapsForVocoder()` 的尾部延伸语义：

    - 让 helper 接收显式的 trailing-extension policy（例如 `allowTrailingExtension` 或等价 sample-based 条件），由 worker 在最后一个 padded chunk 上关闭尾部延伸。
    - 内部 gap 填补与 leading-zero 处理可以继续保留，但 trailing-zero extension 在 `allowTrailingExtension == false` 时必须完全跳过。
    - worker 根据 `FrozenRenderBoundaries` 判断是否为 padded last chunk，并把该结论传给 helper。
    - 最终代码必须保证：`publishSampleCount < synthSampleCount` 的最后一个 chunk，其 padding 尾部 frame 仍保持无声/zero-F0，不被 `lastVoicedF0` 或 lookahead F0 改写。

    约束：
    - 不允许通过“查询更远的 future F0”来规避这个约束。
    - 不允许把 pad tail 直接裁掉后再少跑 frame，frameCount 必须继续与 `synthSampleCount / hopSize` 一致。
  </action>
  <acceptance_criteria>
    - `Source/PluginProcessor.cpp` contains `allowTrailingExtension`
    - `Source/PluginProcessor.cpp` contains `if (allowTrailingExtension`
    - `Source/PluginProcessor.cpp` contains `fillF0GapsForVocoder(`
    - `Source/PluginProcessor.cpp` contains `boundaries.synthSampleCount > boundaries.publishSampleCount`
  </acceptance_criteria>
  <verify>
    <automated>rg "allowTrailingExtension|fillF0GapsForVocoder|synthSampleCount > boundaries.publishSampleCount" Source/PluginProcessor.cpp</automated>
  </verify>
  <done>
    最后一个 padded chunk 的尾部 frame 已经不会再被解释成 voiced 延伸，TASK-04 的核心语义在 worker 路径里成立。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| render pending job -> worker frozen boundaries | worker 必须以 sample truth 区分真实末端与推理末端 |
| real audio tail -> padded inference tail | padding 只允许存在于推理输入，不允许污染 F0 与发布语义 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | T | `freezeRenderBoundaries` | mitigate | 非末块若不整 hop 立即拒绝，只有最后一个 chunk 允许生成 `synthSampleCount > publishSampleCount`。 |
| T-16-05 | T | worker mel input prep | mitigate | 只把 zero padding 写进局部 `monoAudio` 输入缓冲，发布长度继续只认 `publishSampleCount`。 |
| T-16-06 | T | `fillF0GapsForVocoder` | mitigate | 最后一个 padded chunk 显式关闭 trailing extension，阻断 pad tail 被解释成 voiced。 |
</threat_model>

<verification>
- `rg` 检查 `freezeRenderBoundaries` 只从 sample 输入构造边界，并且 only-last-chunk padding 条件已落地。
- `rg` 检查 worker 在 `monoAudio` 上显式补零而不是继续靠长度反射推导。
- `rg` 检查 `fillF0GapsForVocoder` 已存在 `allowTrailingExtension` 或等价 gating 条件。
</verification>

<success_criteria>
- [ ] 真实 `clipEndSample` 保持不变，只有最后一个 chunk 允许产生 `synthSampleCount > publishSampleCount`。
- [ ] padding 只存在于 mel/vocoder 输入准备阶段，不进入发布长度语义。
- [ ] padded last chunk 的尾部 frame 不会再被 trailing-F0 extension 改写。
</success_criteria>

<output>
After completion, create `.planning/phases/16-hop-对齐切分与末块语义/16-02-SUMMARY.md`
</output>
