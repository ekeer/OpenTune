---
phase: quick-260406-3z5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
  - Source/ARA/OpenTuneDocumentController.h
  - Source/ARA/OpenTuneDocumentController.cpp
  - Source/ARA/TimelineSync.h
  - Source/ARA/TimelineSync.cpp
  - Source/ARA/PlayheadSync.h
  - Source/ARA/PlayheadSync.cpp
  - Source/Plugin/PluginEditor.h
  - Source/Plugin/PluginEditor.cpp
  - CMakeLists.txt
autonomous: true
requirements: [PROC-03, PROC-05, PROC-06, ARA-02, ARA-03, EDTR-02, HOST-03, PLAY-04, PLAY-05]
must_haves:
  truths:
    - "VST3 当前激活 Clip 只由单一权威源推导，不再维护并行状态缓存。"
    - "ARA 控制器中未被消费的同步组件和接口被移除，VST3 ARA 主链路保持可用。"
    - "VST3 编辑器不再保留静默空实现回调（无效接口语义被收敛为明确行为）。"
    - "Source/Standalone/** 零改动。"
  artifacts:
    - path: "Source/PluginProcessor.h"
      provides: "移除 VST3 死定义与并行状态成员，保留对外接口稳定"
    - path: "Source/PluginProcessor.cpp"
      provides: "VST3 当前 Clip 解析改为单源推导，去除散落状态同步"
    - path: "Source/ARA/OpenTuneDocumentController.h"
      provides: "移除未使用 Timeline/Playhead 同步接口"
    - path: "Source/ARA/OpenTuneDocumentController.cpp"
      provides: "ARA 控制器实现去除未使用同步器逻辑"
    - path: "Source/Plugin/PluginEditor.cpp"
      provides: "VST3 编辑器无效回调语义化实现"
    - path: "CMakeLists.txt"
      provides: "ARA 死文件从构建清单移除"
  key_links:
    - from: "Source/Plugin/PluginEditor.cpp::resolveCurrentClipId"
      to: "Source/PluginProcessor.cpp::getCurrentClipIdForPlugin"
      via: "VST3 单 Clip 解析链路"
      pattern: "resolveCurrentClipId|getCurrentClipIdForPlugin"
    - from: "Source/ARA/OpenTuneDocumentController.cpp::doCreatePlaybackRenderer"
      to: "Source/ARA/OpenTunePlaybackRenderer.cpp"
      via: "ARA PlaybackRenderer 工厂连接"
      pattern: "doCreatePlaybackRenderer|OpenTunePlaybackRenderer"
    - from: "CMakeLists.txt"
      to: "Source/ARA/*"
      via: "target_sources(OpenTune PRIVATE ...)"
      pattern: "Source/ARA/"
---

<objective>
对 VST3 路径与共享 Processor 中仅服务 VST3 语义的冗余代码、死代码、无效接口、并行结构做一次结构收敛清理。

Purpose: 以第一性原理消除并行状态与死接口，降低维护成本并避免状态漂移。
Output: 三个互斥文件域可并行执行的清理任务（3 executors 无冲突）。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/VST3Merge.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md

<interfaces>
From Source/PluginProcessor.h:
```cpp
uint64_t getCurrentClipIdForPlugin() const;
int findClipIndexById(int trackId, uint64_t clipId) const;
bool setClipStartSecondsById(int trackId, uint64_t clipId, double startSeconds);
bool commitPreparedImportClip(PreparedImportClip&& prepared);
bool insertClipSnapshot(int trackId, int insertIndex, const ClipSnapshot& snap, uint64_t forcedClipId);
bool deleteClipById(int trackId, uint64_t clipId, ClipSnapshot* deletedOut, int* deletedIndexOut);
```

From Source/ARA/OpenTuneDocumentController.h:
```cpp
bool requestSetPlaybackPosition(double timeInSeconds);
bool requestStartPlayback();
bool requestStopPlayback();
```

From Source/Standalone/UI/MenuBarComponent.h:
```cpp
virtual void savePresetRequested() = 0;
virtual void loadPresetRequested() = 0;
virtual void helpRequested() = 0;
```

From Source/Standalone/UI/TransportBarComponent.h:
```cpp
virtual void viewToggled(bool workspaceView) = 0;
void setWorkspaceView(bool workspaceView);
void setLayoutProfile(LayoutProfile profile);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1 (Executor-A / Processor 域): 消除 VST3 并行 Clip 状态，收敛为单一权威源</name>
  <files>Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <behavior>
    - VST3 当前激活 Clip ID 由 track0 + selectedClipIndex 推导，而不是并行缓存成员。
    - 导入/插入/删除/移动后，激活 Clip 语义与当前 VST3 UI 一致（不会漂移到无效 clipId）。
    - Standalone 分支行为不退化。
  </behavior>
  <action>
    文件所有权边界（互斥）：仅允许修改 `Source/PluginProcessor.h/cpp`。

    具体实施：
    1) 删除 `CurrentClipState` 死定义与 `currentClipIdForPlugin_` 并行状态成员（仅 VST3 语义遗留）。
    2) 将 `getCurrentClipIdForPlugin()` 改为读锁下基于 `tracks_[0].clips` + `selectedClipIndex` 的单源推导。
    3) 清理 `.store/.load` 形式的散落同步分支；改为在 track0 变更路径上维护 `selectedClipIndex` 一致性（导入、insert、move、delete 等）。
    4) 保持对外接口签名稳定，禁止引入兼容层/并行状态。
    5) 严禁改动 `Source/Standalone/**`。
  </action>
  <verify>
    <automated>rg "struct CurrentClipState|currentClipIdForPlugin_" Source/PluginProcessor.h Source/PluginProcessor.cpp</automated>
    <automated>rg "currentClipIdForPlugin_\.store|currentClipIdForPlugin_\.load" Source/PluginProcessor.cpp</automated>
    <automated>cmake --build build-vst3 --config Debug --target OpenTune_VST3</automated>
  </verify>
  <done>Processor 中不再存在 VST3 并行 Clip 状态，VST3 当前 Clip 解析链路单一且可编译通过。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (Executor-B / ARA 域): 删除未使用 Timeline/Playhead 同步链路与构建残留</name>
  <files>Source/ARA/OpenTuneDocumentController.h, Source/ARA/OpenTuneDocumentController.cpp, Source/ARA/TimelineSync.h, Source/ARA/TimelineSync.cpp, Source/ARA/PlayheadSync.h, Source/ARA/PlayheadSync.cpp, CMakeLists.txt</files>
  <behavior>
    - ARA 主链路（DocumentController -> PlaybackRenderer -> PlaybackController 请求）保持可用。
    - 未被任何调用点消费的同步组件彻底移除，不留声明/构建残留。
  </behavior>
  <action>
    文件所有权边界（互斥）：仅允许修改 `Source/ARA/**` 与 `CMakeLists.txt`（不得触碰 PluginProcessor 与 PluginEditor）。

    具体实施：
    1) 从 `OpenTuneDocumentController.h/.cpp` 移除 `TimelineSync/PlayheadSync` include、getter、成员、构造初始化、无效更新代码。
    2) 删除 `Source/ARA/TimelineSync.*` 与 `Source/ARA/PlayheadSync.*` 文件。
    3) 在 `CMakeLists.txt` 的 `target_sources(OpenTune PRIVATE ...)` 中移除上述四个已删文件。
    4) 保留 `requestSetPlaybackPosition/requestStartPlayback/requestStopPlayback` 与 `doCreatePlaybackRenderer()` 行为不变。
    5) 严禁改动 `Source/Standalone/**`。
  </action>
  <verify>
    <automated>rg "TimelineSync|PlayheadSync" Source/ARA/OpenTuneDocumentController.h Source/ARA/OpenTuneDocumentController.cpp CMakeLists.txt</automated>
    <automated>rg "requestSetPlaybackPosition|requestStartPlayback|requestStopPlayback|doCreatePlaybackRenderer" Source/ARA/OpenTuneDocumentController.cpp</automated>
    <automated>cmake --build build-vst3 --config Debug --target OpenTune_VST3</automated>
  </verify>
  <done>ARA 无效同步组件完全移除且构建清单收敛，VST3 ARA 播放控制主链路保持可用。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3 (Executor-C / VST3 Editor 域): 清理无效回调接口并收敛单视图语义</name>
  <files>Source/Plugin/PluginEditor.h, Source/Plugin/PluginEditor.cpp</files>
  <behavior>
    - VST3 编辑器不再包含静默空实现回调。
    - 单 Clip/单视图语义明确，避免“触发了但什么都不做”的无效接口。
    - ARA 传输控制链路（PLAY-04/PLAY-05）保持。
  </behavior>
  <action>
    文件所有权边界（互斥）：仅允许修改 `Source/Plugin/PluginEditor.h/cpp`。

    具体实施：
    1) 清理可移除的默认 no-op override（例如来自带默认实现的监听接口），降低无效实现面。
    2) 对必须实现但当前为空的方法（如 `savePresetRequested/loadPresetRequested/helpRequested`）改为明确 VST3 语义（用户提示/日志，声明由 DAW 管理），禁止静默空函数。
    3) `viewToggled` 收敛为单视图语义（强制保持 VST3 单剪辑布局，不创建并行视图状态）。
    4) `trackTimeOffsetChanged` 不再静默忽略：按单 Clip 语义显式处理（更新 clip start 并同步 UI，必要时走 ARA 位置请求）。
    5) 保持 `requestStartPlayback/requestStopPlayback/requestSetPlaybackPosition` 现有调用链不退化。
    6) 严禁改动 `Source/Standalone/**`。
  </action>
  <verify>
    <automated>rg "savePresetRequested\(\) \{\}|loadPresetRequested\(\) \{\}|helpRequested\(\) \{\}|juce::ignoreUnused\(workspaceView\)|juce::ignoreUnused\(trackId, newOffset\)" Source/Plugin/PluginEditor.cpp</automated>
    <automated>rg "requestStartPlayback|requestStopPlayback|requestSetPlaybackPosition" Source/Plugin/PluginEditor.cpp</automated>
    <automated>cmake --build build-vst3 --config Debug --target OpenTune_VST3</automated>
  </verify>
  <done>VST3 编辑器无效回调已语义化，单视图/单 Clip 约束清晰且 ARA 播放控制链路保持可用。</done>
</task>

</tasks>

<verification>
1) 三任务可并行执行（文件域互斥，无冲突）。
2) `git diff --name-only -- Source/Standalone` 输出必须为空。
3) 最终以 VST3 Debug 目标编译通过作为闭环。
</verification>

<success_criteria>
- VST3 路径中无并行 Clip 状态缓存、无未引用 ARA 同步组件、无静默空接口。
- `Source/Standalone/**` 完全未改动。
- `cmake --build build-vst3 --config Debug --target OpenTune_VST3` 通过。
</success_criteria>

<output>
After completion, create `.planning/quick/260406-3z5-3-5-gsd-project-roadmap-codebase-standal/260406-3z5-SUMMARY.md`
</output>
