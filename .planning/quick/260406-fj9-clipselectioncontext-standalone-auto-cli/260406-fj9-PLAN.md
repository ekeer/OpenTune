---
phase: quick-260406-fj9-clipselectioncontext-standalone-auto-cli
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Source/Standalone/PluginEditor.h
  - Source/Standalone/PluginEditor.cpp
  - Source/Standalone/UI/ArrangementViewComponent.h
  - Source/Standalone/UI/ArrangementViewComponent.cpp
  - Source/Standalone/UI/PianoRollComponent.h
  - Source/Standalone/UI/PianoRollComponent.cpp
  - Source/PluginProcessor.h
  - Source/PluginProcessor.cpp
autonomous: true
requirements: [PROC-05, PROC-06]
must_haves:
  truths:
    - "用户切换 Clip 时，编辑器内只有一个选择真相：ClipSelectionContext{trackId, clipId}。"
    - "无选中时为显式 NoSelection，AUTO 不会以伪有效 clipIndex=0 启动。"
    - "AUTO 仅在统一上下文有效且 OriginalF0=Ready 时触发。"
  artifacts:
    - path: "Source/Standalone/PluginEditor.h"
      provides: "统一 ClipSelectionContext 合约与 NoSelection 状态"
    - path: "Source/Standalone/PluginEditor.cpp"
      provides: "单向选择分发、AUTO 前置校验统一入口"
    - path: "Source/Standalone/UI/ArrangementViewComponent.cpp"
      provides: "去除并行选择写路径，只上报选择事件"
    - path: "Source/Standalone/UI/PianoRollComponent.cpp"
      provides: "按 clipId 派生 clipIndex，缺失时安全返回"
    - path: "Source/PluginProcessor.cpp"
      provides: "NoSelection 语义：空轨道不再回写伪 0 选中"
  key_links:
    - from: "ArrangementViewComponent::mouseDown / 选择流程"
      to: "OpenTuneAudioProcessorEditor::clipSelectionChanged"
      via: "事件上报 + 统一选择入口"
      pattern: "clipId 作为跨组件唯一标识"
    - from: "OpenTuneAudioProcessorEditor::startAutoTuneAsUnifiedEdit"
      to: "PianoRollComponent::applyAutoTuneToSelection"
      via: "统一 ClipSelectionContext 有效性校验"
      pattern: "NoSelection 时直接拒绝启动"
---

<objective>
先清理 Standalone 选择链路中的并行/冗余状态写入，再统一为单向数据流与单一真相（clipId 主导），彻底修复 AUTO 因 clip context 无效而不生效的问题。

Purpose: 结构性消除“伪有效索引 + 多路径写状态”带来的不一致与崩溃风险。
Output: 单向选择链路、显式 NoSelection 语义、AUTO 前置条件统一校验。
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/VST3Merge.md
@Source/Standalone/PluginEditor.h
@Source/Standalone/PluginEditor.cpp
@Source/Standalone/UI/ArrangementViewComponent.h
@Source/Standalone/UI/ArrangementViewComponent.cpp
@Source/Standalone/UI/PianoRollComponent.h
@Source/Standalone/UI/PianoRollComponent.cpp
@Source/PluginProcessor.h
@Source/PluginProcessor.cpp

<interfaces>
From Source/Standalone/UI/PianoRollComponent.h:
```cpp
void setCurrentClipContext(int trackId, uint64_t clipId);
void clearClipContext();
bool hasActiveClipContext() const;
int getCurrentTrackId() const;
uint64_t getCurrentClipId() const;
```

From Source/PluginProcessor.h:
```cpp
int getSelectedClip(int trackId) const;
void setSelectedClip(int trackId, int clipIndex);
uint64_t getClipId(int trackId, int clipIndex) const;
int findClipIndexById(int trackId, uint64_t clipId) const;
```

From Source/Standalone/UI/ArrangementViewComponent.h:
```cpp
virtual void clipSelectionChanged(int trackId, int clipIndex) = 0;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: 先清理并行选择写路径，建立唯一选择入口</name>
  <files>Source/Standalone/PluginEditor.h, Source/Standalone/PluginEditor.cpp, Source/Standalone/UI/ArrangementViewComponent.h, Source/Standalone/UI/ArrangementViewComponent.cpp</files>
  <action>
    先做结构清理（用户要求：先清冗余再统一）：删除/收敛 ArrangementView 与 Editor 内对 activeTrack + selectedClip 的并行写入路径，保留单一入口（例如 applyClipSelectionContext）。禁止增加兼容层或双轨并行逻辑。

    将选择事件语义改为“上报事实（trackId + clipId）”，不再以 clipIndex 作为跨组件主键；clipIndex 只允许在入口处按 clipId 派生。

    所有入口在 clipId 不存在时统一走 NoSelection，不得回落为 clipIndex=0。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_Standalone</automated>
  </verify>
  <done>代码中不再存在多处独立 setActiveTrack/setSelectedClip 竞争写；选择更新路径可从单入口追踪到 UI 与 Processor。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 统一 ClipSelectionContext 单一真相并显式 NoSelection</name>
  <files>Source/Standalone/PluginEditor.h, Source/Standalone/PluginEditor.cpp, Source/Standalone/UI/PianoRollComponent.h, Source/Standalone/UI/PianoRollComponent.cpp, Source/PluginProcessor.h, Source/PluginProcessor.cpp</files>
  <behavior>
    - Test 1: 空轨道/删除 clip 后，选择状态必须变为 NoSelection，而非 clipIndex=0。
    - Test 2: 给定有效 {trackId, clipId} 时，clipIndex 仅通过 findClipIndexById 派生。
    - Test 3: clipId 失效（被删/移动）时，PianoRoll 读写接口安全返回，不崩溃。
  </behavior>
  <action>
    定义统一上下文：ClipSelectionContext {trackId, clipId} + 显式 NoSelection（例如 invalid trackId + clipId=0 或等价强类型表达）。

    替换 Editor/PianoRoll 中依赖 selectedClipIndex 的跨组件契约；所有跨组件读写以 clipId 为主键，clipIndex 改为派生值。

    修正 Processor 的选择语义：在 clips.empty() 分支不再写入伪有效 selectedClipIndex=0，改为 NoSelection 语义（例如 -1）。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTuneTests; if ($?) { .\build\Debug\OpenTuneTests.exe }</automated>
  </verify>
  <done>ClipSelectionContext 成为唯一真相；NoSelection 在 Editor/PianoRoll/Processor 语义一致，且不存在伪 0 索引。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: AUTO 前置条件绑定统一上下文，修复 Standalone AUTO 不生效</name>
  <files>Source/Standalone/PluginEditor.cpp, Source/Standalone/UI/PianoRollComponent.cpp</files>
  <behavior>
    - Test 1: NoSelection 时触发 AUTO 必须立即失败并不进入渲染流程。
    - Test 2: 上下文有效 + OriginalF0=Ready 时，AUTO 必须成功 enqueue。
    - Test 3: 上下文失效（clip 被删除/切换）时，AUTO overlay 与 in-flight 状态可安全释放。
  </behavior>
  <action>
    在 startAutoTuneAsUnifiedEdit 与 applyAutoTuneToSelection 两端统一使用 ClipSelectionContext 有效性判断，移除对“activeTrack + selectedClipIndex”的并行前置条件判断。

    AUTO precondition 仅接受“统一上下文有效 + OriginalF0 Ready”；任一条件不满足必须直接拒绝，不做兜底推测。

    保持线程安全：沿用现有 correctionInFlight_ 与 clipContextGeneration_ 失效机制，确保无竞态崩溃。
  </action>
  <verify>
    <automated>cmake --build build --config Debug --target OpenTune_Standalone</automated>
  </verify>
  <done>Standalone AUTO 在有效上下文下稳定生效；无效上下文下可预期拒绝，且无崩溃/悬挂状态。</done>
</task>

</tasks>

<verification>
- 结构验证：搜索确认不存在“clipIndex=0 代表无选择”的语义分支。
- 线程安全验证：AUTO 并发触发 + clip 切换/删除场景下无崩溃。
- 行为验证：NoSelection、有效选择、失效选择三类场景均符合预期。
</verification>

<success_criteria>
- 选择链路从 Arrangement 到 Editor 到 PianoRoll 为单向数据流。
- ClipSelectionContext{trackId, clipId} 成为单一真相，clipIndex 全部派生。
- 显式 NoSelection 全链路生效，AUTO 不再受伪有效上下文污染。
</success_criteria>

<output>
After completion, create `.planning/quick/260406-fj9-clipselectioncontext-standalone-auto-cli/260406-fj9-SUMMARY.md`
</output>
