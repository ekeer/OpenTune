#include "TimelineRenderingDiagnostics.h"

namespace OpenTune {

// TimelineRenderingDiagnostics is header-only so tests can use the surface
// without requiring a processor-owned or build-system-owned diagnostics object.

} // namespace OpenTune
