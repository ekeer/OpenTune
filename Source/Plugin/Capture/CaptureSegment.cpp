
#include "CaptureSegment.h"

namespace OpenTune::Capture {

// CaptureSegment is a passive aggregate; all behavior is defined inline in the header.
// This .cpp exists only so the build system has a translation unit for symbol emission.

}  // namespace OpenTune::Capture

